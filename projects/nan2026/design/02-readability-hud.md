# 가독성 · 비주얼 언어 · HUD — 섹션 v1.1 (정본 v1.1 정합)

> **지위**: 이 문서는 `design/CANON.md`(**정본 v1.1**)의 **하위 섹션**이다. 정본의 값·공식·어휘·스키마는 **인용만** 하며 재정의하지 않는다(C-1·C-2). 이 문서가 정본과 충돌하면 **이 문서가 틀린 것이다.**
> **위임 범위** (정본 §17, v1.1에서 개정됨): 「HUD」 = **패널 내부 레이아웃 · 폰트 사다리 · 아이콘 9종 · 글리프 LOD · 강등 사다리의 값**. 다음은 **정본이 고정**한다 — 요소 목록 · 순서 `Q W E R` · 기체 부착 규격 · **§1.2 띠 내용** · **§7.12 경계면 6건**.
> **단위**: 모든 시간은 **게임초(`Sec`)**, 모든 거리는 **논리 픽셀**(정본 §0.2). 실제ms는 **정확히 4곳**이며 이 섹션은 그중 `render.fxMinRealMs` · `render.degradeOnFrameMs` 둘을 인용한다.
> **틱은 JSON에 등장하지 않는다**(정본 §0.2) — 이 섹션의 v1.0이 제안했던 `visual.telegraph.fireFlashTicks`는 **그 규약 위반이므로 스스로 폐기**한다(§3.3).

---

## 0. 이 섹션의 경계 (v1.1 개정)

| | |
|---|---|
| **닫는다** | 진영 배타 규격의 집행 · 텔레그래프 진행률 채널 · 히트 피드백의 렌더 치수 · 스탠스 판독의 패널 측 · **HUD 전 좌표**(정본이 자리를 준 3요소의 좌표 포함) · 글리프 LOD의 판정식 · 강등 사다리의 값 · 색맹 검증 항목 |
| **인용만 한다** | 팔레트 · 글리프 4종 · 불변식 I-1~I-4 · 레이어 0~13 + 레이어 1의 판/내용 알파 · 2층 캡 · 공정성 하한 · 텔레그래프 리드타임 · 히트 피드백 3배율표 · 기체 부착 3중 · 오디오 큐 13종 · **§7.12 경계면 6건** · `rules.json > hud`(§9.4.1) · `rules.json > render`(§9.4.2) |
| **★ v1.1에서 이 섹션이 잃은 것** | **보스 타이머의 색**(§7.12.1) · **위기 세션 예고의 시각 규격**(§7.12.2) · **최종 로테이션의 시각 규격**(§7.12.3 — *규격이 없다는 것이 규격이다*) · **호박 예약의 범위**(§7.12.4) · **무피격/퍼펙트/시간보너스 3요소의 자리와 형식**(§7.12.5) · **플레이어 텔레그래프의 색**(§7.12.6) · `hud`/`render` 스코프의 소유권(§9.4.1·§9.4.2) · 강등 사다리의 **순서**(§9.4.2) |
| **소관 아님** | 무기/패시브 수치(03) · 적 로스터·보스 부위(04) · 화면의 내용·전이·정산·온보딩의 인과(05) · 점수/상점 규칙(정본 §11) |

---

## 1. 정본 인용 지도 (여기서 값을 찾지 말 것)

| 필요한 것 | 정본 위치 |
|---|---|
| 4속성 색 · 글리프(●▲◆✚) · 자홍 · 호박 · 코인/XP · 차콜 · **예약 규칙** | §7.2 `palette` |
| 색맹 팔레트 `elementCvd` (명도 사다리 96/76/55/40) | §7.3 |
| 적 탄 vs 플레이어 탄 7채널 배타표 · 텔레그래프 2상태 · 거동별 모양 · 리드타임 하한 · `emphasisBySpeed` | §7.4 |
| 기체 부착 3중 + z-order(도트 10 / 스트립·배지 6) + `visual.stance` | §7.5 |
| 적 속성 판독(외곽선·글리프·림·중립 본체) · 엘리트 · 부위 | §7.6 |
| 히트 피드백 ×2/×1/×0.5 · `visual.hitFx` · 잡몹 숫자 없음 | §7.7 |
| 픽업 vs 유도탄 운동 분리 · 자석 링 | §7.8 |
| 배경 상한 · 광과민 캡 · `visual.a11y` · `visual.text` | §7.9 |
| 오디오 큐 13종(전부 시각 짝 보유) | §7.10 |
| 상태이상 호박 3층 + 전용층 · 피격 vs 실드 흡수 | §7.11 |
| ★ **보스 타이머 3단** (호박 없음) · `visual.timer.*` | **§7.12.1** |
| ★ **위기 예고** = 자홍 점선 수평선 (`crisisWarn` 키 **없음**) | **§7.12.2** |
| ★ **최종 로테이션** = 시각 규격 **없음** | **§7.12.3** |
| ★ **호박 채널 = 정확히 5곳** | **§7.12.4** |
| ★ **무피격 pip 6칸 · 퍼펙트 ✕ · 시간 보너스 ✕** | **§7.12.5** |
| ★ **플레이어 텔레그래프 = 스탠스색, 자홍 금지** | **§7.12.6** |
| 좌표 1280×720 · 아레나 `{350,0,580,720}` · 패널 350/350 · 띠 48(=40+8)/24/24 · **코어 HP 바** | §1.1, §1.2 |
| 레이어 스택 0~13 · **레이어 1의 판/내용 알파** · 플레이어 FX 합성 0.45 · **처치 FX 합성 0.55** | §12.2, §12.3 |
| 2층 캡 (A층 오써링 예산 < B층 안전망) · **텔레그래프 A 80 < B 96** | §12.1, §12.4 |
| `rules.json > hud` 전 키 · `rules.json > render` 전 키 · **강등 사다리의 순서** | §9.4.1, §9.4.2 |
| `bullets[].shape` 어휘 폐쇄 + **S14 동치 검사** | §9.7, §13.4 |
| `stanceHintPulse`의 타겟 = 최빈 속성 | §9.9.1 |
| 파 고스트 사상식 `ghostHpPct(t)` | §11.5 |
| 테마 고지 = **저작 리스트 기준 + 물결표** | §8.2.1 |

---

## 2. 진영 배타 규격 — "이건 누구 것인가"를 0ms에 판정

### 2.1 집행 규칙 (정본 §7.4 표의 실행형)

정본 §7.4가 7채널(색·코어·엣지·합성·알파·레이어·모양) 전부를 배타로 정의했고, §7.12.6이 **플레이어 텔레그래프**를 그 배타 안으로 끌어들였다. 이 섹션은 **그 배타가 코드에서 깨지지 않게 하는 규칙**을 더한다.

| # | 규칙 |
|---|---|
| **V-1** | **팔레트 접근 경로가 진영을 강제한다.** 플레이어 측 렌더는 `palette.threat.*`를, 적 측 렌더는 `palette.element.*`를 **읽지 않는다.** 예외는 정확히 둘: ① 적 본체의 속성 외곽선·글리프·림(정본 §7.6) — 이것은 "내가 무엇으로 때려야 하는가"의 정보이지 위협 표시가 아니다 ② **적 탄의 호박 테두리**(정본 §7.12.4-⑤) — 이것은 `palette.status.*`이며 `element`도 `threat`도 아니다. |
| **V-2** | **하드 외곽선의 유일성.** 검은 외곽선(`palette.threat.outline`)을 가진 것은 **전부 나를 아프게 한다.** 역도 참이다 — 나를 아프게 하는 것 중 외곽선이 없는 것은 존재하지 않는다(적 탄 / 활성 장판 / 레이저 / 몸통 = 적 본체 실루엣). = I-1의 쌍방향 형태. **정본 §7.12.6이 이것을 플레이어 측에서 확인했다** — 플레이어 텔레그래프는 외곽선 **금지**. |
| **V-3** | **꼬리 = 유도.** 잔상 꼬리는 `turnRateDegSec > 0`인 개체 **전용 표식**이며 진영을 가리지 않는다(적 유도탄에도, 플레이어 `seeker` 탄에도 붙는다). 유도가 아닌 어떤 탄도 꼬리를 갖지 않는다. → **꼬리 = 쫓는다, 색 = 누구 것인가.** 두 축이 직교하므로 조합 학습이 불필요하다. |
| **V-4** | **크기 하한이 진영을 보조한다.** 적 탄 `fairness.minBulletRadiusPx = 4`, 플레이어 탄 `render.playerBulletMaxRadiusPx = 10`, 픽업 ≤ 6px(정본 §7.8). 겹치는 구간이 존재하나 **운동·레이어·렌더모드가 이미 분리**하므로 크기는 보조 채널이다. |

> **V-2의 값어치**: 이 한 줄이 `mono` 모드(전 채도 0)에서 **위협 분리를 단독으로 보증**한다(§9.3). 그러므로 I-1은 색맹 모드에서 **협상 불가**다.
> **★ V-3의 강등 하 지위 (정본 §9.4.2 사다리 ④와의 관계)**: 사다리 ④가 잔상을 끄면 꼬리가 사라진다. **정보 손실이 아니다** — 정본 §7.8이 유도의 판독을 **운동**에 걸었기 때문이다("천천히 곡선으로 쫓아오면 100% 탄"). 꼬리는 그 운동의 **리드타임 증폭기**이지 유일 채널이 아니다. 사다리는 하한기에서만 작동하고 정본의 불가침 목록(히트박스 도트·스탠스 링·적 외곽선·글리프·텔레그래프·적 탄)을 건드리지 않는다.

### 2.2 적 탄 실루엣 — 어휘는 둘뿐 (정본이 폐쇄했다)

정본 §7.4: 적 탄 모양 = **원 / 육각(상태이상)**. 정본 §9.7: **`bullets[].shape` 어휘 = `["circle", "hex"]` (폐쇄)** — 02-B4 채택.

| `shape` | 조건 | 부가 |
|---|---|---|
| `circle` | `status === null` | 유도(`turnRateDegSec > 0`)면 **꼬리 3장**(V-3) |
| `hex` | `status !== null` | 크기 ×1.5(slow) / ×1.8(stun), 속도 `visual.statusBulletSpeedMul = 0.6`, 흰 점선 궤적(slow) / 흰 이중 링 4Hz(stun) — 전부 정본 §7.11. **+ 호박 테두리**(정본 §7.12.4-⑤) |

- ★ **3중 동치 (정본 §7.12.4가 02-B4를 확장했다)**: `(status ≠ null) ⟺ (shape == "hex") ⟺ (호박 테두리)`. `check.mjs` **S14**가 `(status === null) === (shape === "circle")`을 강제하고, 어긋나면 **색이 거짓말하는 것**(I-2)이므로 **로드 실패**가 옳다. 02의 v1.0 요청은 2중 동치였고 **정본이 한 겹 더 쌓았다.**
- **호박 테두리의 적층 순서 (I-1과의 정합)**: I-1은 불변식이므로 **검은 하드 외곽선 2px는 삭제 불가**다. → 바깥부터 **검은 2px → 호박 1px → 자홍 채움 → 흰 스페큘러 코어**. 호박이 검은 외곽선을 **대체하지 않고 안쪽에 얹힌다.** 이 순서 외의 어떤 배치도 I-1을 깬다.
- ★ **초안 B의 "유도탄 = 마름모" · "설치 장판 = 링"은 폐기한다.** ◆는 **물 속성의 예약 글리프**다(정본 §7.2). 유도는 **꼬리**로 읽는다(V-3).
- **레이저**: 탄이 아니라 이미터(정본 §8.5, `widthPx`). 실루엣 = 자홍 채움 라인 + 흰 코어 + 검은 외곽선 2px, 레이어 9.
- **활성 장판(`zone`)**: §3.4 — **정본 §12.3이 v1.1에서 알파 모순을 닫았다.**

### 2.3 글리프 예약 — 색과 같은 강도로 예약된다

| 규칙 |
|---|
| **속성 글리프 4종(● ▲ ◆ ✚)은 속성 주장 전용이다.** 아이콘·마커·불릿포인트·장식으로 쓸 수 없다. 자홍 개체·픽업·HUD 크롬·버튼·커서 어디에도 등장하지 않는다. |
| **예외 1 — 무채색 pip.** `●`/`○`/`⊘`는 **수량·상태 표시**로 허용한다(투자 pip, 소지품 pip, 패시브 레벨 pip, **무피격 pip 6칸**). 속성색이 아니므로 주장이 아니다. |
| **예외 2 — 은색 `●` = 노말.** 정본 §7.5의 "나머지 = 은색 ●"은 **정확한 주장**이다: 부여 안 됨 = ×1 = 노말. I-2 준수. |
| **★ 은색의 정의 — 정본이 v1.1에서 직접 답했다.** 정본 §7.12.1이 타이머 정상 색을 **`palette.element.normal` (은색)**이라고 **키와 이름을 나란히** 적었다. → **은색 ≡ `palette.element.normal` (`#DCE6F0`)**. 이 섹션의 v1.0 추론(은색이 등장하는 전 지점에서 의미가 정확히 "×1"이므로 별도 `palette.silver`는 I-2를 깬다)은 **정본이 같은 결론에 도달함으로써 인용으로 강등된다.** |
| **XP 마름모의 해명**: 정본 §7.2가 XP 픽업을 `작은 마름모`로 지정했다(◆ = 물). **충돌이 아니다** — 픽업/유도탄 분리는 **운동**이 보증하고(정본 §7.8), 픽업은 무광·무외곽선·레이어 2·≤6px이며 플레이어 탄은 글로우·additive·레이어 4다. **7채널 중 6채널이 다르다.** 그러나 이 좁음은 실재하므로 §9.4의 색맹 검증 항목에 명시적으로 남긴다. |
| **✔ / ✕ / ◈의 지위**: `✔`·`✕`(무피격 pip·퍼펙트·시간 보너스)는 속성 글리프가 아니며 **속성색으로 그리지 않는다**(정본 §7.12.5: 은색 / `palette.neutralGray`). ★ **v1.0의 패널 ASCII에 있던 진화 표식 `◈◈`는 삭제한다** — ◆(물)와 형태가 근접해 예약의 정신을 흐린다. 진화는 **이중선 테두리**가 단독으로 나른다(§7.3-B). |

---

## 3. 텔레그래프 언어

### 3.1 정본이 이미 닫은 것

2상태(점선 = 미래 / 채움 + 검은 외곽선 = 현재) · 전환 순간 흰 1프레임 플래시 · 거동별 모양 8종 + 상황 4종 · 리드타임 하한(`straight` 0.55 ~ `bossPattern`·`stun` 1.50) · `fairness.minTelegraphSec = 0.55` · `telegraphConcurrentMaxPerEntity = 2` · **`fairness.telegraphConcurrentMaxGlobal = 80`(A층, 파생: 40 × 2)** · **`caps.telegraphs = 96`(B층) + `deferAttack`** · `emphasisBySpeed`(렌더 전용) · 레이어 1(지면) / 8(공중) · 알파 0.50. **전부 정본 §7.4 · §12.1 · §12.3 · §12.4.**

> ★ **v1.0의 이 섹션은 "전역 캡 8"이라고 적었다. 정본 v1.1이 그것을 폐기했다** (04-R4 수정채택): v1.0 정본은 「A 2/개체 < B 8」로 **단위가 다른 두 수를 부등호로 이었다.** 정정 = **A층 80(파생) < B층 96(20% 여유)**. 이 섹션의 §8.1 표도 함께 고쳤다.

### 3.2 ★ 진행률 채널 (정본 §19.1-B3 **채택**, 거처 = `visual.telegraph`)

**문제**: `laser`의 리드타임은 1.20 게임초다. 점선이 1.2초 동안 그냥 있으면 플레이어는 "곧"은 알지만 **"언제"**를 모른다. Normal에서 1.2초는 여유지만 Disaster(×3)에서 실제 0.4초다 — 그때 필요한 것은 강조가 아니라 **카운트다운**이다.

> **정본의 채택 근거 (§19.1-B3)**: *"I-3(「텔레그래프 없이는 발사 없다」)은 **리드타임의 존재**만 보장하고 **남은 시간의 판독**을 보장하지 않았다. `dashTightenAtPct` = 형상 무관 백스톱."*

**규칙**: **모든 텔레그래프는 진행률을 형상으로 인코딩한다.** 채널은 2개이며 항상 둘 다 작동한다.

**① 형상 고유 채널** — 정본이 이미 4종에 넣어 두었고, 나머지 4종을 여기서 지정한다.

| 이미터 | 정본 §7.4가 지정한 모양 | 진행률 = |
|---|---|---|
| `straight` | 발사구 자홍 원 **확대** | 반경 (정본 내장) ✔ |
| `aimed` | 조준 브래킷 4각 **수축** | 수축량 (정본 내장) ✔ |
| `zone` | 지면 점선 원 **수축** | 수축량 (정본 내장) ✔ |
| `laser` | 점선 라인 → **폭 0→최종폭 보간** | 폭 (정본 내장) ✔ |
| **`fan`** | 점선 부채꼴 | ★ **호가 중심에서 바깥으로 자라남** (길이 = 진행률). 각도는 처음부터 실제 각도 고정 |
| **`ring`** | 발사구 점선 원 | ★ **반경 0 → 최종 반경 확대** (`straight`와 동일 규칙 = 학습 이월) |
| **`spiral`** | 점선 원 + 회전 화살 | ★ **화살이 1회전을 완주하면 발사** (진행률 = 각도) |
| **`wall`** | 점선 수평선 + 틈이 미리 비어 있음 | ★ **좌우 양끝에서 중앙으로 선이 그려지고, 완성되면 발사**. 틈은 **처음부터** 비어 있다(정본 규정 유지) |

**② 전역 dash 백스톱** — 형상과 무관하게 항상 작동한다.

```
리드타임의 0 ~ 85% 구간:   dash 간격 = visual.telegraph.dashPx = [6, 5]
리드타임의 85 ~ 100% 구간: 간격이 dashTightenMul(0.35)까지 선형 수축 → [6, 1.75]에 근접
발사 순간:                 흰색 플래시 → 채움 (정본 §7.4의 2상태)
```

> **왜 이게 옳은가**: 점선이 실선에 **근접**할 뿐 채워지지 않으므로 정본의 2상태 규칙("채움 금지")이 그대로 유지되고, 동시에 **"점선이 조여들면 발사 직전"**이라는 연속 판독이 8종 전부에 공짜로 붙는다. 규칙 1개, 새 색 0, 새 형태 0, 색맹 안전(형태 채널). `emphasisBySpeed`(굵기·알파)와 직교하므로 고배속에서 두 채널이 **겹쳐서 강해진다.**

### 3.3 텔레그래프 규격 — ★ `fireFlashTicks` 자진 폐기

```
visual.telegraph = { strokePx: 2, dashPx: [6, 5],
                     dashTightenAtPct: 0.85, dashTightenMul: 0.35 }
                     // alpha 0.50은 정본 §7.4·§12.3 소유 — 여기서 재인쇄하지 않는다
```

- ★ **v1.0의 `fireFlashTicks: 1`은 폐기한다.** 정본 §0.2: **"틱은 JSON에 등장하지 않는다. 틱(1/60 게임초)은 엔진 내부 단위이며 데이터 어휘가 아니다. `*_ticks` 키는 전부 폐기한다."** → **`fireFlashTicks`는 그 규약의 정면 위반이었고, 정본이 그것을 지적하지 않은 것은 이 키가 요청 대장에 `visual.telegraph.*` 묶음으로만 올라갔기 때문이다. 이 섹션이 스스로 거둔다.**
- **대체 = 구조 (새 키 0)**: **발사 플래시는 판정 1틱**(엔진 내부 단위, JSON 키 없음)이고, **표시는 `render.fxMinRealMs = 50`(정본 §9.4.2) 하한을 탄다** → **Disaster(×3)에서도 발사 순간이 소실되지 않는다.** 정본 §7.4의 "흰색 1프레임 플래시"가 판정 1틱인지 렌더 1프레임인지의 모호함은 **키가 아니라 이 한 줄이 닫는다.** (`fxMinRealMs`의 정본 근거 문장이 정확히 이것이다: *"순수 장식이 고배속에서 소실되지 않게(판정 무관)"* — §6.2.)
- **텔레그래프는 발사 주체에 귀속된다** — 개체가 죽으면 그 개체의 텔레그래프도 즉시 소멸한다(발사되지 않으므로 그려질 이유가 없다 = I-2).
- **플레이어 텔레그래프**(`lance.chargeSec` · `barrage.telegraphSec` · `nova.telegraphSec`)는 **정본 §7.12.6 소관**이다: 색 = 현재 스탠스색 / 외곽선 **금지** / 레이어 **3** / additive + `playerFxCompositeAlpha` 하드캡 / **자홍 금지**. **2상태 규칙과 `dashPx`는 그대로 이월되고 색만 진영을 따른다** → §3.2의 백스톱도 그대로 적용된다(형태 채널이므로 진영 무관).

### 3.4 활성 장판 — ★ 정본 §12.3이 닫았다 (02-A2 **채택**, blocker 해소)

v1.0의 모순: 정본 §7.4("지금 아픈 것 = 자홍 채움 + **불투명** + 검은 외곽선") vs 정본 §12.3(레이어 1 = "**알파 ≤0.35**") → **평가 불가.**

**정본 v1.1 §12.3의 확정 (인용)**:

| 레이어 1의 요소 | 확정 알파 |
|---|---|
| 띠의 **배경 판** | `visual.band.plateAlpha` = **0.30** |
| 띠의 **내용** (타이머·HP 세그먼트·XP 바·라벨·코어 HP 바) | **불투명** (`visual.band.contentOpaque = true`) |
| 지면 **텔레그래프** | 점선 외곽선 알파 1.0 + 내부 채움 `visual.zone.fillAlpha` **0.30** |
| **활성 장판** | **외곽선 불투명 + 검은 외곽선 + 내부 채움 0.30 + `visual.zone.pulseHz` 1.0 맥동** |

> **정본의 근거 (인용)**: *"「알파 ≤0.35」의 진짜 목적은 「지면이 게임플레이 오브젝트와 경쟁하지 않게」였다. 레이어 1은 모든 게임플레이 오브젝트 아래이므로 **불투명해도 아무것도 가릴 수 없다** — **I-4는 알파가 아니라 레이어 구조가 보장한다.** v1.0은 같은 목적을 두 장치(레이어 + 알파)로 중복 집행하다가 §7.4와 충돌했다."*

**이 섹션의 처리**: v1.0이 제안했던 `visual.zone.outlinePx: 2` · `outlineAlpha: 1.0`은 **삭제한다** — 외곽선 2px는 `palette.threat.outline`의 정의(정본 §7.2: "하드 외곽선 `#0A0C10` **(2px)**")가 이미 소유하고, 불투명은 정본 §12.3이 확정했다. **파생 가능한 값에 키를 만들면 두 값이 갈라진다.** 남는 것은 정본이 소유한 `fillAlpha` · `pulseHz` 둘뿐이다.

---

## 4. 상태이상 표시

### 4.1 정본이 이미 닫은 것

호박 `#FFB000` 단일 채널 · 3층 표(적 tell / 온-플레이어 / 화면 가장자리) + 전용층(둔화 잔상 3장 / 스턴 링 점멸) · 리드 0.80(slow) / 1.50(stun) + 가장자리 사전 펄스 · `visual.statusBulletSpeedMul = 0.6` · 저항 방패 pip · 저체력 1Hz/2Hz · 피격 vs 실드 흡수 · **`stunMinDifficulty` 미달 시 = 발사 스킵**(정본 §2.7, 04-R3). **전부 정본 §7.11 · §2.7.**

### 4.2 적 측 — 상시 표식을 **두지 않는다** (유지)

`stage.statusStunMaxPerStage = 2`이므로 "스턴을 쏘는 2기"를 미리 표시할 유인이 있다. **그러나 두지 않는다.**

| | |
|---|---|
| **왜** | 적 본체에 호박을 올리면 정본 §7.12.4의 5곳 목록 **밖**이다 = 정의상 **버그**. 그리고 호박이 적 **본체**에 나오면 "내 몸에 무슨 일이 일어난다"는 단일 문장이 소멸한다. |
| **왜 2** | 회전하는 이중 외곽선은 **엘리트가 이미 점유**했다(정본 §7.6). 재사용하면 두 개념이 같은 형태를 갖는다. |
| **대신 무엇이 보증하나** | `minStunTelegraphSec = 1.5` + **화면 가장자리 호박 1회 사전 펄스** + 육각 ×1.8 + **호박 테두리**(§7.12.4-⑤) + 흰 이중 링 4Hz + 속도 ×0.6 + 정본 **S13**(스턴 탄은 **보스 부위의 페이즈 3에만** 존재). **채널 6개**가 이미 붙어 있다. 7번째는 5곳 목록을 깨는 값만큼 벌지 못한다. |

### 4.3 ★ 호박 예약 — 정본 §7.12.4가 **5곳으로 확정**했다 (02-A4 **수정채택**)

**이 섹션의 v1.0은 "정확히 4곳"을 제안했고, 정본은 그것을 고쳐서 채택했다. 승복한다.**

> **정본의 판정 (인용)**: *"02는 「정확히 4곳」을 제안하면서 **자기가 쓰는 2곳(타이머·저체력 HP 바)을 뺐다.**"*

| # | 지점 | 위치 |
|---|---|---|
| **①** | **화면 가장자리 밴드** (둔화 1Hz / 스턴 4Hz / 저체력 1Hz→2Hz) — **HUD 저체력 HP 바 채움은 ①의 두 번째 표면** | 플레이필드 **밖** (레이어 13) |
| **②** | 온-플레이어 상태이상 배지 (∿ / ✳ + 잔여 바) | 기체 부착 (레이어 6) |
| **③** | 온-플레이어 저항 방패 pip | 기체 부착 |
| **④** | 상태이상 탄의 **텔레그래프 링** | 플레이필드 **안** |
| **⑤** | 상태이상 탄 **자체의 테두리** | 플레이필드 **안** |

**이 섹션이 승복한 3건**

| 무엇 | 정본의 처분 |
|---|---|
| **타이머 ≤60초 = 호박** (v1.0 §7.2) | ★ **삭제.** *"호박의 정의는 「내 상태」다. 타이머는 내 상태가 아니라 **외부 시계**다. 02의 논거는 **은유이지 채널이 아니다.** 그 은유를 허용하면 「코인이 부족한 것도 내 상태가 나쁜 것」이 되어 호박이 무한 확장된다. 그리고 타이머는 **플레이필드 안**이라 §7.2 문면 위반이다."* → §7.2.1이 정본 규격으로 재작성됨. |
| **저체력 HP 바 = 6번째 지점** | ★ **①에 흡수.** *"하나의 사건이 두 표면에 같은 주파수로 나타나는 것은 채널 확장이 아니라 **채널 강화**다."* → 이 섹션의 §7.2.2 하단 A 띠 규격은 **그대로 유효**하며, 지위만 "①의 두 번째 표면"으로 바뀐다. |
| **④⑤** | ★ **정본이 채택하고 명문화했다.** 02가 옳게 지적했다 — v1.0 정본의 §7.2는 **자기 §7.4를 위반하고 있었다.** ⑤ 덕에 `shape ↔ status ↔ 호박` **3중 동치**가 되고 **S14**가 강제한다. |

> **정본의 통합 문장 (인용)**: *"①~⑤ 전부가 **하나의 문장**이다 — 「호박 = 내 몸에 무슨 일이 일어난다/일어났다.」 탄 쪽 호박은 아직 일어나지 않은 내 상태의 **예고**이며 같은 문장의 **미래형**이다."*

### 4.4 저항 유틸의 상시 가시성 (유지)

정본 §7.11: "저항 유틸 보유·발동 → **온-플레이어 아이콘 옆** 호박 방패 pip 상시 표시". 문언상 **상태이상이 걸린 동안에만** 보인다(아이콘이 없으면 옆도 없다). → 평시에는 "내가 저항을 샀다"가 화면에 없다.

**이 섹션의 처리**: 우 패널 `유틸 요약` 행(§7.4-C)이 `저항 40%`를 상시 표시한다. **온-플레이어는 정본 규격 그대로 두고, 평시 가시성은 패널이 맡는다** — 영역이 다르므로 §7.12.4의 5곳 목록과 충돌하지 않는다(**호박은 패널에 쓰지 않는다**; 패널의 유틸 요약은 `palette.hud.textDim` 텍스트다).

---

## 5. 히트 피드백 — ×2 / ×1 / ×0.5

### 5.1 정본이 이미 닫은 것

3배율 표(적 반응 / 파티클 / 처치 FX / SFX / 숫자) · 임팩트 프리즈 0.04s ×1.12 · `markerPolicy: "maxOnly"` · `markerCooldownSecPerEntity: 0.25` · **잡몹 데미지 숫자 없음** · `numberTargets: ["boss","midboss","elite"]` · 0.15초 합산 · 레이어 11 · `caps.damageNumbers = 10` / `effectMarkers = 12`. **전부 정본 §7.7 · §12.1.**

### 5.2 렌더 규격 (정본이 형태만 말하고 치수를 안 준 것)

| 요소 | 규격 |
|---|---|
| **×0.5 회색 방패 호** | `palette.neutralGray`, **호 각도 90°**, 반경 = 개체 `radius + 4`, 두께 3px, **탄이 온 방향을 중심으로** 배치, 0.15초 후 소멸. 파티클 0(정본). |
| **×2 화이트-핫** | 개체 실루엣 전체를 속성색 → 백색으로 **1틱** 치환. 표시는 `render.fxMinRealMs = 50` 하한. |
| **×1 백색 플래시** | 실루엣 알파 가산 0.5, 1틱. |
| **숫자 배치** | 개체 실루엣 **밖 위쪽**, `numberDriftPx = 18` 상승(정본), 레이어 11. |
| **피격 귀속(부위)** | 맞은 부위 외곽선 0.1초 밝게 점멸(정본 §7.6) — ×0.5 방패 호와 **동시에** 뜬다. 둘은 다른 질문의 답이다(어디를 / 몇 배로). |

> **`palette.neutralGray`가 은색과 달라야 하는 이유 (정본 §19.1-B6 채택 근거 인용)**: *"×0.5는 「×1」이 아니라 **「튕겼다」**이므로 `element.normal`(은색)을 쓸 수 없다 — I-2."*

### 5.3 ★ 처치 FX 합성 하드캡 — 요청 채택, **근거는 정본이 갈아엎었다** (02-A3/B5 **수정채택**)

**정본의 보증**: 플레이어 FX(레이어 3~4)는 오프스크린 합성 후 `render.playerFxCompositeAlpha = 0.45`로 1회 합성 → "맥스 빌드에서 화면이 하얗게 날아가는 실패 모드가 **구조적으로 발생 불가**"(§12.2).

**구멍**: **처치 FX는 레이어 7이다.** 합성 하드캡의 대상이 아니었다. 걸린 것은 `particleMaxAlpha = 0.60` + `caps.particles = 400` + `particleMaxLifeSec = 0.35`뿐.

**정본 v1.1의 확정 (§9.4.2 · §12.3, 인용)**: **레이어 7도 오프스크린 합성 + `render.killFxCompositeAlpha = 0.55`.**

**★ 이 섹션의 v1.0 근거는 무효다 — 정본이 산술을 검산했고 틀렸다:**

| | v1.0의 주장 | **정본의 검산** |
|---|---|---|
| 위기 세션 처치 밀도 | "25초 동안 60회 = 화면이 하얗게 날아간다" | `crisisTotal 60 ÷ crisisDurationSec 25` = **2.4 kill/s**. × `particleMaxLifeSec 0.35` → **동시 처치 FX 기대값 = 0.84** |
| 판정 | — | ★ **10배 부풀려진 근거였다.** |

> **정본의 문장 (인용)**: *"**요청은 옳고 산술은 틀렸다 — 정본은 요청을 옳은 산술 위에 다시 세운다.**"*

**정본이 채택한 진짜 근거 (인용)**: **`nova` 광역이 서브웨이브 10기를 동시 처치하는 경우(= 10)** + `caps.particles = 400`이 400개를 동시에 허용한다는 것.

- **왜 그 근거가 더 강한가**: 02의 근거는 **평균**을 말했고 정본의 근거는 **최악**을 말한다. 하드캡은 평균이 아니라 최악을 위해 존재한다. 그리고 정본의 케이스는 실제로 도달 가능하다 — 정본 §8.10이 위기 세션의 정답 3개 중 하나로 **`nova`(캡스톤 정답)**를 지목했고, 서브웨이브 = **10기 × 6파**(§8.10)이므로 **`nova` 1발 = 서브웨이브 1파 전멸**이 설계된 장면이다. 그 순간 ×2 처치 FX("속성색 대형 파열 + 흰 링 확장") 10개가 **같은 프레임에** 뜬다.
- **효과**: 몇 기가 동시에 죽든 **처치 측 밝기 총량이 수학적으로 0.55를 넘지 못한다.** 파열의 **밀도는 그대로 보이고**(클라이맥스 보존) 밝기만 상한을 갖는다.
- **부수 효과**: 레이어 6(플레이어 기체)이 파열 아래에 있으므로, 합성 알파 0.55 덕에 **내 기체가 파티클에 삼켜지지 않는다.**
- **결정성**: 레이어 7은 core 바깥(정본 §10.1) → **시뮬 무영향.**
- **I-4는 원래부터 살아남는다** — 레이어 7 < 9이므로 적 탄은 위에 있고, **검은 하드 외곽선(I-1)이 백색 위에서 오히려 더 잘 보인다.**

---

## 6. 스탠스 판독

### 6.1 정본이 이미 닫은 것 (기체 측)

① 히트박스 코어 링(흰 r=4 + 스탠스색 링 2px, **레이어 10**) ② 슬롯 스트립(pip 7px × 4, 기체 하단 +14px, **레이어 6**) ③ 상태이상 배지(기체 위 −16px, 레이어 6) ④ 림 오라(알파 0.40) · 전환 3케이스 연출(정상 / 탈색 / 노말) · `visual.stance` 전 키. **전부 정본 §7.5.**

### 6.2 ★ 투자 pip = 슬롯 지도 (패널 측의 핵심)

정본 §4.2: `elementCapPerElement = 4` = **무기 슬롯 수**(`player.weaponSlots = 4`) (LOCKED).
정본 §11.1 유효성 필터: **"속성 레벨 ≥ 현재 무기 수 → 그 속성 카드 제외 — 죽은 투자 차단"** → `투자 레벨 N ≤ 무기 수`가 **항상 참**이다(무기는 절대 잃지 않으므로 단조 — 정본 §11.1: "무기 교체를 거부하는 이유").

**귀결**: 좌 패널의 4칸 투자 pip은 **4칸 무기 슬롯과 1:1이며, pip을 그대로 슬롯 지도로 읽을 수 있다.**

```
W 불  ▲  ●●○○      ← "불 스탠스를 켜면 슬롯 1·2가 불로 발사된다"를 그대로 읽는다
E 물  ◆  ●○○○      ← "물 스탠스를 켜면 슬롯 1만 물"
R 풀  ✚  ○○○○      ← "풀 스탠스는 전부 무속성" (= 탈색 링이 뜬다)
```

- **부여 미리보기를 따로 그리지 않는다.** 투자 pip과 정보가 **동형**이기 때문이다. 4행 × 4pip = **부여 행렬 전체가 상시 화면에 있다** — 키를 눌러보지 않고도 4개 스탠스의 결과를 전부 안다.
- **3상태 pip**: `●` 채움 = 부여됨 / `○` 빈 원 = 슬롯은 있으나 투자 없음 / **`⊘` 점선 원 = 그 슬롯 자체가 아직 없음**. 세 번째 상태가 초반(무기 1~2개)에 "칸이 왜 4개인가"를 말없이 가르친다.
- **혼동 방지**: 투자 = **무채색 도트**(●/○/⊘), 부여 = **속성 글리프 칩**(▲/◆/✚) 또는 은색 `무`. 같은 화면에 두 종류의 "칸"이 있지만 **형태 어휘가 다르다.**

### 6.3 1:1 대응 강제

| 표면 | 규칙 |
|---|---|
| 기체 스트립 pip *i* (레이어 6) | ↔ 좌 패널 무기 슬롯 *i*의 부여 배지 |
| 좌 패널 스탠스 행의 투자 pip | ↔ 그 스탠스를 켰을 때의 스트립 |
| 히트박스 링 색 (레이어 10) | ↔ 좌 패널 `▶` 마커가 가리키는 행 |
| **어긋나면** | **구현 버그**(I-2). 세 표면은 같은 하나의 상태를 읽는다. |

### 6.4 온보딩 훅의 구체화 (정본 §9.9의 3키)

| 정본 키 | 이 섹션의 규격 |
|---|---|
| `autoEquipFirstElement` | 속성 카드 확정 순간 → 그 스탠스 자동 장착 + **좌 패널 해당 행의 키캡 박스가 흰색 → 속성색으로 0.4초 전환** + 행 하이라이트 이동. 투자 0→1 최초 전이에서만. |
| `stanceHintPulse` + `stanceHintPulseStageMax: 1` | **좌 패널 키캡 박스**가 그 속성색으로 **1Hz 맥동**(알파 0.40 ↔ 1.0). 스테이지 1 전용, 이후 영구 OFF. |
| **타겟의 정의** | ★ **정본 §9.9.1이 소유한다** (02-B8 **채택**). 인용: **아레나 안에 살아있는 적 개체 수 기준 최빈(mode) 속성의 `counter` 키.** 세는 대상 = 잡몹·엘리트·새떼 (**중간보스·보스·부위 제외** — 이들은 개체 위 HP 바 + 부위 글리프가 이미 답을 말한다). 타이 → `Q W E R` 순. 최빈이 노말이거나 개체 0 → **맥동 없음.** 키 = `hud.stanceHintTargetsMajorityElement`(§9.4.1). |

> ★ **정본이 v1.0의 이 섹션보다 정밀했다**: 02는 "아레나 내 살아있는 적 개체 수 기준 최빈"이라고만 했고, **무엇을 세는지**(중간보스·보스·부위의 포함 여부)를 정하지 않았다. 정본이 그 구멍을 닫았다.

---

## 7. HUD 레이아웃

### 7.1 좌표 (정본 §1.1 · §1.2 인용)

```
x:  0 ─────── 350 ──────────────────── 930 ─────── 1280
    │ 좌 패널  │        아레나 580        │ 우 패널   │
    │   350    │   (고정 화면, 카메라 X)  │   350     │
y 0 ┌──────────┬─────────────────────────┬──────────┐
    │          │ 상단 띠      0~40        │          │
    │  빌드    │ 코어 HP 바  40~48        │  자원    │
    │          │ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │  상황    │
    │          │ 이동 영역 540×608@(370,56)│         │
    │          │ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │          │
    │          │ 하단A HP    672~696      │          │
    │          │ 하단B XP    696~720      │          │
720 └──────────┴─────────────────────────┴──────────┘
```
- 패널은 **아레나 rect를 침범하지 않는다**(정본 §1.1). 예외는 정확히 둘: 띠 3개(레이어 1) + 기체 부착 요소(레이어 6·10).
- **패널 = 레이어 12** (정본 §12.3). 아레나와 겹치지 않으므로 밀도 예산과 무관하며 **항상 100% 렌더된다.**
- 패널 내부 여백 `hud.panelPadPx = 16` → 콘텐츠 폭 **318** (좌: x 16~334 / 우: x 946~1264).
- 패널 배경 = `palette.hud.panelBg`, 구분선 1px = `palette.hud.panelRule`. **무채색 저채도** — 4속성색·자홍과 경쟁 금지.
- ★ **띠의 알파는 정본 §1.2 · §12.3 소유**: 배경 판 `visual.band.plateAlpha = 0.30`, **내용은 불투명**(`visual.band.contentOpaque = true`). 이 섹션의 v1.0 요청(A2)이 **채택**되어 정본이 이미 그렇게 적었다.

### 7.2 아레나 오버레이 띠 3개 — 내부 규격

#### 7.2.1 상단 띠 (y 0~48, x 350~930)

**★ 상단 띠는 두 행이다** (정본 §1.2): **y 0~40 = 타이머 행** / **y 40~48 = 코어 HP 바**(전폭 580).

| 요소 | 좌표 | 규격 |
|---|---|---|
| **보스 타이머** | 중앙 x 640, **y 0~30** | `hud.fontHeroPx = 40`, `textBaseline = "middle"` @ y 15 (숫자는 디센더가 없으므로 캡 29px가 y 0.5~29.5에 든다). **게임시간 `M:SS`, 항상 3:00 → 0:00**(정본 §1.2). |
| **★ 배타 서브행** | 중앙 x 640, **y 30~40** | `hud.fontSmallPx = 14`, `textBaseline = "middle"` @ y 35. **`시간 보너스 ✕`**(정본 §7.12.5) **또는** `[Shift] +30초` 힌트. 아래 배타 규칙. |
| **배속 배지** | 우 x 862~922, y 8~28 | `hud.fontBodyPx = 16`, `palette.hud.textDim`. `×1.5` — **라벨 없는 `×`는 배속 하나뿐**(정본 §11.3, 02-B9 채택). |
| **개체 이름** | 좌 x 358~, y 8~28 | `fontBodyPx`. 보스전 = 보스 이름 / 중간보스 생존 중 = 중간보스 이름 / 그 외 = 없음. |
| **잡몹 페이즈** | 중앙, 타이머와 같은 자리 | 페이즈 잔여 `2:00 → 0:00`, 같은 hero 폰트. **위기 예고 3초 동안만 `새떼 3 → 0`**(정본 §7.12.2), 예고가 끝나면 **페이즈 잔여로 복귀**한다 — `crisisSuspendsWaves`는 웨이브만 멈추고 페이즈 클럭은 계속 흐르므로(95 + 25 = 120 = `mobPhaseSec`) 잔여를 계속 보여주는 것이 **참**이다(I-2). |
| **★ 코어 HP 바** | y 40~48, x 350~930 (580×8) | **정본 §1.2 소유 — 아래는 인용.** |

**그리기 순서** (전부 레이어 1): 배경 판(알파 0.30) → 타이머 → 배타 서브행 → 코어 HP 바.
- 텍스트 외곽선(`visual.text.outlinePx = 2`)이 캡 박스 밖으로 1px 번지므로 세 행이 **각 1px 접한다.** 정보 손실 0 — 나중에 그리는 것이 위에 오고, 세 요소는 **서로의 정보를 지우지 않는다**(검은 외곽선 위에 흰 바). 그리고 전부 레이어 1이므로 **I-4와 무관**하다.

**★ 타이머 3단 — 정본 §7.12.1 소유 (02 기각, 05 채택). 승복한다.**

| 상태 | 조건 | 색 | 연출 | 오디오 |
|---|---|---|---|---|
| 정상 | 잔여 > 60 | `palette.element.normal` (은색) | — | — |
| **경고** | 잔여 ≤ `timerWarnSec` 60 | ★ **색 불변 (은색)** | `visual.timer.warnScale` **1.15** + `warnPulseHz` **0.5** | — |
| **빨간불** | 잔여 ≤ `timerRedAlertSec` 30 | ★ **`palette.threat.enemyBullet` (자홍)** | `alertScale` **1.30** + `alertPulseHz` **1.0** | 1Hz 틱(§7.10) |

> **이 섹션이 승복한 것**: v1.0은 **경고 60초 = 호박**을 제안했다. 정본의 기각 근거를 그대로 인용한다 — *"두 섹션 모두 「새 색 0개」를 표방했으나 **02는 채널을 하나 늘렸다.** 호박의 정의는 「내 상태」다. 타이머는 **외부 시계**다. (…) 그리고 **위치가 결정적이다** — 타이머는 아레나 오버레이 띠 = **플레이필드 안**이고, §7.2의 예약은 「호박은 **플레이필드 밖** + 온-플레이어 아이콘에만」이다. **02의 안은 문면 위반이다.** (…) **60초에 색이 필요 없다** — 크기(1.15)·맥동(0.5Hz)·그리고 30초에 오는 것이 이미 2단 경고다."*
> **02의 v1.0 주장 중 살아남은 것 하나**: **자홍 = 빨간불** (02-A5 **채택**). 정본 인용 — *"`timerExpire = "kill"` → 자홍(「이것이 나를 죽인다」)이 **은유가 아니라 참**이다. 이 게임에 빨강은 없다."* **v1.0 §11의 `visual.timer.*` 신설 요청은 05가 냈고 정본이 05를 채택했다 — 이 섹션은 인용만 한다.**

**★ 배타 서브행 (y 30~40) — 정본이 배타를 선언했고, 이 섹션이 우선순위를 준다**

정본 §7.12.5: *"타이머 아래 y 30~40은 **빨간불 시 `[Shift] +30초` 힌트와 같은 자리**이며 **배타적이다**(토큰을 쓰면 힌트가 사라지고 ✕가 온다 — 같은 픽셀, 같은 의미축)."*

| 조건 | 표시 |
|---|---|
| `시간 보너스 ✕`가 점등된 상태 | ★ **`시간 보너스 ✕` (`fontSmallPx`, `palette.neutralGray`)** — 힌트를 그리지 않는다 |
| ✕ 미점등 ∧ 빨간불 ∧ 토큰 스톡 > 0 | `[Shift] +30초` (`fontSmallPx`, `palette.hud.textDim`) |
| 그 외 | 표시 없음 |

- ★ **왜 ✕가 이긴다**: 정본의 "배타"는 **토큰을 두 번 쓸 수 있다**(`timeTokenStockMax = 2`)는 사실 위에서 무너질 수 있다 — 첫 토큰으로 잔여가 60이 되고, 다시 30 아래로 내려오면 **✕가 잔류한 채 힌트 조건이 재성립**한다. 이 섹션이 그 케이스에 우선순위를 준다: **✕ 우선.** 근거 = ① 힌트의 목적은 *"Shift가 지금 유효하다"*를 **가르치는 것**인데, ✕가 떠 있다는 것은 **플레이어가 이미 토큰을 써서 그것을 배웠다**는 뜻이다 ② 대가(`timeTokenForfeitsTimeBonus`, LOCKED)는 **이미 확정**되었으므로 ✕가 더 중요한 정보다 ③ **두 번째 토큰의 가용성은 우 패널 C 블록의 토큰 키캡 활성화**(`hud.tokenKeycapGatedDisplay`)가 **여전히 말한다** → **정보 손실 0.** 새 키 0, 새 색 0.
- `시간 보너스 ✕`는 **토큰 사용 순간 점등 → 그 보스전 내내 잔류 → 스테이지 클리어 시 소멸**(정본 §7.12.5).

**★ 토큰 게이팅의 가시화 (02 §7.2 → `hud.tokenKeycapGatedDisplay`, 정본 §9.4.1 **채택**)**: 정본 §11.4는 "빨간불일 때만 유효, 그 외엔 **입력 무시**"라고만 한다. **입력이 무시된다는 것을 알리는 채널이 없었다.** → 힌트 + 우 패널 키캡 명도(§7.4-C). **"왜 Shift가 안 먹지"가 발생 불가.**

##### 코어 HP 바 (y 40~48) — ★ 정본 §1.2 소유 (02-A6 **채택**)

이 섹션의 v1.0이 "정본에 자리가 없다"고 지적했고, **정본이 자리를 만들어 §1.2에 넣었다.** 이제 인용한다.

| | 정본 §1.2의 확정 |
|---|---|
| 자리 | **y 40~48, x 350~930 (580×8)**, `hud.bossHpBarH = 8` |
| 무엇의 HP | **코어 HP만.** 부위 HP는 부위 위 소형 바(§7.6)가 맡는다 |
| 색 | 그 개체의 **속성색**. 스테이지 보스 코어 = 항상 노말(R1) → **은색**. 중간보스 = `notThemeAndNotNormal` → 불/물/풀 중 하나 → **바 색이 곧 "무슨 스탠스로 때려야 하나"** |
| 페이즈 노치 | `boss.phaseThresholds`(0.6 / 0.3) 위치에 **높이 4px 흰 눈금 2개** |
| 파 고스트 마커 | **높이 8px 흰 세로선 2px + 상단 3px 사각 캡**. 위치 = **정본 §11.5의 사상식**. `flow.stagePar[i] == 0`이면 **숨김**(미측정 센티넬) |
| 중첩 불가 | 중간보스 100초 이탈 < `mobPhaseSec` 120 → **두 사용자가 같은 8px를 다툴 수 없다** |
| 없을 때 | **바 자체를 그리지 않는다** (빈 트랙 금지) |

파 고스트의 사상식 (정본 §11.5 인용, **재정의 금지**):
```
ghostHpPct(t) = clamp( 1 − t / (stage.bossTimerSec − flow.stagePar[i]) , 0, 1 )
                                                  // t = 보스전 경과 게임초
```
> **정본이 이 섹션보다 정밀했던 점**: v1.0의 02는 같은 식을 `parRatio(t)`로 적었으나 **`stagePar[i] == 0` 센티넬을 다루지 않았다.** `flow.stagePar`의 초기값이 `[0,0,0,0,0,0]`이므로 그대로면 **시뮬 인증 전에 분모가 180이 되어 「거짓 파」를 그린다.** 정본(05-⑥ 채택)은 **숨김**으로 닫았다 — *"거짓 정보를 표시하지 않는다."*

**★ 페이즈 노치의 값어치** (이 섹션이 남기는 근거): 무적 전환(= 공짜 숨돌릴 틈, `phaseTransitionSec 1.5` × 2, `timerPausesOnPhaseTransition`)이 **언제 오는지 미리 보인다** = 정보 기반 선택. 노치(짧음 4px·고정)와 파 마커(8px·이동·캡)는 **형태·운동이 모두 다르다.**

#### 7.2.2 하단 A 띠 (y 672~696) — HP

| 요소 | 좌표 | 규격 |
|---|---|---|
| **HP 세그먼트 바** | x 358~838 (480), y 676~692 (16) | **`hud.hpBarSegCount = 5`칸 × 92px, 간격 `hud.hpBarSegGapPx = 5`** (5×92 + 4×5 = 480 ✔). 칸당 `hpMax / 5`. 부분 감소는 **칸 내부가 좌→우로** 깎인다. |
| 채움 색 | | `palette.hud.hpFill`. **`hp/hpMax ≤ player.lowHpThreshold(0.30)` → `palette.status.band`(호박) + 1Hz** / **≤ `lowHpCriticalThreshold`(0.15) → 2Hz**. 정본 §7.11 ③의 가장자리 밴드와 **같은 주파수로 동기** — 정본 §7.12.4가 이것을 **호박 ①의 두 번째 표면**으로 확정했다(*"하나의 사건이 두 표면에 같은 주파수로 나타나는 것은 채널 확장이 아니라 채널 강화"*). |
| 수치 | 우정렬 x 922, y 678~692 | `fontBodyPx = 16`. `86/100`. **`maxhp` 구매 시 분모가 커진다** — **칸 수는 5 고정.** |

> **칸 수 고정의 근거 = 정본 §9.4.1이 소유한다** (인용): *"`hpBarSegCount: 5` (고정) — `maxhp` 구매로 `hpMax`가 140까지 커져도 **칸 수는 5**, 칸당 = `hpMax/5`. 칸 수가 변하면 「칸 하나 = 몇 대」의 감각이 **구매마다 리셋**된다. 절대값은 수치(`86/140`)가 나른다."* — v1.0의 02가 같은 결론을 냈고 **정본이 키로 못박았다.** 이 섹션은 인용한다.

#### 7.2.3 하단 B 띠 (y 696~720) — XP

| 요소 | 좌표 | 규격 |
|---|---|---|
| `Lv.23` | x 358~412, y 700~716 | `fontBodyPx = 16` |
| **XP 바** | x 420~840 (420), y 705~711 (`hud.xpBarH = 6`) | 채움 = **`palette.pickup.xp`** — **XP 오브의 색이 그대로 바가 된다**(줍는 것 = 차는 것). |
| **`LV UP ×2` 배지** | 우정렬 x 922, y 700~716 | `fontSmallPx = 14`, 흰색 **1Hz 맥동**. 정본 §6.4의 "위기 세션 중 큐 적립 → HUD에 `LV UP ×N` 배지"의 자리이며 **정본 §1.2가 이 띠에 명기**했다. 레이어 1이므로 I-4 안전. |

### 7.3 좌 패널 (x 0~350) — 빌드

| 블록 | y | 내용 |
|---|---|---|
| **A** | 16~200 | 스탠스 · 속성 투자 |
| **B** | 216~432 | 무기 슬롯 4칸 |
| **C** | 448~604 | 패시브 6칸 |
| **D** | 620~712 | **상성표 4×4 (상시)** |

#### A. 스탠스 · 속성 투자 (y 16~200)

```
스탠스                              투자 4 / 6     ← 헤더 y16~40, hud.showElementBudget
 [Q] 노말  ●   — 전부 ×1                          ← 행 h38
▶[W] 불    ▲   ●●○○                              ← ▶ + 행 배경 속성색 alpha 0.15
 [E] 물    ◆   ●○○○
 [R] 풀    ✚   ⊘⊘⊘⊘                              ← 무기가 2개뿐이면 3·4칸은 점선
```

| 요소 | x | 규격 |
|---|---|---|
| `▶` 현재 마커 | 4~14 | `fontMediumPx = 20`, 속성색 |
| **키캡 박스** | 18~46 | `hud.keycapBoxPx = 28`, 테두리 1px, 글자 `fontMediumPx = 20` 중앙. **`stanceHintPulse`·`autoEquipFirstElement`의 대상**(§6.4) |
| 속성명 | 54~ | `fontMediumPx = 20`, `palette.hud.textPrimary`. **텍스트가 상시 존재**하므로 `cvd`의 "텍스트 라벨 강제"가 이미 충족 |
| 글리프 | 106~124 | 18px, 속성색 |
| **투자 pip 4** | 140~190 | 11px × 4, 간격 2. **3상태**(§6.2): `●` 부여 / `○` 미투자 / `⊘` 슬롯 없음 |
| 노말 행 | 140~ | pip 대신 **`— 전부 ×1`** (`fontSmallPx = 14`, textDim). **노말은 투자축이 아니다**(정본 §4.2) |
| `투자 4 / 6` | 우정렬 334 | `fontBodyPx = 16`. `player.elementCapTotal = 6`. **`4/2/0` vs `2/2/2` 다이얼이 암산이 아니게 되는 지점**(정본 §15-m1, `hud.showElementBudget`) |

- **행 순서 = `Q W E R` 고정** (정본 §4.1: "전 문서·전 UI·전 카드·전 패널이 이 순서"). 정렬·재배치 금지.

#### B. 무기 슬롯 4칸 (y 216~432)

```
무기                                              ← 헤더 y216~240
┌────────────────────────────────────┐
│ 1  벌컨                      [▲ 불]│  ← 행 h46
│    Lv5                             │
├────────────────────────────────────┤
│ 2  펄스필드                  [▲ 불]│
│    Lv3                             │
╔════════════════════════════════════╗
║ 3  시커                       [ 무 ]║  ← 은색 무 = ×1 (I-2)
║    스웜                            ║  ← 진화: 이중 테두리 + 진화명 (글리프 없음)
╚════════════════════════════════════╝
│ 4  — 빈 칸                          │  ← 점선 테두리
└────────────────────────────────────┘
```

| 요소 | x | 규격 |
|---|---|---|
| **슬롯 번호** | 16~40 | **`fontLargePx = 26`** — 번호가 **부여 우선순위**이므로 패널에서 두 번째로 큰 글자다 |
| 이름 | 48~ | `fontMediumPx = 20` (행 상단) |
| 레벨 | 48~ | `fontSmallPx = 14`, textDim (행 하단). `Lv5` / 진화 시 **진화명**(예: `스웜`) |
| **부여 배지** | 286~326 (40×28) | 부여됨 = **속성색 채움 + 글리프 18px** / 미부여 = **은색 테두리 + `무`**(`fontSmallPx`). 기체 스트립 pip *i*와 **1:1**(§6.3) |
| **진화 표식** | 행 테두리 | **이중선(2px + 1px, 간격 2px)**. 금색을 쓰지 않는 이유: 코인 금(`palette.pickup.coin`)과 의미 충돌. 이중선은 **형태 채널**이므로 색맹 안전. 엘리트의 "회전하는 이중 외곽선"과 영역이 다르다(패널 vs 플레이필드) — 그리고 **패널의 것은 회전하지 않는다.** ★ **v1.0의 `◈◈` 마크는 삭제**(§2.3: ◆ 예약과 형태 근접) |
| 빈 슬롯 | | 점선 테두리 + `— 빈 칸`(textDim). **`draft.newWeaponSlotScale = [3.0, 1.5, 1.0]`**이 초반에 강하게 채워주므로 이 상태는 짧다 |

- **무기 아이콘은 존재하지 않는다.** 정본 §9.10: "무기별 별도 아트 **0**". 슬롯 행은 번호·이름·레벨·부여 배지로 구성한다. 아이콘을 발명하면 아트 방침을 어긴다.

#### C. 패시브 6칸 (y 448~604)

- 헤더 y 448~472: `패시브`
- **2열 × 3행**, 셀 155×40, 간격 x 8 / y 4 → `2×155 + 8 = 318` ✔
- 셀 내부: 이름 `fontBodyPx = 16`(좌) + **레벨 pip 5개**(6px, 간격 3 = 42px, 우). 빈 칸 = 점선 + `—`
- **6칸이다** (`player.passiveSlots = 6`, 정본 §9.4). `maxLevel = 5` → pip 5개.
- 아이콘 없음(무기와 동일 근거). 패시브 이름 최장 = `노획 프로토콜`(6자 × 16px ≈ 96px) < 셀 내 텍스트 폭 107px ✔

#### D. ★ 상성표 4×4 — 상시 (y 620~712) — `hud.elementMatrixInPanel` (02-B12 **채택**)

정본 §6.6은 상성표를 **PAUSE**에 두었다. 이 섹션은 **좌 패널 하단에도** 둔다. 정본 §19.1-B12 인용: *"PAUSE 판본과 **중복이며 충돌 아님.** 온보딩 비용 0의 원칙을 인게임으로 확장."*

```
        ●노말  ▲불   ◆물   ✚풀        ← 열 = 적 속성
 [Q]     ×1    ×1    ×1    ×1
▶[W]     ×1    ×1    ×½    ×2         ← 현재 스탠스 행 하이라이트
 [E]     ×1    ×2    ×1    ×½
 [R]     ×1    ×½    ×2    ×1
```

| | |
|---|---|
| 크기 | 5열 × 60px = 300 (x 22~322) / 5행 × 18px = 90 (y 620~710) |
| 셀 | `fontSmallPx = 14`. **×2 = 그 스탠스 색 + 굵게 / ×1 = textDim / ×½ = `palette.neutralGray`** |
| 행 헤더 | 키캡 글자 (`Q`/`W`/`E`/`R`) — **§7.3-A의 키캡과 같은 어휘** |
| 열 헤더 | 적 속성 글리프 + 이름 (`cvd`의 텍스트 라벨 강제를 상시 충족) |
| 현재 스탠스 행 | `▶` + 배경 속성색 알파 0.15 — **A 블록과 동기** |
| **값어치** | ① 정본 §6.6의 근거("상성표 상시 비치 = 온보딩 비용 0")가 **일시정지 없이** 성립한다 ② 현재 스탠스 행 하이라이트 = **"내가 지금 각 적 속성에 몇 배인가"가 한 줄로 읽힌다** = 정보 루프의 폐쇄 ③ 심사자 30초 테스트가 정지 없이 통과 ④ 좌 패널 92px는 다른 용도가 없었다 ⑤ 정적 표이므로 체스식 최적화를 만들지 않는다 |

### 7.4 우 패널 (x 930~1280) — 자원 · 상황

| 블록 | y | 내용 |
|---|---|---|
| **A** | 16~82 | SCORE / BEST |
| **B** | 102~132 | 코인 (+ 점수 환산) |
| **C** | 152~366 | 소지품 · 유틸 |
| **D** | 386~472 | 스테이지 · 테마 · 혼합 |
| **E** | 492~528 | ★ **무피격 pip 6칸 + 퍼펙트 ✕** (정본 §7.12.5) |
| **F** | 546~586 | 난이도 · 시드 |
| — | 586~720 | **의도적 여백** |

#### A. 점수 (y 16~82)
- `SCORE` (`fontSmallPx`, textDim) y 16~30 / 값 (`fontLargePx = 26`, 우정렬 x 1264) y 32~62
- `BEST` + 값 한 줄 (`fontBodyPx = 16`) y 68~82. **갱신 중이면 값 옆 `NEW` 텍스트 배지** — 금색을 쓰지 않는다(코인과 충돌). 텍스트 채널이므로 색맹 안전.
- 구분선 y 94

#### B. 코인 (y 102~132) — `hud.coinShowsScoreValue` (02-B10 **채택**)
- `coinDisc` 아이콘 20px (x 946) + 수량 (`fontLargePx = 26`, x 974) + **`= 2,740 점`** (`fontSmallPx`, textDim, 우정렬 x 1264)
- 정본 §19.1-B10 인용: *"§11.2는 `= −N 점` 환산을 **상점에만** 요구했다 → 코인을 **버는** 순간에 환산이 없으면 「아껴서 점수 vs 써서 생존」 긴장이 **상점 앞 5회에만** 존재한다. **규칙 변경 0, 새 데이터 0.**"* (`score.coinToScore = 20`, 정본 §11.3)
- 구분선 y 144

#### C. 소지품 · 유틸 (y 152~366)
- 헤더 y 152~166: `소지품`
- **4행 × 42px** (y 170~338):

| 행 | 아이콘 | 키캡 | pip | 상한(정본) |
|---|---|---|---|---|
| 폭탄 | `bombRound` | `[Space]` | ●●○ | `bomb.stockMax = 3` |
| 실드 | `shieldHex` | — (수동 발동 아님) | ●○ | `shop.shieldStockMax = 2` |
| 시간 토큰 | `tokenClock` | `[Shift]` | ●○ | `timeTokenStockMax = 2` |
| 리롤 | `rerollArrows` | `[F]` | ●●● | `rerollStockMax = 3` |

- ★ **`hud.tokenKeycapGatedDisplay = true` — 키캡은 지금 유효할 때만 밝다** (정본 §9.4.1 **채택**).
  - 시간 토큰 `[Shift]`: **빨간불(잔여 ≤ `timerRedAlertSec` 30)에서만 활성.** 그 외엔 키캡 dim + 행 우측에 `빨간불에서만`(`fontSmallPx`, textDim).
  - 리롤 `[F]`: **드래프트 화면에서만 활성**(정본 §5.1: 인게임의 `F`는 **죽은 키**). 인게임에선 dim.
  - 폭탄 `[Space]`: 스톡 0이거나 발동 차단 구간(드래프트/상점/일시정지/보스 인트로/DEATH, 정본 §11.5)이면 dim.
  - **왜**: 정본 인용 — *"§11.4는 「빨간불일 때만 유효, 그 외 **입력 무시**」라고만 했다 → **입력이 무시된다는 것을 알리는 채널이 없었다.** 「왜 Shift가 안 먹지」가 발생 불가해진다."*
  - **실드 행에는 키캡이 없다** — 자동이기 때문이다. 키캡의 유무 자체가 "이건 내가 누르는 것 / 이건 알아서 되는 것"을 가른다.
- **유틸 요약 1행** (y 346~366, `fontSmallPx`, textDim): `방어 6 · 저항 40% · 이속 +12% · 자석 +60%` — **구매한 항목만** 표시, 없으면 행 생략.
  - 근거: 정본 §7.8은 자석 반경을 "상점에서 사는 것이 **보여야 한다**"고 명시했다(플레이필드의 은색 점선 원). 같은 논리가 `defense`·`resist`·`movespeed`에도 적용되는데, 이들은 **볼 수 있는 표면이 없었다.** §4.4의 저항 평시 가시성도 여기서 닫힌다. (상한: 방어 8 / 저항 −60% / 이속 +18% / 자석 +90% — 정본 §11.2)
- 구분선 y 378

#### D. 스테이지 · 테마 (y 386~472)
- `STAGE 3 / 6` (`fontMediumPx = 20`) y 386~406
- 테마 `화산 ▲ 불` (`fontMediumPx = 20`, 글리프는 속성색) y 406~430
- **혼합 바** (318 × 12px) y 438~450 — ★ **`Q W E R` 순서 고정**

```
│ 무 ~10 │███████ 불 ~70 ███████│ 물 ~10 │ 풀 ~10 │
   ↑ 크기순 정렬 금지. 노말 → 불 → 물 → 풀 순서 고정 (정본 §4.1)
```
  - 세그먼트 색 = 속성색, 각 세그먼트에 글리프. 라벨 행 y 456~472: `무~10 · 불~70 · 물~10 · 풀~10` (`fontSmallPx`)
  - ★ **물결표(`~`)는 필수다 — 정본 §8.2.1 (사용자 확정 2).** v1.0의 이 섹션은 *"4개 수치를 **그대로** 표시한다(정본 §8.2: 「대략이 아님」)"*고 적었다. **그 문장은 폐기되었다.**
  - **정본 §8.2.1의 확정 (인용)**: S8 검사 대상 = **저작 리스트**(`stages[].waves[]` 전체의 개체 수 기준, 엘리트·중간보스·새떼·보스 제외), 허용 오차 ±3%p. **배너·바 표기 = `물 ~70% · 불 ~10% · 풀 ~10% · 노말 ~10%`.** 실제 조우와의 괴리 **≤ 5%p 허용(검사하지 않음)**.
  - **왜 그것이 옳은가 (정본 인용)**: *"v1.0의 「그대로 표시(대략이 아님)」는 **지킬 수 없는 약속**이었다. `waveClearAdvance`가 남은 스케줄 전체를 압축하므로 **조우하는 웨이브 집합이 플레이 스타일의 함수**다. `70%`라고 쓰면 75%를 만난 플레이어가 **배신당한다.** `~70%`라고 쓰면 **약속의 정밀도가 약속의 이행 가능성과 일치**한다. (…) 정보 기반 선택 기둥이 요구하는 것은 **소수점의 진실이 아니라 「무엇이 다수인가」의 진실**이고, 그것은 어떤 파밍 스타일에서도 참이다."* 최악 편차 = **5.0%p**(`sea` s1 · 소극 파밍 11웨이브 순환 = 75.0/8.3/8.3/8.3).
  - `finale`: `테마 없음 — 4속성 혼재` + `무~10 · 불~30 · 물~30 · 풀~30` (정본 §8.16)
  - **왜 패널에도 두는가**: 테마 배너는 `flow.themeBannerSec`(스킵 가능)면 사라진다. 정보 기반 선택 기둥은 **스테이지 내내** 그 정보를 요구한다.
- 구분선 y 484

#### E. ★ 무피격 pip 6칸 + 퍼펙트 ✕ (y 492~528) — 정본 §7.12.5 소유

**이 섹션의 v1.0은 텍스트 1행(`무피격 ✔ 진행 중 · 런 2/2`)으로 구현했고, 정본이 그것을 기각했다. 승복한다.**

> **정본의 기각 근거 (인용)**: *"① 05가 계약으로 요구한 것은 **6칸**이고 그 이유가 명확하다 — 「**컨티뉴의 대가(런 전체 소급 무효)를 그 자리에서 6칸이 동시에 꺼지는 것으로 보여준다.**」 텍스트 1행은 그 장면을 만들지 못한다 ② `noHitScope = "perStage"`이므로 **스테이지별 상태가 6개 있다** — 1행 텍스트는 그것을 표현할 수 없다."*

**정본이 준 형식 (인용)** — 자리 = 우 패널 `hud.noHitRow`, **아이콘 6개 가로 1행**:

| 상태 | 표시 |
|---|---|
| **진행 중** | 은색 외곽선 **빈 원** |
| **성립** | 은색 **채움** |
| **깨짐** | `palette.neutralGray` **✕** |
| **컨티뉴 시** | ★ **6칸 전부 즉시 ✕** (런 전체 소급 무효, 정본 §11.4) — *그 자리에서 대가가 보인다* |

**이 섹션이 주는 좌표** (§17 위임 = 패널 내부 레이아웃):

| 요소 | 좌표 | 규격 |
|---|---|---|
| 헤더 `무피격` | x 946~, y 492~506 | `fontSmallPx = 14`, textDim |
| **pip 6칸** | x 946~1106, y 508~528 | **20px × 6, 간격 8** (6×20 + 5×8 = 160 ✔). **좌→우 = 스테이지 1~6** |
| **`퍼펙트 ✕`** | 우정렬 x 1264, y 508~528 | `fontSmallPx`, `palette.neutralGray`. **평시 표시 없음.** `perfectBonus` 상실 확정 시(= pip 하나라도 ✕ **또는** 컨티뉴) 점등, **런 끝까지 잔류**(정본 §7.12.5) |

- ★ **미도달 스테이지 = 「진행 중」과 같은 표시** (은색 외곽선 빈 원). **4번째 상태를 만들지 않는다.** 근거: ① 정본이 상태를 **정확히 3개**로 확정했다 ② 미도달과 진행 중은 **"아직 3000점이 살아있다"**는 **같은 사실**을 말한다 ③ **현재 스테이지는 D 블록의 `STAGE 3 / 6`이 이미 말한다** → 4번째 상태는 정보를 더하지 않고 어휘만 늘린다.
- ★ **좌→우 = 순서**는 **기체 슬롯 스트립과 같은 어휘**다(정본 §7.5: "좌→우 = 슬롯 1~4"). 번호를 인쇄하지 않아도 읽힌다.
- `✔`/`✕`는 **속성 글리프가 아니다**(§2.3 예약 준수). `shieldPreservesNoHit = true`(정본 §11.3)이므로 **실드 흡수는 pip을 끄지 않는다.**
- 근거 (정본 §19.1-B11 인용): *"`stageNoHitBonus 3000` / `perfectBonus 30000`이 살아있는지 **화면에 없으면 플레이에 영향을 줄 수 없다.**"*
- 구분선 y 538

#### F. 난이도 · 시드 (y 546~586)
- ★ **`NORMAL · 점수 ×1.0`** (`fontBodyPx = 16`) y 546~566
  - **표기 규칙 = 정본 §11.3 소유** (02-B9 **채택**, 인용): *"`difficulty[].speed`(1.5)와 `scoreMul`(1.6)은 **Hard에서 다른 수**다. **규칙: 라벨 없는 `×N`은 배속 하나뿐(상단 띠의 배속 배지). 점수 배율은 반드시 `점수 ×N`으로 라벨한다.**"* (배율: Normal 1.0 / Hard 1.6 / Hell 2.5 / Disaster 4.0 — 배속: 1.0 / 1.5 / 2.0 / 3.0)
- `SEED 3F2A9C41` (`fontSmallPx`, textDim) y 572~586 — 정본 §2.6의 8자리 hex. 결과 화면 전에도 보이면 버그 리포트 비용이 0이 된다.

#### 여백 (y 586~720)
**의도적으로 비운다.** 정보를 발명해 채우지 않는다. 패널은 `hud.panelCacheDirtyOnly`로 값 변경 시에만 갱신되므로 여백의 비용은 0이며, **여백이 위 6블록의 판독 속도를 만든다.**

### 7.5 오버레이 화면 (좌표만 — 내용은 정본 소관)

정본이 카드·항목의 **필수 표기**를 이미 확정했다(§11.1 드래프트 / §11.2 상점 / §11.4 DEATH·RESULTS / §6.6 PAUSE). 이 섹션은 **어디에 놓는가**만 정한다. **화면의 내용·전이·정산 순서는 05 소관**(정본 §17)이다.

| 화면 | 레이아웃 |
|---|---|
| **DRAFT** | 아레나 위에 전체 화면 딤(`palette.hud.panelBg` 알파 0.85). **카드 3장 = 300×420, 간격 40, 중앙 정렬** (3×300 + 2×40 = 980, x 150~1130). 카드 상단에 `1`/`2`/`3` 키캡(`hud.keycapBoxPx`) = **즉시 선택**(정본 §5.2). 하단 바 y 620~700: `[← →] 커서 · [Enter] 확정 · [F] 리롤 2/2 · [Tab] 슬롯 재정렬`. **`Space`는 표시하지 않는다**(정본 §5.2: 드래프트에서 **죽은 키** — *"인게임에서 폭탄이므로 드래프트가 뜬 순간 Space 연타 = 카드 즉시 오선택"*). 게임 클럭 **완전 정지**(정본 §11.1). |
| **슬롯 재정렬 모드** | DRAFT 하단에 4칸 스트립(각 140×88, 간격 12, 중앙). **좌 패널 B 블록의 확대판이며 같은 어휘**(번호 크게 + 이름 + 부여 배지). ★ **집은 슬롯 = 8px 부상 + 그림자 + 현재 스탠스색 배경 알파 0.25.** (v1.0의 "속성색 **이중 테두리**"는 폐기 — B 블록의 **진화 표식**이 이미 이중선이며 같은 스트립에 공존한다.) |
| **SHOP** | 세로 목록 10행(정본 §11.2의 10종), 각 행 720×56, x 280~1000, y 100~660. 행: 아이콘 · 이름 · 효과 1줄 · `2/3` · 현재가 · 다음가 · **`= −N 점`**. 우상단 보유 코인 + 환산. 커서 `↑↓`, `[Enter] 구매`, `[Esc] 나가기(확인 1회)`. **시간 제한 없음**(정본). |
| **PAUSE** | 좌 = 현재 빌드(좌 패널 A·B·C 블록 **확대 복제**), 우 = **상성표 4×4 확대** + 키캡 그림. 조회 전용. `[O] 옵션` · `[Esc] 재개(1.5초)`. **좌 패널과 같은 어휘를 쓰므로 새 UI가 0이다.** **`포기` 항목 없음**(`flow.pauseAllowsAbandon = false`, 정본 §5.5 — *"누락이 아니라 결정"*). |
| **DEATH** | 상단 = **사인 아이콘 + 1단어**(`체력 소진` / `시간 초과`). 중단 = **스테이지별 보스 잔여시간 vs 파 막대 6개**(§7.2.1과 같은 파 문법). 하단 = 컨티뉴 제시(150코인, 부족하면 회색 + 가격) / `포기 → RESULTS`. **카운트다운 없음**(정본 §11.4). `flow.edgeTriggerOnStateEnter`(정본 §5.5)에 의해 **진입 프레임에 눌려 있던 `Space`는 무시**된다. |
| **RESULTS** | 항목별 순차 타이핑(`Space` 즉시 완료) + **놓친 보너스 회색 병기** + 파 막대 6개 + 난이도별 최고점 비교 + 시드. `[Space] 같은 난이도 재시작` / `[Esc] 타이틀`. |

### 7.6 폰트 · 아이콘

**폰트 사다리 = `rules.json > hud` (정본 §9.4.1 소유 — 인용)**

| 키 | px | 용도 |
|---|---|---|
| `hud.fontHeroPx` | **40** | 보스 타이머 / 페이즈 잔여 (화면에서 가장 큰 글자 = 가장 중요한 시계) |
| `hud.fontLargePx` | **26** | SCORE 값 · 코인 수량 · **무기 슬롯 번호** |
| `hud.fontMediumPx` | **20** | 속성명 · 무기명 · 키캡 글자 · STAGE · 테마명 |
| `hud.fontBodyPx` | **16** | 라벨 · 수치 · BEST · 난이도 · HP 수치 · Lv |
| `hud.fontSmallPx` | **14** | 보조 · 환산 · 유틸 요약 · 상성표 셀 · 무피격 헤더 · 배타 서브행 — **`visual.text.minPx`(14)와 동일. 이보다 작은 텍스트는 존재하지 않는다** |

- 전 텍스트 `strokeText`(검은 2px, `visual.text.outlineColor`) → `fillText` 순서(정본 §7.9). **패널 위 텍스트도 예외 없다** — 규칙 분기가 늘지 않는다.
- ★ **초안 A의 `hud.minFontPx: 12`는 폐기한다** — 정본 `visual.text.minPx = 14` 위반이다.
- 웹폰트 금지(정본 §7.9). `visual.text.family` 하나만 쓴다.

**아이콘 어휘 `hud.icons` (9종, 폐쇄 — 정본 §9.4.1 소유)** — 전부 **절차적 도형**(정본 §9.10: 아트 에셋 0바이트)

| `iconId` | 도형 | 색 | 출처 |
|---|---|---|---|
| `coinDisc` | 원반 + 중앙 구멍 | `palette.pickup.coin` | 정본 §7.2 |
| `xpDiamond` | 작은 마름모 | `palette.pickup.xp` | 정본 §7.2 |
| `bombRound` | 원 + 짧은 심지 선 | `palette.hud.textPrimary` | 신규 |
| `shieldHex` | **육각 링** | `palette.hud.textPrimary` | 정본 §7.11 재사용 |
| `tokenClock` | 원 + 바늘 2개 | `palette.hud.textPrimary` | 신규 |
| `rerollArrows` | 순환 화살 2개 | `palette.hud.textPrimary` | 신규 |
| `statusSlow` | `∿` | `palette.status.band` | 정본 §7.11 |
| `statusStun` | `✳` | `palette.status.band` | 정본 §7.11 |
| `resistShield` | 방패 pip | `palette.status.band` | 정본 §7.11 |

- **9종이 전부다.** 정본 §19.1 인용: *"어휘를 닫아 **AI가 아이콘을 발명하지 못하게** 한다."* 아이콘이 필요한데 목록에 없으면 = 설계 변경 = 정본 개정(C-6).
- `statusSlow`·`statusStun`·`resistShield`가 호박인 것은 **정본 §7.12.4의 ②③**이다(온-플레이어). **HUD 패널에서는 이 3종을 쓰지 않는다** — 5곳 목록 밖이기 때문이다. 우 패널의 저항 표시는 **텍스트**다(§4.4).
- `shieldHex`의 육각은 상태이상 탄의 육각과 **같은 형태**다. 충돌이 아니다: **자홍 채움 육각 + 호박 테두리(레이어 9, 플레이필드) vs 흰 육각 링(온-플레이어 · 패널)** — 색·채움·영역 3채널이 다르고, 정본 §7.11이 이미 실드를 "흰 육각 링"으로 확정했다.
- 속성 글리프 4종은 **아이콘이 아니다**(§2.3 예약).

---

## 8. 밀도 하 가독성

### 8.1 정본이 이미 구조적으로 보증한 것

| 실패 모드 | 정본의 구조적 차단 |
|---|---|
| 플레이어 탄이 적 탄을 덮음 | 레이어 4 < 9 + 적 탄 불투명 + additive 금지 (I-4) |
| 맥스 빌드에서 화면 백화 | 레이어 3~4 오프스크린 합성 `render.playerFxCompositeAlpha = 0.45` (§12.2) |
| ★ **처치 FX 백화** (`nova` 광역 10기 동시) | ★ **레이어 7 오프스크린 합성 `render.killFxCompositeAlpha = 0.55`** (§9.4.2 · §12.3 — v1.1 신설, §5.3) |
| 데미지 숫자 폭발 | 잡몹 숫자 **없음** + 0.15초 개체별 합산 + `caps.damageNumbers = 10` |
| 화면이 텔레그래프로 뒤덮임 | `telegraphConcurrentMaxPerEntity = 2` + ★ **A층 전역 `telegraphConcurrentMaxGlobal = 80`(파생: 40 × 2)** + ★ **B층 `caps.telegraphs = 96`** + **`deferAttack`**(I-3) |
| 탄이 사라지거나 순간이동 | `rejectSpawn` (`evictOldest` 금지) |
| 배경이 탄처럼 보임 | 자홍 배경 금지 + 소프트 엣지만 + 채도/명도 상한 |
| **적 탄의 절대 상한** | `maxSimultaneousEnemyBullets = 320`(A층) < `caps.enemyBullets = 384`(B층), `capHits == 0` 인증 |

> ★ **텔레그래프 행은 v1.1에서 통째로 바뀌었다** (04-R4 수정채택). v1.0은 "전역 8"이었고, **그 8은 「A 2/개체 < B 8」이라는 단위 불일치 부등호의 산물이었다** — 정본의 검산: 사격 개체 30 × duty 0.15 = **λ = 4.5**, 포아송 `P(X > 8) ≈ 4.0%` → `deferAttack` 상시 작동 → **`--certify` 영구 실패.** 정정 후 실측 최악 ≈ 13(λ의 3σ) → **A층 80 대비 여유 84%.** 이 섹션의 v1.0 §3.1·§8.1·§12-C가 전부 "전역 8"을 인용하고 있었다 — **셋 다 고쳤다.**

**남은 것 3가지를 이 섹션이 닫는다.**

### 8.2 ★ 글리프 LOD의 정의 (02-A7/B2 **채택**, 거처 = `visual.glyph`)

정본 §7.3은 `off`에서 "**글리프 렌더: LOD 적용**", `cvd`/`mono`에서 "**항상 렌더**"라고 대비시킨다 → **LOD는 실재하는 규칙인데 v1.0 어디에도 정의가 없었다.**

**정본의 채택 근거 (§19.1-A7 인용)**: *"크기 기준 → **팝핑 0**, 외곽선이 속성을 나르므로 **정보 손실 0**."*

**확정: `visual.glyph.lodMinBodyPx = 16`**

```
글리프를 그린다  ⟺  cbMode !== "off"                          // cvd·mono는 무조건 (정본 §7.3)
                  ∨ 개체가 엘리트·중간보스·보스·부위          // 정본 §7.6 "상시 글리프"
                  ∨ bodyPx >= visual.glyph.lodMinBodyPx       // ← 이 섹션
                 ∧ !occluded                                   // 정본 glyph.occludedSkip
```

| | |
|---|---|
| **무엇이 걸리나** | `chaff` 밴드(정본 §9.7 저작 가이드: `radius` 12~16) · 새떼 — **가장 많고 가장 작은 것들** |
| **정보 손실 0인 이유** | 정본 §7.6: **속성색 외곽선은 "본체 크기 무관 항상"** 그려진다. `off` 모드에서 속성은 **외곽선 색이 이미 100% 나른다.** 글리프는 색맹 이중화 채널이고, 색맹 모드에서는 LOD가 꺼진다. → **어떤 모드에서도 정보가 사라지지 않는다.** |
| **왜 이 값인가** | `chaff` 밴드 상한 16 = 경계. `line`(14~18)·`turret`(18~24)·`bruiser`(24~32)는 대부분 넘는다. **`elite.sizeMult = 1.4`는 `radius`에 곱해지고**(정본 §9.7, 04-R17: *"`radius`가 유일 진실"*) 엘리트는 어차피 규칙이 상시 렌더한다. |
| **왜 "개수 기준"이 아닌가** | 개수 기준 LOD는 적이 하나 더 스폰되는 순간 **화면 전체의 글리프가 사라진다**(팝핑). 크기 기준은 개체마다 결정이 고정이므로 **팝핑이 물리적으로 발생하지 않는다.** |
| **위기 세션에서 특히 옳은 이유** | `crisisElementRule = "themePure"` → **60기가 전부 같은 속성**이다. 개체별 속성 판독이 **애초에 불필요**하다. 판독해야 하는 것은 ① 덩어리의 가장자리 ② 적 탄 — 둘 다 글리프와 무관하다. **위기 세션의 가독성 예산은 전액 적 탄으로 간다.** |

### 8.3 ★ 강등 사다리 — **순서는 정본 §9.4.2 소유, 값은 이 섹션** (02-A9 **채택**)

정본 §12.5는 기준기를 **2020 MacBook Air M1**으로 잡고 "Disaster 최악 프레임 10ms / 예산 16.6ms"를 계산했다. 하한기는 **2019 Intel i5**이며 v1.0에는 **그 기계에서 예산을 넘겼을 때의 정책이 없었다.**

**정본 §0.2가 `Ms` 목록을 3곳 → 4곳으로 개정하며 이 요청을 채택했다 (인용)**: *"`fxMinRealMs`와 **완전히 같은 성격**이다 — 순수 렌더, 판정 무관, `core` 바깥, `render` 스코프 거주. **「정확히 3곳」은 성격의 목록이지 개수의 미신이 아니었다.** 기각하면 §12.5의 하한기 정책이 **공백으로 남고**, 공백은 개발 중 즉석 결정을 부른다(C-6 위반)."*

**트리거 = 정본 §9.4.2 소유 (인용)**: *"`render()` 프레임이 **14.0 실제ms**를 넘으면 강등 사다리를 **한 단 내리고**, `degradeRecoverFrames`(**120**) 동안 초과가 없으면 **한 단 올린다.**"*

> ★ **이 섹션의 v1.0은 "최근 120 프레임의 렌더 시간 **중앙값**"이라고 썼다. 정본은 중앙값이 아니라 **초과 즉시 강등 / 120프레임 무초과 시 복구**로 확정했다. 승복하고 고친다.** (그리고 정본의 것이 낫다 — 중앙값은 강등을 **60프레임 늦추고**, 강등이 필요한 순간은 정확히 **최악 프레임이 오는 순간**이다.)

**사다리 = 정본 §9.4.2 소유 (인용, 순서는 구조이며 JSON 키가 아니다)**

| 단 | 버리는 것 |
|---|---|
| ① | **파티클 캡 ×0.5** |
| ② | **배경 패럴랙스 2레이어 → 1** |
| ③ | **파티클 전면 OFF** |
| ④ | **림 오라 · 잔상 OFF** |
| ⑤ | **글리프 LOD 강제** — ★ **`cbMode != "off"`이면 이 단을 건너뛴다** |

**정본의 불가침 선언 (인용)**: *"**판정 채널·정보 채널(히트박스 도트·스탠스 링·적 외곽선·글리프·텔레그래프·적 탄)은 사다리의 어느 단에서도 불가침**이다. (…) 사다리가 **순수 렌더만 건드리므로 결정성·시뮬 무영향.**"*

**이 섹션이 정본 사다리에 대해 확정하는 것 (§17 위임 = "강등 사다리의 값")**

| 항목 | 확정 |
|---|---|
| **④의 "잔상"이 가리키는 것** | **정확히 둘 — `visual.trail`(유도 꼬리, V-3) + 둔화 잔상 3장(정본 §7.11 ④).** 둘 다 **끈다.** 근거: ① 유도 판독은 정본 §7.8이 **운동**에 걸었다("천천히 곡선으로 쫓아오면 100% 탄") → 꼬리는 리드타임 증폭기이지 유일 채널이 아니다 ② 둔화 판독은 **호박 밴드 1Hz + 온-플레이어 ∿ + 실제 속도 + 육각탄 tell + 호박 테두리**로 **5중**이다. **어느 쪽도 채널을 0으로 만들지 않는다.** |
| **⑤의 "강제"의 값** | `visual.glyph.lodDegradedBodyPx = **24**` — 강등 시 §8.2의 판정식이 `lodMinBodyPx`(16) 대신 이 값을 쓴다. 24는 `turret` 밴드 하한(18~24)의 상단 = **`chaff`·`line` 전량 + `turret` 하단이 글리프를 잃는다.** `off` 모드 전용이므로 **외곽선 색이 속성을 100% 나른다**(§8.2의 논거 그대로). |
| **왜 `evictOldest`로 안 되나** | 캡 초과 정책(정본 §12.1)은 **개체 수**의 문제를 풀고, 사다리는 **프레임 시간**의 문제를 푼다. **`capHits == 0`이 인증된 콘텐츠도 느린 기계에서는 프레임을 넘길 수 있다** — 다른 문제, 다른 장치. |

### 8.4 패널 캐시

`hud.panelCacheDirtyOnly = true`(정본 §9.4.1) — 패널은 **값이 바뀔 때만** 오프스크린 캔버스에 갱신하고 매 프레임 `drawImage` 2회(좌·우)로 blit한다.

- 좌 패널이 갱신되는 순간: 스탠스 전환 · 투자/무기/패시브 획득 · 슬롯 재정렬 · `stanceHintPulse` 프레임(스테이지 1 한정 — 이때만 매 프레임 갱신).
- 우 패널: 점수 · 코인 · 스톡 · 스테이지 · **무피격 pip 상태** 변화.
- ×3 배속에서 한 프레임에 최대 `loop.maxStepsPerFrame`(6)틱을 돌리므로(정본 §6.1) **렌더 예산이 가장 빡빡한 지점**이며, 패널 2장을 매 프레임 새로 그리는 것은 그 예산의 순손실이다.
- `cbMode`·`reduceFlash` 변경 시 **양쪽 캐시 즉시 무효화** (OPTIONS는 TITLE·PAUSE 양쪽에서 열린다 — 정본 §5.5 · §14).

### 8.5 겹침

- **적 겹침**: 속성색 외곽선이 각 개체를 분리(정본 §7.6) + `glyph.occludedSkip`. 새떼는 §8.2의 근거로 개체 판독이 비-목표다.
- **적 탄 겹침**: 검은 하드 외곽선 2px(cvd 3px) + 흰 스페큘러 코어 + 불투명 → **탄 20개가 뭉쳐도 각 탄의 경계가 남는다.** 이것이 `additive 금지`의 이유다(additive면 뭉치는 순간 한 덩어리가 된다).
- **픽업 누적**: 정본 §7.8 — 아레나 하단 경계에서 정지·누적. `caps.pickups = 256` 초과 시 `merge`(손실 0 = 무-노가다 기둥 보존). **누적 더미는 무광·≤6px·레이어 2**이므로 그 위 모든 것이 읽힌다.
- **HUD ↔ 아레나**: 물리적으로 겹치지 않는다(1280 = 350 + 580 + 350).

---

## 9. 색맹 모드

### 9.1 정본이 이미 닫은 것

`cbMode ∈ {off, cvd, mono}`(`visual.a11y.cbMode`) · `palette.elementCvd` 명도 사다리(96/76/55/40, 인접 ≥15 L\*) · 글리프 ×1.5 · 항상 렌더 · 적 외곽선 3px · 배경 명도 상한 0.22 · HUD/카드/고지 텍스트 라벨 강제 · OPTIONS에서 **TITLE·PAUSE 양쪽** 진입. **전부 정본 §7.3 · §5.5 · §14.**

> 정본의 철학 인용: *"4개 카테고리를 어두운 배경 위에서 **색만으로** 이색각자에게 분리시키는 것은 이론적으로 불가능하다. 목표는 **색이 완전히 실패해도 게임이 100% 성립하는 것**이고, 그 보증은 **글리프**다."*

### 9.2 `mono`의 범위 — 이 섹션의 읽기 (잔여 요청 R3)

정본 §7.3의 `mono` 열은 팔레트 행에 "`elementCvd` + 채도 0"이라고만 적었다. **문면상 `element`만 무채화되고 `threat`·`status`·`pickup`·`hud`는 색을 유지한다.**

**이 섹션의 읽기: `mono`는 전 팔레트를 무채화한다** (`element` · `threat` · `status` · `pickup` · `hud` · `bg` 전부). 각 색은 자신의 L\*를 유지한 회색이 된다.

| | |
|---|---|
| **왜 전부인가** | ① `mono`의 대상은 **전색맹(achromatopsia)**이며 그들에게 색은 이미 존재하지 않는다 — 부분 무채화는 그들에게 아무 의미가 없고 **개발자에게만 거짓 안심**을 준다. ② `mono`의 두 번째 목적은 **"색 채널이 완전히 죽어도 성립한다"의 증명 스위치**다. 일부를 남기면 **그 증명을 하지 못한다.** ③ 하드웨어가 임의로 hue→휘도를 사상하게 두는 것보다, **우리가 L\*를 통제**하는 편이 전색맹 사용자에게 실제로 더 낫다. |
| **지위** | 정본 §19.5가 02 본문의 값을 **전수 심사**하고 이 항목은 **뒤집지 않았다** → 유효. 그러나 §17의 위임 목록에 명시가 없고 §7.3의 표 한 줄이 **반대로 읽힐 수 있으므로**, 정본 한 줄 명문화를 요청한다(**R3**, minor). |

### 9.3 ★ `mono`의 위협 분리 — I-1이 단독 보증한다

**정직한 산술** (L\*는 sRGB → CIE L\* 실계산):

```
elementCvd 사다리:  normal L*96 │ fire L*76 │ water L*55 │ grass L*40   (정본 §7.3)
위협(자홍 #FF2E88):                              L*57  ← water L*55와 2 차이
배경 상한 (HSL L 0.22):                          ≈ L*24
```

> ★ **v1.0의 이 섹션은 자홍을 L\*54로 적었다. 재계산 결과 L\*57이다** (Y = 0.2126×1.0 + 0.7152×0.0273 + 0.0722×0.2463 = 0.2499 → L\* = 116·0.2499^⅓ − 16 = **57.1**). **결론은 강화된다** — 차이가 1이 아니라 2여도 `elementCvd` 자신의 기준(인접 ≥15)에 **13 미달**이다.
> ★ **정본의 사다리 4색도 전부 재검산했다** (sRGB → CIE L\*, D65): `normal #F2F6FA` = **96.7** / `fire #FFA83A` = **75.8** / `water #2E86E0` = **55.1** / `grass #1F6B3A` = **39.8**. 정본 표기(96 / 76 / 55 / 40)와 **fire·water·grass는 일치**하고 normal만 **반올림 차 0.7**이다(96.7 → 정본 96). **정본의 계약(인접 ≥15 L\*)은 전 구간에서 성립한다**: 96.7−75.8 = **20.9** / 75.8−55.1 = **20.7** / 55.1−39.8 = **15.3**. ★ **마지막 구간이 15.3으로 계약에 0.3만 남기고 붙어 있다** — `elementCvd`의 4색 중 **어느 하나라도 값을 바꾸면 이 계약이 먼저 깨진다.** 이 섹션은 그 사실을 기록만 하고 값을 만지지 않는다(팔레트는 정본 §7.3 소유).

**즉, `mono`에서 적 탄과 물 속성 플레이어 탄은 명도로 구분되지 않는다.**

**자유 슬롯이 없다는 증명**: 사다리가 40~96을 점유하고 배경이 ≤24를 점유한다. 남은 구간은 24~40뿐이며 그 중앙(L\*32)은 `grass`(40)와 **8 차이**, 배경(24)과도 **8 차이** — 사다리 자신의 기준(≥15)에 **양쪽 다 미달**이다. **`mono`에서 위협에 줄 수 있는 명도는 존재하지 않는다.**

**그러므로 분리는 명도가 아니라 다음이 한다** (정본 §7.4의 7채널 중 6개가 살아있다):

| 채널 | 적 탄 | 플레이어 탄 |
|---|---|---|
| **엣지** | **검은 하드 외곽선 3px** (cvd/mono) | **없음** (I-1) |
| 코어 | 흰 스페큘러 점 | 같은 회색의 밝은 코어 |
| 합성 | 불투명 | additive |
| 알파 | 1.0 | ≤ 0.80 (`render.playerBulletMaxAlpha`) |
| 레이어 | 9 | 4 |
| 모양 | 원 / 육각 | **글리프만** (정본 §7.3의 `mono` 행) |

> **결론: `mono`에서 위협 분리를 하는 것은 I-1이다.** 어두운 배경(L\*24) 위의 **검은 3px 테두리**는 그 자체로 L\*≈4의 국소 후광이며, 플레이어 측 어디에도 존재하지 않는다. **그러므로 I-1은 색맹 모드에서 협상 불가다** — "플레이어 탄에 외곽선을 조금 넣으면 예쁘겠는데"는 **접근성 기능 하나를 통째로 삭제하는 변경**이다. §2.1-V-2가 이것을 쌍방향으로 못 박고, **정본 §7.12.6이 플레이어 텔레그래프에 대해 같은 결론을 독립적으로 냈다**(*"외곽선 금지 (I-1)"*).

### 9.4 검증 항목 (구현 체크리스트 — 시뮬레이터로 확인)

| # | 확인 | 답 |
|---|---|---|
| 1 | deuteranopia에서 **자홍(적 탄) vs 호박(상태이상 탄의 테두리)** — ★ **v1.1에서 이 항목이 바뀌었다** | ⚠ **두 색이 같은 개체 위에 있다.** 정본 §7.12.4-④⑤가 호박을 **플레이필드 안**으로 들여왔으므로 v1.0의 답("영역이 배타")은 **더 이상 참이 아니다.** 재분석: 호박은 자홍 육각탄의 **1px 테두리**이고 채움은 자홍이다 → 두 색이 **경쟁하지 않고 적층**된다(§2.2의 적층 순서). 그리고 **그 탄이 상태이상 탄이라는 사실은 `shape == "hex"`가 이미 나른다** (S14 3중 동치). → **호박이 회색으로 읽혀도 정보 손실 0.** ✔ |
| 2 | deuteranopia에서 **호박(가장자리 밴드) vs 코인 금** | ⚠ **거의 동일한 황색.** 분리 = **영역**(레이어 13 밴드 vs 레이어 2 픽업) + **형태**(밴드 vs 원반+구멍) + **운동**(정지 vs 하강/스트릭). **색은 분리에 기여하지 않으며, 기여할 필요가 없다.** ✔ |
| 2b | deuteranopia에서 **호박 테두리(④⑤) vs 코인 금** — ★ **v1.1 신규** | ✔ 호박이 플레이필드에 들어왔으므로 새로 확인해야 한다. 분리 = **채움**(자홍 vs 금) + **레이어**(9 vs 2) + **형태**(육각 vs 원반+구멍) + **크기**(≥8 vs ≤6) + **운동**. 호박은 **1px 테두리일 뿐** 개체의 정체성이 아니다. ✔ |
| 3 | deuteranopia에서 fire(L\*76) vs grass(L\*40) | ✔ `elementCvd` 36 L\* 차이 + 글리프 ▲/✚. |
| 4 | **XP 마름모(백) vs 물 탄(◆)** | ⚠ **가장 좁은 케이스.** 분리 = 글로우 유무 · 외곽선 유무 · 레이어(2 vs 4) · 크기(≤6 vs ≤10) · **운동**(정본 §7.8: "순식간에 직선으로 빨려오면 100% 보상"). **7채널 중 6채널.** 통과하나 **좁으므로 회귀 검사로 남긴다.** |
| 5 | `mono`에서 적 탄 vs 물 탄 | ⚠ **L\* 2 차이.** §9.3 — **I-1 단독 보증.** 통과하되 **I-1 변경 시 이 항목이 즉시 깨진다**는 사실을 명시적으로 기록한다. |
| 6 | `mono`에서 ×0.5 회색 방패 호 | ✔ 채널은 **형태(호) + 파티클 0 + 금속 "틴"**이며 애초에 색이 아니다(정본 §7.7). |
| 7 | `mono`에서 배경 vs 위협 | ✔ L\*24 vs L\*57 = 33 차이 + 소프트 엣지 vs 하드 엣지. |
| 8 | `cvd`에서 기체 슬롯 스트립 | ✖ → **§9.5** |
| 9 | ★ **`mono`에서 무피격 pip 6칸** (v1.1 신규 — 정본 §7.12.5) | ✔ 3상태 전부 **형태 채널**이다: 빈 원 / 채움 원 / **✕**. `palette.neutralGray`가 회색으로 읽혀도 **✕는 ✕**다. **색에 의존하는 상태가 하나도 없다.** |
| 10 | ★ **`cvd`에서 코어 HP 바의 속성색** (v1.1 신규 — 정본 §1.2) | ⚠ 바 색 = 그 개체의 속성색이고 **바에는 글리프가 없다.** 그러나 정보 손실 0 — **그 개체 자신이 화면에 있고**(중간보스: 본체 외곽선 + 코어 글리프 / 보스: 코어 = 은색 ● 상시 글리프, 정본 §7.6) **바는 그 개체의 반복**이다. 그리고 상단 띠 좌측의 **개체 이름 텍스트**(§7.2.1)가 `cvd`의 "텍스트 라벨 강제"를 충족한다. ✔ |

### 9.5 ★ `cvd`의 기체 스트립 오버플로 (02-A8/B2 **채택**)

정본 §7.3: `cvd` → **글리프 크기 ×1.5**
정본 §7.5: 슬롯 스트립 pip = **`visual.stance.pipPx = 7`**

→ 7px pip 안에 ×1.5 배율의 글리프를 그릴 수 없다. **정본의 두 값이 서로를 초과했다.**

**정본의 채택 근거 (§19.1-A8 인용)**: *"7px pip에 `cvd` ×1.5 글리프가 안 들어간다 → **색맹 사용자에게 슬롯 스트립(= 시그니처 시스템의 유일한 판독 채널)이 사라진다.**"*

**해소: `visual.stance.pipPxCvd = 10`** (`cbMode !== "off"`일 때 `pipPx` 대신 사용)

```
off:  pip 7  × 4 + 간격 2 × 3 = 34px   (정본 §7.5 그대로)
cvd:  pip 10 × 4 + 간격 2 × 3 = 46px
```
- 기체 스프라이트 반경 `player.spriteRadius = 14` → 지름 28px. 46px 스트립은 기체보다 넓지만 **기체 하단 +14px, 레이어 6**이므로 적 탄을 가리지 않는다(I-4 안전). 이동 영역 좌우 인셋 20px(정본 §1.1) → 스트립 좌우 23px가 아레나 밖으로 3px 나가는 경우가 있다 → **스트립은 아레나 rect로 클리핑**한다(pip이 잘려도 순서·색은 읽힌다).
- **`cvd`에서 스트립이 안 읽히면 정본 §7.5의 3중 표시 중 하나가 죽고, 그 순간 "슬롯 순서 부여"라는 이 게임의 시그니처 시스템이 색맹 사용자에게 존재하지 않게 된다.** 3px는 그 대가로 싸다.

### 9.6 텍스트 라벨 강제의 적용 지점 (`cbMode !== "off"`)

| 표면 | `off`에서도 텍스트 있음? | `cvd`에서 추가 |
|---|---|---|
| 좌 패널 스탠스 행 | ✔ (`노말`/`불`/`물`/`풀`) | — |
| 좌 패널 상성표 | ✔ (열 헤더 이름) | — |
| 무기 부여 배지 | ✖ (글리프만) | **속성명 1자** (`불`/`물`/`풀`/`무`) |
| 우 패널 혼합 바 | ✔ (라벨 행 `불~70` 등) | — |
| 코어 HP 바 | ✖ | — (상단 띠의 **개체 이름**이 이미 텍스트다 — §9.4-10) |
| 드래프트 `elementLevel` 카드 | ✔ (정본 §11.1 필수 표기) | — |
| 테마 배너 | ✔ | — |
| **기체 슬롯 스트립** | ✖ | **불가**(7~10px) → **글리프 ×1.5가 유일한 채널** → §9.5가 필수인 이유 |

- `off`에서 이미 텍스트를 가진 표면이 대부분이다 = **`cbMode` 분기가 2곳뿐**이다(부여 배지 · pip 크기). 접근성 비용이 구조적으로 낮다.
- OPTIONS에서 `cbMode` 변경 → **즉시 반영**(재시작·재로드 없음). 패널 캐시 무효화(§8.4).

---

## 10. 30초 온보딩 체크리스트 (설명 0으로 전달되어야 하는 것)

| t | 배우는 것 | 채널 | 정본 |
|---|---|---|---|
| 3초 | **테두리 있고 딱딱한 자홍 = 나를 아프게 한다** | I-1 · V-2 | §7.2, §7.4 |
| 5초 | **점선 = 곧 위험 / 채움 = 지금 위험**, 점선이 조여들면 발사 직전 | 2상태 + §3.2 백스톱 | §7.4 |
| 8초 | **히트박스 도트 = 나** (r=4, 레이어 10, 상시) | `hud.hitboxAlwaysVisible` | §2.3, §7.5 |
| 10초 | **도트 링 색 = 내 스탠스** | 정본 §7.5 | §7.5 |
| 15초 | **기체 아래 4칸 = 지금 어느 무기가 부여됐나** | 스트립 ↔ 좌 패널 1:1 (§6.3) | §7.5 |
| 20초 | **×2 크런치 + 파열 / ×0.5 방패 호 + "틴"** = 스탠스를 바꿔야 하는 이유 | 정본 §7.7 · §7.10 | §7.7 |
| 25초 | **투자 0 전환의 탈색 링** = "이 속성은 투자가 필요하다" | 정본 §7.5 | §7.5 |
| 30초 | **좌 패널 pip 4칸 = 슬롯 지도** — 키를 안 눌러도 4개 스탠스의 결과를 안다 | §6.2 | — |

> 정본 §8.2.1이 이미 보증한다: 스테이지 1은 `introOk` 테마 + **~70/10/10/10**이므로 **한 스탠스를 켜고 있으면 ×2(~70%)와 ×0.5(~10%)가 자연히 같은 화면에 나온다.** 이 섹션이 하는 일은 **그 순간에 감각이 다르다는 것**(§5)과 **왜 그런지 어디를 봐야 하는지**(§6·§7.3)를 보장하는 것이다. **튜토리얼 스크립트는 없다**(정본 §15-M9: *"트리거는 XP뿐. 시각 스크립트를 전부 폐기한다"*).

---

## 11. 값의 거처

> 9파일 매니페스트(정본 §9.2)는 불변이다. 시각 관련 값은 **전부 `data/rules.json`**에 산다(`palette` · `visual` · `hud` · `render`).

### 11.1 정본이 소유 — **인용만 한다 (여기서 값을 다시 적지 않는다)**

| 스코프 | 정본 위치 | 비고 |
|---|---|---|
| `rules.json > hud` **전 키** | **§9.4.1** | 02-A1(blocker) 채택의 실체. 폰트 5단 · 패널 치수 · `hpBarSegCount` · 게이팅 3키 · `icons` 9종 · `stanceHintTargetsMajorityElement` |
| `rules.json > render` **전 키** | **§9.4.2** | `playerFxCompositeAlpha` 0.45 · **`killFxCompositeAlpha` 0.55** · `fxMinRealMs` 50 · **`degradeOnFrameMs` 14.0** · `degradeRecoverFrames` 120 |
| `visual.timer.*` (`warnScale` · `warnPulseHz` · `alertScale` · `alertPulseHz`) | **§7.12.1** | **05가 낸 요청, 정본이 채택. 02는 인용만 한다** |
| `visual.band.plateAlpha` · `contentOpaque` | **§1.2 · §12.3** | 02-A2 채택의 실체 |
| `visual.zone.fillAlpha` · `pulseHz` | **§12.3** | 02-A2 채택의 실체 |
| `visual.stance.*` (`pipPx` 7 등) · `visual.hitFx.*` · `visual.a11y.*` · `visual.text.*` · `visual.statusBulletSpeedMul` | §7.5 · §7.7 · §7.9 · §7.11 | |
| `palette.*` (element · elementCvd · threat · status · pickup · enemyBody · partDestroyed · bg) | §7.2 · §9.4 | |
| **`rules.json` 루트 키 = 정확히 17개** | §9.4 | `check.mjs` **S2**가 강제 |

### 11.2 이 섹션에 위임된 값 (정본 §17: *패널 내부 레이아웃 · 폰트 사다리 · 아이콘 9종 · 글리프 LOD · 강등 사다리의 값*)

```json
// rules.json > visual  — 02가 소유하는 하위 키
"visual": {
  "glyph":       { "lodMinBodyPx": 16,            // §8.2 — 정본 §7.3 "LOD 적용"의 정의 (A7 채택)
                   "lodDegradedBodyPx": 24 },     // §8.3 — 정본 §9.4.2 사다리 ⑤의 값
  "stance":      { "pipPxCvd": 10 },              // §9.5 — A8 채택
  "trail":       { "homingOnly": true, "count": 3,
                   "alphaStart": 0.50, "stepSec": 0.04 },   // §2.1 V-3
  "telegraph":   { "strokePx": 2, "dashPx": [6, 5],
                   "dashTightenAtPct": 0.85, "dashTightenMul": 0.35 },  // §3.2 — B3 채택
  "hitFx":       { "resistArcDeg": 90, "resistArcSec": 0.15,
                   "resistArcOffsetPx": 4 }       // §5.2
}

// rules.json > palette  — B6 채택분 (정본 §9.4의 palette 블록에 아직 인쇄되지 않았다 → R2)
"palette": {
  "neutralGray": "#8A8F98",             // ×0.5 방패 호 · ×0.5 숫자 · 상성표 ×½ · 무피격 ✕ · 퍼펙트 ✕ · 시간 보너스 ✕
  "hud": { "panelBg":   "#0E1116", "panelRule": "#232833",
           "textPrimary":"#E8EEF6", "textDim":  "#7E8796",
           "hpFill":    "#E8EEF6" }
}
```

**★ v1.1에서 이 섹션이 삭제한 키 3건**

| 키 | 삭제 사유 |
|---|---|
| **`visual.crisisWarn`** (`bandPulseCount` · `bandPulseHz`) | ★ **정본 §7.12.2가 요청(02-B7)을 기각하고 「`crisisWarn` 키는 신설하지 않는다」고 명시했다.** 예고 = **자홍 점선 수평선**(= `wall` 텔레그래프와 문자 그대로 같은 도형 → **새 어휘 0**). §12.1 승복 대장 참조 |
| **`visual.telegraph.fireFlashTicks`** | ★ **자진 폐기.** 정본 §0.2: "틱은 JSON에 등장하지 않는다. `*_ticks` 키는 전부 폐기한다." → 구조로 대체(§3.3), **새 키 0** |
| **`visual.zone.outlinePx` · `outlineAlpha`** | ★ **자진 폐기.** 2px는 `palette.threat.outline`의 정의가, 불투명은 정본 §12.3이 이미 소유한다(§3.4). **파생 가능한 값에 키를 만들면 두 값이 갈라진다** |

**★ 값이 아니라 구조인 것 (JSON 키를 만들지 않는다)**
- `Q W E R` 순서 — 정본 §4.1이 구조로 고정. 키로 만들면 뒤집을 수 있게 된다.
- **은색 ≡ `palette.element.normal`** — 정본 §7.12.1이 키와 이름을 나란히 적었다. 별도 키를 만들면 I-2가 깨진다(§2.3).
- **강등 사다리의 순서** — **정본 §9.4.2가 "구조 (JSON 키 아님)"이라고 명시.** 트리거(`degradeOnFrameMs`)와 각 단의 값만 값이다.
- **발사 플래시 = 판정 1틱 / 표시 `fxMinRealMs` 하한** — §3.3.
- **배타 서브행의 우선순위(✕ > 힌트)** — §7.2.1. 조건 분기이지 값이 아니다.
- 타이머 빨간불 = `palette.threat.enemyBullet` / XP 바 = `palette.pickup.xp` / 코인 = `palette.pickup.coin` — **채널 사상은 구조**다. 값은 이미 팔레트에 있다.

---

## 12. 정본 v1.1과의 정합

### 12.1 ★ 승복 대장 — 정본이 이 섹션을 뒤집은 것 (전수 8건)

> **반박하지 않는다.** 각 행은 **이 문서의 어디를 고쳤는지**를 가리킨다.

| # | v1.0의 02 | 정본의 처분 | 이 문서의 처리 |
|---|---|---|---|
| **1** | 보스 타이머 ≤60초 = **호박** | ★ **기각** (§7.12.1) — *"호박은 「내 상태」 전용, 타이머는 **외부 시계**. 그리고 타이머는 **플레이필드 안**이라 §7.2 **문면 위반**"* | §7.2.1 표를 **정본 3단으로 교체**(경고 = **색 불변** + `warnScale 1.15` + `warnPulseHz 0.5`). §4.3에 승복 명시 |
| **2** | 위기 예고 = 띠가 **테마 속성색으로 3회 펄스** (B7) | ★ **기각** (§7.12.2) — *"§7.2의 **가장 강한 예약**(「적 탄·텔레그래프에 4속성 색 사용 절대 금지」)의 **정면 위반**. 그리고 **불필요**하다"* | **`visual.crisisWarn` 키 삭제**(§11.2). §7.2.1의 상단 띠 = `새떼 3→0`만 인용 |
| **3** | 최종 로테이션 = 서브웨이브마다 띠 펄스 + 글리프 (B7) | ★ **기각** (§7.12.3) — *"**시각 규격 없음.** 적의 외곽선이 이미 답이다(<200ms 판독 계약). **미리 알려주면 시험이 아니라 대본이다**"* | 이 문서에서 **삭제.** 규격을 쓰지 않는 것이 규격이다 |
| **4** | 호박 예약 = **정확히 4곳** (A4) | ★ **수정채택 → 5곳** (§7.12.4) — *"02가 **자기가 쓰는 2곳을 뺐다**"* | §4.3을 **5곳 인용표로 재작성** |
| **5** | 무피격 = 우 패널 **텍스트 1행** | ★ **기각** (§7.12.5) — *"`noHitScope = "perStage"`이므로 **스테이지별 상태가 6개**다. 1행 텍스트는 **컨티뉴 시 6칸이 동시에 꺼지는 장면**을 만들지 못한다"* | 우 패널 **E 블록 신설** = pip 6칸 + 퍼펙트 ✕ (§7.4-E). 우 패널 전 좌표 재배치 |
| **6** | §5.3 백화 근거 = **"25초에 60회"** | ★ **수정채택** (§9.4.2) — 요청은 옳고 **근거가 10배 부풀려졌다**(기대값 **0.84**). 진짜 근거 = **`nova` 동시 처치 10 + `caps.particles 400`** | §5.3의 근거를 **통째로 교체**하고 오산을 명시 |
| **7** | 강등 사다리 = **02가 정한 5단** (숫자·마커 강등 포함) + **"120프레임 중앙값"** 트리거 | ★ **순서는 정본이 소유** (§9.4.2). 사다리 = ①파티클×0.5 ②패럴랙스 ③파티클OFF ④림오라·잔상OFF ⑤글리프LOD. 트리거 = **초과 즉시 강등 / 120프레임 무초과 시 복구** | §8.3을 **정본 사다리로 교체**. 02는 **④의 해석과 ⑤의 값**만 남긴다 |
| **8** | 테마 표시 = **"4개 수치를 그대로(대략이 아님)"** | ★ **폐기** (§8.2.1, 사용자 확정 2) — **저작 리스트 기준 + 물결표.** *"「그대로 표시」는 **지킬 수 없는 약속**이었다"* | §7.4-D 혼합 바·라벨을 **`~` 표기로 교체** + ≤5%p 괴리 명시 |

**★ 이 섹션이 스스로 거둔 것 3건** (정본이 지적하지 않았으나 정본의 규약 위반이었다)

| # | 무엇 | 위반한 규약 |
|---|---|---|
| 1 | `visual.telegraph.fireFlashTicks` | **정본 §0.2**: "틱은 JSON에 등장하지 않는다. `*_ticks` 키는 전부 폐기." → 구조로 대체(§3.3) |
| 2 | 무기 슬롯 진화 마크 `◈◈` | **§2.3 예약의 정신** — ◆(물)와 형태 근접. 이중선 테두리가 단독으로 나른다 |
| 3 | 재정렬 모드의 "집은 슬롯 = **이중 테두리**" | 같은 스트립의 **진화 표식**이 이미 이중선이다 → 8px 부상 + 스탠스색 배경으로 교체(§7.5) |

### 12.2 ★ 정본이 이 섹션보다 정밀했던 것 (기록)

| 정본 | 02의 v1.0이 놓친 것 |
|---|---|
| §9.9.1 | `stanceHintPulse` 타겟에서 **무엇을 세는지**(중간보스·보스·부위 **제외**)를 02가 정하지 않았다 |
| §11.5 | 파 고스트에서 **`stagePar[i] == 0` 센티넬**을 02가 다루지 않았다 → 인증 전에 **거짓 파**를 그린다 |
| §7.12.4 | `shape ↔ status` 동치(02-B4)를 **호박 테두리와 합쳐 3중**으로 확장. 검사는 S6이 아니라 **S14** |
| §12.1 | 02가 3곳에서 인용한 **"전역 캡 8"**이 애초에 **단위 불일치의 산물**이었다 (04가 잡아냈다) |
| §9.4.1 | 02가 산문으로만 논증한 **HP 바 5칸 고정**을 **`hpBarSegCount` 키**로 못박았다 |

### 12.3 잔여 요청 4건 (최소화됨)

| # | 요청 | 심각도 | 근거 · 제안 |
|---|---|---|---|
| **R1** | ★ **`hud.noHitRow`의 지위** — 키인가, 행의 이름인가? | **major** | §7.12.5가 **`hud.noHitRow`를 백틱 키 형식**으로 참조하는데 §9.4.1의 `hud` 블록(= `hud`의 확정 인쇄본)에 **없다.** 로더 규칙(§9.3) = 「미지 키 = 에러」·「누락 키 = 에러, 폴백 금지」 + **S2가 `rules.json` 루트/필드 집합을 검사** → 코드가 `hud.noHitRow`를 읽으면 **A1과 정확히 같은 클래스의 부팅 실패**다. **제안: 「행의 이름」으로 명문화**(§7.12.5의 문면에 *"`noHitRow`는 키가 아니라 우 패널 행의 이름이며 좌표는 02 §7.4-E가 소유한다"* 한 줄) — **새 키 0**이고 §17의 "패널 내부 레이아웃" 위임과 정합한다. 대안(§9.4.1에 `noHitRow` 좌표 키 편입)은 **패널 좌표를 정본으로 끌어올려 §17을 되돌린다** → 비권장 |
| **R2** | **채택·위임된 신규 키의 인쇄 자리** | **minor** | 아래 8키는 **정본이 채택**(또는 §17로 위임)했으나 **정본의 인쇄 블록에 없다.** C-7이 "인쇄된 스키마 블록은 **필드 집합을 확정**한다"고 했으므로, 그대로면 **S2(미지 키 거부)가 이들을 죽인다.** 값은 이 문서 §11.2가 보유하고 있으니 **인쇄 자리만** 주면 된다. ⟨§9.4의 `palette` 블록 → `neutralGray` · `hud.{panelBg, panelRule, textPrimary, textDim, hpFill}` (**B6 채택**)⟩ ⟨§7.5의 `visual.stance` 블록 → `pipPxCvd` (**A8 채택**)⟩ ⟨§9.4의 `visual` 축약(`"...§7의 ... 전 키..."`) → `visual.glyph.{lodMinBodyPx, lodDegradedBodyPx}` (**A7 채택 + §17 위임**) · `visual.telegraph.{strokePx, dashPx, dashTightenAtPct, dashTightenMul}` (**B3 채택**) · `visual.trail.*` · `visual.hitFx.resistArc*`⟩. ★ **그중 `visual.playerBullet.coreRadiusRatio`(0.45)는 미심사 신규다** — 정본 §7.4가 플레이어 탄에 "같은 hue의 **밝은 코어**(L\*+25)"를 요구하는데 **반경 규격이 없다** → 그대로면 구현자가 즉석 결정한다(C-6). 채택 또는 명시적 기각을 요청한다 |
| **R3** | **`mono`의 범위** | **minor** | §7.3의 `mono` 팔레트 행이 **"`elementCvd` + 채도 0"**이라 **문면상 `element`만** 무채화된다 → `threat`·`status`·`pickup`·`hud`가 색을 유지하는 것으로 읽힌다. 02 §9.2가 **전 팔레트**로 읽었고 §19.5의 전수 심사가 뒤집지 않았으나, **§17의 위임 목록에 없다.** **제안: §7.3에 한 줄** — *"`mono`는 **전 팔레트**를 무채화한다(각 색은 자신의 L\*를 유지). 근거: `mono`의 목적은 「색 채널이 완전히 죽어도 성립한다」의 **증명 스위치**이고, 일부를 남기면 그 증명을 하지 못한다."* 새 키 0 |
| **R4** | **§7.12.2 자홍 점선 수평선의 y** | **minor** | 정본 §7.12.2: *"`view.spawnLineY`(**−40**) **바로 아래**에 자홍 점선 수평선 1개, 전폭 580."* **`spawnLineY = −40`은 아레나 좌표계에서 화면 밖(y < 0)이다** → "바로 아래"를 문면대로 읽으면 선이 **−40 ~ 0 사이 = 보이지 않는 곳**에 그려진다. **예고가 안 보이면 예고가 아니다.** 02의 읽기 = **y 48** (상단 띠 바로 아래 = 화면 안 최상단 가시 행, 코어 HP 바와 접하지 않는 첫 픽셀). **제안: §7.12.2에 y를 한 줄 명시.** 그리고 레이어를 함께 명시 요망 — 이 선은 `wall` 텔레그래프와 같은 도형이므로 **레이어 8(공중 텔레그래프, 알파 0.50)**이 자연스러우나 정본이 말하지 않았다. 경계면 6건은 정본 소유(§17)이므로 **02가 정하지 않는다** |

### 12.4 초안 정정 (재료 → 폐기, 회귀 방지용 기록)

| 초안 | 값 | 폐기 사유 |
|---|---|---|
| A | `hud.minFontPx: 12` | 정본 `visual.text.minPx = 14` 위반 |
| A | HUD ASCII의 **패시브 5칸** | 정본 §9.4: `player.passiveSlots = **6**` |
| A | 아레나 560×720 @ x360, 패널 360 | 정본 §1.1: **580×720 @ x350, 패널 350** |
| A | 히트박스 도트가 적 탄 **아래** | 정본 §7.5: 도트 = **레이어 10(위)** / 스트립·배지 = 레이어 6(아래). **둘 다 옳았고 대상이 달랐다** |
| A | `hud.shipImbueChips` 토글 | 정본 OPTIONS 항목 5개(mute/volume/cbMode/reduceFlash/shake)에 없다. **끌 수 있으면 안 된다** |
| A | 임뷰 칩 5×4px, 기체 아래 10px | 정본 §7.5: **pip 7px, +14px** |
| A | 상점 화면에서 슬롯 재정렬 | 정본 §5.3: **드래프트 화면에서만** |
| B | 유도탄 = **마름모** / 설치 장판 = **링** | 정본 §7.4·§9.7: 적 탄 모양 = **`["circle","hex"]` 폐쇄**. ◆는 물의 예약 글리프. 유도는 **꼬리**(V-3) |
| B | 히트박스 도트 r=3, 링 1px | 정본 §2.3·§7.5: **r=4(= `player.hitboxRadius`), 링 2px** |
| B | `minLeadSecGame: 0.40` / 탄속 420 / 틈 56 | 정본 §12.4: **0.55 / 260 / 46** |
| B | `caps.telegraphsVisible: 6`(전역) | 정본 §12.1·§12.4: **개체당 2 + A층 전역 80(파생) + B층 `caps.telegraphs` 96.** ★ v1.0 정본의 "전역 8"도 함께 폐기됨 |
| B | `render.refPlayfieldPx: [640,960]` + `caps.areaScale` | 정본 §1.3: **폐기.** 모든 캡·밀도는 아레나 580×720 기준 직접 정의 |
| B | 배경 스크롤 ≤ "탄 최저속의 절반" | 정본 §7.9: **폐기**(B 내부 오류). 배경/탄 분리는 hue·엣지·레이어가 보증 |
| B | `data/visual.json` | 정본 §9.2: **9파일**. `rules.json > palette` + `> visual`로 흡수 |
| B | `a11y.cvdMode` / `paletteCvd` / `legibilityInvariants` | 정본 명명: **`visual.a11y.cbMode` / `palette.elementCvd` / `rules.fairness`** |
| B | 텔레그래프 `homing`·`spread`·`status` 어휘 | 정본 §8.5: 이미터는 **8종**. 유도·상태이상은 **탄 속성** |

---

## 13. 이 섹션이 닫은 것 (요약)

| | |
|---|---|
| **진영 배타** | V-1(팔레트 접근 경로, **호박 예외 명시**) · **V-2(외곽선의 쌍방향 유일성)** · **V-3(꼬리 = 유도, 진영 무관, 강등 하 지위)** · V-4(크기 보조) · **호박 테두리의 적층 순서**(I-1 불가침) · 글리프 4종의 예약 + 3예외 · **은색 ≡ `element.normal`**(정본이 §7.12.1에서 확인) |
| **텔레그래프** | **진행률 채널 2중** — 형상 고유(정본 4종 + 신규 4종) + **전역 dash 조임 백스톱**. `fireFlashTicks` **자진 폐기 → 구조로 대체**. 플레이어 텔레그래프는 정본 §7.12.6 인용 |
| **상태이상** | 적 상시 표식 **두지 않음**(5곳 목록 보호, 채널 6개로 충분) · 저항 평시 가시성을 패널로 이관 |
| **히트 피드백** | ×0.5 방패 호 치수 · ×2 화이트-핫의 실시간 하한 · **레이어 7 합성 하드캡**(요청 채택, **근거는 정본 것으로 교체**) |
| **스탠스 판독** | ★ **투자 pip = 슬롯 지도**(`elementCapPerElement = 4 = weaponSlots`의 귀결) · 3상태 pip · 3표면 1:1 강제 |
| **HUD** | 전 좌표 확정 — 상단 띠 **3행 분할**(타이머 0~30 / **배타 서브행 30~40** / 코어 HP 바 40~48) · 좌 패널 4블록 · **우 패널 6블록 + 여백**(무피격 E 블록 신설) · 오버레이 5화면 · **배타 서브행의 우선순위(✕ > 힌트)** · 혼합 바 **`~` 표기** · 폰트 사다리·아이콘은 §9.4.1 인용 |
| **밀도** | **글리프 LOD 정의**(정보 손실 0 · 팝핑 0) · **강등 사다리 ④의 해석 + ⑤의 값**(순서는 정본) · 패널 캐시 · 겹침 4케이스 |
| **색맹** | `mono` 범위(→ R3) · **자유 명도 슬롯 부재의 증명**(L\* 재검산: 자홍 **57**) → **I-1 단독 보증** → I-1의 협상 불가 지위 · **검증 10항목**(호박이 플레이필드에 들어온 v1.1을 반영해 1·2b 재분석, 9·10 신설) · `pipPxCvd` · `cbMode` 분기가 **2곳뿐**임 |

**남은 것은 값 튜닝뿐**(`rules.json > visual` · `> palette`의 02 위임분). 규칙 · 역할 · 레이어 · 예약 · 불변식 · **§7.12 경계면 6건**은 **계약**이며 변경 시 정본 개정(C-6)이다.
