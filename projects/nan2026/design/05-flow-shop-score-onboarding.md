# 섹션 — 런 플로우 · 상점 · 점수 상세 · 온보딩

> **대상 정본: CANON.md v1.2.** 이 문서는 v1.2에 맞춰 전면 개정되었다(개정 내역 = §13).
>
> **지위**: 이 문서는 정본(`design/CANON.md`)의 **하위 상세 계층**이다. **§17이 이 섹션에 위임한 범위 = 「화면의 내용 · 전이의 트리거 · 입력의 경계 · 정산의 순서 · 온보딩의 인과」(정본의 상태 기계·키맵·점수식·상점 모델·드래프트 코어 안에서만)** — v1.0에는 이 행이 없어 이 문서는 권한 근거 없이 작성되었고, **v1.1이 §17에 행을 추가해 권한을 부여했다**(05-⑨ 채택).
> 정본이 확정한 상태 기계(§6.5)·페이즈 길이(§6.3)·드래프트 발생(§6.4)·키맵(§5)·상점 모델(§11.2)·점수 키(§11.3)·컨티뉴(§11.4)·파 사상(§11.5)·온보딩 3키(§9.9)·**경계면 8절(§7.12 — ★ v1.2에서 6 → 8)**·**DPS 기준선(§13.5 · §13.5.1)**·**보스 HP(§13.6 · §13.6.5)**를 **참조만** 하고 **재정의하지 않는다.**
> 정본이 닫은 것 = **모델·키·값.** 이 문서가 닫는 것 = **화면의 내용, 전이의 트리거, 입력의 경계, 정산의 순서, 온보딩의 인과 사슬.**
> 정본과 충돌하는 서술이 이 문서에 있다면 **이 문서가 틀린 것이다** (C-1). 정본의 표에 인쇄된 숫자는 **인용만** 하며 바꾸지 않는다 (C-8). **인쇄된 스키마 예시는 전부 예시다** (C-7) — 이 문서의 화면 목업에 든 숫자는 **전부 예시**이고, 확정값은 정본의 표에만 있다.
>
> **★ 값의 거처 (v1.2 — 이제 전부 정본의 인쇄 블록에 자리가 있다)**: 플로우·점수·온보딩 값은 `data/meta.json > flow / score / onboarding`, 상점 값은 `meta.json > shop`에 산다. 그러나 ★ **`visual.timer.*`는 `rules.json > visual` 소유다**(§9.2 · §9.4.3 · §7.12.1). v1.0판 이 문서의 §10 제목 「전부 `meta.json`」은 **오기**였고, **그대로 구현하면 「미지 키 = 에러」로 로드가 실패한다**(§9.3).
> ★ **그리고 v1.1에서는 거처를 알아도 로드가 실패했다**: 05가 §10에 「거처 = `meta.json > flow`」라고 표까지 만들었으나 **정본 §9.9의 `flow` 블록은 축약 없이 전부 인쇄되어 있었고 채택된 7키가 하나도 없었다**(C-7 = 인쇄된 블록은 필드 집합을 확정 · S2 = 미지 키 거부). **이 섹션은 거처를 정정하면서 그 거처에 자리가 있는지 확인하지 않았다** — v1.2 §21-A2/A3가 `flow` 7키 + `score.roundMode`를 **실제로 인쇄**해 닫았고(§9.9 · §11.3), §10은 이제 **순수한 인용**이다.

> ★ **v1.2가 이 문서에 가한 변경의 요약** (§20.3-05 + 이 문서가 자기 산술을 다시 돌려 찾은 것):
> ① **`stanceHintPulse`의 색 = 05가 옳았다** — 정본 §7.12.7이 **7번째 경계면**으로 흡수(§8.5는 이제 인용) ② **`flow` 7키 + `roundMode`가 인쇄 자리를 얻었다**(§10) ③ **`hud.noHitRow`는 키가 아니다** — 05-⑩의 지적이 옳았다(§9 · 요청 목록에서 제거) ④ ★ **`dpsRef` = `maxFarm` 화력으로 정의가 바뀌었고 곡선·보스 HP·`bossHpScale`이 전부 재산출됐다**(§13.5.1 · §13.6) → **이 문서의 §6.5 파 검산이 통째로 낡았다** ⑤ ★ **`stagePar` 목표 밴드 25~70 → 30~60**(§13.6.4) ⑥ **위기 예고 y = 48 · 레이어 8 · 알파 0.50**(§7.12.2 — v1.1의 `spawnLineY(−40) 바로 아래`는 **화면 밖**이었다) ⑦ **`flow.attract.draftDwellSec` = 0.8 → 1.2**(§9.9) ⑧ **`mono` = 전 팔레트 무채화**(§7.3) ⑨ **§7.2 예약 문구가 실제로 개정됐다**(05-⑪ 종결).

---

## 0. 이 문서가 지키는 3개의 파생 규칙

정본에서 직접 파생되며, 아래 모든 화면 명세가 이 셋을 전제한다. **셋 다 v1.1에서 정본이 흡수했거나(F-3) 정본의 규격으로 재작성됐다(F-1·F-2).**

| # | 규칙 | 파생원 |
|---|---|---|
| **F-1** | **클럭이 멈춘 전체화면 상태는 플레이필드를 덮어도 된다.** DRAFT·SHOP·PAUSE·DEATH·STAGE_CLEAR·HEAL·THEME_BANNER는 게임 클럭이 정지하므로 **회피가 존재하지 않는다.** ★ **v1.1 정정**: I-4(적 탄은 절대 가려지지 않는다)는 **§12.3의 레이어 스택 0~13이 물리적으로 보장**한다 — 「I-4는 알파가 아니라 레이어 구조가 보장한다」(§12.3). 이 화면들은 **그 스택 안의 레이어가 아니라 스택 위의 화면**이므로 I-4의 정의역 자체에 들어오지 않는다. **I-4를 "무력화"하는 것이 아니라, 애초에 적용 대상이 아니다.** | §7.1 I-4 · §12.3 · §6.4 · §6.6 |
| **F-2** | **새 색을 만들지 않는다.** 이 섹션의 모든 화면은 §7.2 팔레트의 기존 키만 쓴다. 위협은 자홍(`palette.threat.enemyBullet`), 중립·정상은 은색(`palette.element.normal`), 회색은 `palette.neutralGray`, 속성은 4속성 키. ★ **호박(`palette.status.band`)은 §7.12.4가 확정한 5곳 전수 목록 밖에서 쓰면 버그다** — 그리고 **이 섹션의 화면은 그 5곳에 하나도 해당하지 않는다.** → 타이머 경고·위기 예고·파 고스트는 전부 **기존 채널의 재사용**이다(§4.1, §3.2, §6.5). | §7.2 · **§7.12.4** |
| **F-3** | **화면이 바뀌는 순간 눌려 있던 키는 먹지 않는다.** ★ **v1.1에서 정본이 흡수했다** — `flow.edgeTriggerOnStateEnter = true` (§5.5, 05-⑧ 채택). 상태 진입 프레임에 이미 눌려 있던 키는 그 상태에서 무시하고 **떼었다 다시 눌러야 유효**하다. 정본 §5.2가 드래프트에서 막은 `Space` 오발이 **DEATH·RESULTS에는 그대로 남아 있었고**(죽는 순간 연타 = 컨티뉴 화면 즉시 통과 = 회복 불가 손실), F-3이 그 계열 사고 **전부를 규칙 하나로** 닫는다. 구현 = 직전 틱 키상태 비교 1회(§5.7과 충돌 없음). | **§5.5** · §5.2 · §5.7 |

---

## 1. 상태 기계 — 화면 단위 전개

정본 §6.5의 다이어그램을 **화면·트리거·입력·클럭** 4열로 전개한 것이다. 상태·전이는 정본과 1:1이며 추가·삭제가 없다.

| 상태 | 클럭 | 나가는 트리거 | 다음 |
|---|---|---|---|
| `TITLE` | 메뉴(§4.4) | `Space` / `O` / 무입력 `flow.attractIdleSec`(20) | DIFFICULTY / OPTIONS / ATTRACT |
| `ATTRACT` | 게임(§2.2) | 아무 키 / 잡몹 페이즈 종료 | TITLE |
| `DIFFICULTY` | 메뉴 | `Enter` / `Escape` | RUN_START / TITLE |
| `RUN_START` | — | **0프레임 논리 상태** (시드 발급 → 테마 5종 일괄 추첨 → 로드아웃 초기화) | THEME_BANNER(1) |
| `THEME_BANNER` | **정지** | `flow.themeBannerSec`(2.5) 경과 또는 `Space` | MOB_PHASE(i) |
| `MOB_PHASE` | **진행** | `stage.mobPhaseSec`(120) 경과 | DRAFT_QUEUE |
| ┗ `DRAFT` | **완전 정지** | 카드 확정 | MOB_PHASE 복귀 |
| ┗ `CRISIS` | **진행** | — (내부 서브구간 95~120, 독립 상태 아님) | — |
| `DRAFT_QUEUE` | **정지** | 큐 소진 (0장이면 0프레임 통과) | BOSS_INTRO / HEAL |
| `BOSS_INTRO` | **진행**(보스 무적·발사 없음·**타이머 정지**) | `stage.bossEntrySec`(3.0) 경과 | BOSS |
| `BOSS` | **진행** | 코어 격파 / HP 0 / 타이머 만료 | STAGE_CLEAR / DEATH |
| `STAGE_CLEAR` | 정지 | `flow.stageClearSec`(2.0) 또는 `Space` | DRAFT_QUEUE |
| `HEAL` | 정지 | `flow.healSec`(1.5) 또는 `Space` | THEME_BANNER(i+1) |
| `SHOP` | 정지 | `Escape` + 확인 1회 | MOB_PHASE(i+1) |
| `PAUSE` | **전 클럭 정지** | `Escape` → `flow.pauseResumeCountdownSec`(1.5) | 직전 상태 |
| `OPTIONS` | 정지 | `Escape` | 직전 상태(TITLE 또는 PAUSE) |
| `DEATH` | 정지 | 컨티뉴 / 포기 / `Space` | 직전 상태 재개 / RESULTS / RUN_START |
| `RESULTS` | 메뉴 | `Space` / `Escape` | RUN_START / TITLE |

**★ 스테이지 루프에 `DRAFT_QUEUE`가 두 번 있다 (정본 §6.4·§6.5의 정확한 독해)**
`MOB_PHASE → DRAFT_QUEUE → BOSS_INTRO` (위기 세션 25초 동안 쌓인 큐를 **보스 인트로 전에** 소화) 와
`STAGE_CLEAR → DRAFT_QUEUE → HEAL` (보스 페이즈 큐 — **구조적으로 항상 0장**, §6.4).
전자가 실질이고 후자는 **안전망**이다. 후자가 0장이므로 §6.5의 `STAGE_CLEAR → DRAFT_QUEUE`는 정상 런에서 0프레임을 통과한다. → **"보스 깨고 나서 카드를 고른다"는 체감은 존재하지 않고, 카드는 항상 보스 *전에* 소화된다** = 보스 = 가져온 빌드의 시험지(§8.8).

**포기(abandon)는 PAUSE에 없다 — 이제 정본의 키다.** ★ **`flow.pauseAllowsAbandon = false`**(§5.5, 05-④ 채택). 정본이 명문화한 근거: ① 포기 키가 있으면 **"지금 죽어서 점수를 확정할까"라는 체스식 최적화**가 생긴다 ② 30분 런의 탈출은 `Escape` 무제한 일시정지 + `blur` 자동 정지가 이미 준다 ③ 오락실에 "포기" 버튼은 없다. **런을 끝내는 방법은 클리어 / 사망(→ DEATH에서 `포기` → RESULTS) 둘뿐**이고, 그래서 `bossTimerSec`가 진짜 허들이 된다. 런 이탈 = 새로고침 = 런 소멸(§6.5).

**TITLE에서 `O`가 열린다 (v1.1 정정).** §5.5의 v1.0 표는 `O`를 PAUSE 전용으로 적었으나 §6.5 다이어그램·§14와 **2:1로 그 표가 오기**였고, v1.1이 정정했다(05-② blocker 채택). 그대로 뒀다면 §14가 "TITLE에서만"으로 확정한 **`기록 삭제`가 도달 불가능한 기능**이 됐다.

---

## 2. 화면 명세 — 런 진입까지

### 2.1 TITLE

| 요소 | 내용 |
|---|---|
| 헤더 | **`TETRA`** (§m5) |
| 기록 | 난이도별 최고점수 4줄 — `Q W E R` 아닌 **난이도 순서 Normal → Hard → Hell → Disaster** 고정. 각 줄에 `점수 · 도달 스테이지 · 시드(8자리 hex)` (localStorage **단일 키 `nan2026.v1` > `best.<diff>`**, §14 — 블로그와 origin을 공유하므로 접두사 필수). 미기록은 `— — —` |
| 조작 | **키캡 그림**(텍스트 아님): 방향키 / `Q W E R` + 4속성 글리프(●▲◆✚) / `Space` 폭탄 / `Shift` 토큰 |
| 상성 | **4×4 상성표**(§6.6이 PAUSE에 비치한 것과 **같은 컴포넌트 재사용**, 순서 `Q W E R`) |
| 키 | `Space` = DIFFICULTY · `O` = OPTIONS · 무입력 20초 = ATTRACT |
| 배경 | 어트랙트 대기 중에도 배경은 절차적 스크롤(§9.10) — 정지 화면 아님 |

> 상성표를 TITLE·PAUSE 두 곳에 **같은 컴포넌트로** 비치하는 것이 "튜토리얼 텍스트 0"의 인프라다. 온보딩이 못 가르치면 플레이어는 언제든 `Esc`로 표를 본다. 비용 = 컴포넌트 1개.

### 2.2 ATTRACT — 심사자가 보는 첫 3초

**정의: 인증 봇이 실제로 스테이지 1을 플레이하는 것.** 리플레이도, 스크립트도, 영상도 아니다.

| 항목 | 확정 |
|---|---|
| 구현 | `flow.attract.difficulty = "normal"`, 스테이지 1, 봇 정책 = **`bot.baseline`** — ★ **v1.1에서 4축이 되었다**: `draft: generalist` / `farm: balanced` / `stance: greedyNearest` / **`shop: survivalFirst`**(§10.4 · §10.4.1). 시드는 매 회 새로 발급 |
| ★ `shop` 축 | **어트랙트에서는 도달하지 않는다** — 상점은 스테이지 1 **클리어 후**에 열리고(§6.5) 어트랙트는 **잡몹 페이즈에서 끝난다**(아래). 축은 4개지만 어트랙트가 실제로 소비하는 것은 3개다. **정합이지 예외가 아니다** |
| 루프 | 게임과 **문자 그대로 동일한 `step()`** 에 `bot(world)`의 입력을 주입 (§10.4의 시뮬 루프와 동일) |
| 반응 지연 | `latencyTicks = round(bot.reactionMs / 1000 × 60 × speed)` (§10.4.1). 어트랙트 = Normal(`speed 1.0`) → **15틱.** 봇이 인간처럼 늦게 반응하는 것이 화면에 보인다 |
| 드래프트 | 카드 3장을 **표시하고** ★ **`flow.attract.draftDwellSec`(1.2 — v1.2 확정값, §9.9)** 후 봇이 선택 → 카드가 뽑히는 것이 보인다. ★ **게임초다**(`flow.attractIdleSec`(20)과 달리 **런 안**이므로) — 어트랙트는 Normal(`speed 1.0`) 고정이라 실제 1.2초 |
| ★ 재정렬 | `bot.slotOrder = "dpsDescending"`(§10.4) → 봇은 드래프트 화면에서 `Tab`으로 **슬롯을 명목 ST DPS 내림차순 정렬**한다. **이것이 화면에 보인다** — 즉 어트랙트가 §11.1의 `elementLevel` 카드가 가르치는 그 최적화를 **시연**한다. 봇의 편의가 아니라 **게임의 산술적 골격**이다(§13.2-③의 전제) |
| 종료 | **잡몹 페이즈 종료 시점**(`flow.attract.endAfterMobPhase = true`) → TITLE. 보스전은 보여주지 않는다 |
| 중단 | **아무 키** → 즉시 TITLE (F-3에 의해 그 키는 TITLE에서 다시 먹지 않는다) |
| 기록 | 어트랙트 런은 **점수를 집계하지 않고 localStorage를 건드리지 않는다** |
| HUD | **전체 HUD 그대로** — 봇의 스탠스 전환이 좌 패널에서 보여야 한다 |

**★ 왜 잡몹 페이즈에서 끊는가**: 잡몹 페이즈 120초의 마지막 25초 = **위기 세션**이고, 그 60기(스테이지 1은 `swarmTotalScale[0]=0.5` → 30기)는 **전원 테마 속성**(`crisisElementRule: "themePure"`, §8.10)이다. 봇이 정답 스탠스를 켜두면 화면이 ×2 화이트-핫 + 저역 크런치로 뒤덮인다. **어트랙트가 게임의 셀링 포인트에서 정확히 끝난다.** 보스전(최대 3분)을 붙이면 어트랙트 1회가 5분이 되고, 클라이맥스가 중간에 파묻힌다.

**★ 왜 봇인가 (비용 논증) — v1.1이 구조를 승인했다**: 봇은 인증 파이프라인(§10.4)에 **이미 존재하는 코드**다. 어트랙트를 위해 만드는 것이 아니라, 만들어진 것을 화면에 띄우는 것이다. 그리고 이것은 공모전 스토리의 실물이다 — **"타이틀 화면에서 지금 플레이하고 있는 그 봇이 이 게임의 밸런스를 인증했습니다."**

정본이 채택한 구조 3건(05-⑤, §10.2):
| 구조 | 확정 |
|---|---|
| **`src/core/bot.js`** | 봇을 `tools/`에서 **core로 승격**. 순수(주입된 rng만 사용, §9.1의 금지 식별자 전부 적용). 봇이 `tools/`에 있으면 배포물이 `tools/`를 import하게 되어 **§9.1의 모듈 경계가 무너진다** |
| **RNG 스트림 8번째 `bot`** | `bot.reactionJitterMs` 용. ★ **동결 파기의 대가가 0이다** — 독립 스트림의 정의상 **인간 런에서 `bot` 스트림은 draw가 0회**이므로 기존 7 스트림의 수열이 **비트 단위로 불변**이다. **이전 인증이 무효가 되지 않는다.** 정본: *"이것이 스트림 분리를 애초에 한 이유이며, 지금 그 배당금을 받는 것"* |
| **`check.mjs` S11** | 스트림 목록 7 → **8종** |

### 2.3 DIFFICULTY

4종 **전부 처음부터 선택 가능**(§6.1, 해금 그라인드 = 기둥 위반). 커서 기본 = **Normal 고정**(마지막 선택을 기억하지 않는다 — `opts`에 그 키가 없고, 만들면 §14 스키마 변경이다).

각 줄에 §6.1 표의 값을 **그대로** 표시한다:

```
NORMAL    ×1.0   보스 타이머 3:00   런 ~30분   점수 ×1.0    최고 ——
HARD      ×1.5   보스 타이머 2:00   런 ~20분   점수 ×1.6    최고 124,300
HELL      ×2.0   보스 타이머 1:30   런 ~15분   점수 ×2.5    최고 ——
DISASTER  ×3.0   보스 타이머 1:00   런 ~10분   점수 ×4.0    최고 ——
```

> **"Hell = 적이 세다"가 아니라 "Hell = 15분 스피드런"임이 선택 시점에 읽혀야 한다.** 난이도의 유일한 축이 배속이므로(§6.1), 화면이 보여줄 것도 **시간**이다. 적 스탯을 언급하는 문구는 거짓말이 된다(적 스탯은 난이도로 변하지 않는다).
> **난이도는 런 시작 후 변경 불가**(§6.1). 이 화면이 그 결정의 유일한 지점이다.
> ★ **표기 규칙 준수 (§11.3, 02-B9 채택)**: `difficulty[].speed`(1.5)와 `scoreMul`(1.6)은 **Hard에서 다른 수**다. **라벨 없는 `×N`은 배속 하나뿐**이고 **점수 배율은 반드시 `점수 ×N`으로 라벨**한다 — 위 4줄이 그 규칙의 형태다(`×1.5` = 배속 / `점수 ×1.6` = 배율). 이 문서에서 라벨 없는 `×N`이 등장하는 곳은 **상단 띠의 배속 배지 하나뿐**이다(§4.1).

### 2.4 RUN_START — 0프레임 논리 상태

| 순서 | 동작 |
|---|---|
| 1 | 마스터 시드 발급 — `main.js`가 생성해 core에 주입 (§10.2). **비결정성이 들어오는 유일한 지점** |
| 2 | `rng.theme`로 **5개 테마를 한 번에 전부 추첨**(§8.1). 스테이지 6 = `finale` 고정 |
| 3 | 로드아웃 초기화 = **정본 §2.6 표 그대로** (HP 100 / 스탠스 노말 / `forward` Lv1 슬롯 1 / 투자 0-0-0 / 폭탄 1 / 코인 0 / Lv1) |
| 4 | → THEME_BANNER(1) |

**시작 무기 선택 화면 없음**(§2.6). t=0의 선택은 정보가 없어 가짜다.

---

## 3. 화면 명세 — 스테이지 루프

### 3.1 THEME_BANNER — 정보 기둥의 유일한 사전 전달 지점

전체화면, `flow.themeBannerSec`(2.5), `Space` 스킵.

| 요소 | 내용 |
|---|---|
| 테마 | 이름(`themes[].name`) + 스테이지 번호 `2 / 6` |
| **구성비** | ★ **`stages[].mix`의 4개 수치를 물결표와 함께** — **`물 ~70% · 불 ~10% · 풀 ~10% · 노말 ~10%`**. ★ **물결표(`~`) 필수**(§8.2.1 확정). **순서는 `Q W E R` = 노말 · 불 · 물 · 풀**(§4.1). 각 항목에 속성색 + 글리프 + (cvd/mono에서) 텍스트 라벨 |
| **상성 미니휠** | 4속성 글리프의 순환 다이어그램. **이 테마의 ~70%에 ×2인 글리프가 밝게** — 나머지는 알파 0.35. 텍스트 0 |
| 보유 | 현재 속성 투자 3종 + 합계 예산 `투자 4 / 6` (§m1) — **다음 상점에서 무엇을 살지, 다음 드래프트에서 무엇을 고를지를 지금 결정하게 한다** |
| finale | `element = null` → 이름 `최종`, 구성비 `물 ~30% · 불 ~30% · 풀 ~30% · 노말 ~10%`, 배너 문구 **`테마 없음 — 4속성 혼재`**(§8.16). 미니휠은 **4글리프 전부 밝게** |

**★ v1.1 정정 — 물결표가 이 배너의 정직성이다 (§8.2.1 확정)**

v1.0판 이 문서는 「`mix`의 4개 수치를 **그대로**」 표시하고 「수치를 숨기지 않는다」고 썼다. **그 약속은 지킬 수 없는 것이었다** — 정본 v1.1이 그 이유를 산술로 밝혔다:

| 확정 (정본 소유) | |
|---|---|
| **S8 검사 대상** | ★ **저작 리스트** (`stages[].waves[]` 전체의 개체 수 기준). **실제 스폰이 아니다** |
| 허용 오차 | **±3%p** (저작 리스트 대비) |
| **배너 표기** | ★ **물결표 필수** |
| **실제 조우와의 괴리** | **≤ 5%p** — 검사하지 않으나 **정본이 명문으로 허용**한다 |

- **왜 「그대로」가 거짓말이었나**: 플레이어가 실제로 만나는 것은 **앞 ≤14 웨이브**이고 `waveClearAdvance`(§8.7)가 남은 스케줄을 압축하므로 **조우 비율이 플레이 스타일의 함수**다. 정본의 실측: `sea` s1 소극 파밍 11웨이브 → **물 75.0% (+5.0%p).** `70%`라고 쓰면 **75%를 만난 플레이어가 배신당한다.** `~70%`라고 쓰면 **약속의 정밀도가 약속의 이행 가능성과 일치한다.**
- **정보 기둥은 손상되지 않는다**: `elementCapTotal = 6`의 다이얼(`4/2/0` vs `2/2/2`)은 **70 대 10의 차이**에서 결정되지 70 대 75에서 결정되지 않는다. **정보 기둥이 요구하는 것은 소수점의 진실이 아니라 "무엇이 다수인가"의 진실**이고, 그것은 어떤 파밍 스타일에서도 참이다(70→75는 **더 참이 된다**).
- **그리고 게이트는 여전히 계약이다**: `mixGranularity: "perWave"` + **`check.mjs` S8(저작 리스트 ±3%p)**이 배너를 **광고가 아니라 계약**으로 만든다. 바뀐 것은 **계약의 대상이 명시됐다는 것**뿐이다 — v1.0은 "무엇의 비율인지"를 정하지 않아 **저작 리스트는 0.0%p인데 실제 조우가 5%p 벗어나는** 구멍 위에 서 있었다.
- **물결표는 데이터가 아니라 표기다** — 초안 E의 `70/12/12/6`을 폐기한 근거("규칙에서 파생되지 않는 임의 데이터")는 그대로 유효하다.

### 3.2 MOB_PHASE — HUD 계약

이 상태에는 전용 화면이 없다. 플로우가 성립하기 위해 **상시 표시되어야 하는 것**의 목록은 §9의 계약표에 있다. 이 상태에서 이 섹션이 소유하는 것은 **위기 세션 예고**와 **드래프트 큐 배지** 둘뿐이다.

**위기 세션 예고 (`stage.crisisWarnSec` = 3.0) — ★ 정본이 흡수했다 (§7.12.2, 05 채택 / 02-B7 기각)**

**이 규격은 이제 정본 §7.12.2가 소유한다. 아래는 인용이다.**

| 채널 | 확정 규격 (§7.12.2) |
|---|---|
| ① 상단 띠 | 페이즈 잔여 표시가 **`새떼 3 → 0` 카운트**로 전환 (`hud.fontHeroPx`, 은색) |
| ② 아레나 | ★ **`y = 48`에 자홍 점선 수평선 1개** (= `view.bandTopH`, **상단 띠 바로 아래**), **전폭 580**, ★ **레이어 8** · ★ **알파 `visual.telegraph.airAlpha`(0.50)** · `visual.telegraph.strokePx`/`dashPx` 그대로, **3.0 게임초** 지속 |
| ③ 오디오 | 상승 스윕 + `bgm ×0.5` 덕킹(§7.10) |
| **금지** | 화면 가장자리 호박 펄스 **금지** · 띠의 속성색 펄스 **금지** |

> ★ **v1.2 정정 — v1.1의 y는 화면 밖이었다 (판정 8, 02-R4가 옳았다).** v1.1의 §7.12.2(그리고 이 문서의 v1.1판)는 「`view.spawnLineY`(**−40**) **바로 아래**」였다. 그러나 **`spawnLineY = −40`은 아레나 rect(y 0~720)의 위쪽 = 화면 밖**이므로(§1.1) 문면대로 그리면 선이 y ≈ −35에 놓여 **보이지 않는다. 예고가 안 보이면 예고가 아니다** — 이 섹션의 **유일한 신규 예고 채널이 화면에 없었다.** ★ **y = 48은 새 자유 숫자가 아니다** — `view.bandTopH`(48)의 파생이며 **아레나 안에서 가장 위** = 「위에서 온다」를 `spawnLineY`가 뜻하려던 바로 그것이다. 레이어 8 · 알파 0.50도 §12.3의 **「공중 텔레그래프」 규격의 재사용**이지 새 규격이 아니다.
> ★ **②가 이 설계의 핵심**: 자홍 점선 수평선은 §7.4의 **`wall` 이미터 텔레그래프와 문자 그대로 같은 도형**이다. 새 어휘 0, 새 색 0, ★ **새 레이어 0 · 새 알파 0**(v1.2가 그 두 개를 마저 재사용으로 닫았다). 플레이어는 이미 "자홍 점선 = 곧 저기가 아프다"를 배웠으므로 **위기 예고를 배울 필요가 없다.** 그리고 3.0 ≥ 모든 텔레그래프 하한(§12.4)이므로 예고 없는 스파이크가 구조적으로 불가능하다.
> ★ **`visual.crisisWarn` 키는 존재하지 않는다** (§7.12.2 · §18.3-02-2). 02가 제안한 「띠 전체가 테마 속성색으로 3회 펄스」는 **§7.2의 가장 강한 예약(「적 탄·텔레그래프에 4속성 색 사용 절대 금지」)의 정면 위반**으로 기각됐다 — 위기 예고는 **위협 예고 = 텔레그래프**다. 정본이 덧붙인 결정적 근거: 02의 우려("예고가 어느 키를 누를지를 말해야 한다")는 정당하나 **그 정보는 이미 화면에 3중으로 있다** — `crisisElementRule = "themePure"`이므로 새떼는 100% 테마 속성이고, 테마 속성은 **배너 · 좌 패널 혼합 바 · 화면의 모든 적 외곽선**이 말한다. **예고가 그것을 네 번째로 말할 이유가 없다.**
> 화면 가장자리 호박 펄스도 **쓰지 않는다** — 호박은 §7.12.4의 **5곳 전수 목록** 전용이며 전부 **"내 몸에 무슨 일이 일어난다/일어났다"** 한 문장이다. 새떼 진입은 내 상태가 아니라 외부 위협이다(F-2).

**드래프트 큐 배지**: 위기 세션 중 레벨업 = 큐 적립(§6.4) → HUD에 **`LV UP ×N`**. 이 배지는 위기 세션 25초 동안만 존재하고, 페이즈 종료 시 DRAFT_QUEUE가 소진한다.

### 3.3 DRAFT — 화면

**전체화면 상태.** 정지된 플레이필드가 알파 0.25로 비친다(정지되어 있으므로 **맥락**이지 정보가 아니다 — F-1).

```
┌──────────────────────────────────────────────────────────────┐
│  Lv.23 → 24     테마 뱃지 [화산 불~70/물~10/풀~10/노말~10]      │
│  투자  불 +2   물 +1   풀 0        합계 3 / 6      리롤 ×2    │
├────────────────┬────────────────┬────────────────────────────┤
│    [ 1 ]       │    [ 2 ]       │    [ 3 ]                   │
│    카드        │    카드        │    카드                    │
│                │                │                            │
├────────────────┴────────────────┴────────────────────────────┤
│  슬롯   ① forward Lv5 [불]   ② lance Lv3 [불]   ③ aura Lv1 [—]   ④ —   │
│  [1][2][3] 즉시 선택   [←→] 커서   [Enter] 확정   [F] 리롤   [Tab] 재정렬 │
└──────────────────────────────────────────────────────────────┘
```

| 영역 | 내용 |
|---|---|
| 상단 | 레벨 전이 · **테마 뱃지**(구성비 축약) · **속성 투자 3종 + 합계 예산**(§m1) · 리롤 스톡 |
| 카드 3장 | §11.1의 **필수 표기 표를 그대로** 렌더 (카테고리별) |
| **하단 슬롯 스트립** | ★ **무기 4칸 상시 표시 + 현재 스탠스 기준 부여 상태.** 기체의 임뷰 칩(§7.5 ②)과 **같은 표기 언어**(속성색 채움 = 부여 / 은색 = 무속성) |
| 하단 키 힌트 | 키캡 그림 |

**★ 하단 슬롯 스트립이 없으면 `elementLevel` 카드는 무의미한 숫자다.** "불 +2 → +3"이 무엇을 바꾸는지는 **슬롯 순서를 볼 때만** 읽힌다. 그래서 이 스트립은 장식이 아니라 **§11.1의 `elementLevel` 필수 표기(`전환 시 부여 무기: 2개 → 3개` + before/after 슬롯 그림)의 그릇**이다.

**입력** (§5.2 그대로, 재정의 없음): `←→` 커서 / `Digit1~3` 즉시 선택 / `Enter` 확정 / `F` 리롤(최대 2회/드래프트) / `Tab` 재정렬 / `QWER`·`Space`·`Shift` **죽은 키**.

**클럭**: 완전 정지(§6.4). 적·탄·스폰·페이즈 타이머 전부. **무제한 사고 시간.**

**카드 선택 직후 (같은 화면에서 일어나는 일)**
- `elementLevel` 확정 → **하단 스트립이 그 자리에서 갱신**(§4.3 "재계산 시점 = 즉시"). 부여가 1칸 늘어나는 것을 **결정한 화면에서 본다.**
- `newWeapon` 확정 → `draft.slotAssign = "append"` = 가장 앞의 빈 슬롯에 자동 배치 → **그 화면에서 바로 `Tab` 재정렬 가능**(§5.3). 획득 순서가 몰래 부여 우선순위가 되는 사고를 여기서 막는다.
- `resupply` 확정 → 코인 +40, 코인 표시와 **환산 점수 표시가 동시에** 오른다.

### 3.4 슬롯 재정렬 모드

| 항목 | 확정 |
|---|---|
| 진입 | 드래프트 화면에서 `Tab` (§5.3) |
| 조작 | `←→` 슬롯 커서 / `Space` 집기·놓기(스왑) / `Tab`·`Escape` 종료 |
| 표시 | 하단 스트립이 확대되어 화면 중앙으로 승격. 집은 슬롯은 들린 상태로 표시. **집는 순간 부여 프리뷰가 실시간 갱신** — 놓기 전에 결과가 보인다 |
| 비용 | 없음. 횟수 제한 없음 |
| **가능 시점** | ★ **드래프트 화면에서만.** 상점·일시정지·인게임 전부 불가(§5.3) |

**정규 창구 = `MOB_PHASE → DRAFT_QUEUE`** (위기 세션 큐 소화 = 보스 직전). 보스가 요구할 스탠스 퍼즐을 **알기 전**이 아니라 **테마를 알고 중간보스를 겪은 뒤**에 순서를 정한다.

**보스전 재정렬은 코드 한 줄 없이 봉쇄된다**: 재정렬 = 드래프트 전용 + 보스전에 드래프트가 구조적으로 발생 불가(§6.4) → **곱하면 0.** "부위마다 정지 → 재정렬"이라는 체스식 최적화가 **금지 규칙 없이** 사라진다.

### 3.5 BOSS_INTRO — 3초는 연출이 아니라 정보 창이다

`stage.bossEntrySec`(3.0). 보스 무적 + 발사 없음 + **타이머 정지**(§6.3, `timerStartsAfterIntro`).

| 초 | 무슨 일이 |
|---|---|
| 0.0 | 보스가 스폰 라인에서 하강. 상단 띠 타이머는 **`3:00`에 정지한 채 표시** |
| 0.5~2.5 | ★ **부위가 하나씩 점등** — `parts[]` 순서대로 속성색 외곽선 3px + 부위 글리프 + 소형 HP 바가 켜진다. 코어는 마지막, 은색 + ● |
| 2.5~3.0 | 전신 자홍 림 라이트 1회(§7.4 보스 대형 패턴 언어의 예고편) |
| 3.0 | **타이머 시작 + 보스 피격 가능** (같은 프레임) |

> **★ 부위 순차 점등이 이 3초의 정체다.** 강철 가오리라면 `물 → 불 → 풀 → 노말(코어)` 이 순서대로 켜지고, 플레이어는 **3초 동안 정답 스탠스 3개를 읽는다**(풀 → 물 → 불). 이것이 "정보 기반 선택 > 운빨"의 보스전 판이며, 정본이 이미 가진 채널(§7.6 부위 판독)의 **재생 순서만 정한 것**이다. 새 어휘 0.
> 이 3초는 **타이머 180에서 차감되지 않는다**(§6.3). 정보는 공짜로 준다.

### 3.6 STAGE_CLEAR → HEAL

| # | 단계 | 내용 |
|---|---|---|
| 1 | **STAGE_CLEAR** (`flow.stageClearSec` 2.0, `Space` 스킵) | 스테이지 소계 4줄: `처치 · 상성 보너스 · 보스 격파 · 남은 시간 (잔여 M:SS × 50)` + **파 막대 1개**(§6.5) + **`무피격 ✔ / ✕`** |
| 2 | **DRAFT_QUEUE** | 큐 소화 (§1 — 정상 런에서 0장, 0프레임 통과) |
| 3 | **HEAL** (`flow.healSec` 1.5, 스킵 가능) | **회복량 = `ceil(현재 최대HP × flow.stageClearHealPct(0.25))`.** "현재 최대HP" = `player.hpMax`(100) + 상점 `maxhp` 스택(+10 × `maxPurchases` 4 = **+40** → 140) + 패시브 `bulkhead`(Lv5 = **+30**) → **100~170** (§11.2 · §9.6 · §16-#40). **비율이므로 최대HP 투자가 회복량에도 되돌아온다** |
| 4 | **THEME_BANNER(i+1)** | 다음 테마 고지 (§3.1) |
| 5 | **SHOP** | 지갑을 여는 유일한 지점 (§7) |
| 6 | **MOB_PHASE(i+1)** | 다음 스테이지 |

**`무피격 ✔`는 약속이지 지급이 아니다.** STAGE_CLEAR는 무피격 성립을 **표시**하고 HUD 무피격 pip 6칸 중 그 스테이지 칸을 **은색 채움**으로 바꾸지만(§7.12.5의 3상태: 진행 중 = 은색 외곽선 빈 원 / **성립 = 은색 채움** / 깨짐 = `palette.neutralGray` ✕), **점수는 붙지 않는다.** 근거는 §6.2 — 컨티뉴가 무피격을 **런 전체 소급 무효**(§11.4)로 만들 수 있으므로, 이미 지급한 점수를 회수하는 UI가 존재해서는 안 된다.
**실드로 막은 피격은 피격이 아니다** (`shieldPreservesNoHit: true`, §11.3) → pip는 채움을 유지한다.

**스테이지 6은 STAGE_CLEAR → RESULTS**(§6.5). HEAL·THEME_BANNER·SHOP 없음.

---

## 4. 보스 타이머 UX

### 4.1 3단 상태 — ★ v1.1에서 정본이 흡수했다 (§7.12.1, 05 채택 / 02 기각)

타이머는 상단 띠 중앙(§1.2, y 0~40), 게임시간 `3:00 → 0:00` 표시. 옆에 **배속 배지 `×3.0`** (= 이 문서에서 라벨 없는 `×N`이 등장하는 유일한 자리, §11.3의 표기 규칙).

**이 규격은 이제 정본 §7.12.1이 소유한다. 아래는 인용이다.**

| 상태 | 조건 | 색 | 연출 | 오디오 |
|---|---|---|---|---|
| **정상** | 잔여 > `timerWarnSec`(60) | `palette.element.normal` (은색) | — | — |
| **경고** | 잔여 ≤ 60 | ★ **색 불변 (은색)** | `visual.timer.warnScale` **1.15** + `warnPulseHz` **0.5** | — |
| **빨간불** | 잔여 ≤ `timerRedAlertSec`(30) | ★ **`palette.threat.enemyBullet` (자홍)** | `alertScale` **1.30** + `alertPulseHz` **1.0** | **1Hz 틱**(§7.10) |

> **★ 「노랑」 라벨은 삭제됐다 (05-① 채택)**: v1.0 §6.3의 `타이머 경고 1 (노랑)`은 **팔레트 키가 아니었다** — §7.2의 `palette`에 노랑은 **존재하지 않고**, 노랑 `#FFE24A`는 §7.2가 **호박 상태이상 채널과 명도·색상이 사실상 같다는 이유로 명시적으로 배제**한 색이다. 그대로 뒀다면 **구현자가 팔레트에 없는 색을 발명**했다. v1.1의 §6.3은 이제 `timerWarnSec | 60 (색 라벨 없음 — §7.12)`이다.
> **★ 왜 경고(60초)에 색이 필요 없는가**: 크기(1.15)·맥동(0.5Hz)·그리고 **30초에 오는 것**이 이미 2단 경고다. §7.2 대원칙: **hue는 속성 전용 예약 채널.**
> **★ 왜 빨간불에 자홍인가**: 자홍 = **"이것이 나를 죽인다"의 단일 채널**(적 탄·텔레그래프). 타이머 만료 = **즉사**(`boss.timerExpire: "kill"`). **자홍은 은유가 아니라 참이다.** 정본의 정리: ***"이 게임에 빨강은 없다. 자홍이 이 게임의 빨강이다"*** — 「빨간불」은 정본이 붙인 **이름**이지 색 지정이 아니었다. 그리고 자홍은 **`ShiftLeft`가 유효한 창의 시각 정의**이기도 하다(§4.3).
> **★ 호박은 여기 없다 (02 기각의 근거)**: 02는 「≤60초 = 호박」을 제안했고 정본이 기각했다 — ① **호박의 정의는 "내 상태"**인데 타이머는 **외부 시계**다("코인이 부족한 것도 내 상태가 나쁜 것"이 되어 호박이 무한 확장된다) ② **위치가 결정적이다** — 타이머는 **아레나 오버레이 띠 = 플레이필드 안**이고 §7.2의 예약은 플레이필드 밖 + 온-플레이어다 → **문면 위반.** 화면 가장자리 비네트도 같은 이유로 쓰지 않는다(F-2).
> **저체력 HP 바의 호박은 유지된다** — 그것은 §7.12.4-①의 **두 번째 표면**(같은 사건·같은 주파수)이지 새 채널이 아니다.

### 4.2 타이머가 멈추는 곳 = 정확히 둘

`PAUSE` + `boss.phaseTransitionSec`(1.5, 최대 2회) — §6.3. **드래프트는 보스전에 발생하지 않으므로 세 번째 후보가 없다**(§6.4). 실효 최대 = **183 게임초**이고 표시는 180→0이며 전환 중 **멈춘 채로 보인다**(멈춘 것이 보이는 것이 곧 "이 3초는 공짜"라는 고지다).

### 4.3 시간 연장 토큰 (`ShiftLeft`)

정본 §11.4 그대로(`ShiftLeft` · 빨간불에서만 유효 · **+30 게임초** · 보유 상한 2 · 대가 = 그 보스의 남은 시간 보너스 0). ★ **v1.1에서 이 섹션의 UX 경계가 두 개의 정본 키로 흡수됐다** — `hud.tokenKeycapGatedDisplay`(§9.4.1)와 `시간 보너스 ✕`의 자리(§7.12.5).

| 상황 | 화면 |
|---|---|
| 토큰 보유 + **빨간불 아님** | HUD 토큰 아이콘 **은색·비활성**. `Shift` 입력 = **완전 무시, 피드백 없음** |
| 토큰 보유 + **빨간불** | 토큰 아이콘이 **자홍으로 점등** + 1Hz 동기 펄스(타이머와 같은 위상) → **"지금 이 키가 먹는다"**. ★ **동시에 상단 띠 y 30~40(중앙)에 `[Shift] +30초` 힌트**(§7.12.5) |
| 발동 | 타이머 숫자가 `+0:30` 오버슈트 애니메이션 후 자홍 → **은색 복귀**. ★ **동시에 같은 자리(y 30~40)의 힌트가 사라지고 `시간 보너스 ✕`(회색 `palette.neutralGray`, `fontSmallPx`)가 켜져 그 보스전 내내 남는다. 스테이지 클리어 시 소멸** |
| 토큰 0 | 아이콘 미표시 |

**★ 발동 후 타이머는 「경고」이지 「정상」이 아니다 (산술 확인)**: 발동 조건이 잔여 ≤ 30이므로 발동 후 잔여 ∈ (30, 60]이다 → §7.12.1의 **경고 조건(≤60)에 그대로 걸린다.** 즉 자홍은 풀리지만 **`warnScale` 1.15 + `warnPulseHz` 0.5는 유지**된다. (v1.0판 이 문서는 "정상 상태로 강등"이라 썼다 — **틀렸다.** 60을 넘길 방법은 연속 사용뿐이다.)

> ★ **`시간 보너스 ✕` 마크가 `timeTokenForfeitsTimeBonus`(LOCKED, §11.3)를 화면에 존재하게 한다.** 대가가 결과 화면에서 처음 드러나면 그건 함정이지 결정이 아니다. 마크가 보스전 내내 남아 있어야 **"이 판을 포기할래 점수를 포기할래"**가 진짜 결정이 된다.
> ★ **정본이 자리를 준 근거 (§7.12.5)**: *"토큰은 **타이머의 대가**다. 대가를 타이머 옆에 두지 않으면 `timeTokenForfeitsTimeBonus`가 결과 화면에서 처음 알려지는 함정이 된다."* 그리고 y 30~40은 **`[Shift] +30초` 힌트와 같은 자리이며 배타적이다** — 토큰을 쓰면 힌트가 사라지고 ✕가 온다. **같은 픽셀, 같은 의미축.**
> ★ **`hud.tokenKeycapGatedDisplay` (§9.4.1)**: 정본 §11.4는 "빨간불일 때만 유효, 그 외 **입력 무시**"라고만 했고 **입력이 무시된다는 것을 알리는 채널이 없었다** → *"왜 Shift가 안 먹지"*가 발생 불가해진다. **오발 불가**(§11.4)이므로 은색·비활성 아이콘은 **금지 표시가 아니라 상태 표시**다.

---

## 5. 사망 · 컨티뉴

### 5.1 사망 연출 — 두 사인 공통

| 항목 | 확정 |
|---|---|
| 트리거 | **HP 0** 또는 **보스 타이머 만료** (잔기 없음, `player.lives = 0`, §2.1) |
| 연출 | ★ **`flow.deathAnimSec` = 1.5, 두 사인 공통** (§5.5 · 05-⑧ 채택). 게임 클럭 정지 + **입력 전면 무시** |
| ★ **어느 시계로 흐르나** | ★ **`flow.menuSpeed`(1.0)로 흐른다** (§0.2.1 확정). 정본: *"`DEATH`는 **런 안이다**(컨티뉴로 복귀 가능). 그러나 게임 클럭은 정지 상태이며 `deathAnimSec`는 `menuSpeed`로 흐른다 — **사망 연출이 Disaster에서 1/3로 짧아지면 사인을 못 읽는다.**"* → 1.5 게임초 = **전 난이도에서 실제 1.5초** |
| 근거 | 정본 §6.3이 타이머 만료에만 "1.5초 처형 연출"을 명시했다. HP 0에 연출이 없으면 **죽는 순간과 DEATH 화면 사이에 0프레임** → F-3(엣지 트리거)만으로는 "죽고 0.1초 뒤에 누른 `Space`"를 못 막는다 → **컨티뉴 기회를 손가락이 먹는다.** 규칙을 하나로 통일하면 연출 비용 0에 사고가 사라진다 |

### 5.2 DEATH 화면

```
┌─────────────────────────────────────────────────┐
│              ✕  체력 소진                        │   ← 사인: 아이콘 + 1단어
│         스테이지 4 / 6 · 경과 18:42              │
│              현재 점수  84,300                   │
├─────────────────────────────────────────────────┤
│   파 막대                                        │
│   S1 ████████████▏      S2 ██████████▏          │
│   S3 ███████▏           S4 ██▏  ← 여기서 뒤처졌다│
├─────────────────────────────────────────────────┤
│   ▶ 컨티뉴   150 코인   (보유 210)               │
│     포기 → 결과                                  │
└─────────────────────────────────────────────────┘
```

| # | 요소 | 규칙 |
|---|---|---|
| 1 | **사인** | `체력 소진` / `시간 초과` — **아이콘 + 1단어**(§11.4). *왜 죽었는지 모르는 죽음을 만들지 않는다* |
| 2 | 진행 | 도달 스테이지 / 경과 게임시간 / **라이브 점수**(§6.2의 확정 항목 합 × 배율) |
| 3 | **파 막대** | 스테이지별 보스 잔여시간 vs `flow.stagePar[i]` (§6.5) — **어디서 뒤처지기 시작했는지가 그림으로** |
| 4 | 컨티뉴 | `flow.continueCost` **150 코인 고정**(1회 캡이라 인상 불필요, §11.4). 보유 ≥ 150 → 활성 / 미만 → **회색 + 가격 표시**(다음 런의 학습: "아껴야 하는구나") |
| 4′ | ★ **2회차 없음** | ★ **`flow.continueMaxPerRun = 1` — 런당 1회 하드캡**(§11.4). 이미 쓴 런의 두 번째 사망에서는 **항목 자체가 뜨지 않는다**(회색이 아니라 부재 — 회색은 "코인이 모자란다"는 뜻이고 여기선 코인의 문제가 아니다) → 커서는 `포기` 하나만 |
| 5 | 포기 | → RESULTS |

**입력**: `↑↓` 커서 · `Enter` 확정 · `Space` 같은 난이도 즉시 재시작(§5.5) · `Escape` = **포기**(= 커서를 포기에 두고 Enter와 동일).
**F-3 + `deathAnimSec` 1.5초**가 `Space` 오발을 이중으로 막는다. **카운트다운 없음 — 무기한 대기**(§11.4).

### 5.3 컨티뉴 — 부활

정본 §11.4의 표를 **재정의하지 않는다.** 이 섹션이 더하는 것은 **소급 무효의 화면 표현** 하나:

| 순간 | 화면 |
|---|---|
| 컨티뉴 확정 | 코인 −150 (코인 표시와 **환산 점수 표시가 동시에** `150 × coinToScore(20)` = **3,000 × 난이도 배율**만큼 떨어진다 — §11.4가 "코인 150 = 점수 −3000 자동"이라 부르는 그것) |
| **동시** | ★ **HUD 무피격 pip 6칸이 전부 즉시 `palette.neutralGray` ✕로 바뀌고**(§7.12.5 — **소등이 아니라 ✕다**) `퍼펙트 ✕` 마크가 **같은 행 우측 끝**에 켜져 **런 종료까지 남는다** |
| 부활 | 아레나 하단 중앙 · HP 만피(`continueHealToFull`) · 적 탄 전량 소거(**잡몹은 유지**) · `flow.continueIframeSec`(3.0) 점멸 무적 · **빌드 전부 유지**(레벨·무기·속성·패시브·슬롯 순서·스탠스·스톡) |
| 보스전이면 | 보스 HP **깎은 그대로 유지** · 타이머 = **`max(현재 잔여, flow.continueTimerRestoreSec)`**(60) |
| ★ 재개 직후 타이머 | ★ **「경고」다. 빨간불이 아니다.** `max(·, 60) ≥ 60`이므로 §7.12.1의 빨간불 조건(≤30)에 **산술적으로 걸릴 수 없다.** 잔여가 60을 넘던 중 HP로 죽었다면 그 값이 유지되므로 **정상**일 수도 있다. (v1.0판 이 문서는 "재개 즉시 빨간불"이라 썼다 — **틀렸다.** 같은 칸 안에서 자기모순이었고 정본의 어떤 문장도 그것을 지지하지 않았다.) |

> ★ **pip 6칸이 그 자리에서 ✕가 되는 것이 "이중 비용"을 즉시 보여준다.** 정본은 컨티뉴의 대가를 `perfectBonus` + **모든 스테이지의 `stageNoHitBonus` 소급 무효** + 코인 150으로 정했다(§11.4). 그 대가가 20분 뒤 결과 화면에서 처음 보이면 **함정**이다. 지금 6칸이 꺼지는 것을 보고 코인을 내는 것이 **결정**이다. — ★ **정본 §7.12.5가 02의 「텍스트 1행」안을 기각하며 인용한 것이 정확히 이 논거다**: *"05가 계약으로 요구한 것은 6칸이고 그 이유가 명확하다 — 컨티뉴의 대가를 그 자리에서 6칸이 동시에 꺼지는 것으로 보여준다. 텍스트 1행은 그 장면을 만들지 못한다."*
> ★ **타이머를 3:00으로 리셋하지 않는 것이 핵심**(§11.4): 리셋하면 컨티뉴는 "6분 보스전"이 되어 **게임의 유일한 허들이 코인으로 삭제**된다. **60초 = 마무리할 수 있는 시간이지, 다시 싸울 수 있는 시간이 아니다.**
> 그리고 §6.2의 "무피격은 정산 항목이지 라이브 항목이 아니다"가 여기서 값을 한다 — **회수해야 할 점수가 애초에 지급된 적이 없으므로**, 라이브 점수가 뒤로 가는 일이 발생하지 않는다.

---

## 6. 점수 상세

### 6.1 항목 — 정본 §11.3이 유일한 소유자

이 섹션은 **키도 값도 판정 규칙도 재정의하지 않는다.** 아래는 정본이 정한 것을 **언제 어디에 어떻게 보여주는가**만 확정한다.

### 6.2 ★ 라이브 점수 vs 정산 점수 (이 섹션의 결정)

| 구분 | 항목 | 확정 시점 |
|---|---|---|
| **라이브** (HUD에 실시간) | 처치 점수 · **상성 처치 보너스** · `midBossClearBonus` · `bossClearBonus` · **남은 시간 보너스** | 발생 즉시 |
| **정산** (RESULTS에서 일괄) | `stageNoHitBonus` · `perfectBonus` · `runClearBonus` · **남은 코인 환산** | 런 종료 |

**★ 왜 나누는가 — 3개의 문제가 한 번에 닫힌다**
1. **소급 무효가 자연스러워진다.** 컨티뉴는 `stageNoHitBonus`를 **런 전체 소급 무효**로 만든다(§11.4). 라이브에 넣으면 점수가 −18,000 뒤로 가는 화면을 만들어야 한다. 정산 항목이면 **회수할 것이 없다.**
2. **남은 코인은 원래 라이브가 될 수 없다.** 잔액 × 20은 런이 끝나야 확정된다 — 상점의 `= −N 점`은 **환산 예고**이지 점수 이벤트가 아니다.
3. **오락실 정산 의식이 성립한다.** RESULTS에서 순차 타이핑으로 붙는 항목이 있어야 결과 화면이 **의식**이 된다(§11.3).

**배율 처리 (산술 동치 확인)**
```
라이브 표시 = floor( (확정 항목 합) × difficultyMul )
최종 점수   = floor( (확정 항목 합 + 정산 항목 합) × difficultyMul )
```
곱셈이 합에 분배되므로 **"전부의 합계에 최종 곱"(§11.3)과 완전히 동일**하다. 배율을 라이브에서 이미 곱하는 이유: Disaster를 고른 이유가 **실시간으로 보여야** 한다.

★ **`score.roundMode = "floor"`는 이제 정본의 키다** (§11.3, 05-③ 채택): *"배율은 항목 합계에 **1회** 곱하고 `floor`는 **최종에 1회만**."* 정본이 명문화한 근거 — 배율이 비정수(1.6 / 2.5)라 정수화 규칙이 **반드시 필요**하고, 규칙이 없으면 구현자가 항목마다 반올림해 **라이브 점수와 정산 점수가 갈린다** → §11.3이 요구하는 "놓친 보너스를 회색으로 함께 표시"가 **1점 어긋난 숫자를 보여준다 = 신뢰 붕괴.**
> **주의**: 위 두 식에 `floor`가 각각 하나씩 있는 것은 **두 벌의 반올림이 아니다** — 라이브 표시는 **표시일 뿐 누산되지 않는다.** 누산되는 것은 항목 합(정수)이고, `floor`는 **화면에 숫자를 그리는 순간에만** 적용된다. 최종 점수의 `floor`가 정본이 말하는 "최종 1회"다.

### 6.3 상성 처치 보너스 — 화면에서 보이는 곳

정본 §11.3: 개체마다 `총 피해`·`×2 피해` **2개 누산기**, 처치 시 지분 ≥ 0.5면 성립 → 처치 점수 ×1.5.

**막타가 아니라 지분이므로, 이 판정은 UI로 노출할 수 없고 노출해서도 안 된다.** 개체마다 지분 게이지를 띄우면 그건 §11.3이 배제한 **체스식 최적화**를 도로 초대하는 것이다. 이 섹션의 확정:

| 지점 | 표현 |
|---|---|
| 인게임 | **없음.** ×2 히트의 감각(§7.7: 화이트-핫 + 임팩트 프리즈 + 파티클 3 + 저역 크런치)이 곧 지분을 쌓고 있다는 피드백이다 |
| STAGE_CLEAR | 스테이지 소계에 `상성 보너스 +12,400` 1줄 |
| RESULTS | 런 합계 1줄 |

> **최적 플레이 = "제대로 된 스탠스로 싸우기"**(§11.3). 화면이 지분을 보여주면 최적 플레이가 "지분을 관리하기"로 변질된다. **보여주지 않는 것이 설계다.**

### 6.4 RESULTS 화면

```
┌───────────────────────────────────────────────────────────┐
│                     C L E A R                              │   ← 또는 GAME OVER — 체력 소진 / 시간 초과
│                  NORMAL · 시드 3F8A21C0                    │
├───────────────────────────────────────────────────────────┤
│  처치                                        128,400       │
│  상성 처치 보너스                             41,200       │
│  중간보스 격파      ×7                         3,500       │
│  보스 격파          ×6                        12,000       │
│  남은 시간          누적 4:20 (260초)         13,000       │
│                     ↳ 시간토큰 1회 · S4 보너스 0   ⌐회색⌐   │
│  스테이지 무피격    3 / 6                      9,000       │
│  퍼펙트             ✕                              0  ⌐회색⌐│
│  클리어 보너스                                10,000       │
│  남은 코인          62 × 20                    1,240       │
│  ─────────────────────────────────────────────────────    │
│  소계                                        218,340       │
│  난이도 NORMAL                             점수 ×1.0       │
│  최종                                        218,340       │
├───────────────────────────────────────────────────────────┤
│  파 막대  S1 ███████▏ S2 █████▏ S3 ████▏ S4 ██▏ S5 ███▏ S6 █▏│
│  최고 기록  198,200  →  NEW RECORD                         │
│  [Space] 같은 난이도로 다시   [Esc] 타이틀                  │
└───────────────────────────────────────────────────────────┘
```

| 규칙 | 확정 |
|---|---|
| 헤더 | `CLEAR` / `GAME OVER — 체력 소진` / `GAME OVER — 시간 초과` (§11.3) |
| 연출 | **오락실식 순차 타이핑**, `Space` 즉시 완료. (F-3에 의해 진입 시 눌려 있던 `Space`는 안 먹는다 → **완료와 재시작이 같은 키를 공유해도 안전**하다: 첫 상승 엣지 = 완료, 두 번째 = 재시작) |
| ★ **놓친 보너스** | **회색으로 함께 표시**(§11.3). `퍼펙트 ✕` · `무피격 3/6` · `시간토큰 1회 → S4 시간 보너스 0` · 미도달 스테이지 |
| 파 막대 | 6개 (§6.5). 미도달 스테이지 = 회색 빈 막대 |
| 기록 | **난이도별 최고점수 비교.** 갱신 시 전용 연출 + SFX → localStorage `best.<diff> = {score, seed, at, stage}`(§14) |
| **기록 조건** | ★ **사망해도 기록한다**(§11.3 "사망해도 점수는 집계된다"). `score > best.<diff>.score` 일 때만 갱신. **어트랙트 런은 제외**(§2.2) |
| 시드 | 8자리 hex (§10.2) |
| 키 | `Space` = **같은 난이도로 즉시 재시작**(TITLE·DIFFICULTY 경유 없음) / `Escape` = TITLE |
| **표기** | 배율 행은 **`점수 ×N`으로 라벨**한다(§11.3, 02-B9) — 라벨 없는 `×N`은 배속 전용이므로 결과 화면에 등장할 수 없다 |

> **이 게임엔 언락이 없다. 결과 화면이 유일한 교사다**(§11.3). "다음엔 어느 레버를 당길까"가 여기서만 학습되므로, **놓친 보너스를 회색으로 보여주는 것은 장식이 아니라 유일한 교육 채널**이다.

**★ 위 목업의 산술 검산** (C-7: 목업의 숫자는 전부 예시이며 확정값이 아니다. 검산은 **정본의 계수와 정합함**을 보이기 위한 것이다)

| 항목 | 식 (정본 §11.3의 계수) | 값 |
|---|---|---|
| 중간보스 ×7 | `7 × midBossClearBonus(500)` | 3,500 ✔ |
| 보스 ×6 | `6 × bossClearBonus(2000)` | 12,000 ✔ |
| 남은 시간 | `260 게임초 × timeBonusPerGameSec(50)` | 13,000 ✔ |
| 무피격 3/6 | `3 × stageNoHitBonus(3000)` | 9,000 ✔ |
| 퍼펙트 ✕ | 6/6 미달 → `perfectBonus` 상실 | 0 ✔ |
| 클리어 | `runClearBonus` | 10,000 ✔ |
| 남은 코인 | `62 × coinToScore(20)` | 1,240 ✔ |
| **소계** | `128,400+41,200+3,500+12,000+13,000+9,000+0+10,000+1,240` | **218,340** ✔ |
| **최종** | `floor(218,340 × 1.0)` | **218,340** ✔ |

- **중간보스 ×7의 정합**: `curve.midBossCount = [1,1,2,2,2,2]` → 6스테이지 완주 시 **조우 상한 10**. 7 격파 = 3기를 `midBossLeaveAfterSec`(30) 안에 못 죽여 놓쳤다는 뜻 → **7 ≤ 10 ✔ 성립하는 런이다.**
- ★ **v1.2에서 이 항목을 두 군데 고쳤다 (내 산술 오류 1건 + 정본 변경 1건)**:
  ① ★ **내 오류 — 분모가 6이 아니라 5였다.** v1.1판은 「372초 = **보스당 평균 62초**」라고 372를 **6으로** 나눴다. 그러나 **같은 칸에 `시간토큰 1회 · S4 보너스 0`이 적혀 있다** — `timeTokenForfeitsTimeBonus`(LOCKED, §11.3)에 의해 **S4는 누적에 1초도 기여하지 않는다.** 372 × 50 = 18,600이 정확히 인쇄된 보너스이므로 **372는 보너스가 붙은 5스테이지의 합**이고 평균은 62가 아니라 **74.4초**였다. 즉 목업이 「밴드의 상단」이라고 자평한 런은 실은 **밴드를 크게 넘는 런**이었다 — **자기 칸 안의 두 줄을 대조하지 않은 오류**이며 컨티뉴 「즉시 빨간불」(§5.3)과 같은 종류다.
  ② ★ **정본 변경 — 밴드가 25~70 → 30~60이 됐다**(§13.6.4, v1.2). `killTimeMedianBalanced` 밴드가 [110,155] → **[120, 150]**으로 좁아진 것의 여집합이다.
  → **260초 = 보스가 붙은 5스테이지 × 평균 52.0초.** 밴드 **30~60 ✔** 안이고 파 평균 51.1(§6.5)의 **바로 위** = **파 페이스에 정확히 붙은 런.** 그런데 `무피격 3/6`이고 `시간토큰 1회`다 → **"표준 페이스로 갔지만 맞으면서 갔고 한 판은 시간에 쫓겼다"**는 하나의 이야기가 숫자만으로 읽힌다. 목업이 정본의 밴드 안에서 **서사적으로도 정합**하다.
- **남은 코인 62**: `certify.coinScarcity.medianEndCoins ∈ [0, 120]` ✔ 중앙값 밴드 안.
- **신기록 정합**: 218,340 > `최고 기록 198,200` → `NEW RECORD` ✔ (소계가 바뀌었으므로 다시 확인했다).

### 6.5 ★ 파(par) — ★ 정본이 흡수했다 (§11.5, 05-⑥ 채택)

v1.0의 정본은 `stagePar[i]`를 **스칼라**로 정의하고(§10.4) **라이브 고스트 마커**를 요구했으나(§11.4) **스칼라 → 라이브 위치의 사상이 없었다** → 구현자가 즉석 결정한다(C-6 위반). **이 섹션이 제안한 사상식을 정본이 §11.5로 채택했다. 아래는 인용이다.**

```
ghostHpPct(t) = clamp( 1 − t / (stage.bossTimerSec − flow.stagePar[i]) , 0, 1 )
                                                   // t = 보스전 경과 게임초
```

| 규칙 | 확정 (§11.5 · §1.2) |
|---|---|
| 의미 | **파 페이스는 `180 − stagePar[i]` 시점에 코어를 정확히 0으로 만든다** — 고스트는 "표준 플레이어의 코어 HP 곡선"의 **선형 근사** |
| ★ **마커 규격** | ★ **높이 8px 흰 세로선 2px + 상단 3px 사각 캡** (§1.2 · §11.5). **페이즈 노치(`phaseThresholds` 0.6/0.3 위치의 높이 4px 흰 눈금 2개)와 형태·운동이 다르다** — 노치는 고정, 고스트는 움직인다. 자리 = **코어 HP 바(y 40~48, x 350~930)** 위. 텍스트 0 |
| 읽는 법 | 실제 HP가 고스트보다 **오른쪽 = 언더파**(뒤처짐). **왼쪽 = 오버파** |
| 정지 | 타이머가 멈추면(`PAUSE`·페이즈 전환) `t`도 멈추므로 **고스트도 멈춘다** — 타이머와 일관 |
| ★ **미측정 센티넬** | **`flow.stagePar[i] == 0` = 미측정** → **마커를 그리지 않는다.** `flow.stagePar` 초기값 `[0,0,0,0,0,0]`이므로 **시뮬 인증 전에는 UI가 조용히 사라진다 = 거짓 정보를 표시하지 않는다** |
| 선형인 이유 | 실제 곡선은 부위 파괴 구간에서 **평평하다**(코어를 안 때린다). 그러나 **선형이 아니면 `stagePar` 스칼라 하나로 표현할 수 없고**, 곡선을 저장하면 `report/stages.csv`가 스칼라가 아니게 된다. **"지금 뒤처졌는가"만 답하면 되므로 선형으로 충분하다** |
| 데이터 소스 | `--certify` 통과 시 `report/stages.csv`의 중앙값이 `meta.json > flow.stagePar[6]`에 **기입**(§10.4) |
| ★ **목표 밴드** | ★ **`stagePar[i]` = 30~60** (§13.6.4 — **v1.2 개정**, v1.1의 25~70 폐기). `killTimeMedianBalanced ∈ **[120, 150]**`의 여집합(`180 − 그것`)이다 — ★ **그 밴드가 v1.2에서 사용자 요구 「균형 ~2~2.5분」 그 자체가 되었다**(§13.6.5) |

**★ v1.2와의 정합 재검산 (v1.1판의 이 표는 통째로 낡았다 — `dpsRef`가 재정의되며 보스 HP 전량이 ×1.20됐다)**

정본 §13.6.2·§13.6.3이 **v1.2에서 재산출한** 균형 빌드 격파 시간(**probe 모드 = `maxFarm`**)과 위 밴드를 대조한다:

| 스테이지 | 1 | 2 | 3 | 4 | 5 | 6 (테트라크) |
|---|---|---|---|---|---|---|
| ~~v1.1판 이 표~~ | ~~125.8~~ | ~~125.8~~ | ~~135.7~~ | ~~127.8~~ | ~~134.1~~ | ~~125.9~~ |
| ★ **`killTime` 균형 (§13.6.2 · §13.6.3, v1.2)** | **125.5** | **125.5** | **135.1** | **128.3** | **133.3** | **125.9** |
| → **예상 `stagePar` = `180 − killTime`** | **54.5** | **54.5** | **44.9** | **51.7** | **46.7** | **54.1** |
| ★ 밴드 **30~60** (§13.6.4) | ✔ | ✔ | ✔ | ✔ | ✔ | ✔ |

→ **6/6 전부 밴드 안**(평균 **51.1**)이고 `ghostHpPct`의 분모(`180 − stagePar` = `killTime`)가 **[120, 150]에 든다** ✔. 즉 고스트는 **보스전의 70~75% 지점에서 0에 닿는 선**이며, 어느 스테이지에서도 **화면 밖으로 나가거나 즉시 0이 되지 않는다.** 사상식이 **v1.2의 새 보스 HP(core 976 / armor 2,391×2 / 선택 478) 위에서도 그대로 작동한다** — 사상식은 `killTime`만 보고 HP를 보지 않으므로 **HP 전량이 ×1.20 되어도 구조가 흔들리지 않는다.** (이것이 §11.5가 스칼라 하나로 사상한 배당금이다.)

**★ 내가 정본의 산술을 독립 재현했다** (인용이 옳은지 확인하는 것이 인용의 의무다 — C-8):
```
killTime(균형, i) = (2·armor + core) × bossRamp[i] / (dpsRef[1] × uptimeRef × m[i])  ... armor 구간
                  + core × bossRamp[i] / (dpsRef[1] × uptimeRef)                      ... 코어 구간(R1: 항상 ×1)
  dpsRef[1] × uptimeRef = 60 × 0.60 = 36     m = [1.35, 1.35, 1.43, 1.53, 1.62, 1.62]
  i=1: 4,782×1.000/(36×1.35) + 976×1.000/36 =  98.4 + 27.1 = 125.5 ✔ (정본 125.5)
  i=3: 4,782×1.125/(36×1.43) + 976×1.125/36 = 104.5 + 30.5 = 135.0 ✔ (정본 135.1)
  i=4: 4,782×1.125/(36×1.53) + 976×1.125/36 =  97.7 + 30.5 = 128.2 ✔ (정본 128.3)
  i=5: 4,782×1.222/(36×1.62) + 976×1.222/36 = 100.2 + 33.1 = 133.3 ✔ (정본 133.3)
  중앙값 = (125.9 + 128.3)/2 = 127.1 = killTimeMedianBalanced ✔
```

> ★ **선형 근사의 오차 — v1.2에서 비율은 그대로다**: `armorCoreRatio` = **4.90**(테마 보스) / **13.58**(테트라크)이므로 **보스전 시간의 대부분이 armor를 깨는 데 쓰이고 그동안 코어 HP는 움직이지 않는다.** 정본 §13.6의 **v1.2 수치**로 다시 계산하면 — 테마 보스 s1: armor `4,782/(36×1.35)` = **98.4s** / 코어 `976/36` = **27.1s** → **armor가 78%**(v1.1: 98.6/27.2 → 78%). 테트라크: armor `77,400/688.2` = **112.5s** / 코어 `5,700/424.8` = **13.4s** → **armor가 89%.** ★ **HP가 ×1.20 됐는데 비율이 한 자리도 안 바뀐 이유**: 앵커(`dpsRef[1] × uptimeRef`)가 30 → 36으로 **똑같이 ×1.20** 됐기 때문이다 — **분자와 분모가 같은 배로 커지면 시간도 비율도 불변**이다. 이것이 §13.6.2가 「v1.2가 바꾼 것은 앵커 하나다」라고 부른 것의 이 섹션 판이다.
> 실제 코어 곡선은 **긴 평평함 + 짧은 급락**이고, 고스트는 그것을 직선으로 근사한다 → **보스전 중반에는 거의 항상 "언더파"로 보인다.** 이것은 **버그가 아니라 §11.5가 명시적으로 수용한 근사**다("실제 곡선은 부위 파괴 구간에서 평평하다"). 플레이어가 읽는 것은 **"앞서는가 뒤처지는가" 한 비트**이고, 그 비트는 **격파 시점 근처에서 정확**하다 — 그리고 파 막대(사후)는 애초에 **잔여시간 스칼라의 비교**라 근사가 개입하지 않는다.

**★ ⚠ 위 표는 「probe로 읽었을 때」의 결과다 — 그리고 「run으로 읽으면 6/6이 밴드 밖」이다 (요청 ⑫)**

v1.2가 farm 축을 소유하면서(§13.5.1) **같은 「균형 빌드」에 두 개의 격파 시간이 생겼다**(§13.6.5). 그런데 `stagePar`가 **어느 쪽으로 측정되는지**는 정본이 말하지 않는다 — 그리고 두 읽기가 **정반대의 판정**을 낸다:

| 읽기 | `stagePar[i]` = `180 − ?` | 값 (i=1..6) | 밴드 30~60 |
|---|---|---|---|
| **ⓐ probe** (`maxFarm`, 무적 봇) | `killTimeMedianBalanced` 계열 | 54.5 / 54.5 / 44.9 / 51.7 / 46.7 / 54.1 | ★ **6/6 통과** |
| **ⓑ run** (`balanced` farm, 죽는 봇) | `killTime ÷ runFarmDpsRatio(0.83)` | **28.8 / 28.8 / 17.2 / 25.4 / 19.4 / 28.3** | ★ **6/6 실패** (전부 하한 30 미만) |

- **정본이 ⓐ를 시사하는 곳**: §13.6.4가 「`flow.stagePar[i]` = **`180 − killTimeMedianBalanced`** = 30~60」이라고 **직접** 쓴다. `killTimeMedianBalanced`는 §13.6.5가 **probe 모드로 정의**한 수다.
- **정본이 ⓑ를 시사하는 곳**: §11.5·§10.4는 데이터 소스를 **`report/stages.csv`의 중앙값**이라 하고 §10.4는 그것을 「스테이지별 **표준** 보스 잔여시간」이라 부른다. **`dpsProbe`는 무적이고 사망이 판정 대상이 아니다**(§10.4) → 그 봇의 페이스는 「**표준 플레이어**의 코어 HP 곡선」(§11.5의 자기 문장)이 아니다. 그리고 §13.6.5가 baseline(run)을 「**진단**」이라 부르며 `report/`로 출력한다.
- ★ **정본 자신이 ⓑ의 수를 이미 인쇄했다**: §13.6.5는 *「스테이지 3의 baseline 격파가 2.71분(타이머 180s 대비 **17.3초 여유**)」*라고 쓴다. **17.3 = `stagePar[3]`(run)이며, 정본 자신의 밴드 하한 30의 절반 이하다.** 즉 **정본이 자기 밴드 밖의 수를 자기 본문에 인쇄해 두고 두 문장이 서로를 못 보고 있다.**
- **이 섹션의 판단**: **고스트의 의미론은 ⓑ를 요구한다.** 고스트는 **지금 조종간을 잡은 사람**에게 "뒤처졌는가"를 묻는 UI이고, 그 사람은 무적도 `maxFarm`도 아니다. ⓐ로 채우면 **balanced-farm 플레이어는 6스테이지 내내 구조적으로 언더파**이며(격파 시점에도 24~28초 뒤처진 채 끝난다) — 그러면 고스트는 **"너는 항상 뒤처져 있다"만 말하는 장식**이 된다. 이것은 §11.5가 없애려던 것(「20분 뒤에야 알게 되는 언더파를 지금 알려준다」)의 **정확한 반대**다.
- ★ **그러나 이 섹션은 정할 수 없다.** 시뮬 모드(`run` / `dpsProbe`)의 소유자는 §10.4이고 밴드의 소유자는 §13.6.4다 — **둘 다 §17이 정본에 남긴 것**이며, 어느 쪽으로 정하든 **밴드 30~60 또는 `stagePar`의 정의 중 하나가 바뀐다**(= 정본 개정). → **요청 ⑫**(§11.3.1).
- **어느 읽기에서도 사상식은 죽지 않는다**(그래서 blocker가 아니다): ⓑ에서도 분모 `180 − stagePar` = 151.2~162.8 ≤ 180이므로 **고스트는 보스전 84~90% 지점에서 0에 닿고**, 화면 밖으로 나가지도 즉시 0이 되지도 않는다. 그리고 **`stagePar[i] == 0` 센티넬**이 인증 전의 모든 경우를 이미 덮는다. **바뀌는 것은 "누구의 페이스인가"이지 "그릴 수 있는가"가 아니다.**

> **"20분 뒤에야 알게 되는 언더파를 지금 알려준다"**(§11.4). 4스테이지에서 처음 파선을 밑돌았다면, 그 원인은 **1~3스테이지의 파밍**이다. 파 막대 6개가 그 인과를 그림 한 장으로 보여준다 — **텍스트 0, 판정 0, 잔소리 0.**

---

## 7. 상점 (SHOP) — 화면

### 7.1 진열 — 전체 목록 상시, 순서 고정

정본 §11.2: **전체 목록 상시 · 랜덤 진열 없음 · 재고 제한 없음(`maxPurchases`만) · 시간 제한 없음 · 스테이지 1~5 클리어 후 총 5회.**

**표시 순서 = §11.2 표의 순서로 고정** (이 섹션의 결정, 근거 = 머슬메모리):

```
① reroll     ② potion    ③ bomb      ④ shield    ⑤ timeToken
⑥ defense    ⑦ maxhp     ⑧ movespeed ⑨ magnet    ⑩ resist
```
**선택권 → 생존/보험 → 비-빌드 유틸.** 방문 5회 내내 같은 자리. 커서 시작 = **항상 ①**(결정적, 기억하지 않음).

### 7.2 화면

```
┌──────────────────────────────────────────────────────────────────────┐
│  상점 · 스테이지 3 클리어 후        다음: [숲 · 풀~70 불~10 물~10 노말~10] │
│  코인 210  ( = 4,200 점 )                            HP 88 / 110      │
├──────────────────────────────────────────────────────────────────────┤
│ ▶ ① 리롤        드래프트 1회 재추첨    1/3(스톡) 1/6(구매)  48 → 77  = −960 점│
│   ② 회복 물약   HP 35% 즉시 회복            2/8(구매)      90 → 135 = −1,800 점│
│   ③ 폭탄        스톡 +1                3/3(스톡) MAX       ⌐회색⌐            │
│   ④ 실드        피격 1회 무효          0/2(스톡) 0/4(구매) 60 → 90  = −1,200 점│
│   ⑤ 시간 토큰   보스 타이머 +30초      0/2(스톡) 0/2(구매) 80 → 144 = −1,600 점│
│   ⑥ 방어력      정액 감산 +2           2/4(구매) → 현재 4  95 → 138 = −1,900 점│
│   ...                                                                          │
├──────────────────────────────────────────────────────────────────────┤
│  [↑↓] 커서   [Enter] 구매   [Esc] 나가기                              │
└──────────────────────────────────────────────────────────────────────┘
```

**★ 위 목업의 가격 검산** (C-7: 목업의 숫자는 예시. 검산은 §11.2의 공식 `price(item, n) = ceil(basePrice × growth^n)`과 정합함을 보이기 위한 것 — **`n` = 그 항목의 런 내 구매 횟수**이므로 **구매 `n`회를 마친 항목의 「현재가」는 `price(·, n)`이지 `basePrice`가 아니다**)

| 항목 | `basePrice` / `growth` | 구매 `n` | 현재가 `ceil(b×g^n)` | 다음가 `ceil(b×g^(n+1))` | `= −N 점` (`×20 ×1.0`) |
|---|---|---|---|---|---|
| `reroll` | 30 / 1.60 | 1 | `ceil(48.0)` = **48** | `ceil(76.8)` = **77** | −960 |
| `potion` | 40 / 1.50 | 2 | `ceil(90.0)` = **90** | `ceil(135.0)` = **135** | −1,800 |
| `bomb` | 50 / 1.35 | — | 스톡 `3/3` = `bombStockMax` 도달 → **회색** | — | — |
| `shield` | 60 / 1.50 | 0 | `ceil(60.0)` = **60** | `ceil(90.0)` = **90** | −1,200 |
| `timeToken` | 80 / 1.80 | 0 | `ceil(80.0)` = **80** | `ceil(144.0)` = **144** | −1,600 |
| `defense` | 45 / 1.45 | 2 | `ceil(94.61)` = **95** | `ceil(137.19)` = **138** | −1,900 |

- ★ **v1.0판의 오류 2건을 정정했다**: ① `reroll`을 「1회 구매함 + 현재가 30」으로 그렸다 — **구매했으면 현재가는 48이다.** 스톡도 `2/3`이었으나 1회 구매 = 스톡 1. ② `defense`를 `94 → 136`으로 그렸다 — **`ceil(45×1.45²) = ceil(94.61) = 95`, `ceil(45×1.45³) = ceil(137.19) = 138`이다.** `ceil`을 빼먹고 절삭한 값이었다. 환산 점수도 −1,880 → **−1,900**으로 정정.
- **`⌐회색⌐` 조건 확인**: 코인 210이므로 표의 5항목 전부 구매 가능하다 → 회색의 유일한 사유는 `bomb`의 **스톡 상한**(`bombStockMax: 3`)이다. 코인 부족이 아니라 **상한 도달**이며, 이 구분이 §5.2의 컨티뉴 회색(= 코인 부족)과 다른 의미축임에 주의.

| 요소 | 규칙 |
|---|---|
| 헤더 | 방문 시점 · **★ 다음 스테이지 테마 뱃지(구성비 4수치, ★ 물결표 표기 — §8.2.1)** · 보유 코인 + **환산 점수**(`hud.coinShowsScoreValue`, §9.4.1) · 현재 HP / 최대 HP |
| 각 줄 | 아이콘 · 이름 · **효과 1줄** · **보유/상한** · **현재가 → 다음가** · ★ **`= −N 점`** (§11.2 표시 필수) |
| 구매 불가 | 코인 부족 / `maxPurchases` 도달 / 스톡 상한 도달 → **회색 + `Enter` 무시** |
| 구매 | `Enter` 즉시 (확인 없음, §5.4). **구매 불가 항목의 Enter가 무시되므로 오발의 대부분이 애초에 발생하지 않는다** |
| 나가기 | `Escape` → **확인 1회**(§5.4) → MOB_PHASE(i+1) |

**★ 헤더에 다음 테마를 다시 띄우는 이유**: §6.5의 전이 순서 근거가 **"테마가 상점 뒤면 유틸 구매가 장님"**이다. 배너는 2.5초 뒤 사라지므로, **상점에서 결정하는 순간에 테마가 화면에 없으면 그 순서의 의미가 반쯤 사라진다.** 배너(사전 고지) + 상점 헤더(결정 시점 재고지) = 순서 근거의 완성. 새 정보 0, 재배치뿐.

**★ `= −N 점`의 계산 (이 섹션의 확정 — 「정산의 순서」 위임 범위, §17)**
```
표시 = price × score.coinToScore(20) × difficulty[].scoreMul
```
난이도 배율을 **포함한다.** Disaster에서 리롤 첫 구매(30코인)는 −600점이 아니라 **−2,400점**이다. 배율을 빼고 표시하면 **고난이도에서 기회비용이 4배로 커진다는 사실이 화면에서 사라진다** — 그리고 그것이 정확히 이 게임의 중심 긴장(§11.2 "아껴서 점수 vs 써서 생존")이다. 보유 코인 옆의 환산 점수(`hud.coinShowsScoreValue`)도 같은 식.

**★ 이 식이 §11.3의 「배율은 합계에 최종 1회」와 충돌하지 않는 증명**: 남은 코인 환산은 **정산 항목**이므로 최종 점수는 `floor((… + 잔액×20) × mul)`이다. 코인을 `p` 쓰면 잔액이 `p` 줄어 최종 점수가 정확히 `p × 20 × mul` 준다 — **분배법칙.** 즉 `= −N 점`은 **새 점수 규칙이 아니라 §11.3의 식을 미분한 값**이며, 화면에 배율을 곱해 보여주는 것이 **실제 손실과 일치하는 유일한 표기**다. (`floor`는 최종에 1회만 걸리므로 이 예고값은 ±1점 오차를 가질 수 있다 — 예고이지 정산이 아니다.)

### 7.3 상점에 **없는** 것 (정본 확인)

| 없음 | 근거 |
|---|---|
| **컨티뉴** | 사망 시점 제시(§11.4). 상점 판매 모델은 폐기됨 |
| **슬롯 재정렬('빌드' 탭)** | 재정렬 = 드래프트 전용(§5.3). 창구가 둘이 되면 §6.4의 보스전 봉쇄 논증이 무너진다 |
| **무기·속성 레벨** | LOCKED. 코인이 빌드를 사면 성장의 의미가 무너진다 |
| **상점 리롤 / 로테이션 / 시간 제한** | 전체 목록 상시(§11.2). 랜덤 위에 랜덤 금지, 무-트위치 |

---

## 8. 온보딩 — 첫 60초, 텍스트 0줄

### 8.1 전제 — 정본이 온보딩에서 무엇을 빼앗았는가

| 폐기됨 | 결과 |
|---|---|
| **시각 스크립트**(`0:12 Lv2` …) | ★ **트리거는 XP뿐이다**(§9.9 M9). 아래 서술은 **트리거가 아니라 결과로서 관찰되는 순서**다 |
| **잡몹 데미지 숫자** | §7.7. **숫자로는 아무것도 못 가르친다** |
| **강제 드래프트 3장** | §9.9. 3장을 다 고정하면 선택이 아니다 |
| **좌/우 대비 웨이브** | §8.2. ±3%p 게이트 위반 + **불필요** |
| **온보딩 전용 보스** | 부위 수 4는 구조(§8.11). HP만 낮게 저작 |

**남은 채널은 정확히 이것뿐이다**: ① 스테이지 1의 **구성**(테마 `introOk` + 70/10/10/10 + `curve[0]`) ② `guaranteeElementCardOnFirstDraft` ③ `guaranteeNewWeaponUntilSlots: 2` ④ `autoEquipFirstElement` ⑤ `stanceHintPulse`(스테이지 1 전용) ⑥ §7.5의 스탠스 링·탈색 링·슬롯 스트립 ⑦ §7.7의 3배율 감각 ⑧ §11.1의 카드 필수 표기.

**새 코드 0, 새 트리거 0, 새 화면 0.**

### 8.2 ★ 첫 60초가 가르치는 것은 3개의 사실뿐

> ① **QWER은 뭔가의 스위치다** → ② **지금 내 스위치는 비어 있다** → ③ **스위치를 채우면 적이 다르게 반응한다.**

나머지(투자 상한, 슬롯 순서, 합계 예산, 보스 퍼즐)는 **60초의 과제가 아니다.** 60초 안에 ①②③이 서면 그 다음은 카드가 가르친다.

### 8.3 인과 사슬 (트리거 = XP · 시각 아님)

| # | 언제 | 무슨 일이 | 무엇을 가르치나 | 채널 |
|---|---|---|---|---|
| **0** | THEME_BANNER | `물 ~70% · 불 ~10% · 풀 ~10% · 노말 ~10%` + 미니휠에서 **풀 글리프가 밝다** | "이 판은 이런 색이다" | §3.1 |
| **1** | t=0, 첫 웨이브 | `forward` Lv1 · 노말 스탠스. **모든 히트가 ×1** = 얇은 틱 + 파티클 1 | **기준선(×1)의 감각** | §7.7 |
| **2** | 아무 때나 | 플레이어가 `W`를 눌러본다 → ★ **링이 불색으로 시작해 확장 중 회색으로 탈색되며 소멸 + "빈" 저음** | ★ **①과 ② 동시** — "이 키는 뭔가의 스위치인데 **지금 비어 있다**" | §7.5 |
| **3** | 첫 드래프트(Lv2) | 3장 = **[속성 보장] + [새 무기 보장] + [자유 1장]** | **속성축의 존재가 우회 불가능** + **선택은 진짜** | §11.1 |
| **4** | 속성 카드 확정 시 | `autoEquipFirstElement` → **그 스탠스 자동 장착** + HUD 키캡 번쩍 + **슬롯① pip가 은색 → 속성색** | "이 글자 = 이 상태" 결속 | §9.9 |
| **5** | 그 직후 첫 교전 | ★ **같은 무기, 같은 화면, 두 개의 다른 감각** — 화이트-핫 + 임팩트 프리즈 + 파티클 3 + **저역 크런치** vs **회색 방패 호 + 파티클 0 + 금속 "틴"** | ★ **③ = 유레카** | §7.7 |
| **6** | 상시(스테이지 1만) | `stanceHintPulse` — HUD 키캡 중 **지금 화면 적에게 ×2인 키**가 맥동 | 손가락이 텍스트 없이 따라간다 | §9.9 |
| **7** | 두 번째 무기 획득 후 | 투자 +1 · 무기 2 → 스트립 = **[속성색][은색][—][—]** | ★ **"앞의 N개만 부여"가 대비로 드러남** | §7.5 ② |
| **8** | 중간보스 (`midBossAtSec [35]`) | 속성 = `notThemeAndNotNormal` → **켜둔 스탠스로 잘 안 죽는다** | **전환의 필요** = 보스전 예고편 | §8.9 |
| **9** | 위기 세션 (95~120초, 30기) | 전원 테마 속성(`themePure`) → 정답 스탠스면 **25초 동안 전부 ×2** | **투자의 첫 페이오프** | §8.10 |
| **10** | 스테이지 1 보스 | 부위마다 다른 정답 스탠스 (R1~R6 보장) | **배운 것의 시험** | §8.14 |

**#5의 유레카가 60초 안에 도착한다**: 스테이지 1은 `enemyHpScale 1.0` · `spawnDensityScale 0.70` · `xpScale 1.0`(§8.3)이고 `forward` Lv1은 `chaff`를 1~2히트에 죽인다(§8.6). **첫 드래프트는 첫 웨이브 안에서 뜬다.**

> ★ **#9의 근거가 v1.2에서 바뀌었다 — 그리고 이 섹션이 쓰던 문장이 옳았다 (판정 7, §8.10)**. v1.1의 §8.10은 「새떼 = **스테이지 총 XP의 ~25%**」를 확정값으로 인쇄하고 그것을 「투자의 첫 페이오프」의 근거로 삼았다. ★ **v1.2가 그것을 폐기했다** — 04의 **실측 12~13%가 정본에 기입**되었고(`sea` s4 재검산은 **8.8%**), **S22(≤0.30)의 상한까지 올려도 총 XP 대비 18.2%가 천장**이므로 **~25%는 산술적으로 도달 불가능한 수**였다. ★ **S22는 상한(cap)이지 목표가 아니다** — 실측이 그 절반 아래인 것은 **위반이 아니라 여유**다.
> ★ **그런데 이 사슬의 #9 칸은 한 글자도 안 고친다.** 이 문서는 **처음부터 페이오프를 XP로 세지 않았다** — 칸에 적힌 것은 「전원 테마 속성 → **25초 동안 전부 ×2**」, 즉 **밀도와 화면**이다. 정본이 v1.2에 도달한 답이 정확히 그것이다: *「위기 세션이 특별한 이유는 「총 XP의 몇 %인가」가 아니라 **「25초 안에 60기가 전부 테마 속성으로 온다」**는 것이다 — **초당 2.4기 × 100% ×2**는 잡몹 페이즈 어디에도 없는 밀도이고, **그 순간의 화면**이 페이오프다. 12%든 25%든 **그 장면은 같다.** 정본이 25%에 매달린 것은 **페이오프를 XP로 착각한 것**이다」*(§8.10).
> **스테이지 1의 실제 밀도 검산 (이 섹션이 직접 했다)**: `crisisTotal`(60) × `swarmTotalScale[0]`(0.5) = **30기 / `crisisDurationSec` 25초 = 초당 1.2기 · 100% 한 색.** 대조군 = 같은 스테이지의 잡몹 페이즈 — 저작 리스트 **66~88기**(§13.2-⑪의 v1.2 밴드) ÷ `crisisStartSec` 95초 = **초당 0.7~0.9기 · 70% 한 색.** → ★ **위기 세션은 스테이지 1에서도 밀도 1.33~1.71배 × 순도 1.43배 = 「정답 스탠스로 ×2가 뜨는 적」이 초당 1.9~2.4배**다(1.2×1.00 = 1.20/s vs 0.7~0.9×0.70 = 0.49~0.63/s). `swarmTotalScale[0] = 0.5`가 밀도를 절반으로 낮추는 것은 **온보딩 배려이지 페이오프의 훼손이 아니다** — 세는 것이 %가 아니라 **한 색으로 뒤덮인 화면**이기 때문이다.

### 8.4 ★ 증명 — 어느 속성 카드를 골라도 ×2와 ×0.5가 같은 화면에 나온다

정본 §8.2는 "한 스탠스를 켜고 있으면 ×2(70%)와 ×0.5(10%)가 자연히 같은 화면에 나온다"고 선언한다. 그 선언은 **정답 스탠스를 켰을 때**의 서술이다. 그런데 첫 드래프트의 속성 카드는 **무엇이 뜰지 모르고, 무엇을 고를지는 플레이어의 자유**다. → **3개 경우 전수 확인이 필요하다.**

테마 속성 `T`, `counter`(T를 이김), `prey`(T가 이김). 혼합 = `T .70 / counter .10 / prey .10 / normal .10`(§8.2). 플레이어가 켠 스탠스 `S`:

| `S` | vs `T` (70%) | vs `counter` (10%) | vs `prey` (10%) | vs `normal` (10%) | ×2 있음 | ×0.5 있음 |
|---|---|---|---|---|---|---|
| **`counter`** | **×2** | ×1 | **×0.5** | ×1 | ✔ | ✔ |
| **`T`** | ×1 | **×0.5** | **×2** | ×1 | ✔ | ✔ |
| **`prey`** | **×0.5** | **×2** | ×1 | ×1 | ✔ | ✔ |

**3/3 경우 전부에서 ×2와 ×0.5가 같은 혼합 안에 존재한다** ✔
(검산 — 바다(T=물): `S=풀` → 풀→물 ×2 / 풀→불 ×0.5. `S=물` → 물→물 ×1 / 물→풀 ×0.5 / 물→불 ×2. `S=불` → 불→물 ×0.5 / 불→풀 ×2.)

> **★ 이것이 "속성 2개가 필요하다"(미결 M10)가 소멸한 정확한 이유다.** 대비 웨이브도, 강제 3장도, 두 번째 속성도 필요 없다 — **속성 1개 + 70/10/10/10이면 유레카가 산술적으로 보장된다.** 그리고 이 보장은 **6개 테마 전부**에서 성립한다(혼합 규칙이 counter/prey에서 파생되므로, §8.2 + `check.mjs` S8). 정본 §8.2.1이 같은 결론을 **온보딩의 관점에서 독립적으로** 쓴다: *"스테이지 1은 `introOk` 테마 + ~70/10/10/10이므로, 한 스탠스를 켜고 있으면 ×2(~70%)와 ×0.5(~10%)가 자연히 같은 화면에 나온다."*
> ★ **물결표는 이 증명을 흔들지 않는다** (v1.1 확인): 위 표는 **존재 명제**("×2와 ×0.5가 **같은 혼합 안에 있는가**")이지 비율의 명제가 아니다. §8.2.1이 허용한 **실제 조우 ≤5%p 괴리**(최악: 물 70.0 → 75.0)는 **어떤 칸의 부호도 바꾸지 않는다** — 0이 아닌 비율은 5%p로 0이 되지 않기 때문이다(최악 케이스에서도 소수 속성은 8.3%로 살아 있다). **표의 3/3 전수 확인은 저작 리스트에서도 실제 조우에서도 참이다.**
> 부수 효과: `S = prey`(최악의 선택)를 골라도 교육적이다 — 70%에 ×0.5를 맞으며 **"틀린 스탠스"의 감각**을 배우고, `stanceHintPulse`가 정답 키를 가리키고 있다.

### 8.5 ★ `stanceHintPulse`의 정확한 동작

★ **v1.2에서 이 절 전체가 정본의 인용이 되었다 — 남은 것이 없다.** 대상 선정은 v1.1이 흡수했고(§9.9.1, **02-B8 채택**), ★ **색·주파수·알파는 v1.2가 §7.12.7로 흡수했다(7번째 경계면, 판정 4)**. **아래는 전부 인용이다.**

| 항목 | 확정 |
|---|---|
| **타겟** (§9.9.1) | **아레나 안에 살아있는 적 개체 수 기준 최빈(mode) 속성의 `counter` 키** |
| **세는 대상** (§9.9.1) | 잡몹 · 엘리트 · 새떼. ★ **중간보스 · 보스 · 보스 부위 제외** — 이들은 **개체 위 HP 바 + 부위 글리프가 이미 답을 말한다** |
| **타이 / 최빈이 노말 / 개체 0** (§9.9.1) | 타이 = **`Q W E R` 순**(§4.1의 전역 순서) / 최빈이 노말이면 ★ **맥동 없음**(counter가 존재하지 않는다) / 0기면 맥동 없음 |
| 범위 (§9.9.1) | `stanceHintPulseStageMax = 1` → 스테이지 2부터 **영구 OFF** |
| **투자 > 0인 키** (★ §7.12.7) | **그 속성색** (`palette.element[e]`) |
| ★ **투자 0인 키** (★ §7.12.7) | ★ **은색(탈색)** = `palette.element.normal` |
| ★ **주파수** (★ §7.12.7) | ★ **`visual.stance.hintPulseHz` = 1.0** — **거처 = `rules.json > visual.stance`**(§9.4.3) |
| ★ **알파** (★ §7.12.7) | ★ **`visual.stance.hintPulseAlpha` = [0.40, 1.0]** — 동일 |

**★ v1.2의 판정 (§7.12.7): 05 채택 / 02 기각 — 그러나 이 섹션도 절반만 옳았다.**

| 축 | v1.1의 05 (이 문서) | v1.1의 02 | **v1.2 정본** |
|---|---|---|---|
| **색** | 투자 0 = **은색** | 무조건 **속성색** | ★ **05 채택** |
| **주파수·알파** | 「`visual.stance` 어휘 재사용」 | **1.0Hz / 0.40↔1.0** | ★ **02 채택** |

> ★ **주파수·알파에서 이 섹션이 틀린 방식이 중요하다.** 05는 「`visual.stance` 어휘 재사용」이라고만 썼다 — **그것은 값이 아니라 값이 있을 것이라는 기대다.** 02는 수를 냈고, 05는 안 냈다 → **둘 다 확정하지 않은 것이므로 구현자가 즉석 결정한다(C-6 위반).** 정본의 지적: *「02는 1Hz/0.40↔1.0을, 05는 「`visual.stance` 어휘 재사용」을 말해 **둘 다 값을 확정하지 않았다**」* — 그리고 정본이 02의 값을 채택해 `visual.stance`에 편입하면서 **05의 「어휘 재사용」 주장에 실물을 준 것**이 `hintPulseHz`·`hintPulseAlpha`의 거처다. **주장이 옳아도 수를 안 내면 절반만 옳다.**
> ★ **「부드러운 맥동(트위치 유발 금지)」이 이제 1.0Hz라는 수다 — 그리고 이 섹션이 그 수를 검산했다.** ① **광과민**: §7.9의 `visual.a11y.fullscreenFlashMaxPerSec`(3)는 **전체화면 플래시**의 상한이라 키캡 맥동에 직접 걸리지 않으나, 1.0Hz는 그 상한의 **1/3**이고 `hintPulseAlpha`가 **[0.40, 1.0]** = 0으로 꺼지지 않는 **연속 사인**이라 플래시가 아니다 → **광과민 축에서 안전.** ② **트위치**: 1.0Hz는 **주기 1초**이고 이 힌트는 **아무것도 요구하지 않는다**(누를 창이 없다) → 타이밍 압박 0. ③ ★ **§7.12.1의 빨간불 `alertPulseHz`도 1.0이다** — 화면에 1Hz 맥동이 둘 동시에 떠 서로를 흉내낼 수 있는가를 확인했다: ★ **불가능하다. 그리고 그 근거가 `stanceHintPulseStageMax`가 아니다.** 스테이지 1에는 보스전이 있으므로 `stageMax = 1`만으로는 둘이 겹칠 수 있다 — **겹치지 않는 진짜 이유는 §9.9.1의 「세는 대상」이다**: 힌트는 **잡몹·엘리트·새떼만** 세고 **보스·부위는 제외**하므로, **보스전에는 세어지는 개체가 0기** → §9.9.1의 「**0기면 맥동 없음**」이 발동한다. ★ **즉 두 1Hz는 「스테이지가 달라서」가 아니라 「힌트가 보스를 안 세기 때문에」 배타적이다** — 그리고 그 규칙은 02-B8이 **다른 이유로**(중간보스가 최빈을 오염시키지 않게) 제안한 것이다. **한 규칙이 두 번째 일을 공짜로 한다.**

> ★ **「중간보스 제외」가 사슬 #8과 정합한다**: 중간보스는 `midBossAtSec [35]`에 오고 속성은 `notThemeAndNotNormal`이다(§8.9). 만약 중간보스를 셌다면 **1기짜리 중간보스가 30기짜리 잡몹의 최빈을 뒤집을 수 없어** 어차피 영향이 없거나, 잡몹이 비는 순간 **힌트가 중간보스를 가리켜 사슬 #8의 레슨("전환의 필요")을 대신 풀어버린다.** 정본의 제외 규칙이 옳고, 그 근거("개체 위 HP 바가 이미 답을 말한다")는 §7.6의 엘리트·중간보스 판독 규격에서 나온다.

> ★ **투자 0인 정답 키를 은색으로 맥동시키는 것이 이 힌트의 절반이다.** 맥동이 "저 키가 정답"을 말하고, 은색이 "그런데 너는 비어 있다"를 말한다 — 그리고 §7.5의 **탈색 링**이 그 문장을 이미 가르쳤으므로(사슬 #2) 플레이어는 **눌러보지 않고도 안다.** → 다음 드래프트에서 **어느 속성 카드를 고를지가 결정된다.** 이것이 "정보 기반 선택"이다.
> ★ **정본이 채택한 근거 4개는 이제 §7.12.7이 소유한다. 인용**: ① *「스테이지 1 전용 기능이고 런 시작 투자는 0-0-0이다(§2.6) → **힌트가 처음 뜨는 순간은 거의 항상 투자 0인 키**다. 02의 스펙대로면 **온보딩 첫 순간부터 화면이 거짓말한다** — I-2(No Lying Color): 화면에 그려진 속성색 = 그 개체가 **지금** 적에게 적용할 배율. **투자 0인 키는 아무것도 적용하지 않는다**」* ② *「§7.5는 **투자 0인 스탠스 = 은색 링**, **투자 0 전환 = 탈색 링**을 이미 확정했다. 05의 안은 **그 어휘의 재사용이지 새 색이 아니다** — 02의 안이야말로 §7.5와 정면 충돌한다(같은 키가 링에서는 은색인데 키캡에서는 속성색)」* ③ *「은색 맥동도 **맥동은 한다** → 「이 키를 봐라」는 그대로 전달되고, 「아직 투자가 없다」가 **덤으로** 전달된다」* ④ 주파수·알파의 거처 = `visual.stance`(위).
> ★ **③이 사슬 #4와 하나의 인과가 된다 (정본이 이 문서보다 한 걸음 더 갔다)**: *「`autoEquipFirstElement`와 이어지면 **은색 맥동 → 카드 획득 → 속성색 점등**이 하나의 인과가 된다. **온보딩이 색으로 서사를 만든다**」* — 이 문서의 사슬 #4·#6이 **따로 적어 둔 두 칸이 실은 한 문장**이었다.

### 8.6 `Q`(노말)는 no-op이 아니다 — 온보딩이 그것도 가르친다

| 순간 | 무슨 일이 |
|---|---|
| 사슬 #2 | 투자 0인 `W` = **탈색 링 + "빈" 저음** |
| 같은 시점 | `Q` = ★ **은색 링 정상 확장 + 정상 SFX** (탈색 없음, §7.5) |
| → | **두 개가 시각적으로 다르다** = "Q는 비어 있는 게 아니라 **선택이다**" |
| `S = prey`로 70%에 ×0.5를 맞는 중 | `Q`를 누르면 **×1로 개선** — 방패 호가 사라지고 얇은 틱이 돌아온다 |
| → | ★ **"모르겠으면 Q"** = 절대 브릭 없음의 바닥이 몸으로 학습된다 |

이 레슨은 **최종 보스에서 정확히 값을 한다** — 그리고 **정본이 그 값을 숫자로 확정했다**:

> §8.16: *"4/2/0 특화는 한 관에 ×2, 한 관에 ×1.62(2투자), 남은 한 관에 **노말 스탠스로 ×1**(잘못된 스탠스로 ×0.5를 맞느니 **Q가 낫다** — **Q의 존재 이유가 여기서 증명된다**)."*
> §13.6.3의 실측: **특화 142.0s (여유 38초로 성공)** / 무투자 195.6s (15.6초 초과로 실패) / 코어 직행 209.7s (완전 불가).

**★ `Q`의 값을 이 섹션이 직접 계산한다 (정본 §13.5·§13.5.1·§13.6의 수치로)**
```
dpsRef[6] = 708            ← 명목 · 단일표적 · 무속성 · 보스전 진입 시점 · ★ farm = maxFarm
                             (정본 §13.5 소유, 사용자 확정 1)
× uptimeRef 0.60           ← 실효 가동률 (정본 §10.4.3 소유 — 「520 vs 708」의 진짜 원인이 이 단위였다)
= 424.8                    ← 실효 DPS. 아래 모든 시간이 이 수에서 나온다
```
★ **v1.2에서 `dpsRef`의 정의가 5중이 되었다** — `maxFarm`이 5번째다(§13.5.1). **곡선은 50/90/195/335/485/708 → `[60, 108, 249, 371, 708, 708]`로 재산출됐고 보스 HP 전량이 ×1.20 됐다.** 그런데 ★ **위 세 줄과 아래 표는 한 자리도 안 바뀐다.** 두 가지가 동시에 성립하기 때문이다:
- ★ **`dpsRef[6] = 708`이 보존된다 (사용자 확정, 재논의 금지)** — 제너럴리스트의 화력 패시브가 `warhead` Lv4 × `overclock` Lv4에서 멈추므로 **708 = 명목 ST의 천장**이고, `maxFarm`은 그 천장에 **스테이지 5에서**, `balanced`는 **스테이지 6에서** 도달한다 → **`dpsRef[5] = dpsRef[6] = 708`이며 두 farm 정책의 공통 천장이다**(§13.5.1). **스테이지 6은 farm 정책이 화력을 가르지 않는 유일한 스테이지**다(`runFarmDpsRatio` 비율이 s6에서 **정확히 1.000**).
- ★ **테트라크 HP는 v1.2에서 불변이다**(§13.6.3 · §20.3-04-1: *「테트라크는 불변 — `dpsRef[6] = 708`이 보존되므로」*) — 관 25,800×3 · 왕좌 5,200 · 핵 5,700. **`tier: "final"`은 `bossHpScale` 미적용**(§13.6.1)이라 `bossHpScale[5] = 11.9 → 14.42`의 재산출도 닿지 않는다.

→ ★ **`Q`의 논증은 v1.2의 재산출을 통과한다.** 이 섹션의 결론이 **살아남은 것이 아니라 애초에 흔들릴 수 없는 자리에 있었다** — 최종 보스는 정본이 v1.2에서 건드린 두 축(farm 정책 · 스테이지 스케일) **어느 것의 정의역에도 들어오지 않는다.**
| 4/2/0 특화가 남은 관(투자 0)을 때리는 법 | 실효 DPS | 그 관 1개(25,800) 소요 | 전체 |
|---|---|---|---|
| ★ **`Q` = 노말 스탠스 → ×1** | 424.8 | **60.7s** | 30.4 + 37.5 + 60.7 + 13.4 = ★ **142.0s ✔ 성공** (여유 38초) |
| **`Q`가 없어 틀린 스탠스 → ×0.5** | 212.4 | **121.5s** | 30.4 + 37.5 + 121.5 + 13.4 = ★ **202.7s ✗ 22.7초 초과 = 탈락** |

★ **`Q` 한 키가 특화 빌드의 최종 보스 클리어 가부를 가른다**(142.0 vs 202.7). 정본 §13.6.3의 특화 142.0s는 **`Q`를 누른다는 전제 위에 서 있고**, `specialistPass ≥ 0.85` 게이트(실측 **0.994**)가 그 전제 위에 서 있다. → **그 학습 경로가 스테이지 1의 첫 30초에 있다** — 사슬 #2의 탈색 링과 위 표의 `Q` 대비가 그것이다. **온보딩이 게이트를 지탱한다**는 말이 은유가 아니라 산술이다.

### 8.7 온보딩이 거절당했을 때 (구조적 안전망)

첫 드래프트의 속성 카드는 **보장이지 강제가 아니다.** 플레이어가 무기·패시브를 고르면?

| 안전망 | 동작 |
|---|---|
| ① `guaranteeNewWeaponUntilSlots: 2` | 무기를 골랐다면 슬롯 2가 찬다 → ★ **스트립이 [은색][은색][—][—]** = **"부여 우선순위의 그릇"이 먼저 보인다.** 나중에 속성을 고르면 **①만 물든다** = "앞의 N개"가 **대비로** 드러난다 → **순서가 반대여도 교육적** |
| ② `elementCardPity: 6` | 6드래프트 내 속성 카드 **확정 재제시** |
| ③ 산술 | ★ **정본 §13.5·§13.5.1이 이 수를 소유한다**. ★ **v1.2에서 farm 정책별로 갈라졌다**: `balanced` 픽 = `[11, 18, 25, 33, 42, 53]` / `maxFarm` 픽 = `[12, 20, 28, 37, 47, 59]`(§13.5.1) → **스테이지 1의 보스전 진입 시점까지 11픽(balanced) ~ 12픽(maxFarm).** **보수적으로 11 > `elementCardPity` 6** → **피티가 스테이지 1 안에서, 그것도 보스를 만나기 전에 반드시 걸린다.** ★ **farm이 갈려도 결론이 안 흔들린다** — 파밍을 **덜 하는** 쪽(balanced)이 하한이고 그것도 6의 1.8배다 |
| ④ `stanceHintPulse` | 그동안 정답 키가 **은색으로 계속 맥동**한다 → 거절이 길어질수록 힌트가 커진다 |

**→ 어떤 선택 순서로도 스테이지 1 안에서 ①②③이 선다.** 강제가 없어도 우회가 없다.

> ★ **v1.0판의 산술을 정정했다**: 이 문서는 「런당 레벨업 ~54회 / 6스테이지 ≈ 스테이지 1에만 ~9회」라고 썼다 — **레벨업을 스테이지에 균등 분배한 것이며 틀렸다.** XP 배분은 `xpScale[i] × spawnDensityScale[i]`에 비례하고(§13.5) 그 곱은 `[0.70, 1.36, 2.40, 3.91, 5.98, 9.00]`으로 **후반이 13배 무겁다.** 정본의 실제 계산은 **스테이지 1 = 11픽**(균등 가정보다 오히려 **많다** — 초반 레벨이 싸기 때문). **결론은 유지되고 근거만 정본의 것으로 교체한다** — 그리고 정정된 수가 논거를 **더 강하게** 만든다(11 > 6이 9 > 6보다 여유가 크다).
> ★ **v1.2에서 이 수의 소유자가 §13.5 → §13.5.1로 옮겨졌다**: `levelUpsPerRunTarget(54)`·`maxLevelUps(60)`이 **「레벨업 횟수」인지 「레벨」인지 미정**이었고, v1.2가 **「보스 6 진입 레벨」로 명문화**했다(**픽 = 레벨 − 1**, §20.4-8). 그리고 **farm 정책별 픽 사다리가 처음으로 인쇄**됐다. ★ **주의 — 정본 안에 두 개의 사다리가 남아 있다**: §13.5(재산출 블록)는 `[11, 18, 25, 34, 43, 53]`, §13.5.1(farm 사다리)은 balanced `[11, 18, 25, 33, 42, 53]`으로 **i=4·5가 한 픽씩 다르다**(원인 = `누적 XP(Lv54)`를 §13.5는 27,029, §13.5.1은 26,450으로 쓴다 — 내가 `Σ_{L=1}^{53} 6·L^1.32`를 직접 계산하니 **26,449.7**이라 **§13.5.1이 옳다**). **이 섹션이 쓰는 것은 i=1(둘 다 11)뿐이라 논증은 어느 사다리에서도 불변**이며, 두 사다리의 존재는 **요청 ⑬**(minor)으로 올린다.
> ★ **같은 정본 계산이 사슬 #7의 전제도 준다**: §13.5의 제너럴리스트 궤적에서 **s1 (11픽) = 4무기 · 6레벨 · 2속성**(= 3 `newWeapon` + 6 `weaponLevel` + 2 `elementLevel` = 11 ✔ — `forward`는 시작 무기라 픽이 아니다) → 스테이지 1 보스전 진입 시점에 **무기 4칸이 이미 차 있고 속성 투자가 2**다. 즉 사슬 #7의 스트립 `[속성색][은색][—][—]`은 **중간 상태**이고, 보스를 만날 때쯤엔 `[속성색][속성색][은색][은색]`이 된다 — **"앞의 N개"의 대비가 스테이지 1 안에서 두 번 보인다.**

### 8.8 온보딩이 쓰는 텍스트: 0줄

사용 채널: ① 테마 배너의 **수치와 글리프**(문장 아님) ② **탈색 링** ③ **자동 장착 + 키캡 번쩍** ④ **3배율 감각**(시각 + 오디오) ⑤ **슬롯 스트립 pip** ⑥ **은색/속성색 키캡 맥동** ⑦ **카드의 before → after 슬롯 그림.**
**설명문은 어디에도 없다.** 그리고 `Esc` → PAUSE에 **4×4 상성표가 상시 비치**되어 있다(§6.6) — 배움이 실패해도 참조가 실패하지 않는다.

---

## 9. 이 섹션이 다른 섹션에 요구하는 계약

> ★ **이 표의 계약 5건이 이행됐다** (v1.1에서 4건 + ★ **v1.2에서 `stanceHintPulse` 1건**). 정본이 자리를 줬으므로 **요구가 아니라 인용**이 된 행은 **「이행」**으로 표시한다.

| 대상 | 요구 | 상태 |
|---|---|---|
| **「HUD」** | **상시 표시**: 테마 뱃지(구성비) · 페이즈/보스 타이머 · **`LV UP ×N` 큐 배지**(하단 B 띠 우측, §1.2) · 현재 스탠스(키캡) · **슬롯 4칸 부여 상태** · 속성 투자 3종 + **합계 예산 `4 / 6`**(`hud.showElementBudget`) · HP · 코인 + **환산 점수**(`hud.coinShowsScoreValue`) · 리롤/폭탄/실드/토큰 스톡 · **라이브 점수**(§6.2) | 요구 |
| **「HUD」** | ★ **HUD 3요소 — 정본이 자리를 줬다** (§7.12.5, `hud.noHitIndicator`): **무피격 pip 6칸** = **우 패널 「무피격 행」** 가로 1행(3상태: 은색 외곽선 빈 원 / 은색 채움 / `neutralGray` ✕) · **`퍼펙트 ✕`** = 같은 행 우측 끝(평시 표시 없음) · **`시간 보너스 ✕`** = **상단 띠 y 30~40 중앙**(`[Shift] +30초` 힌트와 **같은 픽셀·배타**) <br>★ **v1.2 정정 — `noHitRow`는 키가 아니다**(판정 5, §7.12.5 · §21-B4): **우 패널 행의 「이름」**이며 **좌표는 02 §7.4-E가 소유**한다(§17의 「패널 내부 레이아웃」 위임과 정합). **새 키 0** — 켜고 끄는 스위치는 `hud.noHitIndicator`가 **이미 있다.** 정본이 소유하는 것은 **행의 존재 · 칸 수(6) · 각 칸의 3상태 · 컨티뉴 시 전량 ✕**뿐이다. → **05-⑩ 종결** | ★ **이행** |
| **「HUD」** | ★ **`stanceHintPulse`의 색 — 정본이 확정했다** (§7.12.7, 7번째 경계면): 투자 > 0 = **속성색** / ★ **투자 0 = 은색**(`palette.element.normal`) · `visual.stance.hintPulseHz` **1.0** · `hintPulseAlpha` **[0.40, 1.0]**. **05 채택 / 02 기각** | ★ **이행** |
| **「HUD」** | 모든 UI의 속성 순서 = **`Q(노말) → W(불) → E(물) → R(풀)`** (§4.1) | 요구 |
| **「HUD」** | **보스 코어 HP 바에 파 고스트 마커** — ★ 정본이 규격을 확정했다(§1.2 · §11.5 · `hud.parGhostEnabled`): **높이 8px 흰 세로선 2px + 상단 3px 사각 캡**, 자리 = y 40~48, `stagePar[i] == 0`이면 **숨김**. **페이즈 노치(높이 4px 흰 눈금)와 형태·운동이 다르다** | ★ **이행** |
| **「HUD」** | **타이머 3단 상태** — ★ 정본이 확정했다(§7.12.1): 은색 / **색 불변** + `warnScale 1.15`·`warnPulseHz 0.5` / **자홍** + `alertScale 1.30`·`alertPulseHz 1.0`. **호박 없음** | ★ **이행** |
| **「HUD」** | ★ **`hud.tokenKeycapGatedDisplay`** (§9.4.1) — 토큰 아이콘의 게이팅 표시. §4.3의 UX 경계가 키가 됐다 | ★ **이행** |
| **「보스」** | 부위는 **개체**이며 자체 `score`를 갖고 상성 처치 보너스 대상(§8.11). 인트로 3초의 **부위 순차 점등 순서 = `parts[]` 배열 순서, 코어 마지막**(§3.5) | 요구 |
| **「보스」** | **변경 반영 필요**: `armorPartCountRange = [2,2]`(§8.13.2) → **테마 보스는 전 6종이 `armor` 2 + 선택 부위 1 + `core` 1 = `partCount` 4**로 고정됐다(§13.6.2). §3.5의 부위 순차 점등은 **항상 4개**(최종만 5개)이며 **`bossEntrySec` 3.0 안에 4~5개를 점등**해야 한다 → 0.5~2.5초 구간에 **부위 3~4개 + 코어 1개**. ★ **v1.2에서 `partCount`는 안 바뀌었고 HP만 ×1.20 됐다**(core **976** / armor **2,391**×2 / 선택 **478**) → **§3.5의 점등 규격은 무영향**(개수의 함수이지 HP의 함수가 아니다) | 요구 |
| **「적·스테이지」** | 잡몹의 **화면 이탈 = XP·코인 영구 소멸**(`enemyExitForfeitsReward`) — 파밍 리스크의 실제 메커니즘이며 `farmXpRatio ≥ 2.0` 게이트의 근원 | 요구 |
| **「밸런싱」/시뮬** | `report/stages.csv` → **`flow.stagePar[6]` 기입**(§10.4). **미기입 = 0 = UI 숨김**(§6.5 · §11.5). ★ **목표 밴드 30~60**(§13.6.4 — v1.2 개정, 25~70 폐기) <br>★ **단 「어느 시뮬 모드로 재는가」가 미정이고 두 읽기가 정반대 판정을 낸다** — probe 6/6 통과 vs run 6/6 실패(§6.5). **요청 ⑫** | **요구 (재개봉)** |
| **「밸런싱」/시뮬** | 어트랙트가 `bot.baseline`(**4축**, §10.4.1)을 그대로 쓴다(§2.2) → 봇 정책 변경은 **타이틀 화면의 첫인상을 바꾼다**. ★ **v1.2 추가**: `farm` 축의 baseline이 `balanced`이고 `dpsRef`는 이제 `maxFarm` 기준이므로(§13.5.1) — **타이틀에서 도는 봇은 `dpsRef` 곡선보다 17% 약한 화력으로 논다**(`runFarmDpsRatio` 0.83). **정합이다**(어트랙트는 인증이 아니라 시연) | 요구 |

---

## 10. 이 섹션이 제안했고 **정본이 채택한** 키 — 거처 (v1.2: 인쇄 자리까지 확인 완료)

> ★ **이 절은 두 번 틀렸고 두 번 다 「자리」의 문제였다.**
> ① **v1.0**: 제목이 「이 섹션이 새로 정한 값 (**전부 `meta.json`**)」이었다. **「이 섹션이 정한」이 아니다**(9건 전부 정본이 채택했으므로 §19.4 — 이 절은 **인용**이다, C-1) — 그리고 ★ **「전부 `meta.json`」은 오기**였다. **`visual.timer.*`는 `rules.json > visual` 소유**(§9.2 · §9.4.3)이고 정본의 통보는 ***"`meta.json`에 넣으면 로드 실패"***였다(§9.3의 「미지 키 = 에러」 + 루트 키 17종에 `visual`이 있고 `meta.json`에는 없다). **v1.1에서 승복하고 정정했다.**
> ② ★ **v1.1**: 거처는 고쳤는데 ★ **그 거처에 자리가 있는지 확인하지 않았다.** 정본 §9.9의 `flow` 블록은 **축약(`...`) 없이 전부 인쇄**되어 있었고 C-7이 「인쇄된 블록은 **필드 집합**을 확정」이라 못박았는데 — **채택된 7키가 하나도 없었다**(grep: JSON 키로 인쇄된 횟수 **0**). `score.roundMode`도 같았다. → ★ **거처를 정정한 이 표가, 그 표대로 구현하면 로드 실패하는 상태로 한 라운드를 서 있었다.**
> ★ **v1.2가 §21-A2/A3로 실제 인쇄해 닫았다.** 아래는 **정본 §9.9·§11.3·§9.4.3의 인쇄 블록에서 그대로 뜯어온 인용**이며, **이 문서가 확인한 것은 「값」이 아니라 「자리」다.**
>
> ★ **이 라운드에 이 섹션이 배운 것 = C-9**: ***「채택은 인쇄가 아니다」***(§21.4). 정본이 「채택」이라 심사해도 §9의 블록에 키가 없으면 **그 채택은 로드 실패로 귀결**된다. **거처를 주장하는 문서는 그 거처의 인쇄 블록을 grep할 의무가 있다** — 이 절이 그것을 안 해서 A2를 v1.2까지 살려 뒀다.

**거처 (§9.2의 9파일 매니페스트 기준 — ★ 전부 정본의 인쇄 블록에 실재함을 확인했다)**

```jsonc
// data/meta.json > flow          ← 플로우 (정본 §9.9가 인쇄 · §0.2.1 · §5.5가 확정)
{ "menuSpeed": 1.0, "deathAnimSec": 1.5,
  "edgeTriggerOnStateEnter": true, "pauseAllowsAbandon": false,
  "attract": { "difficulty": "normal", "draftDwellSec": 1.2, "endAfterMobPhase": true } }
//                                                     ↑ ★ v1.2 확정값 (05가 제안한 0.8이 아니다)

// data/meta.json > score         ← 점수 (정본 §11.3의 블록이 인쇄)
{ "roundMode": "floor" }

// ★ data/rules.json > visual     ← 시각 규격 (정본 §9.4.3이 전 키 인쇄) — meta.json이 아니다
{ "timer": { "warnScale": 1.15, "warnPulseHz": 0.5, "alertScale": 1.30, "alertPulseHz": 1.0 } }
```

| 키 | 값 | **거처** | **정본의 소유 조항 · ★ 인쇄 자리** |
|---|---|---|---|
| `flow.menuSpeed` | **1.0** | `meta.json > flow` | **§0.2.1** (05-⑦ 채택) · ★ **인쇄 = §9.9**. 규약에 예외를 만들지 않고 **메뉴의 배속을 1.0으로 정의**해 §0.2의 구멍을 메웠다. `DEATH`는 **런 안이지만** `deathAnimSec`는 `menuSpeed`로 흐른다 |
| `flow.deathAnimSec` | **1.5** | `meta.json > flow` | **§0.2.1 · §5.5** (05-⑧ 채택) · ★ **인쇄 = §9.9**. §6.3의 「타이머 만료 = 1.5초 처형 연출」과 **같은 수이며 이 키가 그 소유자다**(새 숫자 아님) |
| `flow.edgeTriggerOnStateEnter` | **true** | `meta.json > flow` | **§5.5** (05-⑧ 채택) · ★ **인쇄 = §9.9** |
| `flow.pauseAllowsAbandon` | **false** | `meta.json > flow` | **§5.5** (05-④ 채택) · ★ **인쇄 = §9.9**. 정본: 「**누락이 아니라 결정임을 키로 못박는다**」 |
| `flow.attract.difficulty` | **`"normal"`** | `meta.json > flow` | **§5.5 · §10.2** (05-⑤·⑧ 채택) · ★ **인쇄 = §9.9** |
| ★ `flow.attract.draftDwellSec` | ★ **1.2** (05의 제안 0.8 → **정본이 1.2로 확정**) | `meta.json > flow` | ★ **인쇄 = §9.9**. ★ **게임초다** — 정본의 근거: *「`flow.attractIdleSec`(20)과 달리 **런 안이므로 게임초**」*. 어트랙트는 `attract.difficulty = "normal"`(`speed 1.0`)이라 **실제 1.2초** |
| `flow.attract.endAfterMobPhase` | **true** | `meta.json > flow` | ★ **인쇄 = §9.9**. 정본의 근거: **보스전 스포일러 방지 + 루프 길이** — §2.2가 독립적으로 쓴 것과 같다 |
| `score.roundMode` | **`"floor"`** | `meta.json > score` | **§11.3** (05-③ 채택) · ★ **인쇄 = §11.3의 블록** |
| ★ `visual.timer.warnScale / warnPulseHz` | **1.15 / 0.5** | ★ **`rules.json > visual`** | **§7.12.1** (05-⑧ **수정채택** — 거처 정정) · ★ **인쇄 = §9.4.3** |
| ★ `visual.timer.alertScale / alertPulseHz` | **1.30 / 1.0** | ★ **`rules.json > visual`** | **§7.12.1** · ★ **인쇄 = §9.4.3**. `1.0Hz`는 §7.10의 "보스 타이머 빨간불 = 1Hz 틱"과 **위상 동기** |

★ **`draftDwellSec` 0.8 → 1.2에 승복한다 — 그리고 정본이 이유를 적지 않았으므로 이 문서가 재검산한다.** 05가 0.8을 낸 근거는 「**카드가 뽑히는 것이 보인다**」뿐이었다 — 즉 **선택의 사실**만 보이면 된다고 봤다. 그러나 어트랙트의 관객은 **심사자이고 게임을 모른다.** §3.3이 이 화면에 얹은 것은 카드 3장 + **테마 뱃지 + 투자 3종 + 합계 예산 + 하단 슬롯 스트립 4칸**이고, §11.1이 `elementLevel` 카드에 **`전환 시 부여 무기: 2개 → 3개` + before/after 슬롯 그림**을 강제한다 → **처음 보는 사람이 읽어야 할 것이 「카드 3장」이 아니라 「그림 두 벌의 차이」**다. 0.8초로는 **선택의 사실**만 전달되고 **선택의 내용**은 전달되지 않는다 → ★ **어트랙트가 §2.2에서 스스로 내건 목표(「§11.1의 `elementLevel` 카드가 가르치는 그 최적화를 시연한다」)를 자기 값으로 무너뜨리고 있었다.**
★ **그리고 1.2는 루프 길이를 위협하지 않는다 (이 섹션이 검산했다)**: 어트랙트는 **잡몹 페이즈 1회 = 120 게임초**(`endAfterMobPhase`)이고 그 안의 픽 수는 **11**(§8.7, `farm: balanced`)이다 → 체류 총합 = `11 × 1.2` = **13.2초 = 어트랙트의 11%**(0.8이면 8.8초 = 7.3%). ★ **4%p를 주고 판독 가능성을 산다 → 정본이 옳다.** (드래프트는 **클럭 완전 정지**(§6.4)이므로 이 체류는 **120초를 잡아먹지 않고 그 위에 얹힌다** — 어트랙트 1회의 실제 길이가 120 → 133초가 될 뿐이고, `flow.attractIdleSec`(20)의 대기 루프에 비하면 무시할 수 있다.)

**연쇄로 정본이 승인한 구조 3건** (05-⑤, §10.2 — 값이 아니라 **구조**이므로 위 표와 분리):
`src/core/bot.js` (봇의 core 승격) · **RNG 스트림 8번째 `bot`** (동결 파기, 대가 0) · `check.mjs` **S11** 스트림 목록 7 → 8.

**색은 하나도 만들지 않았다** — 타이머 빨간불 = `palette.threat.enemyBullet`, 타이머 경고·정상 = `palette.element.normal`, 파 고스트 = **흰색**(§1.2), 위기 예고 = `palette.threat.telegraph`, 회색 마크 = `palette.neutralGray`(02-B6이 신설, ★ **v1.2에서 `palette`에 인쇄 자리를 얻었다** — §21-A6. **v1.1에서는 이 문서가 「회색 마크」라 부르던 색이 로드 실패할 상태였다**), ★ **`stanceHintPulse` 은색 = `palette.element.normal`**(§7.12.7). **팔레트 키 추가 0.** 그리고 **호박은 이 섹션의 어느 화면에도 없다**(§7.12.4의 5곳 전수 목록과 교집합 0).

★ **`mono`에서 이 섹션의 화면이 어떻게 되는가 (판정 11 — v1.2가 `mono` = **전 팔레트 무채화**로 명문화, §7.3)**: v1.1의 문면(「`elementCvd` + 채도 0」)은 **`element`만** 무채화되는 것으로 읽혔고, 그 읽기에서는 **타이머 빨간불의 자홍과 위기 예고의 자홍이 색을 유지**한다. **전 팔레트 읽기가 확정되면서 둘 다 무채색이 된다** → 이 섹션의 화면 중 **`mono`에서 채널을 잃는 곳이 있는가**를 전수 확인했다: ★ **없다.**
- **타이머 빨간불**: 색을 잃어도 `alertScale` **1.30** + `alertPulseHz` **1.0** + **1Hz 오디오 틱**(§7.10)이 남는다 — §7.12.1이 「60초에 색이 필요 없다」고 논증한 것과 **같은 논증이 30초에도 성립**한다(크기·맥동·소리 3채널).
- **위기 예고 자홍 점선**: 색을 잃어도 **점선 = 미래**라는 §7.4의 2상태 규칙이 형태로 남는다.
- **`시간 보너스 ✕` · `퍼펙트 ✕` · pip 깨짐**: 애초에 `palette.neutralGray`이고 **✕는 글리프**다 — `mono`에서 **원래부터 무채색**이라 변화 0.
- **파 고스트**: **흰색 + 형태**(8px 세로선 + 3px 캡)로 페이즈 노치(4px)와 구분되므로 색 무관.
→ ★ **「`mono`에서 새로 배울 것이 하나도 없다」(§7.3)는 이 섹션에서도 참이다.**

---

## 11. 정본 추가 요청 — **v1.0의 9건 + v1.1의 2건 전부 종결**

### 11.1 처분 대장 (§19.4 = 정본의 판정)

**9건 중 채택 8 · 수정채택 1 · 기각 0.** 전부 정본에 있으므로 **이 문서에서는 인용으로 전환**했고 요청 목록에서 제거한다.
★ **v1.2 추가 확인 (C-9 = 「채택은 인쇄가 아니다」)**: 위 9건 중 **③⑦⑧은 「채택」이 심사 대장에만 있고 §9의 인쇄 블록에는 자리가 없었다** — `roundMode`(§21-A3) · `flow` 7키(§21-A2). **v1.2가 인쇄해 실제로 닫혔다**(§10). **채택 표시가 곧 이행이 아니다** — 이 대장의 「반영 위치」 열은 이제 **인쇄 블록까지 가리켜야 한다**(§10의 표가 그 형식이다).

| # | 요청 | 판정 | 반영 위치 | 이 문서의 조치 |
|---|---|---|---|---|
| ① | §6.3 `(노랑)` 라벨 정정 | **채택** | §6.3 · §7.12.1 | §4.1을 §7.12.1의 인용으로 재작성 |
| ② | §5.5 `O` 행 정정 (blocker) | **채택** | §5.5 | §1에 정정 사실 명기 |
| ③ | `score.roundMode: "floor"` | **채택** | §11.3 | §6.2를 인용으로 재작성 + `floor` 1회의 의미 정밀화 |
| ④ | `flow.pauseAllowsAbandon: false` | **채택** | §5.5 | §1을 인용으로 재작성 (정본이 근거 3개를 명문화) |
| ⑤ | 어트랙트 구조 3건 (`core/bot.js` · 스트림 8 · S11) | **채택** | §10.2 · S1 · S11 | §2.2를 인용으로 재작성 |
| ⑥ | `stagePar == 0` 센티넬 + 고스트 사상식 | **채택** | **§11.5** · §1.2 | §6.5를 인용으로 재작성 + **마커 규격을 정본의 것으로 교체** |
| ⑦ | `flow.menuSpeed: 1.0` | **채택** | **§0.2.1** | §10 · §5.1(`deathAnimSec`가 `menuSpeed`로 흐름) |
| ⑧ | `edgeTriggerOnStateEnter` · `deathAnimSec` · `attract.*` · `visual.timer.*` | ★ **수정채택** | §5.5 · §7.12.1 | ★ **`visual.timer.*`의 거처를 `rules.json`으로 정정 — §10 전면 재작성** |
| ⑨ | §17 위임표에 05 행 추가 | **채택** | §17 | 문서 머리말에 위임 범위 명기 |

### 11.2 정본이 이 섹션의 **본문**을 뒤집은 것 (요청 행이 아니었던 것) — 승복

**★ v1.2가 뒤집은 것 (5건) — 전부 승복**

| 출처 | 뒤집힌 것 | 정본 v1.2 | 조치 |
|---|---|---|---|
| §6.5 | `stagePar` 목표 밴드 **25~70** · `killTime` 6종 **125.8/125.8/135.7/127.8/134.1/125.9** | ★ **30~60** (§13.6.4) · **125.5/125.5/135.1/128.3/133.3/125.9** (§13.6.2) — `dpsRef`가 `maxFarm`으로 재정의되며 **앵커 30 → 36**, 보스 HP 전량 **×1.20** | **표 전면 재산출 + 내 손으로 독립 재현.** 6/6 밴드 안 유지 ✔ |
| §6.4 | RESULTS 목업 「372초 = 보스당 평균 **62초**」 | — | ★ **정본이 아니라 내 산술 오류.** `시간토큰 1회 · S4 보너스 0`이 같은 칸에 있으므로 **분모는 6이 아니라 5**였다(74.4초). 목업을 **260초 / 5스테이지 = 52.0초**로 교체해 새 밴드 30~60 안에 넣었다 |
| §2.2 · §10 | `flow.attract.draftDwellSec` = **0.8** | ★ **1.2** (§9.9) | 교체 + **정본이 왜 옳은지 재검산**(§10) |
| §3.2 | 위기 예고 = 「`view.spawnLineY`(**−40**) **바로 아래**」 (+ 레이어·알파 미지정) | ★ **y = 48 · 레이어 8 · 알파 0.50** (§7.12.2) | ★ **이건 정본 v1.1의 문면이고 이 문서는 그것을 인용했다** — 그런데 **인용하면서 「−40이 화면 밖」임을 확인하지 않았다.** 02-R4가 잡았고 이 문서는 놓쳤다. **인용도 검산해야 한다**(C-8) |
| §8.5 | `stanceHintPulse` 주기 = 「`visual.stance` 어휘 재사용」 | ★ **`hintPulseHz` 1.0 / `hintPulseAlpha` [0.40, 1.0]** (§7.12.7 — **02의 값 채택**) | ★ **색은 05가 옳았고 수는 02가 옳았다.** 「어휘 재사용」은 **값이 아니라 값이 있을 것이라는 기대**였다 → C-6 위반. **주장이 옳아도 수를 안 내면 절반만 옳다** |

**v1.1이 뒤집은 것 (7건) — 기록 보존**

| 출처 | 뒤집힌 것 | 정본 | 조치 |
|---|---|---|---|
| §3.1 | 테마 배너 = 「`mix` 4수치를 **그대로**」 + 「수치를 숨기지 않는다」 | ★ **물결표 필수 + S8 대상 = 저작 리스트 + 실제 조우 ≤5%p 허용** (§8.2.1) | 전면 재작성. **「그대로」는 지킬 수 없는 약속이었다** — `waveClearAdvance` 때문에 실제 조우가 최악 +5.0%p 벗어난다 |
| §6.5 | 파 고스트 마커 = **은색 2px** | ★ **높이 8px 흰 세로선 2px + 상단 3px 사각 캡** (§1.2 · §11.5) | 정본의 규격으로 교체. 정본이 **페이즈 노치와의 구분**까지 설계했다 |
| §5.3 | 컨티뉴 시 pip 6칸 **회색 소등** | ★ **`neutralGray` ✕** (§7.12.5) | 교체 |
| §5.3 | 컨티뉴 재개 = **"즉시 빨간불"** | `max(잔여, 60)` → **경고 상태** | ★ **이건 정본이 아니라 내 산술 오류였다.** 같은 칸 안에서 자기모순이었다 |
| §2.2 | `bot.baseline` = **3축** | ★ **4축** (`shop: survivalFirst` 신설, §10.4.1) | 교체 + 어트랙트가 `shop` 축에 도달하지 않음을 명기 |
| §8.7 | 「스테이지 1에만 **~9회** 레벨업」 | ★ **11픽** (§13.5의 XP 배분 실계산) | 교체. **결론은 유지되고 근거가 더 강해졌다**(11 > 6) |
| §7.2 | `defense` 가격 `94 → 136` / `reroll` 1회 구매 + 현재가 30 | §11.2의 `ceil(45×1.45²)` = **95**, `ceil(45×1.45³)` = **138** / 구매 1회면 현재가 **48** | ★ **내 산술 오류.** `ceil` 누락 + 가격 곡선 오독 |

### 11.3 v1.1의 남은 요청 2건 — **★ 전부 종결**

| # | v1.1 severity | 요청 | ★ **v1.2의 처분** |
|---|---|---|---|
| **⑩** | major | `hud.noHitRow`를 §9.4.1의 `hud` 블록에 선언해 달라 (또는 키가 아님을 명시) | ★ **종결 — 판정 5로 ⓑ 채택**(§7.12.5 · §21-A8/B4). **「키가 아니라 우 패널 행의 이름이며 좌표는 02 §7.4-E가 소유한다」**로 명문화. **새 키 0**, 스위치는 `hud.noHitIndicator`가 이미 있다. ★ **02-R1과 이 섹션이 독립적으로 같은 것을 올린 것이 실재의 확인이었다** — 정본의 표현: *「어느 쪽으로 읽어도 **절반의 확률로 로드 실패**」*(이 문서가 쓴 문장 그대로) |
| **⑪** | minor | §7.2 「예약 규칙」 3번째 불릿의 문면을 개정해 달라 (§7.12.4가 지시했으나 미이행) | ★ **종결 — 실제로 개정됐다**(§7.2). 현 문면: *「호박은 **§7.12.4가 열거하는 5곳에만**(v1.2 개정 — 위치 기반 → **의미 기반**). 그 5곳을 하나의 문장이 묶는다: **"호박 = 내 몸에 무슨 일이 일어난다/일어났다."**」* → ★ **이 문서의 F-2가 §7.2가 아니라 §7.12.4를 인용하도록 바꾼 것이 이제 문면상으로도 옳다**(두 절이 같은 말을 한다) |

> ★ **⑪이 남긴 교훈이 v1.2의 C-11이 됐다**: *「**개정 이력에 「고쳤다」를 적기 전에 본문을 grep 한다**」*(§21.4). §7.12.4가 **스스로 개정을 지시해 놓고** 그 위반 문장을 남긴 것이 ⑪의 정체였고, 같은 형태가 §16-#34에도 있었다(§18.4-3이 「고쳤다」고 기록했으나 본문은 그대로). **지시가 이행됐는지는 대장이 아니라 본문이 답한다.**

### 11.3.1 ★ 남은 요청 — **2건** (v1.2를 읽으며 새로 발견 · 둘 다 이 섹션 소관 밖)

> **원칙은 그대로다**: 이 섹션이 **자기 권한으로 고칠 수 없는 것만** 남긴다. 둘 다 **정본이 소유한 시뮬 모드(§10.4)·게이트 밴드(§13.6.4)·픽 사다리(§13.5)**의 영역이며 이 문서는 인용만 한다(C-1). **자기 권한 안의 것은 전부 본문에서 고쳤다**(§13).

| # | severity | 요청 | 근거 (★ 이 섹션이 산술로 검증함) |
|---|---|---|---|
| **⑫** | ★ **major** | **`flow.stagePar[i]`를 재는 시뮬 모드를 확정해 달라 — `dpsProbe`(maxFarm)인가 `run`(balanced)인가.** 그리고 그 답에 맞춰 §13.6.4의 밴드(30~60) 또는 §11.5의 데이터 소스 중 **하나를 고쳐 달라.** | ★ **두 읽기가 정반대 판정을 낸다** (§6.5의 표): **ⓐ probe** → `stagePar` = `180 − killTime(probe)` = **54.5/54.5/44.9/51.7/46.7/54.1 → 6/6 밴드 안** ✔ · **ⓑ run** → `180 − killTime(probe)/runFarmDpsRatio(0.83)` = **28.8/28.8/17.2/25.4/19.4/28.3 → 6/6 밴드 밖**(전부 하한 30 미만) ✗. <br>★ **정본이 양쪽을 동시에 시사한다**: §13.6.4는 「`flow.stagePar[i]` = **`180 − killTimeMedianBalanced`** = 30~60」이라 **직접** 쓰는데 그 수는 §13.6.5가 **probe로 정의**했다(ⓐ). 그러나 §11.5·§10.4는 소스를 **`report/stages.csv`의 중앙값 = 「스테이지별 **표준** 보스 잔여시간」**이라 하고, `dpsProbe`는 **무적 + 사망이 판정 대상 아님**(§10.4)이라 「**표준 플레이어**의 코어 HP 곡선」(§11.5의 자기 문장)일 수 없다(ⓑ). <br>★ **결정적 — 정본이 ⓑ의 수를 자기 본문에 이미 인쇄했다**: §13.6.5 *「스테이지 3의 baseline 격파가 2.71분(타이머 180s 대비 **17.3초 여유**)」* — **17.3 = `stagePar[3]`(run)이고 정본 자신의 하한 30의 절반 이하**다. **두 문장이 서로를 못 보고 있다.** <br>★ **major인 이유**: **부팅은 된다**(`stagePar[i] == 0` 센티넬이 인증 전을 덮고, 어느 읽기에서도 분모 `180 − stagePar` ≤ 180이라 고스트가 화면 밖으로 나가지 않는다) → blocker 아님. **그러나 밸런서가 `--certify`를 돌린 뒤 「밴드를 6/6 실패했다」를 보고 `bossHpScale`을 돌리기 시작할 수 있고**, §13.6.5는 그것이 **`noElementPass`(사용자 확정 곡선)를 깨는 손잡이**임을 이미 증명했다. <br>★ **이 섹션의 권고 = ⓑ + 밴드 재산출**: 고스트는 **지금 조종간을 잡은 사람**에게 "뒤처졌는가"를 묻는 UI이고 그 사람은 무적도 `maxFarm`도 아니다. ⓐ로 채우면 **balanced-farm 플레이어가 6스테이지 내내 구조적으로 언더파**(격파 시점에도 24~28초 뒤처진 채 끝난다) → 고스트가 **"너는 항상 뒤처져 있다"만 말하는 장식**이 되어 §11.5의 목적(「20분 뒤에야 알게 되는 언더파를 지금 알려준다」)의 **정확한 반대**가 된다. ⓑ면 밴드는 **`[180 − 150/0.83, 180 − 120/0.83]` = `[−0.7, 35.4]`** → **하한이 음수**가 되므로 **밴드를 `stagePar`가 아니라 `killTime`에만 걸고 `stagePar`는 파생으로 두는 것**이 가장 적은 변경이다(새 값 0). <br>**대안 ⓐ**는 §11.5·§10.4의 「표준」을 「인증 기준선」으로 재정의해야 하고, **고스트의 의미가 바뀐다**(= 이 섹션의 §6.5 전면 재작성) |
| **⑬** | **minor** | **§13.5의 픽 사다리와 §13.5.1의 픽 사다리가 i=4·5에서 갈린다 — 하나로 통일해 달라.** §13.5(재산출 블록) = 「진입 레벨 `[12, 19, 26, 35, 44, 54]` → 픽 `[11, 18, 25, 34, 43, 53]`」 / §13.5.1(farm 사다리) = 「balanced 진입 레벨 `[12, 19, 26, 34, 43, 54]` → 픽 `[11, 18, 25, 33, 42, 53]`」 | **같은 양(balanced 궤적)에 두 값**이다. **원인은 `누적 XP(Lv54)`의 두 값**: §13.5 = **27,029** / §13.5.1 = **26,450**. ★ **내가 직접 계산했다** — `Σ_{L=1}^{53} 6·L^1.32` = **26,449.7** → ★ **§13.5.1이 옳고 §13.5의 27,029는 v1.1 이전의 잔재다.** <br>**minor인 이유**: ① **이 섹션이 쓰는 것은 i=1(둘 다 11)뿐**이라 §8.7의 피티 논증은 **어느 사다리에서도 불변**(11 > 6) ② §13.5.1의 사다리가 **`runFarmDpsRatio` 0.83의 유도에 실제로 쓰인 것**이므로 게이트 산술은 이미 옳은 수 위에 서 있다. **그러나 03이 `dpsRef`를 인용할 때 §13.5를 볼지 §13.5.1을 볼지가 미정**이고, C-8(정본의 표와 문서가 갈라질 수 없다)은 **정본의 두 표끼리도** 적용된다 — 그리고 이것은 v1.2가 §21로 닫은 클래스(「정본이 자기 말을 지키는지 확인하지 않았다」)의 **산술 판**이다 |

**요청이 2건이고 둘 다 「이 섹션이 아니라 정본 안에서 두 문장이 갈라진 곳」인 것이 유지됐다.** v1.0판의 9건은 **전부 "정본에 자리가 없는 것"**이었고, v1.1의 2건은 **"정본 안에서 두 문장이 갈라진 곳"**이었으며 **v1.2가 둘 다 닫았다.** 지금의 2건도 같은 성격이다 — 그리고 **둘 다 v1.2가 새로 만든 축(farm 정책)에서 나왔다.** ★ **`uptimeRef`가 「명목/실효」를 닫은 뒤에도 남은 단위가 있었듯이, `runFarmDpsRatio`가 「probe/run」을 닫은 뒤에도 그 축을 아직 통과하지 않은 수가 하나 남아 있다 — 그것이 `stagePar`다.**

---

## 12. 이 섹션이 제거한 프러스트레이션 (기둥 대조)

| 사용자가 싫어하는 것 | 발생 지점 | **이 섹션의 제거 장치** | 정본 근거 |
|---|---|---|---|
| 트위치 | 죽는 순간 `Space` 연타 → 컨티뉴 상실 | **`edgeTriggerOnStateEnter` + `deathAnimSec` 1.5**(★ `menuSpeed`로 흘러 Disaster에서도 1.5초) | ★ **§5.5** · §0.2.1 |
| 트위치 | 컨티뉴 카운트다운 | **없음. 무기한 대기** | §11.4 |
| 트위치 | 상점 제한시간 | **없음** | §11.2 |
| 트위치 | 위기 세션 예고 없는 스파이크 | **자홍 점선 수평선 3.0초** (= `wall` 텔레그래프 재사용) — ★ **v1.2: `y = 48` · 레이어 8 · 알파 0.50.** v1.1의 「`spawnLineY`(−40) 바로 아래」는 **화면 밖이라 이 제거 장치가 실은 존재하지 않았다** | ★ **§7.12.2** |
| 체스식 최적화 | 보스 부위마다 정지 → 재정렬 | **재정렬 = 드래프트 전용 × 보스전에 드래프트 없음 = 0** | §5.3 · §6.4 |
| 체스식 최적화 | 상성 지분을 관리하기 | ★ **지분 게이지를 화면에 안 띄운다** | §11.3 |
| 체스식 최적화 | 상점 6회 방문 최적해 풀기 | **항목 간 시너지 0 → 풀 문제가 존재하지 않는다** | §11.2 |
| 운빨 | 테마를 모르고 삼 | **배너(사전) + 상점 헤더(결정 시점) 이중 고지** | §6.5 전이 순서 |
| ★ 운빨 | **고지된 비율과 실제가 다름** | ★ **물결표 `~70%` + 정본이 허용 괴리 ≤5%p를 명문화** — 지킬 수 있는 것만 약속한다 | ★ **§8.2.1** |
| feel-bad | 무피격이 2스테이지에 깨져 28분 무의미 | 무피격 = **스테이지 단위** / 퍼펙트 = 별도 | §11.3 |
| feel-bad | 컨티뉴 대가를 20분 뒤에 앎 | ★ **pip 6칸이 그 자리에서 `neutralGray` ✕로** | ★ **§7.12.5** · §11.4 |
| feel-bad | 토큰 대가를 결과에서 처음 앎 | ★ **`시간 보너스 ✕`가 상단 띠 y 30~40에 보스전 내내 잔류** (= `[Shift]` 힌트와 같은 픽셀) | ★ **§7.12.5** · §11.3 LOCKED |
| feel-bad | 라이브 점수가 뒤로 감 | ★ **무피격·퍼펙트 = 정산 항목**(지급된 적 없음) | §6.2 |
| ★ feel-bad | **왜 Shift가 안 먹지** | ★ **`hud.tokenKeycapGatedDisplay`** — 입력이 무시된다는 것을 아이콘이 말한다 | ★ **§9.4.1** |
| 이유 모를 죽음 | 사망 화면이 숫자만 | **사인 1단어 + 파 막대** | §11.4 |
| 이유 모를 죽음 | 20분 전 파밍 부족을 지금 앎 | **파 고스트(라이브) + 파 막대(사후)** | ★ **§11.5** · §1.2 |
| 이유 모를 죽음 | 타이머 만료 즉사 | **60초(크기·맥동) → 30초(자홍·1Hz) 2단** + `Shift` 창 | ★ **§7.12.1** |
| ★ 이유 모를 죽음 | **속성 투자를 안 해서 죽었는데 그걸 모름** | 정본이 **`bossTimeoutRate ∈ [0.02, 0.25]`**를 신설했다(§13.1) — **0이면 3분 타이머가 죽은 규칙**이다. 즉 **"시간 초과로 죽는 런이 반드시 존재"**하고, 그 사인은 `시간 초과` 1단어 + 파 막대가 **어느 스테이지부터 뒤처졌는지**를 그림으로 말한다. ★ **v1.2에서 이 게이트가 처음으로 자기 콘텐츠에서 통과한다: 0.2389** (v1.1은 **0.0009 = 확정 실패**였다 — 게이트를 신설해 놓고 **자기 콘텐츠의 d를 한 번도 대입하지 않았다**). 즉 **이 행이 v1.1에서는 「정본이 신설했다」는 약속뿐이었고 v1.2에서 숫자가 됐다** — 8000런 중 **약 1,911런이 `시간 초과`로 끝난다**(§13.2.1) | ★ **§13.1 · §13.2-④ · §13.2.1** · §11.4 |
| 회복 불가 손실 | 알트탭 중 사망 | **`blur` 자동 일시정지** | §5.7 |
| 언락 그라인드 | 난이도 해금 | **4종 전부 처음부터** | §6.1 |
| 메뉴 순례 | 죽고 다시 하려면 타이틀 → 난이도 | **`Space` = 같은 난이도 즉시 재시작** | §5.5 |
| 튜토리얼 읽기 | 비직관적 코어 시스템 | ★ **텍스트 0줄 — 스테이지 1의 *구성*이 가르친다** | §9.9 M9 |
| 오발 | `Shift` 아무 때나 눌러 토큰 낭비 | **빨간불에서만 유효, 그 외 입력 무시** | §11.4 |
| 오발 | 상점에서 실수로 구매 | **불가 항목 `Enter` 무시** | §5.4 |
| 허들 삭제 | 코인으로 3분 타이머 무력화 | **토큰 = 시간 보너스 0** / 컨티뉴 = **타이머 `max(잔여, continueTimerRestoreSec 60)` + 런당 1회 하드캡** | §11.3 · §11.4 |
| ★ 체스식 최적화 | **"지금 죽어서 점수를 확정할까"** | ★ **`flow.pauseAllowsAbandon = false`** — 포기 키가 없으므로 그 수를 둘 수 없다 | ★ **§5.5** |

---

## 13. 개정 내역 (이 문서)

### 13.1 ★ v1.2 개정 내역

> 정본 v1.1 → **v1.2**에 맞춰 이 섹션이 고친 것의 전수. **§20.3-05의 통보 3건 전부 이행**했고, 그 외에 **정본이 본문을 뒤집은 5건**(§11.2)과 ★ **정본이 통보하지 않았으나 v1.2의 재산출이 낡게 만든 것 4건**, ★ **내 산술 오류 1건**을 정정했다.
>
> ★ **이 라운드의 성격**: v1.2의 통보 3건은 **전부 「05가 옳았다」**였다(`stanceHintPulse` 색 · `noHitRow` · `flow` 7키의 자리). **그런데 이 문서가 고칠 것이 그것보다 많았다** — 통보에 없는 것들이 `dpsRef` 재정의의 **연쇄**로 낡았기 때문이다. ★ **§20.3-05가 3줄인데 이 표가 10줄인 것이 요점이다: 통보 목록은 내 본문의 감사가 아니다.** 정본은 「05가 인용하는 수가 바뀌었다」를 통보 항목으로 세지 않았다(정본의 관점에서 그것은 04의 일이다) — **그러나 §6.5는 그 수 위에 6칸짜리 검산표를 세워 두었다.** **인용은 상속이지 사본이 아니며, 상속받은 것은 원본이 바뀌면 같이 바뀐다.**

| # | 무엇 | 어디 | 출처 |
|---|---|---|---|
| 1 | ★ **파 검산표 전면 재산출** — `killTime` 6종 **125.5/125.5/135.1/128.3/133.3/125.9**(v1.1의 125.8/…/134.1 폐기) · `stagePar` 예상 **54.5/54.5/44.9/51.7/46.7/54.1** · 밴드 **25~70 → 30~60** · 선형 근사 오차를 **v1.2의 HP(4,782/976)로 재계산**(78% / 89% — **비율 불변**, 앵커가 30 → 36으로 같이 커졌으므로) | §6.5 | **연쇄** (통보 없음) |
| 2 | ★ **정본의 산술을 독립 재현했다** — `killTime` 식을 직접 세워 4개 스테이지 전부 ±0.1s 일치 확인 + 중앙값 127.1 = `killTimeMedianBalanced` 확인. **인용하기 전에 검산했다**(C-8) | §6.5 | 자발 |
| 3 | ★ **`stagePar`의 시뮬 모드 미정을 발견 → 요청 ⑫** — probe/run 두 읽기가 **6/6 통과 vs 6/6 실패**로 갈리고, **정본 §13.6.5가 ⓑ의 수(17.3s)를 자기 본문에 인쇄**해 두었다 | §6.5 · §9 · §11.3.1 | ★ **새 발견** |
| 4 | ★ **`stanceHintPulse`를 §7.12.7의 전면 인용으로 전환** — 색은 05 채택 / ★ **주파수·알파는 02 채택**(`hintPulseHz` 1.0 · `hintPulseAlpha` [0.40, 1.0], 거처 `visual.stance`). **「어휘 재사용」은 값이 아니었다** + 1Hz 이중 맥동 불가 검산(정의역 배타) | §8.5 | §20.3-05-1 |
| 5 | ★ **위기 예고 y = −40 → 48 · 레이어 8 · 알파 0.50** — 인용하면서 「−40이 화면 밖」임을 확인하지 않았다. **02-R4가 옳았고 이 문서는 놓쳤다** | §3.2 · §12 | §20.3-02-2 |
| 6 | ★ **RESULTS 목업의 내 산술 오류 정정** — 「372초 = 보스당 **62초**」는 **분모가 6이 아니라 5**였다(`시간토큰 1회 · S4 보너스 0`이 **같은 칸에** 있다 → 74.4초). **260초 / 52.0초**로 교체 + 소계 223,940 → **218,340** + NEW RECORD 재확인 | §6.4 | ★ **내 오류** |
| 7 | ★ **`flow.attract.draftDwellSec` 0.8 → 1.2** + **게임초임을 명기** + **정본이 왜 옳은지 재검산**(11픽 × 1.2 = 13.2s = 어트랙트의 11%) | §2.2 · §10 | **연쇄** (통보 없음) |
| 8 | ★ **§10을 「자리까지 확인한 인용」으로 재작성** — v1.1은 **거처를 고치면서 그 거처에 인쇄 자리가 있는지 확인하지 않았다**(`flow` 7키 · `roundMode` 전부 인쇄 0회 = 로드 실패). **C-9(「채택은 인쇄가 아니다」)를 이 절이 배웠다.** `palette.neutralGray`도 같은 상태였음을 확인 | §10 · §11.1 | §20.3-05-2 |
| 9 | ★ **`hud.noHitRow`는 키가 아니다** (판정 5) — **요청 ⑩ 종결**, §9의 계약 행을 「우 패널 행의 **이름**」으로 정정, 백틱 제거 | §9 · §11.3 | §20.3-05-3 |
| 10 | ★ **`dpsRef` = `maxFarm`(5중 정의) 반영** — §8.6의 `Q` 논증이 **왜 흔들릴 수 없는 자리에 있는지** 증명(`dpsRef[6] = 708`은 두 farm 정책의 **공통 천장** · 테트라크는 `tier: "final"`이라 `bossHpScale` 미적용) · §8.7의 픽 사다리를 **farm별 `[11,…]` / `[12,…]`**로 갈라 인용 → **요청 ⑬**(§13.5 ↔ §13.5.1의 사다리 불일치, 내가 `Σ 6·L^1.32` = 26,449.7을 직접 계산해 §13.5.1이 옳음을 확인) | §8.6 · §8.7 · §11.3.1 | **연쇄** (통보 없음) |
| 11 | ★ **「새떼 = 총 XP의 ~25%」 폐기 → 실측 12~13% 반영**(판정 7) + **사슬 #9의 밀도 검산을 이 섹션이 직접**(위기 1.20/s·100% vs 잡몹 0.49~0.63/s·70% = **×2가 뜨는 적이 초당 1.9~2.4배**). ★ **#9 칸은 한 글자도 안 고쳤다** — 이 문서는 처음부터 페이오프를 **XP가 아니라 밀도와 화면**으로 셌고, 정본이 v1.2에 도달한 답이 그것이다 | §8.3 | **연쇄** (통보 없음) |
| 12 | ★ **`mono` = 전 팔레트 무채화**(판정 11) 반영 — 이 섹션의 화면 5종이 `mono`에서 채널을 잃는지 **전수 확인**(타이머 빨간불 = 크기·맥동·오디오 3채널 잔존 / 위기 점선 = 점선 형태 / ✕ 3종 = 원래 무채색 / 파 고스트 = 흰색+형태) → **잃지 않는다** | §10 | §20.3-02-4 (인접) |
| 13 | ★ **요청 ⑪ 종결 확인** — §7.2의 예약 문구가 **실제로 개정됐다**(위치 기반 → 의미 기반). **F-2가 §7.12.4를 인용하도록 바꾼 것이 이제 문면상으로도 옳다** | §11.3 · §0 | §20.2-M-5 |
| 14 | `bossTimeoutRate`가 **약속에서 숫자가 됐다** — **0.0009(확정 실패) → 0.2389** = 8000런 중 ~1,911런이 `시간 초과`. §12의 제거 장치 표에 반영 | §12 | **연쇄** |
| 15 | 머리말 = **대상 정본 v1.2** · 경계면 **6절 → 8건** · §13.5.1 · §13.6.5 추가 | 머리말 | — |

### 13.2 v1.1 개정 내역 (기록 보존)

> 정본 v1.0 → **v1.1**에 맞춰 이 섹션이 고친 것의 전수. **§18.3-05의 통보 5건 전부 이행**했고, 그 외에 **정본이 본문을 뒤집은 7건**(§11.2)과 **내 산술 오류 3건**을 정정했다.

| # | 무엇 | 어디 |
|---|---|---|
| 1 | ★ **`visual.timer.*`의 거처를 `rules.json > visual`로 정정** (「전부 `meta.json`」은 오기 — **로드 실패**) | §10 · 머리말 |
| 2 | ★ **테마 배너에 물결표 도입** + S8 대상 = 저작 리스트 + 실제 조우 ≤5%p (§8.2.1) | §3.1 · §3.3 · §7.2 · §8.3 · §8.4 |
| 3 | ★ **파 고스트 마커 규격을 정본의 것으로 교체** (흰 세로선 8px + 3px 캡) + `killTime` 6종 대조 검산 + **선형 근사의 오차가 v1.1에서 커진 것을 명시** | §6.5 |
| 4 | ★ **HUD 3요소를 §7.12.5의 인용으로 전환** (pip 3상태 · 퍼펙트 ✕ 자리 · 시간 보너스 ✕ = 상단 띠 y 30~40, `[Shift]` 힌트와 배타) | §4.3 · §5.3 · §9 |
| 5 | ★ **타이머 3단·위기 예고를 §7.12.1·§7.12.2의 인용으로 전환** (05 채택 / 02 기각의 근거 포함) | §4.1 · §3.2 |
| 6 | ★ **`bot.baseline` 3축 → 4축** + `shop` 축이 어트랙트에 도달하지 않음을 명기 + `slotOrder`·`latencyTicks` 추가 | §2.2 |
| 7 | ★ **`stanceHintPulse` 타겟을 §9.9.1의 인용으로 전환** (최빈의 `counter` · 중간보스/보스 제외 · 최빈이 노말이면 없음) | §8.5 |
| 8 | ★ **컨티뉴 재개 = "즉시 빨간불" → "경고"** (내 산술 오류: `max(·,60) ≥ 60`은 ≤30에 걸릴 수 없다) | §5.3 |
| 9 | ★ **상점 가격 정정**: `defense` 94→**95** / 136→**138**(`ceil` 누락), `reroll` 구매 1회의 현재가 30→**48** | §7.2 |
| 10 | ★ **온보딩 산술 정정**: 「스테이지 1에 ~9회」 → **11픽**(§13.5의 실계산). 결론 유지, 근거 강화 | §8.7 |
| 11 | ★ **`deathAnimSec`가 `menuSpeed`로 흐름** 명기(§0.2.1) — Disaster에서 1/3로 짧아지면 사인을 못 읽는다 | §5.1 |
| 12 | ★ **컨티뉴 런당 1회 하드캡**(`continueMaxPerRun`)의 화면 처리 확정 — 2회차는 **회색이 아니라 부재** | §5.2 |
| 13 | ★ **F-1 재작성**: 「I-4는 렌더 순서의 미신이 아니다」 → **「I-4는 레이어 구조가 보장하고(§12.3), 정지 화면은 그 스택 위이므로 정의역 밖」** | §0 |
| 14 | ★ **F-2 재작성**: 호박의 근거를 §7.2(위치 기반)에서 **§7.12.4(의미 기반 5곳 전수)**로 이동 | §0 |
| 15 | **RESULTS·상점 목업 전수 검산** + 배율 표기를 `점수 ×N`으로(02-B9) + C-7 명시(목업의 숫자 = 예시) | §6.4 · §7.2 |
| 16 | **§17 위임 범위 명기**(05-⑨ 채택) · **9건 처분 대장** · **남은 요청 2건으로 축소** | 머리말 · §11 |

**★ v1.1 개정의 성격**: 정본은 이 섹션의 요청 9건을 **전건 채택**했고(기각 0), 뒤집은 것은 **본문의 값 7건**이다. 그중 **3건(⑧의 거처 · 물결표 · 마커 규격)은 정본이 더 옳았고**, **3건(컨티뉴 빨간불 · 상점 가격 · 온보딩 ~9회)은 순수한 내 산술 오류**였으며, **1건(`bot.baseline` 3축)은 정본 자신이 v1.1에서 고친 것**(v1.0에는 상점 정책이 없어 **시뮬이 돌 수 없었다**)이다.

---

**★ v1.2 개정의 성격 — 이 섹션이 세 번째 라운드에서 배운 것**

| v1.0 → v1.1 | v1.1 → v1.2 |
|---|---|
| 요청 9건이 **정본에 자리가 없는 것**이었다 | 요청 2건이 **정본 안에서 두 문장이 갈라진 곳**이었고 **정본이 둘 다 닫았다** |
| 내가 고친 것 = **정본이 통보한 5건 + 내 오류 3건** | 내가 고친 것 = **정본이 통보한 3건 + ★ 통보 없는 연쇄 4건 + 내 오류 1건** |
| 실패 모드 = **요청을 올렸는데 자리가 없었다** | 실패 모드 = ★ **자리를 주장하면서 그 자리가 있는지 grep하지 않았다**(C-9) · ★ **인용하면서 인용한 수를 검산하지 않았다**(y = −40) |

★ **세 라운드가 같은 형태의 실패를 반복했고, 그 형태는 「대장을 보고 본문을 안 봤다」다.** v1.0의 05는 요청 대장을 만들었고, v1.1의 05는 거처 표를 만들었으며 — **둘 다 그것이 정본의 본문에서 참인지 확인하지 않았다.** v1.2에서 이 문서가 한 일은 대부분 **grep과 사칙연산**이었다: `flow` 7키가 인쇄됐는가(§10) · `spawnLineY`가 아레나 안인가(§3.2) · `killTime` 식을 직접 세우면 정본의 수가 나오는가(§6.5) · `Σ 6·L^1.32`가 26,450인가 27,029인가(§11.3.1) · 372를 6으로 나눌 것인가 5로 나눌 것인가(§6.4). ★ **다섯 중 넷이 정본이 통보하지 않은 것이었고, 다섯 중 하나(⑫)는 정본도 아직 모르는 것이다.**

→ ★ **남은 요청 2건(⑫ `stagePar`의 시뮬 모드 · ⑬ 픽 사다리의 이중화)은 둘 다 v1.2가 새로 만든 축(farm 정책)에서 나왔다.** 이 섹션의 값이 아니며, **`runFarmDpsRatio`를 아직 통과하지 않은 수가 정본에 하나 남아 있다는 것**이 정확한 상태다.
