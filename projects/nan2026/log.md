# Game Project — 작업 로그 (log.md)

이 폴더는 **NAN 2026 공모전**(nan2026.nhn.com) 사전과제 — *AI를 활용한 게임 제작* — 참가를 목표로 하는, 웹에서 간단히 플레이할 수 있는 **바닐라 게임** 프로젝트입니다.
이 `log.md`는 게임 기획·구현과 관련된 **나(사용자)의 대답과 Claude의 응답**을 시간순으로 기록합니다.

> **규칙**: 게임 관련 논의가 오갈 때마다, 그 내용(결정·질문·답변·이유)을 이 파일에 이어서 기록한다.
> 목적은 나중에 "왜 이렇게 정했는지"를 되짚기 위함. 장황한 전문(全文)이 아니라 **핵심을 요약**해 남긴다.

---

## 프로젝트 개요 (확정된 방향)

- **목표**: NAN 2026 공모전(nan2026.nhn.com) 사전과제인 *AI 활용 게임 제작*에 참가.
- **구현 방식**: 별도 repo 없이, 이 블로그 `projects/nan2026/` 안에 **바닐라 HTML/Canvas/JS**로 self-contained 정적 페이지 구현 (기존 `projects/exif-frame/` 패턴과 동일).
- **이유**:
  - 플레이 가능 — `https://jasonleex1995.github.io/projects/nan2026/`에서 브라우저로 바로 실행.
  - 최소 용량 · no legacy — 프레임워크 런타임·`node_modules`·빌드 스텝 없음. 게임 하나가 수십 KB 수준(갤러리 사진 1장보다 작음).
  - 관리 단순 — repo 하나, `data/projects.json` 한 줄로 등록, 블로그 톤(styles.css·footer) 공유.
- **슬러그**: `nan2026` (공모전명 기반). 아직 projects.json 미등록이라 필요 시 폴더명(=URL) 변경 비용 0.
- **저장소 (확정, 방법 1)**: 별도 repo 없이 블로그 repo 안 `projects/nan2026/`에서 개발 = **단일 원본**. 복붙 동기화 없음. 공모전 5개 제출 요건 모두 충족. 필요 시 **마감 직전** `git subtree split --prefix=projects/nan2026`로 게임만 히스토리째 별도 repo로 추출 가능(옵션, 지금은 안 함).
- **커밋 규칙 (심사용 clean history)**:
  1. 게임 커밋 메시지는 접두어 **`nan2026:`** 사용.
  2. 한 커밋은 가능한 한 **`projects/nan2026/` 파일만** 건드림 (블로그 콘텐츠 커밋과 섞지 않음).
  3. 의미 단위로 자주 커밋해 개발 과정이 이야기처럼 읽히게.
  → 심사자는 `git log --oneline -- projects/nan2026/` 로 게임 개발 과정만 깔끔히 볼 수 있음.
- **게임 종류**: **미정.** Claude와 차근차근 기획 예정. 용량·단순성 고려 시 에셋 없는 도형 기반 게임(스네이크·2048·테트리스·브레이크아웃 등)이 유력 후보.

---

## 로그

### 2026-07-13 — 프로젝트 시작 & 방향 결정

- **나**: 웹에서 간단히 플레이할 수 있는 게임을 개발하고 싶다. 게임 코드는 legacy가 생길 수 있으니, 게임 repo를 따로 파서 블로그에 이식하는 방식이 가능한지, 그리고 어떻게 생각하는지 물음.
- **Claude**: 가능하다. 통합 방식 3가지 제시 — (A) 별도 repo + 자체 Pages 후 링크, (B) 별도 개발 후 `projects/`에 정적 산출물 복사, (C) submodule(비추천). 처음엔 A/B를 추천.
- **나**: A안으로 하면 내 GitHub 블로그에서 실제로 플레이가 되는지 궁금. 그리고 최대한 legacy 없고 용량을 적게 쓰고 싶다(온라인 에뮬레이터처럼).
- **Claude**: A안도 github.io에서 플레이됨(정적 웹게임은 브라우저에서 실행). 다만 "최소 용량·no legacy" 목표까지 보면 **바닐라 Canvas/JS**가 최적이고, 바닐라는 격리할 무거운 코드가 없어 별도 repo의 이점이 사라짐 → **`projects/` 안에 바닐라로 구현(B안)** 을 추천.
- **나**: 그 추천(=projects 안에 바닐라 구현)이 맞는지 확인. 이해함.
- **나**: 용량이 얼마나 될지 걱정.
- **Claude**: 실측 비교 제시 — 현재 repo 6.1MB(대부분 이미지), 갤러리 사진 1장 663KB. 바닐라 게임은 코드만 10~40KB, 스프라이트 있어도 20~150KB. 게임 하나가 사진 1장보다 작음 → 용량은 신경 쓸 필요 없음. 커지는 원인은 node_modules 커밋·무압축 대용량 에셋뿐(바닐라는 해당 없음).
- **나**: 정확한 기획서는 아직 없고, Claude와 차근차근 만들고 싶다. 프로젝트 폴더를 하나 만들고, 게임 관련 나의 대답과 Claude의 응답을 `log.md`에 저장하는 것을 규칙으로 세우자.
- **Claude**: `projects/game/` 폴더 생성, 이 `log.md`에 규칙과 지금까지의 논의를 기록. 다음 단계로 게임 종류(장르·핵심 재미)부터 함께 정하기로.

### 2026-07-13 — NAN 2026 공모전 요강 확인 (Claude가 nan2026.nhn.com 조사)

**대회 개요**
- **NAN 2026 — NHN Game × AI 해커톤** (주최 NHN, 후원 문화체육관광부·한국콘텐츠진흥원)
- 컨셉: "AI를 활용하는 사람은 많다. 우리가 찾는 건 **AI의 다음 단계를 설계할 디렉터**."
- 본행사: 2026.09.04–09.06, **48시간, NHN 판교 사옥, 전일 참여 필수(온라인 불가)**
- 모집 대상: 만 19세 이상 누구나 / 총 10팀 / 팀당 최대 3인
- 일정: **모집·사전과제 제출 2026.07.10–08.10** → 참가팀 발표 08.22 → 본행사 09.04–06
- 상금 총 3.5억: 대상 5000만·최우수 2000만·우수 1000만(팀). 수상자 전원 최종면접 직행, 최종입사 시 근속보너스 최대 3000만.

**사전 과제 = "AI를 활용한 게임 제작"** — 제출물 5종, 하나라도 누락 시 심사 제외:
1. **플레이 가능한 빌드 + 전체 소스** → **GitHub Pages 링크**(웹 브라우저 실행) 또는 모바일 앱. 소스 동일 저장소 포함, **커밋 기록 유지**, **public 권장**(비공개면 심사계정 `dl_gameai_reviewer@nhn.com` 초대). .exe 직접 제출 불가, 유료 라이선스 없이 실행 가능해야.
2. **플레이 동영상** → YouTube 30~60초, 실제 플레이 화면(AI 합성·도용 불가).
3. **게임 소개/설명 문서** → PDF (제목·한줄소개·게임방법(목표·조작·종료조건)·실행방법·플레이 링크·영상 링크).
4. **AI 활용 기술 문서** → PDF (AI 도구·주요 프롬프트·활용 내역, 구조 설명, 외부 에셋/오픈소스 출처·라이선스).
5. **팀원 롤 기술서** → PDF (2인 이상 팀만; 개인 참여 시 생략).
- 유의: AI 도구 사용 허용·권장하나 도구·활용내역 필수 기재. 외부 에셋 출처·라이선스 명시. 링크는 심사 종료까지 유지. 마감 후 변경 불가.

**우리 계획에 대한 함의**
- "웹 빌드 + GitHub Pages + 소스 포함 + 커밋 기록" 요건 → 우리의 바닐라/GitHub Pages 방향과 정확히 일치. ✓
- "AI 활용 기술 문서"가 필수 → **이 `log.md`(AI와의 기획·구현 대화 기록)가 그 문서의 훌륭한 원천**이 됨. 계속 잘 남길 것.
- **재검토 포인트(저장소 위치)**: 요건이 "동일 저장소에 소스 포함 + 커밋 기록"이라, 심사가 저장소·커밋 히스토리를 봄. 블로그 repo 안(`projects/nan2026/`)에 두면 요건은 충족하지만 **블로그 커밋과 게임 커밋이 섞임**. 공모전 제출용으론 **게임 전용 public repo + 자체 Pages**(예: `jasonleex1995.github.io/<gamerepo>/`)가 커밋 히스토리·심사 관점에서 더 깔끔할 수 있음. → **결정 필요** (아래 참조). 어느 쪽이든 블로그 projects 카드에서 링크는 가능.
- "AI 활용"의 의미: 컨셉·제출물(‘AI 활용 기술 문서’) 정황상 **주로 "AI 도구로 게임을 만드는 제작 과정"**을 뜻함(게임 내부에 AI 기능이 반드시 들어가야 한다는 명시 요건은 없음). 다만 게임 안에 AI를 녹이면 차별화 요소가 될 수 있음. 유료 API 비용 관련 FAQ 존재.

### 2026-07-13 — 저장소 방식 결정: 방법 1 + 커밋 규칙

- **나**: 방법 1(블로그 repo 안에서 개발)로 진행하되, commit history가 잘 보이도록 신경 써 달라.
- **Claude**: 방법 1 확정. 복붙(두 벌 동기화)은 피하고 단일 원본 유지. 커밋을 `nan2026:` 접두어 + `projects/nan2026/`만 건드리는 방식으로 분리해 심사자가 게임 개발 과정만 깔끔히 볼 수 있게 함. 깨끗한 단독 히스토리가 필요하면 마감 직전 `git subtree split`로 추출(옵션). 다음 단계로 게임 아이디어 탐색 시작.

### 2026-07-13 — 게임 아이디어 탐색 (Claude 멀티에이전트 워크플로)

- **방법**: 5개 각도(① AI 핵심 메커닉 ② AI 생성 콘텐츠 ③ 아케이드 리믹스 ④ 언어=조작 ⑤ 심사 임팩트)에서 후보를 병렬 생성 → 심사 에이전트가 AI차별성·데모재미·구현가능성·독창성으로 점수·랭킹. *(이 탐색 과정 자체가 "AI 활용 기술 문서"의 소재.)*
- **Top 6 (총점순)**:
  1. **TELL** (8.3) — 플레이어 이동 패턴을 실시간 학습·예측해 미리 조준하는 보스. 살려면 자기 패턴을 깨야 함. 가변차수 마르코프 앙상블+밴딧, 완전 오프라인 ~300줄. READ 게이지+예측 고스트로 **학습이 30초 영상에 눈에 보임**.
  2. **Verbomancer** (8.0) — 아무 단어나 입력하면 그 의미로 주문 속성·데미지가 정해지는 마법 결투. 빌드타임에 임베딩 베이크(오프라인). 최고의 영상 한 방("apocalypse" 입력).
  3. **Oracle** (7.95) — 원버튼 좌/우 '랜덤' 입력을 예측·패링하는 보스(Aaronson oracle). ~50줄, 가장 안전. TELL의 저위험 형제.
  4. **Director's Deck** (7.85) — Claude가 효과 DSL로 카드/유물을 '설계'하고 빌드타임 밸런스 시뮬로 검증하는 로그라이크. 컨셉("AI 디렉터")에 가장 부합, PDF 스토리 최강. 스코프 리스크.
  5. **Mirror Pong** (7.85) — 작은 신경망이 내 조작을 30초 만에 학습해 '내 클론'과 대결. 순수 바닐라 ML.
  6. **2048: Adversary** (7.6) — 새 타일을 적대적 탐색 AI가 최악의 칸에 놓는 2048. 가장 안전·직관적.
- **추천 1위: TELL** — AI차별성·구현가능성·데모가독성을 동시에 높게 만족(대부분 후보는 셋 중 하나를 희생). 1주차: AI 모듈만 먼저 스파이크해 랜덤 대비 적중률 우위를 영상으로 입증 → 안 되면 Oracle로 피벗.
- **결정**: 게임 선택은 **사용자 확인 대기**.

### 2026-07-13 — AI 활용 방향 확정: "제작 도구로서의 AI" + 무-API 오프라인 게임

- **나**: AI는 게임의 **기획·디자인·개발·디버깅(QA)**에 쓰고 싶다. 게임 안에 AI를 넣으면 API가 필요해 리소스가 크다고 봄. 목표는 웹에서 **저용량·안정적**으로 도는 게임.
- **Claude**: 이해함. **결정 — AI는 제작 프로세스 도구, 게임 자체는 저용량·안정·완전 오프라인·무API.**
  - *정정*: "인게임 AI = 반드시 API"는 아님. TELL·Oracle·Mirror Pong·2048:Adversary의 AI는 **LLM API가 아니라** 브라우저에서 도는 고전 알고리즘(예측 모델·탐색·초소형 신경망) → 몇 KB, 오프라인, 완전 안정. 즉 "저용량·안정" 목표와 충돌하지 않음. (다만 사용자는 심플한 게임을 선호 → 옵션일 뿐, 강요 안 함.) **LLM API 기반 인게임 AI만** 무겁고 불안정하므로 그건 어느 방향이든 배제.
  - *전략 유의*: 공모전이 "AI를 활용하는 사람은 많다"고 못박음 → 단순 게임 + "AI로 코딩함"만으론 차별화가 약함. **AI 제작 과정을 풍부하게(설계 반복·에셋 생성·자동 QA·워크플로) 문서화**해야 경쟁력. 이미 멀티에이전트 기획·이 log.md로 그 방향을 타는 중.
- **결정**: 게임 자체는 **무API·오프라인·저용량** 확정. 구체 게임 선택은 계속 상의.

### 2026-07-13 — 방향 A 확정 (심플 게임 + AI 제작과정) & '오프라인 AI' 오해 해소

- **나**: A가 좋다. 웹에서 '오프라인 AI'가 잘 안 와닿음 — 오프라인이면 모델을 다운받아야 하는 것 아닌가?
- **Claude**: **A 확정** (게임엔 인게임 AI 안 넣음). 오해 해소 — 게임 AI는 대개 '다운받는 모델'이 아니라 ① 순수 알고리즘(데이터 0) ② 플레이 중 실시간으로 채워지는 작은 표(다운로드 0) ③ 소스에 몇 KB 숫자로 박힌 초소형 가중치. 'LLM = 큰 모델 다운로드'와 다른 개념. (참고용 지식; A라서 안 씀.)
- **A 강화 제안 (채택 방향)**: 런타임 AI 없이도 **빌드타임에 AI로 콘텐츠(레벨/아트/난이도) 생성 → 정적 데이터로 탑재.** 무API·오프라인·저용량 유지하면서 "AI 디렉터" 스토리 강화 → 'AI 활용 기술 문서'가 흔한 "AI로 코딩함"을 넘어섬.
- **다음**: 심플 게임 장르/아이디어 선택.

### 2026-07-13 — 플레이어 프로파일 + 게임 추천 (Claude 멀티에이전트: 3 프로파일 × 12 후보 → 심사)

- **나**: 내가 어떤 게임을 왜 좋아하는지 먼저 정확히 파악한 뒤 추천해 달라. (취향: 호흡 긴 게임 X, 한 판 <1시간, 최근 칼바람·보글보글·194X·뱀서, 어릴 때 포켓몬·메탈슬러그·마리오·커비·로그라이크, 바둑/체스식 전략 X, 리듬/반응 X, RPG의 성장→양학이 좋음.)
- **플레이어 프로파일 (한 줄)**: 시간·집중 여유가 적고 향수 있는 올라운더 — *약→신급 성장 후 양학하는 RPG 파워판타지를, 1시간 미만·반사신경 관대·숙제 없는 로그라이트 한 판에 압축*하고 싶어함.
  - 핵심 동기: ① 성장 후 압도(양학)가 감정 절정 ② 한 판 안의 파워 커브 ③ 낮은 인지부하 ④ 짧고 중단 가능한 세션 ⑤ 반사신경 아닌 '선택·누적'의 숙련 ⑥ 런마다 다른 빌드.
  - 회피: 긴 호흡·멀티세션 / 바둑·체스식 전략 / 리듬·반응 / 세션 간 관리.
- **추천 1위: OVERGROWN** — 뱀파이어 서바이버즈류(survivor-like) 로그라이트. 씨앗 하나 → 화면 뒤덮는 식물신. 무브온리+자동공격, 레벨업마다 3택 드래프트(씨앗/양분), 특정 조합이 슈퍼플랜트로 진화 → 18분 런의 양학 피날레.
  - **AI 차별점**: 시너지 트리 = 게임 본체 → AI가 콘텐츠(~40 씨앗·25 양분·60노드 진화그래프)를 **생성 + 헤드리스 봇으로 수천 판 자동 플레이테스트 + 텔레메트리로 밸런싱**. "AI가 게임을 설계·튜닝"이 진짜 스토리('AI로 코딩함'을 넘어섬). 무API·오프라인·~30-60KB 데이터.
  - 런너업: **GLUTTON**(커비 흡수 트위스트), **SKYSWARM**(194X 슈팅 스킨, 데모 임팩트 최고).
  - 리스크: "그냥 뱀서 클론" 인상 → AI 디자이너 파이프라인 + 뚜렷한 테마로 상쇄. 장르가 검증돼 4주 솔로 난이도 낮음.
- **결정**: **사용자 확인 대기** (핵심은 survivor-like 루프; 테마/스킨은 자유 변경 가능).

### 2026-07-13 — 취향 정교화: "선택이 만든 양학" + 적당한 숙련 레이어 (탈-클론)

- **나**: 프로파일 다 맞음. 단 (1) 뱀서 아류 느낌이 안 나야 함 — 더 재밌는 요소 필요. (2) 단순 양학보다 **내 올바른 선택으로 결과가 뚜렷하게 보이는 양학**을 좋아함. (3) 너무 양학만 하면 지루 → **적당히 숙련 쌓는 과정**이 즐거움. 예: 보스몹에 약간의 패턴, 비행기 게임처럼 **탄막을 어느 정도 보고 피하기** (단, 리듬/트위치 반응은 여전히 싫음 = 관대하게 읽고 피하는 수준).
- **설계 타깃 (확정 방향)**: survivor-like 베이스 + ㉠ **빌드 정체성** — 선택이 스탯 +%가 아니라 *플레이스타일을 뚜렷이 바꾸는* 시너지(올바른 선택 → 눈에 보이는 지배) ㉡ **적당한 스킬 레이어** — 텔레그래프된 보스 패턴 + 관대한(읽고 피할 수 있는) 탄막 회피 ㉢ 여전히 무-트위치·짧은 런·무API·오프라인.
- **다음**: 이 타깃에 맞는 구체적 차별화 안(탈-클론 훅) 탐색 → 사용자 선택.

### 2026-07-13 — 차별화 훅 탐색 (Claude 멀티에이전트: 5각도 15안 → 심사)

- **추천 1위: REFRACT (탄환 포식자)** — 핵심 동사 = 회피가 아니라 **HARVEST(흡수)**. 정면 흡수 콘으로 적/보스 탄막을 *먹고*, 드래프트한 **변환 노드**가 흡수한 탄을 무엇으로 바꿀지 결정:
  - **Nova**(모아서 화면청소 역폭발) / **Swarm**(먹은 탄을 유도탄으로 재발사) / **Bulwark**(먹은 탄을 방어막으로).
  - 선택-지배 읽힘: *"내가 Nova 골라서, 보스의 궁극기가 내 궁극기가 됐다."*
  - 왜 맞음: ㉠ **탈-클론**(탄으로 걸어들어가 먹는 건 뱀서와 구조적으로 다름) ㉡ **선택이 플레이스타일을 통째로 바꿈**(+% 아님), 보스의 가장 무서운 순간에 페이오프 ㉢ **관대한 스킬**(흡수는 순수 이득 → 잘못 읽어도 그냥 평범한 관대한 회피, 트위치 아님). 스킬 = 어느 호를 쓸어담을지 읽기 + 언제 방출할지(내 타이밍).
  - 루프: 18분, 미니보스 ~3분/보스 ~4-5분(총 4-5회)+18분 에이펙스. 관대함 노브(작은 판정·넓은 콘·반응창 ≥500ms·텔레그래프·'BIG ABSORB' 표시). 방출=내 미터 소비(강제 QTE 아님). 보스1(겨우 먹음)→보스3(빌드 클릭, 한 방에 HP 절반)→에이펙스(궁 유도→자기 탄으로 삭제=양학).
  - AI 스토리(빌드타임·무API): 보스 탄막 스크립트+변환 노드+시너지 그래프 생성 → 헤드리스 시뮬로 흡수 경제 밸런싱 + **모든 패턴이 "흡수 없이도 회피 가능(공정) & 가만히 있으면 죽음(치명)"인지 자동 인증** → "AI가 설계+밸런싱+공정성 인증". ~30-60KB.
- **런너업**: **USURP**(보스 공격을 이겨서 *훔쳐* 내 무기로 — AI 스토리·선택가독성 최강, 조합 폭발 리스크) / **BREAKPOINT**(올바른 빌드로 보스 공격을 *취소/무력화* — 가장 안전, "무서운 탄막이 아예 안 나옴"=가장 순수한 선택-양학).
- **결정**: 사용자 확인 대기.

### 2026-07-13 — 이동 설계 확정 (뱀서 노가다 제거)

- **나**: 뱀서에서 귀찮았던 건 1+2+3 복합. 단, 탄막 피하며 아이템 먹는 *타이밍 잡는 스킬은 원함* (= 4번 "안 움직이고 싶다"는 아님). 문제는 맵 저 멀리 아이템 주우러 가는 무의미한 노가다가 *중요한 데 집중 못 하게* 만드는 것.
- **결정 (REFRACT에 반영할 이동 규칙)**:
  1. **바운드된 단일 화면 아레나** — 스크롤 맵 횡단 제거(2번).
  2. **자동 자석 수집** — 모트가 플레이어에게 날아오거나 처치 시 자동 수집 → 먼 아이템 줍기 노가다 제거(3번).
  3. **이동은 의미 있는 것에만** — 탄막 회피 + 흡수 콘 포지셔닝 + 먹는 타이밍(= 원하는 "적당한 숙련"). 무의미한 도망 제거(1번).
  → 이동은 유지하되 *알짜만* 남김. 뱀서의 아쉬움을 정확히 겨냥해 제거.

### 2026-07-13 — 피드백: 능동 공격 선호 + "얻어낸 양학"(밸런스 중요) → REFRACT 재검토

- **나**: REFRACT = 총알 막아 기 모아 공격, 으로 이해(맞음). 그런데 (1) 너무 단순·재미가 커 보이지 않음. **쉽게 양학하면 아무도 안 함** → 약간의 허들 + 재밌는 구간을 잘 잡는 밸런스가 핵심. (2) 총알 막기가 **너무 수동적** → **내가 능동적으로 공격**하는 게 더 좋음.
- **방향 전환 (확정)**:
  - 핵심 동사: **수동적 흡수 → 능동 공격.** (REFRACT 흡수는 코어에서 내림; 살리면 선택적 서브 요소로만.)
  - 난이도 철학: **"쉬운 양학"은 노잼 → 양학은 허들을 넘어 *얻어내는* 것.** 약간 빡세지만 넘을 만한 곡선 = 재미의 핵심. 이 곡선 튜닝이 곧 AI 시뮬 밸런싱의 역할 → **밸런스를 차별점으로.**
  - 유지: 선택이 만든 지배(빌드) + 적당한 관대 회피 + 바운드 아레나 + 무-노가다.
- **다음**: 능동 공격형 방향 상의 (예: USURP 탈취형 / 194X 액티브 슈팅 / 메탈슬러그식 런앤건 로그라이트).

### 2026-07-13 — 컨셉 좁힘: 선택형 드래프트 필수, 런앤건 제외 → 194X 액티브 슈팅으로 수렴

- **나**: (1) USURP에서 보스 잡으면 그 패턴을 *무조건* 얻는 건 별로 — 뱀서처럼 레벨업 때 무기를 **선택**하는 권한이 좋음. (2) 런앤건(C)은 리소스 무거우니 **제외**.
- **결정**:
  - **선택형 드래프트(레벨업 3택)는 필수 코어** — 빌드는 강제 획득이 아니라 *내 선택*에서 나와야 함.
  - 런앤건(C) 제외(무게).
  - 수렴: **B = 194X식 능동 슈팅 로그라이트 + 뱀서식 선택 드래프트.** 능동 조준 사격 + 관대 탄막 회피 + 3택 화력 진화 + 보스 허들 + 얻어낸 양학 + 바운드 아레나 + 무-노가다.
  - USURP "보스 기술 탈취"는 **선택형으로 재해석**: 보스 처치 시 그 보스 테마 무기가 *드래프트 풀에 해금*(고를지는 내 선택) — 옵션.
- **미결**: 조준 방식(자동 vs 직접), USURP-선택형 시그니처 채택 여부, 테마.

### 2026-07-13 — 조작 확정(자동발사+방향) & 요구: 194X 아류 탈피(고유 특색)

- **나**: 비행기 슈팅 느낌 맞음. 조작은 **자동발사 + 유저는 방향/위치만**(194X식). 단 걱정: 그냥 **194X 아류작 느낌이 너무 강할까 봐** — 내 게임만의 특색이 필요.
- **결정/요구**: 컨트롤 = 자동발사+방향. + **차별화된 시그니처 정체성**을 얹어 194X 클론 탈피 (능동성·선택 드래프트·얻은 양학·관대 회피·바운드 아레나·무-노가다 유지).
- **다음**: 시그니처 정체성 훅 탐색 → 사용자 선택.

### 2026-07-13 — 시그니처 정체성 탐색 (Claude 멀티에이전트: 5각도 15안 → 심사)

- **추천 1위: ANTIGEN — 되받아 진화하는 군집** — 면역계 테마 불릿헤븐. 적은 **항원(색+글리프) 태그**를 달고, 내 무기는 그 태그를 노리는 **항체**. **한 무기에 과하게 의존하면 살아남은 군집이 새 색으로 변이해 저항 → 내 DPS 폭락(~3초 패닉) → 변형 카운터-항체를 드래프트해야 다시 지배.** = *"적이 나에게 맞춰 진화하는 유일한 불릿헤븐."*
  - 왜 클론 아님: 모든 슈팅은 고정 웨이브. 여기선 *적이 내 빌드에 반응하는 동적 시스템* → 구조적 역전. "co-evolution" 한 단어로 각인.
  - 왜 맞음: 능동 공격(항체가 군집 속으로 자동발사, 파고들어야) / 선택-지배가 곧 코어 루프(저항 스파이크=허들, 카운터-드래프트=얻은 양학) / +% 무의미(저항되면 변형만이 답)=transform-not-creep을 픽션으로 강제 / 관대 회피(느린 큰 텔레그래프 독탄) / 에이전시 보장(시뮬이 모든 변이에 카운터 ≥3장 보증).
  - 루프: 15-18분 3막, ~40초마다 3택, 저항은 한 태그가 데미지 55%↑ 시 발동(막당 2-3회↑). "지배→체크→재지배" 아크. 예시 빌드: 광범위형/단일특화형/억제형 3종 완전 다른 플레이.
  - AI 스토리(최강): 빌드타임에 병원체 생태+항체표+저항스케줄 생성 → 헤드리스 시뮬 수천~만 판으로 "모든 변이에 카운터 ≥3·지배 무기 없음·하드벗페어 창 안착" 수치 증명. 손으로 밸런싱 불가한 조합.
  - 리스크: **두더지잡기/뒤처지는 느낌** — 카운터가 성가심이 될 수 있음 → 1주차에 이 '느낌'부터 검증(변이는 군집 일부만, 항체는 계속 전방 발사, 지배 창 넉넉).
  - 런너업: **CONSTELLATION**(밀집=데미지, 일부러 둘러싸이기 / 뒤집기 훅·라이트쇼) / **BANK SHOT**(당구식 도탄 / 가장 안전·데모 확실).
- **결정**: 사용자 확인 대기.

### 2026-07-13 — 방향 재조정: 공진화 폐기 → 포켓몬식 타입 상성 + 운빨 로그라이크

- **나**: ANTIGEN처럼 계속 카운터가 강제로 나오는 건 별로(두더지잡기). 차라리 **운빨 로그라이크** 느낌이 납득됨. 단 "특정 공격에 강한 적" 아이디어는 좋음 → **포켓몬 타입 상성**이 떠오름(노말은 대부분 ×1, 풀-물-불 상성). 이 느낌으로 가보고 싶음.
- **Claude**: 동의(이게 더 나음). 좋아한 부분만 취하고 싫은 강제 적응은 버린 영리한 편집.
- **결정 방향**:
  - **공진화/강제 적응 폐기.** 적은 정적 타입, 변이로 쫓아오지 않음.
  - **타입 상성(포켓몬식)**: 무기·적에 타입, 효과적 ×2 / 보통 ×1 / 저항 ×0.5. "올바른 타입 커버리지 → 양학" = 선택-지배.
  - **운빨 로그라이크**: 랜덤 3택 + 런마다 다른 적 타입 구성. 강제 카운터 없이 *주어진 운으로 빌드*.
  - 유지: 자동발사+방향, 능동, 관대 회피, 바운드 아레나, 무-노가다. 이동 = 타입 매칭 위치잡기(의미 있는 이동).
  - AI 스토리: AI가 밸런스된 타입표+적 생태+드래프트 풀 생성 → 시뮬로 "어떤 RNG 런도 클리어 가능(막다른 운 없음)·지배 타입 없음·하드벗페어" 증명.
  - 옵션: **타입 융합**(듀얼타입/합성 원소)로 깊이·정체성 강화.
  - 솔직한 트레이드오프: 원소 상성은 흔한 장치라 ANTIGEN만큼 참신하진 않음. 대신 재미 검증·안전·동기부여가 강점.
- **미결**: 타입 융합 채택 여부, 조준 방식(위치잡기 vs 자동 우선타겟).

### 2026-07-13 — 컨셉 구체화: 테마 스테이지 + 복합 보스 + 뱀서식 무기 아스널

- **나**: 너무 순수 로그라이크면 *운빨겜* 느낌 → 싫음. **테마 스테이지**로 완화: 예) 바다 테마 = 물 적 위주 + 풀·노말 소량 혼합. 보스는 **복합 타입**(엔진=불, 날개=물, 몸체=노말 등 부위별). 무기는 다양한 **거동**(유도탄/일자/방사형…). 비행 슈팅이지만 **뱀서도 참고**.
- **결정 (구체화)**:
  - **테마 스테이지** → 적 타입이 테마로 예측 가능(정보) + 소량 혼합 → **정보 기반 선택이 운빨을 이김**(운빨겜 우려 해소). 테마는 미리 고지.
  - **복합 다부위 보스**: 부위별 타입 → 커버리지 + 부위 조준 = 무-트위치 전술 깊이. (부위 파괴로 패턴 변화 가능.)
  - **뱀서식 무기 아스널**: 무브온리, 여러 무기 동시 자동발사. 무기 = **2축 [거동(유도/일자/방사/오빗/설치) × 타입(불/물/풀/전기/노말)]**. 위치잡기로 알맞은 무기를 알맞은 적/부위에.
  - **런 구조**: 4~5 테마 스테이지(바다/숲/화산…) 각 2~4분 + 끝마다 복합 보스, 사이 3택 드래프트. 총 ~15-18분.
  - 정체성: 뱀서 × 타입상성 × 비행슈팅 × 복합보스 = 충분히 신선한 조합.
  - 유지: 능동·관대 회피·의미있는 이동·무-노가다·AI 밸런싱.
- **미결**: 타입 융합(→ 2-3주차 옵션?), 타입 개수, 무기 진화(뱀서식) 여부.

### 2026-07-13 — 타입 4종 확정 + 뱀서식 진화 채택 → 설계 스펙 v0.1 작성

- **나**: 전기는 애매 → **물·불·풀·노말 4종**. 물-불-풀 삼각 상성, 노말은 ×1. 무기는 **뱀서식 진화**(초반 약하지만 맥스까지 키우면 폭발 = 참고 키운 보상).
- **결정**: 타입 4종 확정. 뱀서식 무기 진화 채택(빌드 텐션: 커버리지 vs 몰빵). → 이 결정들로 **한 판 스펙을 [DESIGN.md](DESIGN.md) v0.1**에 문서화(타입표·무기 예시·바다 스테이지·복합 보스 '강철 가오리'·난이도 곡선·AI 파이프라인·1주차 목표).
- **다음**: DESIGN.md 리뷰 → 1주차 프로토타입(타입 상성 손맛 검증).

### 2026-07-13 — 페이스·스테이지·무기 슬롯·슈팅 전통 반영 (DESIGN v0.2)

- **나**: (1) 30분 클리어 목표 → **6 스테이지**(앞 5 랜덤 + 최종 고정, 최종은 중구난방+특색). 레벨업 속도 궁금. (2) 무기는 **특색이 제일 중요**, 먹는 **무기 수 제한** 필요(뱀서도 상한 있음). (3) **중간 보스 1~2회** + **위기 세션(새떼)** 등 비행슈팅 전통.
- **Claude/결정**:
  - 30분 / 6스테이지(~5분each), 앞 5 랜덤·최종 고정. 레벨업 ~50-60회/런 → 무기 6칸 맥스 시점이 후반부 = "참고 키운 양학" 곡선.
  - **무기 슬롯 6칸 + 패시브 4~6칸**(뱀서 원리). 무기 **특색 = 설계 1순위**(거동별 플레이 자세 차별, AI 생성 시 겹침 금지 제약).
  - 스테이지 내부 리듬: 웨이브 → 중간보스 → 위기 세션 → 스테이지 보스.
  - → [DESIGN.md](DESIGN.md) v0.2 갱신(런 구조·무기 슬롯/성장/특색·난이도 곡선).
- **다음**: DESIGN v0.2 리뷰 → 1주차 프로토타입.

### 2026-07-13 — 무기·속성 리서치 (Claude 멀티에이전트: VS+슈팅+로그라이트+속성시스템 → 종합)

- **핵심 통찰 (무기 부족 해결)**: 무기 = **거동 × 속성 매트릭스.** ~12 거동 패밀리(포워드/확산/유도/관통빔/오빗/오라/설치존/부메랑/포격/후방/드론/노바) × 4 속성 = **~48종**을, 12거동+4규칙만 저작하면 됨. AI가 그리드 밸런싱.
- **무기-속성 = 고정(welded) 추천** (정체성·가독성·밸런싱 쉬움). 자유 리임뷰 거부(‘항상 매칭’으로 붕괴). 예외: **코스트 있는 속성전환 1개**(쿨다운, 복합보스 적응 밸브).
- **슬롯: 4무기 + 3패시브 추천** — 6은 자동발사라 내 탄으로 화면이 죽 → 적 탄 못 읽음(정보/포지셔닝 기둥 붕괴). 4 vs 4속성: 매칭 타입 2겹침(보너스+진화)이 하나씩4보다 강 → 특화 시 매 스테이지 2속성 미커버 = 정보기반 선택 긴장(원하던 그것).
- **적 공격 속성?**: 워크플로는 Option B(적 공격도 속성 + 약한 방어배율 1.3-1.5×, Ikaruga식 흡수/즉사는 X) 추천. **Claude 캐치**: 우리 3속성은 *대칭 순환*이라 오펜스/디펜스가 같은 답을 가리켜 "생존≠킬" 긴장이 성립 안 함 → **심플하게 Option A(적 공격 무속성, 회피=방어)로 시작** 추천, typed-defense는 후속 옵션.
- **제안 무기 8종 초안** 확보(거동×속성×진화). DESIGN 반영 예정.
- **결정 대기**: 4무기+3패시브 / 적 공격 속성 A vs B / 고정+전환밸브.

### 2026-07-13 — ★핵심 전환★ 속성 스탠스 시스템 + 오락실 요소 (DESIGN v0.3)

- **나**: (1) 패시브 3개는 적다 — 성장 느낌엔 레벨업이 많아야 하고 선택지·패시브도 다양해야. (2) **적 공격 무속성(A) 확정** — 회피는 내 컨트롤로. (3) 무기-속성 고정이 걱정 → **무기는 고정, 속성을 바꾸는 방식** 제안: 속성별 투자 레벨(불+2/물+1/풀+0)을 쌓고, 스탠스를 바꾸면 **투자 레벨만큼의 무기만** 그 속성으로 발사, 나머진 무속성. (4) imbue 대상은 **슬롯 순서**가 구현상 깔끔. (5) 전환 쿨다운은 보스전에 비현실적 → 대신 **보스/스테이지 시간제한(봄버맨식)** 아이디어. **1234/QWER 키로 속성 전환 → 양손 플레이**. (6) 오락실 구조(세이브 없음) → **점수 설계**와 **재화(코인)** 고민: 상점·리롤 등 "선택하는 재미". **회복 아이템**도 필요.
- **Claude 평가/결정**:
  - **속성 스탠스 시스템 채택 ★** — 무기 정체성 유지 + 속성이 독립 성장축 + **"항상 최적 매칭" 붕괴 방지**(전환 자유지만 *투자한 만큼만*) + 특화↔분산 다이얼 + **노말이 안전 바닥**(브릭 없음) + 복합 보스와 시너지(부위별 스탠스 퍼즐).
  - imbue = **슬롯 순서 + 슬롯 재정렬 허용**(로직 깔끔 + agency).
  - **쿨다운 제거**(투자가 이미 게이트). 키 = **고정 할당**(머슬메모리). 이동=방향키(오른손)+속성=1234/QWER(왼손) 양손 플레이.
  - **시간제한**: 보스당 5분×6 = 런 전체를 잡아먹음 → **스테이지당 5분 제한**(6×5=30분, 목표와 일치)으로 스케일 조정 제안. (대안: 보스 전용 90초~2분.)
  - **패시브 5~6칸**으로 증량(패시브는 화면 안 어지럽힘). **드래프트 4종류**(새 무기/무기 레벨업/**속성 레벨업**/패시브) → 레벨업 50-60+회 전부 유의미.
  - **코인 = 안티-운빨 밸브**: 리롤·밴시·**스테이지 사이 상점**(무기/속성/패시브/회복 직접 구매) → 운 의존↓, 선택 재미↑.
  - **회복은 레벨업 드래프트에서 제외**('회복 vs 성장' feel-bad) → 스테이지 사이 부분 회복 + 상점 + 희귀 드랍.
  - **점수**: 처치 + **상성 처치 보너스**(속성 시스템을 점수로 보상) + **남은 시간 보너스**(좋은 빌드 보상) + 무피격/보스. 세이브 없음, **로컬 최고점수만 localStorage**(오락실 감성).
  - → [DESIGN.md](DESIGN.md) **v0.3** 전면 갱신.
- **미결**: 시간제한 스케일(스테이지 5분 vs 보스 전용), 키(1234 vs QWER), 제목.

### 2026-07-13 — 보스 3분 타이머 · 배속 난이도(스피드런) · 코인 규칙 (DESIGN v0.4)

- **나**: (1) 비행슈팅은 잡몹·중간보스를 *안 잡아도 되지만* 안 잡으면 레벨이 안 올라 최종보스를 못 잡는 구조 → **보스에 3분 제한**이 맞지 않나. (2) **배속 난이도** 아이디어: 3배속이면 적·내 공격 모두 3배, 보스 제한도 1분. 점수에 반영 = 스피드런 느낌. Normal(×1)/Hard(×1.5)/Hell(×2)/Disaster(×2.5~3) 뉘앙스. 설계는 Claude가. (3) 코인은 **항상 많은 느낌이 안 들게** 설계해야. 리롤·회복물약(가격 상승) OK. **무기·속성 레벨 구매는 반대** — 레벨업의 영역을 침범하면 안 됨. 덜 중요한 스킬(방어력·이동속도·코인 자동수집 등)은 납득 → 더 다양하게 제안 요청. (4) 회복을 레벨업에 안 넣는 것 이해.
- **결정**:
  - **보스 타이머 = 게임시간 3분** 채택(스테이지 5분 제한 안은 폐기). 스테이지 = **잡몹 ~2분(선택적 파밍 = 리스크/리워드) + 보스 최대 3분** ≈ 5분 × 6 = 30분. **보스 타이머 = "잘 골랐나"의 DPS 시험지.**
  - **배속 난이도 채택 ★** — 게임 클럭 1개만 곱하면 타이머·런 길이가 자동 스케일(구현 극저비용). Normal ×1(3:00, ~30분, 점수 ×1) / Hard ×1.5(2:00, ~20분, ×1.6) / Hell ×2(1:30, ~15분, ×2.5) / Disaster ×3(1:00, ~10분, ×4). 점수 배율 **초선형**. 난이도는 **옵트인**(Normal은 관대 유지 = 무-트위치 기둥 보존). 로컬 최고점수 난이도별.
  - **코인 규칙 확정**: *선택권·보험·비-빌드 유틸만 판매, 무기/속성 레벨은 금지.* 목록 — 리롤(가격↑)/밴시/회복물약(가격↑)/**폭탄 스톡**(슈팅 전통)/컨티뉴 1회(비쌈)/무적시간↑/방어력·최대체력 소폭↑/이동속도↑/코인·XP 수집 반경↑/**보스 타이머 +15초**.
  - **희소성**: 엘리트·보스 위주 드랍 + 가격 상승 + ★ **남은 코인 = 점수 환산**(쓰면 점수를 잃음 → 모든 구매가 기회비용, "아껴서 점수 vs 써서 생존" 긴장).
  - → [DESIGN.md](DESIGN.md) **v0.4**.
- **미결**: 키(1234 vs QWER), 제목.

### 2026-07-13 — 상점 항목 다듬기 + 적 공격 거동/상태이상 (DESIGN v0.5)

- **나**: 밴시는 너무 강력 → 별로. 폭탄은 화면 광역인데 **보스엔 일부만** 먹혀야. 컨티뉴는 좋되 **남용 방지 설계** 필요(런당 1회 or 지수 가격). **무적시간 ↑는 애매** → 차라리 피격 시 **투명 상태** 패시브가 낫지 않나. **보스 타이머 +15초는 언제 쓸지 애매.** 남은 코인 점수 환산은 좋음. 그리고 **적 공격은 무속성이어도 거동은 다양**할 수 있음(유도탄·레이저·상태이상[둔화·1초 멈춤] 등) → 아이템 느낌으로도 가능.
- **결정**:
  - **밴시 폐기**(운을 과하게 제거 → 로그라이트 긴장 소멸).
  - **폭탄 설계**: 잡몹엔 광역 즉사급 + **적 탄 소거**(정체성 = *살아남는 카드*), **보스는 최대체력 3~5%만 + 상한** → 폭탄 스팸으로 보스 녹이기 차단, 3분 타이머 긴장 유지.
  - **컨티뉴**: **런당 1회 하드캡** + 비쌈 + 퍼펙트/무피격 보너스 상실. (코인이 점수로 환산되므로 이미 이중 비용.)
  - **‘무적시간 ↑’ 폐기** → **실드(1회 피격 무효)**로 대체(명확·가시적). 사용자의 **‘피격 후 투명’**은 별개 메커닉(데미지 무효가 아니라 *적 조준·유도 타겟 제외*)으로 패시브 후보 등록.
  - **‘보스 타이머 +15초’** → **소비형 ‘시간 연장 토큰’**으로 전환(보스전 중 직접 발동) → "언제 쓸지 애매" 해소, 진짜 결정 순간 생성.
  - **적 공격 거동 다양화 채택**(무속성 유지): 직선/확산/**유도**/**레이저(차지)**/설치 장판/**상태이상 탄**. 플레이어 탄 엔진 재사용 → 저비용. 상태이상은 **둔화 기본**, **스턴은 큰 텔레그래프 + 드물게/Hard 이상**(관대함 기둥 보존). 대응책으로 코인 유틸 **상태이상 저항/해제** 추가.
  - → [DESIGN.md](DESIGN.md) **v0.5**.
- **git**: 인증 해결됨(SSH) → 밀린 커밋 푸시 완료.

### 2026-07-15~16 — 기획 문서 완성 작전 (라운드 1~4) + 전사 착수

- **나**: 문서에 구멍이 많다. **기획을 거의 완벽히 확정 → 개발 → 버그 검증 → 밸런싱은 수치만** 조절하고 싶다. 문서가 길어져도 좋으니 **충돌·모순이 없는 게 정말 중요**하다. (코드는 2주면 짜고 QA로 밸런싱 가능.)
- **라운드 1 — 실패**: 갭 215건을 찾아 6개 섹션을 **병렬 작성** → 서로 모순. 각자 핵심 상수·스키마·검증 게이트를 독립적으로 발명(방어력 모델 3종, 텔레그래프 하한 3종이 전부 하드 게이트, 파일 매니페스트 6벌…). **원인 = 공유 정본 없는 병렬화(오케스트레이션 실수).** → `design/_drafts/`로 격리.
- **라운드 2 — 정본 도입**: 단일 권위가 모순 54건을 전수 해결한 **CANON v1.0**(2,387행) 작성 + 섹션 4종 재작성. 섹션이 정본 결함 57건을 역으로 발견. 판정: 불가(정본이 요청을 심사 안 함 + 병렬이 경계면 충돌 3건 생성).
- **라운드 3 — CANON v1.1**(3,532행): 35건 전부 닫음. **핵심 발견 2가지** — ① "DPS 520 vs 708"은 **단위 충돌**이었다(실효 vs 명목; 708×가동률 0.60 = 425) → `uptimeRef`를 정본이 소유해 재발 불가 ② **보스 HP는 코어가 아니라 armor에 있어야** 한다(코어는 R1상 항상 노말 = 속성 무관 → 코어에 HP를 몰면 속성 레버리지가 죽음). 배분을 뒤집자(코어 63%→7%) **사용자의 스테이지 곡선이 산술로 재현**됨. 총 HP는 1%만 변동 = 숫자가 아니라 **축**을 찾은 것.
- **라운드 4 — CANON v1.2**(4,115행): **게이트 12계열 전부 통과**(검증자가 독립 재계산, 소수점 셋째 자리까지 일치). `bossTimeoutRate` 0.0009(확정 실패)→0.2389 · `runClearRate` 0.4477(밴드 한복판) · **무투자 곡선 0.909/0.909/0.501/0.501/0.151/0.149 = 사용자 확정값** · **균형 격파 127.0초 = 2.12분**(사용자 요구가 게이트가 됨). 판정: 여전히 불가.
- **★ 진단 — 산문 리뷰는 수렴하지 않는다**: 라운드 2·3·4가 매번 "한 라운드 남았다"고 했고 매번 새 구석에서 **같은 클래스**가 나왔다. 남은 결함이 **95% 문서 사무**(키가 어느 JSON 블록에 인쇄됐나 / 대장 절의 폐기값 / 같은 스코프 블록 2개)이기 때문. 3라운드·약 7시간으로 증명됨.
- **결정 (사용자 채택)**: **정본을 `data/*.json` 9종 + `tools/check.mjs`로 전사.** 근거 — JSON을 실제로 쓰면 **키의 거처가 구조적으로 하나**가 되고, 애매한 곳은 **전사가 불가능해 자동으로 드러난다**. 검증기가 남은 결함을 몇 초에 찾는다. **설계 결정은 이미 전부 끝났으므로**(게이트 통과) 전사는 "개발 중 설계"가 아니라 *완성된 설계를 기계가 읽는 형태로 옮기는 것* → 사용자 원칙과 정합. 전사자에게 **발명 금지(값 없으면 `__AMBIGUOUS__` + 신고)** 강제.
- **사용자 확정 3건**(라운드 3~4에 반영): DPS 기준 **708** · 테마 고지 **"물 ~70%"**(저작 비율 기준) · **무투자 난이도 = 스테이지 곡선**(초반 가능 → 후반 필수, 단일 값보다 나은 설계).
- **환경 메모**: `node` 미설치 — 검증기 실행과 AI 콘텐츠 파이프라인(생성·헤드리스 시뮬)에 필요하므로 설치 필요.

### 2026-07-16 — 전사 → 이행 완료: `__AMBIGUOUS__` 0 · dangling 0 · S1~S40 38/38 통과

- **전사(정본 → `data/*.json` 9종)가 산문 리뷰 3라운드가 못 잡은 결함 33건을 찾았다.** 감사자 정리: *"산문 리뷰는 「답이 있는가」를 검증하고, 전사는 「그 답을 정확히 한 번 받아적을 수 있는가」를 검증한다."*
  - `bullets[].speed` ↔ `emitters[].speed`가 **이미 갈라져 있었다**(같은 탄이 150/140/120으로 발사) — 둘 다 유효 키라 로더·문법검사 전부 통과. **값을 나란히 써봐야만 보인다.**
  - `player.hpSegment`(20) ↔ `hud.hpBarSegCount`(5)가 hpMax 100에서 **우연히 일치**, 상점 구매로 140이 되면 갈라짐 → **테스트로도 안 잡힌다.**
  - **S20 위반 9건** — `stages.waves` × `enemies.moveId` **조인해야만** 보임.
  - `difficultyMul`은 **점이 없어서** 점표기 키만 추출한 감사 2번을 **둘 다 통과**했다.
- **정본 v1.3 → v1.4**: 전사 감사 59건 전수 반영. ★**C-7의 근본 결함**을 닫음 — 「§9의 블록은 전부 예시」와 「→ 인쇄(§9.4)가 확정값에 거처를 준다」가 동시에 참일 수 없었다(문면대로면 팔레트 30색·fairness 14값·certify 전 게이트가 무효 → 9파일 전사 자체가 불성립). 문면을 뒤집고 예시 블록을 폐쇄 목록으로 관리.
- **이행**: 보스 부위 이미터 66개 저작 → **스턴이 처음으로 게임에 존재**(5개 절에 걸쳐 설계됐는데 참조 이미터가 0개였다). 중간보스 8칸 · `passives[].desc` 12행 · `hitCooldownSec` 3패밀리.
- **check.mjs (3,121행)**: v1.2 → v1.4. ⚠️ 발견 — `themes`→`stages` 개명 후 **참조 45곳이 undefined → `|| []` → 0행 순회 → 공허 통과**. *"콘텐츠를 인증하는 문이 아무것도 안 보고 초록불을 냈다."* → 0행을 **위반**으로 올리고 30게이트에 공허통과 감시(VACUOUS_WATCH) 부착.
- **★ 검증기가 정본의 결함을 잡은 첫 사례**: `S34`의 문면이 같은 절 §9.5의 확정과 충돌해 **12행 전부를 실패**시켰다. **데이터도 검증기도 옳았고 정본의 문장 하나가 틀렸다** → v1.4에서 두 검사로 분리 + 「표의 `+`는 거처 구분자」를 읽는 법 4번째 규칙으로 신설. `canonDefects()`가 설계 목적 그대로 작동했고 이제 비었다.
- **최종 실측**: `__AMBIGUOUS__` **0** · dangling **0** · S1~S40 **38/38 통과**(S1·S11은 `src/` 부재로 정직한 SKIP — 개발 시작 시점에 평가됨) · 정본 인쇄 블록 ↔ 데이터 **leaf 592개 불일치 0** · S8이 정본 인쇄 표를 **31셀 전부 0.0000%p**로 재현.
- **남은 관문**: `node` 설치 → `node tools/check.mjs` **실행**. 지금까지는 *AI가 "통과"라고 말한 것*이지 **기계가 통과시킨 게 아니다** — 그 차이는 check.mjs가 45개 참조를 undefined로 만들고도 초록불을 냈던 것으로 이미 증명됐다.

### 2026-07-16 — ★ 기계 검증 통과 (node) + 194X 충실도 점검 + 문서 정리

- **나**: node 설치해뒀다.
- **Claude**: `node tools/check.mjs` 최초 실행 → **`VIOLATION 0 · CANON 0 · AMBIGUOUS 0 · STUB 20 · SKIP 2` · exit 0.** ★ **AI가 아니라 기계가 판정했다.** 검사한 행 수가 출력에 인쇄된다(S20:169 · S39:156 · S26:162 · REF:564 …) → **공허 통과가 아님이 증명된다.**
  - **STUB 20** = 시뮬(`tools/sim.mjs`)이 있어야 재는 동적 게이트(클리어율·보스 타임아웃·지배도·코인 희소성 등). 미구현이라 TODO 출력. **SKIP 2** = `src/` 부재로 평가 대상이 없는 검사(S1 core 순수성 · S11 RNG 스트림) — 개발 시작 시 살아난다.
- **나**: 게임의 토대는 194X + 로그라이크 + 뱀서 RPG + 상성으로 이해했다. **194X의 적·공격 패턴이 충분히 고려됐나?**
- **Claude (데이터 실사로 답)**: 194X DNA는 **말이 아니라 데이터에** 있다 —
  - **편대 6종**: `vWedge`(V자 편대) · `lineH` · `columnV` · `arc` · `pincer`(협공) · `scatter`
  - **이동 8종**: `dive`(급강하) · `weave` · `column` · `strafe` · `anchor`(포탑) · `orbitDrift` · `rearIn`(후방 진입) · `charge`
  - **적 4밴드**: chaff · line · turret · bruiser (슈팅 정석 분류)
  - **공격 패턴 84개**: 직선 20 · 부채꼴 11 · 조준 11 · 링 10 · 나선 7 · 레이저 5 · 장판 9 · 벽 11
  - 부위 파괴 보스 · 중간보스 · 새떼 · **폭탄(광역 + 탄 소거)**
  - ★ 가장 194X다운 규칙: **`enemyExitForfeitsReward = true`** — 화면 밖으로 나간 적의 XP·코인은 영구 소멸 → *"잡으려면 위로 올라가 요격. 상단 = 반응 여유 최소 = 위치 리스크"* = 194X의 "저 편대 쫓아갈까"를 **트위치가 아니라 '얼마나 앞에 설 것인가'의 각오**로 번역.
  - **빠진 것 (정직하게)**: ① **파워업 드랍(POW)** — 데이터 검색 0건. 뱀서식 레벨업 드래프트로 **의도적 대체**했으나 "편대를 통째로 잡으면 보상"이라는 훅 자체가 사라짐 ② **지상 타겟**(1943) — 복잡도상 제외가 타당 ③ **루프 회피**(1942) — **폭탄**이 그 역할(제한된 위기 탈출)을 대신함.
  - Claude 제안: **편대 전멸 보너스**(#1 복원)는 `formationId`가 이미 데이터에 있어 저비용이고 "선택이 만든 결과"와 정합.
- **나 (결정)**: **지금으로 충분하다. 그대로 간다.** (편대 전멸 보너스 미채택.)
- **나**: 문서를 언제든 다시 확인할 수 있게 정리해 달라. log에도 대화가 잘 저장됐나?
- **Claude (정리)**: 점검 중 **좀비 문서 1건 발견** — ★ **`DESIGN.md`가 CANON 이전의 v0.5 스펙인 채로 살아서 정본과 다른 답을 말하고 있었다**(패시브 "5~6칸" vs 확정 6 / 속성 키 "`1234` 또는 `QWER`" vs 확정 QWER). **우리가 수일간 없애온 바로 그 병(한 항목에 답 두 개)의 재발.** → `DESIGN.md`를 **스펙에서 "입구/안내판"으로 전환**(숫자를 적지 않는다, 정본으로 보낸다) + **[`design/README.md`](design/README.md) 지도 신설**(읽는 순서 · 검증법 · 이력 · 폴더 규칙).

### 2026-07-16 — ★ 1주차 프로토타입: 실제로 플레이된다 (src/ 착수)

- **나**: 시작하자.
- **Claude**: 정본 §9.1 코드 규약대로 `src/` 착수 → **브라우저에서 실제로 플레이됨**(스크린샷 확인). 이동(방향키) + **스탠스 전환(QWER)** + 상성표 하이라이트 + 무기가 스탠스 색으로 물듦(불 스탠스 = 주황 기체) + 자석 반경 + HP/XP/슬롯 HUD.
- **구조** (`src/` 16파일 4,382행, 정본 강제 구조 준수):
  - `core/` 순수 — rng(sfc32·8스트림) · schema(9파일 로더) · state(엔티티 풀, 프레임당 alloc 0) · elements · **stance(슬롯순 부여)** · damage(§3.1) · step(고정 dt, speed 모름) · draft · weapons/{forward,fan,seeker}
  - `render/` draw·hud · `main.js`(tickDur=배속 유일 거처) · index.html
- **★ 검증**: `node tools/check.mjs` → **exit 0** (VIOLATION 0 · S1·S11 발화 후에도). **SKIP 2→1**(S1 core 순수성이 최초 발화·통과). 16모듈 문법 파싱 OK. core 금지 식별자 0(주석 1건은 검증기가 정확히 무시). **결정성 확인**(동일 시드 → 비트 동일, 다른 시드 → 상이). 핫패스 alloc 0.
- **★★ "개발 중 설계 결정 0" 원칙이 시험대에 올랐고 — 대체로 참이었다.** 코드를 쓰다 발견한 **정본 결함/공백을 지어내지 않고 전부 보고**(코드 주석 `보고 대상`):
  - **정본 자기모순 2건**: ① §10.2 `weighted(map)` ↔ §10.3 「Map 순회 금지」 ② §9.6.1의 `base` vs `src` 표기 — 문면대로면 무기 레벨업(`levels[].count/pierce`)이 조용히 증발.
  - **★ D1(구조적) — 도→라디안이 `weapons/**` 안에서 표현 불가**: §9.1이 리터럴을 `{0,1,-1,0.5,2}`로 닫았는데 §9.5는 각도를 **도**로 인쇄 → 변환 상수 `π/180`의 **180이 금지 리터럴**. 정본 5,238행에 "라디안" 0회. → `src/core/angle.js`(리터럴 제약은 `weapons/**`만 → 수학상수는 밖에) 신설로 우회, 정본에 예외 명문화 필요.
  - **D2 탄 소멸 훅 없음** · **D3 killEnemy 미export**(무기 6종이 적을 못 죽임) · 값 공백 6건(시작 좌표·수집반경 등 = 최소 선택 + 명시).
  - **check.mjs 오탐 1건**: import 경계 검사가 이미터 키 `from`을 import로 오인 → 정본 키라 개명 불가, schema.mjs에서 우회.
- **결정**: 손맛 검증(스탠스×상성)이 목표였고 **구조는 통과·플레이 확인**. 위 정본 결함들은 **다음 정본 소폭 개정**에서 닫는다(개발 계속 진행 가능 — 전부 우회+보고돼 있음).

### 2026-07-16 — 1주차 코드 정밀 청소 (2주차 전에 결함 전멸)

- **나**: 1번(코드 결함 해결) 먼저 하자. 지금 파악 가능한 코드 에러를 최대한 다 잡고 넘어가야 나중에 덜 힘들다.
- **Claude**: 4렌즈 감사(정확성·정본정합·결정성·죽은코드) → **findings 24건** → 단일 권위가 정본 편집 + 코드 워크리스트 → 3그룹 분할 수정 → 실행 검증.
- **감사가 신고 24건 외에 SILENT 버그를 잡았다** (2주차에 터질 것들):
  - ★ **관통탄 hitGen 누락** — 관통탄이 풀 슬롯 재사용된 적(release→새 적 alloc, gen++)을 조용히 통과. seeker의 targetGen 방어와 대칭인데 히트추적만 gen을 빠뜨림 → **2주차 스포너 붙는 순간 발현**. `b.hitGen[]` 추가로 수정.
  - **forward.js `world.player.hit` 죽은 코드** — step()이 매 틱 맨 처음 지우고 collide()가 나중에 세팅 → 무기가 보는 값은 항상 false. i-frame 부여 엣지로 교체.
- **정본 편집 14건**(단일 권위): D1(§9.1에 「도→라디안 π/180은 밸런스 값이 아니라 수학상수 → weapons/** 밖 angle.js」 명문화) · §9.6.1 base/src(레벨 오버라이드 증발 결함 문면 확정) · §10.2/§10.3(weighted=배열, Map 금지 자기모순 해소) · 값 공백(시작좌표·픽업반경·XP반올림·드래프트 동시보장 순서·healPickupPct) 거처 부여.
- **코드 수정 19건**: **D2 탄소멸훅**(step.releasePlayerBullet 단일 관문 + fan.onExpire, LIFO 경합 구조적 불가) · **D3 killEnemy export**(멱등 !alive 가드, 2주차 6패밀리 공통 필수) · elements.js를 상성/investable 단일소스로(damage.js·stance.js 중복 제거, drift hazard 차단) · family-키 규칙(슬롯 재정렬 안전).
- **인터페이스 계약**으로 core↔무기 경계(killEnemy·onExpire)를 양쪽 같게 명세 → 경계면 충돌 0.
- **검증(실행)**: `node tools/check.mjs` → **exit 0** · 16모듈 문법 OK · **결정성 실증**(1800틱 롤링해시 `d3da0302` 두 번 동일, 다른 시드 상이, 8스트림 독립) · **브라우저 회귀 없음**(물 스탠스 = 파란 기체 확인).
- **남은 것**: 실질 미결 minor 2건(닫힌 결함의 사문 주석 1 · 정본 대장 텍스트 위생). '보고 대상' 10→**10**(9건은 정당한 개방 리포트 = §9.10이 인쇄 안 한 렌더 계층 정본 공백 — 플레이어 기체 도형/색 등, 2주차 렌더 작업 시 닫힘). wontfix 6건은 전부 1주차 소비처 0 또는 코드 이미 정합.

### 2026-07-16 — 모듈 단위 테스트 (순수 core 완전 커버, 뮤테이션 증명)

- **나**: 모듈 하나하나 꼼꼼히 검수하고 넘어가야 규모 있는 서비스를 안정 배포할 수 있다. 버그를 최대한 찾자.
- **Claude**: core가 **순수**(window/Date/Math.random 없음)라 node에서 함수 단위 검증 가능 — 그 이점을 최대로. `tools/test.mjs`(의존성 0 러너, 단언 0개 = 자동 FAIL로 vacuous 방지) + `tests/*.test.mjs`.
- **224개 테스트** (11파일): rng·elements·angle·damage·stance·state·step·weapons·draft·schema·invariants(퍼즈). 순수 core 전 export 커버 + step.js 효과 로직 8종(shield·heal·slow·stun·reactive·afterimage·enemyExitForfeit·statusResist) 회귀 가드 + applyHit 격리.
- **★ "테스트가 통과한다"가 아니라 "테스트가 버그를 잡는다"를 증명** — 뮤테이션 새너티: 코드에 일부러 버그를 심으면 관련 테스트가 실제 FAIL(직접 확인: elementMul 반전 → 14 FAIL / `p.shields>0` 반전 → shield 테스트 FAIL / 되돌리면 green). 원칙: **버그를 잡으면 코드를 고친다, 테스트를 약화하지 않는다.**
- **회귀 6종 고정**: 관통 hitGen · i-frame 초당1회 · killEnemy 멱등 · fan.onExpire 탄당1회 · 무기 레벨업 증발금지 · 스탠스 슬롯순 → **2주차에 적·보스 얹다 core를 깨면 즉시 빨간불.**
- **불변식 퍼즈**: 4시드×3600틱 랜덤입력 → HP∈[0,hpMax]·엔티티≤캡·NaN 0·풀 무결·힙증가 0·재현성.
- **새 프로덕션 버그 0** — 직전 코드 감사(24건)로 이미 청소된 결과(뮤턴트가 테스트 강도 증명). `node tools/test.mjs` exit 0(224/224) · `node tools/check.mjs` exit 0.
- **남은 리스크(정직)**: 라인커버리지 도구(c8) 미계측 · 동적 밸런스 인증(dominance·dpsProbe 등 STUB 20)은 2주차 `tools/sim.mjs` 필요.

### 2026-07-16 — 2주차 착수: 적 스포너 수직 슬라이스 (핵심 재미 검증용)

- **나**: 지금 상황에서 가장 맞는 판단을 내려서 진행해 달라.
- **Claude 판단**: 1주차 목표였던 *"상성 맞춰 녹이는 손맛"*이 **적이 없어서 아직 미검증** = 이 게임 최대의 미검증 가정. 콘텐츠 대량생산 전에 **얇은 수직 슬라이스로 재미부터 확인**하는 게 맞다.
- **구현**: `src/core/enemies.js` 신설(순수 훅 `enemies(world,dt)`) — sea stage-1 웨이브(unlockStageMin≤1)를 순환 스폰. `world.rng.spawn`만 사용(결정성). 이동 `dive`(직하강)+`weave`(사인). 배치 formation 파라미터. **element가 water/fire/grass/normal 섞여** 내려옴(스탠스 바꿀 이유 발생). `main.js`에 훅 주입. 나머지(충돌·상성데미지·killEnemy·XP·레벨업·드래프트·렌더)는 이미 배선돼 있어 이 한 조각으로 루프 완성.
- **테스트 +9 (총 233)**: 결정성·element편성·dive/weave 속도·캡준수·화면이탈몰수·처치→XP. `node tools/test.mjs` exit 0(vacuous 0), `node tools/check.mjs` exit 0(core 순수성 유지).
- **headless 실증**: 틱0부터 스폰, 최대 20 동시, 4속성 전부 등장. ★상성 손맛: **불 적(hp6)을 물 스탠스=1발·노말/불=2발**(맞추면 빨리 죽음이 숫자로 확인).
- **★ 브라우저 손맛 검증은 못 함(환경 제약)**: 자동화 브라우저가 OS 포커스를 못 잡아 `blur→자동 일시정지`(알트탭 방지, 올바른 동작)에 걸림 → 내가 직접 손으로 플레이해 '재미'를 느끼는 건 불가. **재미 판단은 사용자가 직접 플레이해서 내려야 함**(주관적 + 본인 게임). 부팅·렌더·시뮬은 전부 정상 확인.
- **발견 버그 없음** · 정본 공백 없음(기존 §9.1 주입 구조 위에 얹음).

### 2026-07-16 — 플레이 피드백 3건 반영 (적 공격·드래프트 문구·HP밸런스)

- **나 (실제 플레이 후)**: (1) 레벨업 선택지가 안 와닿는다 — "W/E/R 투자"·"학습 회로" 같은 표현이 직관적이지 않음. (2) 적이 나에게 공격을 안 해서 긴박감이 없다. (3) 밸런스 — 느린 적은 체력이 높아야(버티게), 빠른 적은 낮아야 하는데 안 느껴짐.
- **진단**: 셋 다 슬라이스라서 의도적으로 뺐던 것이 티가 남(정확한 피드백).
- **① 드래프트 문구 = 결과 중심**: 카드가 키 문자/테마 이름 대신 **효과**를 플레이어 언어로. 속성 = "불 강화 — 무기 1개가 불속성 (풀 적에게 ×2)" (색·글리프, W/E/R 노출 제거) / 무기 = "벌컨 Lv.2 — 발사 강화" / 패시브 = 이름 + **효과 한 줄 크게**. `draft.js`(prey를 elements.matrix에서 유도, 순수 유지) + `hud.js`(sub 효과줄 크게 렌더). *(부수 교정: 카드가 상성표대로 "불→풀 ×2"를 정확히 표기 — 내가 예로 든 "불 적에게 2배"는 부정확했고 구현이 데이터대로 맞음.)*
- **② 적 공격 = 긴박감**: `src/core/emitters.js` 신설(순수 훅) — 적 `attack.emitterId`대로 `firstDelaySec+텔레그래프` 후 케이던스 발사, `spawnEnemyBullet`. straight/fan/aimed/ring/spiral/wall 구현(laser/zone은 빔·장판 엔티티 필요 → 슬라이스 밖). **난수 0 = 결정적**(스폰만 rng.spawn). §12.4 공정성(속도 상한·텔레그래프)은 데이터가 보장. `main.js`에 `emitters` 주입.
- **③ HP↔속도 튜닝**(숫자만): 느림=탱키/빠름=약골 단조 성립 — hexer(spd45,hp14)>drifter(60,9)>spitter(95,6)>swarmLancer(120,3)>swarmChaff(150,2). 슬라이스가 5종으로 다양화(전 2종).
- **검증(headless 실측)**: 적 탄 동시 최대 10, 플레이어 HP 100→42(90초, 진짜 피해). 5종 전부 스폰. 조준탄(hexer)이 플레이어 방향. **결정성 비트 동일**(같은 시드). 테스트 **+20 (총 244)** — emitters 8 + enemies/hp 등. `node tools/test.mjs` exit 0(vacuous 0) · `node tools/check.mjs` exit 0.
- **남은 것(minor·비차단)**: minSpawnRadiusPx 근접 공정성은 런타임 assert(STUB, 2주차 sim) · 정본 공백(속성 표시명 거처, elements.json 스키마 밖 → render 리터럴 임시).

### 2026-07-16 — 상성 히트 피드백 §7.7 구현 (속성이 읽히게)

- **나 (재플레이 후)**: 큰 틀은 OK. 근데 (a) 상성을 바꿔야 할 **필요성**이 안 느껴지고 (b) **어느 적에 어느 속성**인지 감이 안 잡힌다. 속성 부분만 해결하면 될 듯.
- **진단**: 상성 데미지 계산·첫 드래프트 속성 보장·자동장착은 다 배선됐는데, **×2로 때렸는지 ×0.5로 튕겼는지 화면 피드백이 전무**(draw.js에 없음). 정본 §7.7이 규정한 "3중 감각"이 미구현 → 포켓몬의 "효과가 굉장했다!"가 없어 상성이 안 읽힘. **숫자가 아니라 피드백 문제**(밸런스 아님).
- **구현 (§7.7대로, render+core-fx)**:
  - `elements.js` `hitTier(matrix,att,def)` — ×2→super/×1→neutral/×0.5→resist (배율 단일소스 elementMul, resonance 무관).
  - collide(step.js)가 데미지에 쓴 그 stamp·matrix로 tier 뽑아 `world.hitFx`(사전할당 링, 결정적·장식전용)에 발행. 매 스텝 리셋.
  - draw.js 3중 감각: **×2** = 속성색 화이트핫 폭발+스파크3+적 본체 팝(처치 시 대형 파열+흰링) / **×0.5** = 회색 방패 호(neutralGray, 튕김) / **×1** = 백색 플래시+스파크1. 레이어 7(적탄 아래, I-4).
  - main.js **WebAudio SFX**(절차합성 0KB): ×2 크런치 / ×1 틱 / ×0.5 금속 틴. 인프라 부재 시 조용히 시각만.
- **이게 두 문제를 동시에 해결**: 잘못된 스탠스로 쏘면 튕기고(×0.5 호), 맞추면 녹는다(×2 폭발) → **표 없이 손으로 배우고**, 맞추는 맛이 나 **바꿀 이유**가 생김.
- **검증**: `hitTier` 16셀 전수 정확. tier가 collide→hitFx→render까지 전달(실측). **결정성 비트 동일**. 테스트 **+11 (총 255)**, 뮤테이션 증명(hitTier 변조 시 7건 FAIL). `test.mjs`·`check.mjs` exit 0. 새 데이터 키 0(기존 visual.hitFx 사용).
- **참고(상성 방향, 정본 매트릭스)**: 불 적(주황)→**물 스탠스 E** · 물 적(청록)→**풀 스탠스 R** · 풀 적(초록)→**불 스탠스 W** · 노말(흰)→아무거나. (니모닉: 적 속성 키의 오른쪽 이웃.)
- **밸런스(×2가 얼마나 강한지 등)는 그대로 시뮬 몫** — 이번은 순수 가독성/피드백.

### 2026-07-16 — 역상성(×0.5) 피드백 강화 + 데미지 계산 검증

- **나 (재플레이)**: 상성(×2)은 잘 보이는데 **역상성(×0.5)은 인식이 어렵다.** 데미지는 맞게 계산되는 거지?
- **데미지 검증(headless 실측)**: 불 적(hp60)을 스탠스별로 — 물(×2) **8발/4.48초** · 노말(×1) 15발/8.10초 · 풀(×0.5) **30발/15.83초**. 정확히 2배/1배/0.5배 = **계산 완벽**. 문제는 순수 피드백(역상성이 안 보임 → "왜 안 죽지"로만 느껴짐).
- **역상성 피드백 강화(render+data+정본, 밸런스 무관)**: 회색·무발광·파티클0 톤은 유지하되 3채널 굵힘 —
  - 방패 호를 「판」으로: `resistArcSweepDeg` 120→**160°**, `resistArcStrokePx` 2→**4px** + 겹판·프레임 틱(반경 12→15).
  - ★ **적 본체 회색 차폐 플래시**(신규) — super의 색 팝에 대응하는 무채색 본체 반응(resistT/resistGen per-entity, 수명 공유 = 신규 키 0) → 시선 있는 자리에서 "맞았지만 안 통함"이 읽힘.
  - resist SFX 트랜지언트 추가(여전히 "안 통함" 톤).
  - super와 색·질감으로 명확히 구분(색폭발+발광 vs 무채색+무발광).
  - 정본 §9.4.3·§7.7 인쇄값 동기화(C-8), 신규 키 0(schema/check 불변).
- **의도적으로 안 함**: 리코셰 스파크(정본 §7.7 resist=파티클0 확정) · 적 상시 표식(HUD 상성표가 지속 채널 담당, 클러터 회피).
- **검증**: `test.mjs` 255 exit 0(변경 없음=약화 없음) · `check.mjs` exit 0 · 결정성/tier 발행 불변(render만 변경). data↔정본 동기 확인(160/4).

### 2026-07-16 — 패시브 슬롯 HUD 표시 + 일시정지/상점 확인

- **나 (플레이)**: 역상성 이제 잘 보임 OK. 먹을 수 있는 **패시브도 6개 한정**인데 화면에 보여줬으면. 일시정지·상점 버튼은 나중?
- **#1 패시브 슬롯 HUD 추가**: 우패널 무기 슬롯 아래에 **패시브 6칸**(§4.2 상한 6)을 무기 슬롯과 같은 어휘로 렌더(hud.js). 빈 슬롯/이름/Lv 표시. 브라우저 확인 완료. render만 변경 → test/check exit 0 불변.
- **#2 확인**: 일시정지 = **이미 Escape 키로 작동**(blur 자동정지도). **클릭 버튼은 없음** — 정본이 "데스크톱 키보드 전용·마우스 미사용(확정)"이라 버튼 UI 자체가 설계에 없음. 상점 = **2주차**(스테이지 사이 상점, 스테이지 붙일 때 구현). 정본에 상점 설계는 완비.
  - ⚠️ **정정**: 위 "이미 작동"은 오판이었음 — 실제로 Escape가 PLAY 중엔 동작 안 했다(아래 2026-07-18 참조).

---

### 2026-07-18 — Escape 일시정지 버그 수정 (PLAY→PAUSE가 안 걸렸음)

- **나 (플레이)**: esc로 일시정지가 안 되는 것 같은데? 나머지는 다 만들어지고 확인.
- **근본 원인**: `edge.pressed(pause)`가 `else` 분기(PAUSE 상태)에서만 호출돼서, **PLAY 중 Escape는 검사조차 안 됨** → 키로는 못 멈추고 blur 자동정지만 걸렸다(그래서 "작동하는 듯" 오판).
- **수정 (main.js, §5.7)**: frame() 루프에서 상태 분기 **이전에** `edge.pressed(pause)`를 **매 프레임 1회 소비**하고, `PLAY↔PAUSE`를 토글. DRAFT/OVER 에선 무시(§5.2 드래프트 스킵 불가). 상승 엣지라 키를 눌러도 1회만 토글, `acc=0`으로 재개 시 스파이럴 방지. PAUSE 전용 재개 체크는 제거(토글이 양방향 처리).
- **검증**: `node --check` OK · `test.mjs` 255 통과 · `check.mjs` exit 0(순수성 불변 — main.js는 비순수 부트). 브라우저에서 Escape→"일시정지" 배너 표시, 다시 Escape→재개 **양방향 실증**.
- render/logic만 변경, 신규 키·데이터 0.

---

### 2026-07-18 — 2주차 착수: gap-map 이해 워크플로 + 무기 A1(omni·boomerang) + 적대적 검증

- **나 (플레이)**: 상성 잘 반영됐음 → 다음으로 진행.
- **2주차 gap-map (멀티에이전트 이해)**: 6개 서브시스템(무기·보스·플로우·상점/점수·시뮬·엔진 seams)을 병렬로 "정본 규정 vs 현재 구현" 계약으로 정밀 매핑. 결론: **콘텐츠 데이터는 이미 다 있고(무기 12·패시브 12·적 17·보스 10·스테이지 7), 엔진이 그걸 아직 소비 못 함** — 재설계 0, 순수 구현. 의존성 순서 확정: **A 무기 9종 → B 보스+스테이지 → C 메타(상점/점수/컨티뉴) → D AI 밸런싱(bot·sim)**.
- **A1 무기 2종 구현** (신규 인프라 0):
  - `omni.js`(리어가드) — 360° 방사 + **후방 반구 탄에 rearBias** + 진화 링 회전(wrapAngle 유계).
  - `boomerang.js`(리턴) — 나갔다 돌아오는 궤적 + `pierce -1`·`hitCooldownSec`로 "두 번 벤다"(collide 위임, 모듈은 궤적만) + 진화 체인 리턴 + 회수는 `age=lifetime`(releasePlayerBullet 미export 회피).
  - `index.js` 등록 → main.js 레지스트리 교집합 시임 덕에 **드래프트 자동 등장**. lance/orbit/aura/mine/barrage/nova/drone(7종)은 빔 렌더·지속 엔티티 인프라와 함께 A2로.
- **적대적 검증 (4에이전트 2렌즈)** → 실 결함 2건 잡고 고침:
  - omni **rearBias 좌우 비대칭**(minor·결정성) — 짝수 dirCount의 정확 수평 탄이 `vy>0` 부동소수 노이즈로 좌우 다르게 분류·엔진간 취약. → **각도 판정 `|wrapAngle(a)|>π/2`**로 대칭·결정 고정(정본 "아래 성분=rear"에 부합).
  - boomerang **체인 진화 붕괴**(major) — 재획득이 직전 적을 제외 안 해 한 적에 3웨이포인트를 ~3틱 소진 → 간판 진화 무효. → **s2(직전 경유 적) 제외**로 다음 적 진행.
  - 각 결함에 회귀 테스트 추가. 남은 minor(아레나 최상단 근처 투척 미회수)는 "화면 밖 소멸=보편 규칙, 정본 무언"으로 수용.
- **검증**: 신규 단위 테스트 13개 → **268 통과**(255→268) · `check.mjs` **exit 0**(S1 순수성·리터럴 화이트리스트·S11 rng·S34 데이터) · 실제 `step()` 통합 무예외 · 브라우저 부팅 콘솔 0.

---

### 2026-07-18 — 최종 완성 지시 → Phase B 착수: 런 오케스트레이션 키스톤(`stage.js`)

- **나**: "난 너가 만든 최종 게임에 대해서 플레이할거야. 네 계획에 맞게 게임을 완성하면 돼." → **로드맵대로 게임을 끝까지 자율 완성**(중간 슬라이스는 검증·커밋된 채로, 최종본만 플레이). 기억에 남김.
- **아키텍처 결정 (코어/셸 분리)**: §6.5 상태기계에서 **게임클럭이 흐르는 전투 페이즈(MOB/BOSS)는 순수 코어**가 소유(시뮬이 `while(!world.over) step()`으로 같은 런 재현 = "certified=shipped", §10.4). **결정 지점(DRAFT/SHOP)은 드라이버**(main 사람 UI / sim 봇)가 해소, **전환·메뉴 화면은 셸**.
- **`src/core/stage.js` (순수 키스톤, 가산적 — 기존 게임 무변경)**:
  - `initRun`/`drawStageOrder` — §8.1 themeDraw: 6테마 중 5 비복원 추첨(rng.theme, Fisher-Yates) + finale = 6포지션. stage-1 introOk 강제. **구조 증명 200시드 전수**: 물·불·풀 각 ≥1 · stage1 introOk · 중복 0.
  - `tickRun` — 페이즈 기계 MOB→(crisis 서브구간)→BOSS_INTRO→BOSS→STAGE_CLEAR. bossTimer 180 만료=즉사(deathCause). finale 격파=승리(엔드리스 없음).
  - `advanceStage`·`applyStageClearHeal`·`stageEntry`·`isFinale`.
- **검증**: 신규 12 테스트 → **280 통과**(268→280) · `check.mjs` exit 0 · rng.theme 독립 스트림 증명(spawn 불변).
- 다음: 라이브 배선(step 훅 + enemies.js 런구동 리팩터 + main 런루프) + `boss.js`(복합보스 스폰·격파).

---

### 2026-07-18 — B1b: 보스 + 라이브 배선 → 헤드리스 한 판 완주(mob→boss→다음→…→승리)

- **나**: A~D 계획대로 쭉, 각 스텝 디버깅하며 에러 없이.
- **파일별 구현 + 스텝마다 게이트 검증**(각 단계 test+check 초록 확인):
  - `state.js` — makeEnemy 보스 필드(isBoss/partType/anchor…) + hooks{run,boss} + `spawnBossCore`/`spawnBossPart`(필드 전량 리셋).
  - `step.js` — run/boss 훅 배선(스폰 게이트·이동 적분 순서) + `killBossEntity`(코어 격파=coin+cleared+전 개체 반납 / armor 파괴=게이트 1단 해제) + moveBullets 보스 이탈 면제.
  - `boss.js` (신설) — `spawnBoss`(파트 먼저=outermostFirst 근사·코어 나중, HP×bossHpScale, finale 절대) + `moveBoss`(sway/holdCenter, 파트가 코어+anchor 추종) + `bossHook`(BOSS 페이즈 1회 스폰 가드) + `clearField`.
  - `enemies.js` — 런구동 리팩터: `stageContext`/`buildSpawner`(스테이지 전환 재빌드) + MOB 페이즈 게이트 + mobPhaseMaxWaves 상한 + applyMovement 보스 가드. 슬라이스 폴백 유지(테스트 무변경).
  - `main.js` — createWorld 훅 배선 + initRun + STAGE_CLEAR 자동전진(회복) + 승리/사망 배너.
- **디버깅 1건**: 프로브가 MOB 빨리감기로 스포너 미생성 → 비-MOB 분기 applyMovement 가 `spawner.archIndex` 참조 크래시. → 스포너 없으면 이동 생략 가드.
- **헤드리스 실증**: (1) 풀런 프로브 = **6 스테이지 진행 → 승리, 예외 0**. (2) 실제 MOB 프로브(빨리감기 없음) = 최대 동시 잡몹 20·14웨이브(상한)·t120 BOSS_INTRO·t123 보스 스폰+필드 정리. (3) 각 보스 파트수 정확(스테이지 2armor+1, finale 3armor+1).
- **검증**: 신규 boss 단위 테스트 10개 → **290 통과**(280→290) · `check.mjs` exit 0 · 브라우저 부팅 콘솔 0.
- **적대적 검증(3렌즈) → 실 결함 5건 수정**(별도 커밋): ① `stageContext` MOB 매 틱 객체 alloc(§10.3 위반, ~43k/런) → ensureSpawner 스칼라 인라인. ② tickRun 타이머 체크가 cleared보다 먼저 → 막판 격파가 타임아웃 패배로 뒤집힘 → cleared를 타이머 위로. ③ partHitPriority 'outermostFirst' 미구현(풀 idx 역전) → collide에서 코어가 파트에 가려지면 파트 흡수. ④ `while(!over)step()` 주석 과장 정정(STAGE_CLEAR=드라이버 소화). ⑤ spawn 헬퍼 mp0/mp1/mp2 미리셋. 회귀 테스트 3개 추가 → **293 통과**, 8/8 시드 승리·예외 0.
- 다음: B2(보스 발사 patternSet·laser/zone/stun·페이즈전환·중간보스·위기) → B3(플로우 화면·HUD 보스타이머).

---

### 2026-07-18 — B2a: 보스가 실제로 쏜다 (patternSet 총알 이미터 라우팅)

- **핵심 통찰**: 보스 파트는 anchor 위치의 개별 엔티티라 `e.x/e.y`=파트 위치 → `from='part'`가 자동 충족. 악절 타이밍(`scheduledVolleys`, repeat/restSec)은 이미 구현됨 → **파트를 patternSet 이미터로 라우팅만** 하면 발사된다.
- **구현**: `state.js` 파트에 `partId` 추가(patternSet 조회) · `emitters.js` `bossById` 조회 + `bossPartEmitterId`(파트→patternSet[phase].emitterIds[0]) + emitters() 훅에 보스 파트 분기(코어 미발사, laser/zone은 B2a-2로 skip).
- **헤드리스 실증**: 6보스 전부 발사(8~15 동시탄), 첫 탄 t≈1.5s(텔레그래프 리드 후), 탄이 y≈183(파트 위치)에서 발생, **결정적**(동일 시드=동일). kiln은 plate=ring 발사(turret/vent=laser/zone 침묵→B2a-2). 풀런 8/8 승리·예외 0.
- **검증**: 신규 boss 발사 테스트 3개 → **296 통과**(293→296) · `check.mjs` exit 0.
- 다음: B2a-2(laser/zone 빔·장판 인프라 = 무기 A2와 공유) → B2b(페이즈 전환·파트파괴 이동페널티) → 중간보스·위기.

---

### 2026-07-18 — B2b: 보스 페이즈 전환 + 파트파괴 이동페널티

- **페이즈 전환(§8.11)**: 코어 HP 임계 [0.6,0.3] 통과 → phase 0/1/2. 전환 = `phaseTransitionSec`(1.5s) 동안 보스 무적(collide) + 타이머 정지(tickRun). 끝나면 전 파트 `e.phase` 각인 → patternSet·이미터 악절이 새 페이즈로 (탄막 층 상승). `boss.js advancePhase` + run 상태(bossPhase/bossTransitionT).
- **파트파괴 이동페널티(§8.12)**: mobility 파트 처치 → `bossMoveSpeedMul=0.5`·`bossMoveAmpMul=0`(스웨이 정지). `killBossEntity` + `moveBoss`. (armament 정지·armor 게이트는 기존.)
- **알려진 경계 오차**: run 훅이 boss 훅보다 파이프라인 앞이라 전환 시작 틱에 타이머가 1틱(0.0167s) 먼저 감소 — 결정적·180s 중 ≤2틱, 무시 가능(비결정성 아님).
- **헤드리스 실증**: 지속 피해로 phase 0→1→2 관측·2회 전환·전환 무적 실증·코어 사망→cleared. 풀런 8/8 승리·예외 0.
- **검증**: 신규 테스트 3개(mobility·전환·재전환없음) → **299 통과**(296→299) · `check.mjs` exit 0.
- 다음: B2a-2(laser/zone 빔·장판 인프라) → 중간보스·위기 → B3(플로우 화면·HUD).

---

### 2026-07-18 — B2a-2: laser/zone 인프라 → **§8.5 이미터 어휘 8종 전부 구현 완료**

- **새 풀 없이 기존 풀 재사용**(§12.1 S2 「풀 10 ⟺ 초과정책 10」 보존): 장판 = `zones` 풀, **활성 빔 = `telegraphs` 풀**(kind='laser', a=진행각·r=widthPx·durSec=activeSec·dmg). makeTelegraph 에 `dmg` 필드만 추가.
- **`state.js`**: `spawnZone`/`spawnBeam`. **`step.js`**: `hazards()` 패스(collide 뒤) — 장판·빔 노화/반납 + 플레이어 피격. 피해는 **적용 1회, i-frame 게이트**(§8.5 「dps 는 없다」). 빔은 **반직선**(뒤쪽·폭 밖 무피격). `fromPlayer` 장판은 플레이어 무해(무기 A2용 예약).
- **`emitters.js`**: `fireZone`/`fireLaser` + `bulletById`(빔 피해는 탄 정의 dmg) + fireVolley 라우팅. 미지 타입은 **소리내어 실패**(폴백 금지). laser 각도 = 화면 표준(0=+x, 90=아래), `trackDuringCharge` 면 발사 시점 플레이어 조준.
- **헤드리스 실증**: kiln(laser turret + zone vent + ring plate) — 장판·빔 생성 확인, **장판 피격 ✓ 빔 피격 ✓**, 풀런 8/8 승리·예외 0.
- **검증**: 신규 `hazards.test.mjs` 8개 → **307 통과**(299→307) · `check.mjs` exit 0.
- 이 인프라는 무기 A2(mine·barrage·nova)가 그대로 재사용한다.

---

### 2026-07-18 — B2c: 위기 세션(새떼) — 투자 페이오프의 장면

- **§8.10 구현**: 잡몹 페이즈 마지막 25초, 정상 웨이브 정지(crisisSuspendsWaves) + 새떼 6파 균등 투입(arc/vWedge 교대, 서브웨이브당 chaff 9 + lancer 1). 속성은 `themePure`(전부 테마 → **정답 스탠스면 ×2로 60기를 갈아버린다**) / `finaleRotating`(1·2 물 → 3·4 불 → 5·6 풀).
- **디버깅 2건**:
  1. 레코드별 반올림이 총량을 부풀림(0.5 배율 30 → **36**). → 소수 캐리로 바꿨더니 이번엔 부동소수 잔차(0.7+0.3=0.9999)가 **랜서를 0으로 절단**. → **아키타입별 누적 반올림**(스테이지 진입 시 `crisisPlan` 1회 확정)으로 총량·9:1 **둘 다** 정확: 30/42/51/60 전부 목표 일치, 90:10 유지.
  2. 테스트가 서브웨이브 경계 정각에 걸려 floor 가 한쪽으로 떨어짐 → 창의 중간점 사용.
- **통합 실증**: 빨리감기 없이 **실시간 잡몹 페이즈 전체 + 위기 + 보스전** 풀런 — seed0 게임시간 739s·위기 150s(6×25)·최대 동시적 27·승리, **4/4 시드 승리·예외 0**(4런 1.5초).
- **검증**: 신규 `crisis.test.mjs` 6개 → **313 통과**(307→313) · `check.mjs` exit 0.

---

### 2026-07-18 — B3a: 상단 띠(스테이지·보스 타이머) + ★ 보스 렌더 크래시 수정

- **상단 띠(§1.2, 1주차엔 안 그리던 것)**: 좌 = `n/6 테마명`(항상), 중앙 = **보스 타이머 §7.12.1 3단**(정상 은색 / ≤60s 경고 1.15배·0.5Hz / ≤30s **자홍** 1.30배·1.0Hz), 하단 = 코어 HP 바 + **살아있는 armor 칸**(소프트게이트 단계가 보인다), 우 = 위기 표식.
- **★ 치명 버그 수정 — 보스가 등장하면 렌더가 매 프레임 터졌다**: `drawEnemies` 가 `archetypeId` 로 아키타입을 찾고 없으면 throw 하는데 **보스 개체는 archetypeId 가 ''** → 첫 보스에서 화면이 깨졌을 것(패널까지 안 그려짐). → **개체가 자기 `shapeId` 를 들고 다니게** 수정(makeEnemy + spawnEnemy/spawnBossCore/spawnBossPart). 덤으로 프레임당 아키타입 스캔 제거(성능).
- **빔 렌더 수정**: `drawTelegraphs` 가 telegraphs 풀을 전부 **원호**로 그려 laser 빔이 작은 원으로 나왔다 → kind='laser' 는 **실선 빔**(검은 외곽선 + 자홍 본체 + 흰 코어, §7.4), 원형 텔레그래프는 기존 점선 유지.
- **검증 방법**: 임시 렌더 프로브(`_probe.html`, 확인 후 삭제)로 보스전 프레임을 직접 렌더 → kiln 코어+파트 3개·수직 레이저 빔·장판·적탄·상단 띠·양 패널 **전부 정상**, `err=none`. (모듈 캐시 때문에 새 포트로 재확인.)
- **검증**: 회귀 테스트 1개(보스·잡몹 shapeId 보유) → **314 통과** · `check.mjs` exit 0.

---

### 2026-07-18 — C1: 상점 (코인이 드디어 쓰인다)

- **`src/core/shop.js`(순수)**: `price` = ceil(basePrice × growth^n), n = **런 누적 구매 수**(`world.purchaseCounts` 신설). `buyBlockedBy` 세 관문(maxPurchases · 재고 상한 · 코인) → UI 가 「왜 못 사는지」를 구분해 표시. `buy` 가 10항목 효과를 **기존 누적 필드**에 적용(core 는 상점을 몰라도 그 합엔 참여해 왔다).
- **상한의 두 거처를 지켰다**(§9.4·§11.2.1): reroll/shield/timeToken = `shop.<id>.stockMax`, **bomb = `rules.bomb.stockMax`**(상점이 아니라 폭탄 자신의 성질). defense/maxhp/movespeed/magnet/resist 는 maxPurchases 가 곧 상한.
- **디버깅 1건**: maxhp 에서 §2.1 「hpMax 증가분만큼 즉시 회복」을 직접 구현했더니 **이중 회복**(hp +20). `recomputeStats` 가 이미 **모든** hpMax 증가에 대해 델타 회복을 하는 공용 경로였다 → 위임으로 수정(패시브 bulkhead 와 한 규칙).
- **상점 화면(§5.4)**: main.js 에 `SHOP` 상태 — STAGE_CLEAR → 회복 → 상점 → (Escape 1회 확인) 나가면 `advanceStage`. ↑↓ 선택 / Enter 구매 / Escape 나가기. 렌더는 이름·한 줄 설명·보유/상한·가격 + **구매 가능 여부 색**(금색/자홍/흐림).
- **검증**: 신규 `shop.test.mjs` 13개(가격 성장·세 관문·10효과 전수) → **327 통과**(314→327) · `check.mjs` exit 0. 임시 프로브로 상점 화면 렌더 실측(`err=none`, 가격·색 판정 일치) 후 삭제.

---

### 2026-07-18 — C2: 점수 + 결과 화면 + 시간 토큰 (런에 결과가 남는다)

- **`src/core/score.js`(순수, import 0)**: 처치·보스/중간보스/런 클리어·시간·무피격·퍼펙트·코인 환산 누적 + `tally`(합 × 난이도 배율, **floor 는 마지막 딱 한 번**).
- **§11.3 attribution "damageShare"**: 초효과 처치 보너스는 **막타가 아니라 누적 피해 지분**이다 → 적 개체에 `dmgTotal`/`dmgSuper` 를 두고 collide 가 히트마다 기록, `addKill` 이 지분 ≥ 0.5 면 ×1.5. (collide 밖 피해(무기 진화 폭발 등)는 지분에 안 잡힌다 — 알려진 근사.)
- **연결**: `killEnemy`/`killBossEntity` → 처치 점수 · `applyHit` → 무피격(실드가 막으면 **유지**, shieldPreservesNoHit) · `tickRun` 격파 → 보스 보너스 + 잔여 타이머 시간 보너스(토큰 쓴 보스전은 0) · finale → 런 클리어.
- **시간 토큰이 실제로 쓰인다**: 보스전에서 `ShiftLeft` → 토큰 −1, 타이머 +30s, `bossTokenUsed=true`(그 보스전 시간 보너스 몰수). 상점에서 산 토큰이 드디어 소비처를 갖는다.
- **결과 화면**: 클리어/게임오버(시간 초과 표기) + 내역 8행(**못 받은 보너스는 회색**) + 난이도 배율 + 총점 + 시드.
- **검증**: 신규 `score.test.mjs` 12개 → **339 통과**(327→339) · `check.mjs` exit 0. 풀런 실측(seed 2, 실시간 잡몹 페이즈) = 승리 739s, 총점 **215,914**. 결과 화면 렌더 프로브 실측(`err=none`, 총점 173,930·회색 처리 정상) 후 삭제.

---

### 2026-07-18 — C3: DEATH + 컨티뉴 (아케이드 루프가 닫혔다)

- **`stage.js`**: `canContinue`(코인 ≥ 150 · 런당 1회 · 승리엔 없음) + `reviveContinue` — HP 만재 · **적 탄만 소거(적은 남는다)** · 하단 중앙 · 무적 3초 · 보스 **HP 는 보존**하고 타이머만 `max(잔여, 60)` · `over/deathCause` 해제.
- **대가는 두 겹**(§11.4): 코인 150 + **퍼펙트·전 스테이지 무피격의 소급 무효**. 그 대가를 **사망 화면에서 미리 말한다**(결과 화면에서 처음 아는 함정을 만들지 않는다).
- **화면**: `DEATH`(격추/시간 초과 + 컨티뉴 제안, 카운트다운 없음, Enter 계속 / Escape 포기) → `RESULTS`. 컨티뉴 불가·승리면 결과로 직행.
- **사인 명시**: HP 사망이 `deathCause` 를 안 남기고 있었다 → `'hp'` 세팅(D 의 `bossTimeoutRate` 게이트가 사인을 센다).
- **검증**: 신규 컨티뉴 테스트 5개 → **344 통과**(339→344) · `check.mjs` exit 0. 통합 실측: 실제 사망(t=130.5s) → 부활(HP 100/100·코인 −150·무적 3s) → 런 재개 10초 진행 → 점수에 무피격 0/6·퍼펙트 상실 반영.

---

### 2026-07-18 — D1: `bot.js` — 결정적 AI 플레이어 (8번째 스트림의 거처)

- **`src/core/bot.js`(순수)**: 매 틱 `makeInput()` 모양을 낸다 = **게임과 같은 입구**. 정책 4축(draft × farm × stance × shop) + `forceNoElement` 프로브. 상태는 `world.bot` 에 1회 alloc.
- **난이도는 봇의 반응 지연으로만 들어온다**(§10.4.1): `latencyTicks = round(reactionMs/1000 × TICK_HZ × speed)` — core 는 전 난이도에서 게임시간 기준 동일하고, 달라지는 건 «눈 감는 창»이다.
- **회피 개선 1건**: 첫 구현은 위험 반경이 `히트박스+탄+12px ≈ 20px` 라 **반응 지연 250ms 안에 이미 늦었다**. → 여유를 **원리적으로** 잡았다: `moveSpeed × reactionSec`(그보다 늦게 알아채면 피할 수 없는 거리). 동시에 위협을 **전부 1/거리 가중 합산**해 합벡터가 빈 공간을 가리키게 하고, 경계 반발을 넣어 «구석에 몰려 맞는» 최빈 사인을 없앴다. → 보스전 180초 완주 시드가 0 → 2 로.
- **★ 첫 밸런스 측정치(정직한 현재 상태)**: baseline 정책 10시드 → **클리어율 0/10, 전부 스테이지 1**. 사인이 둘로 갈린다 — HP 사망(생존 부족) vs **timeout(보스를 못 죽임)**. 도달 레벨 Lv3~6 로 §13.1 의 런당 54 레벨업 목표에 크게 못 미친다. ★ **무기가 5/12 만 구현된 상태의 측정이므로 인증(certify)은 콘텐츠 완성 후에 의미가 있다** — 지금 필요한 건 판정이 아니라 **측정 파이프라인의 확립**이고, 이 수치가 그 첫 기준선이다.
- **검증**: 신규 `bot.test.mjs` 10개(결정성·스트림 독립·정책·회피·난이도 지연) → **354 통과**(344→354) · `check.mjs` exit 0 · **S1 SKIP 1 → 0**(정본이 요구한 8번째 스트림의 거처가 생겼다).

---

### 2026-07-18 — A2a: 무기 3종(aura·nova·lance) + ★ 밸런스 병목 규명

- **인프라 없이 되는 3종을 인라인 피해로 구현**(fan 진화 폭발과 같은 D3 경로: ctx → playerToEnemy → killEnemy):
  - `aura` — 지속 피해장, **live 각인**(적용 순간의 스탠스를 재평가), 중심 1.0 → 가장자리 falloff 선형. 진화 싱귤래리티는 **chaff 만** 끌어당긴다(엘리트·보스 제외).
  - `nova` — intervalSec 주기 자기중심 폭발. 진화 슈퍼노바 = 2단 링(evoSecondaryDmgMul) + **적 탄 소거**.
  - `lance` — 정면 폭 beamWidthPx 의 선, **가까운 순으로 pierce 마리까지**(정렬 배열 없이 0-alloc). 진화 레일건 = 아레나 세로 전체 + 무제한 관통.
- **구현 무기 5 → 8종.** DPS 실측(정면 25~55px, 10초): Lv1 6~28 / 진화 93~576 — 진화의 도약이 설계대로 크다.
- **★ 밸런스 병목 규명(측정 파이프라인의 첫 성과)**: 무기를 8종으로 늘려도 봇 클리어율 **0/12**. 계측해 보니 원인은 무기 수가 아니라 **성장**이었다 —
  - 스테이지1 잡몹 120초: 적 스폰 **95** · 처치 **24** · **73% 이탈**(처치율 25%)
  - 결과: 런당 레벨업 **3회** vs §13.1 목표 **54회** → **XP 3~4배 부족** → 화력 미달 → 보스 timeout / HP 사망
  - ★ 처치율이 레버다. 다만 **콘텐츠 미완(무기 4종·중간보스)** 상태의 숫자를 지금 손대는 건 이르다 — 인증(certify)은 콘텐츠 완성 후. 이 수치를 기준선으로 남긴다.
- **검증**: 신규 무기 테스트 9개(반경·falloff·2단 링·탄 소거·pierce 순서·사거리·진화 격리) + 리터럴 계약 3파일 추가 → **366 통과**(354→366) · `check.mjs` exit 0.

### 2026-07-20 — A2b: 무기 4종(orbit·mine·barrage·drone) → ★ **12 패밀리 전부 구현 완료**

- **지속 엔티티를 쓰는 마지막 4종**. §12.1 S2(풀 10개 ⟺ 오버플로 정책 10개)를 지키려 **새 풀을 만들지 않고** 기존 풀을 빌려 쓴다:
  - `orbit` — playerBullets 를 **날지 않는 탄**으로 재사용(`pierceLeft = -1`, 매 틱 `age = 0`). 진화 이지스는 공전체에 닿은 적 탄을 지운다.
  - `mine` — zones 의 **플레이어 장판**(`fromPlayer`). 진화 클러스터는 폭심에서 `evoClusterCount` 발을 원주에 **균등 배치**(각도가 균등 → RNG 0, 결정적).
  - `barrage` — telegraphs 에 `kind 'strike'` 예고를 놓고 `telegraphSec` 뒤 그 자리에서 폭발. 예고 어휘(§7.4)를 그대로 쓴다.
  - `drone` — drones 풀. 진화 잔상 편대는 이력 버퍼 없이 **지수 추종**(`dt / (evoTrailDelaySec + dt)`)으로 지연을 환원 — 0 alloc·결정적.
- **소유권 규칙을 step 에 명문화**: `hazards` 는 나이만 먹이고, `z.fromPlayer` 인 장판과 `kind !== 'laser'` 인 예고는 **반납하지 않는다**(무기가 소유자). 새 필드·새 풀 0.
- **★ 버그 1건 — barrage 진화가 던졌다**: `weapons.json` 의 **Lv8 행이 `targetMode` 를 `'densest'` 로 바꾼다**. 조준을 `slot.evolved` 가 아니라 **계약 값(`eff.targetMode`)** 이 정하도록 고쳤다. (진화 여부가 정하는 건 반경뿐 — `slot.evolved` 분기는 여전히 정확히 1개.)
- **12종 DPS 실측**(사방 분산 배치, 15초): Lv1 0~42 / 진화 108~811. barrage Lv1 이 0 인 건 **아레나 무작위 착탄**이라 고정 배치를 잘 안 맞는 것 — 진화(밀집 조준) 811 로 뒤집힌다. 12종 전부 정상 작동.
- **밸런스 재측정**: 무기 12종 전부여도 봇 클리어율 **0/12** · 평균 도달 스테이지 1.0 · 평균 레벨업 3.3. → A2a 에서 규명한 **처치율/XP 병목이 무기 수와 무관함이 재확인**됐다. 레버는 여전히 성장이다.
- **검증**: 신규 테스트 12개(공전 균등·수명 회귀·각속도·이지스 격리 / 설치 주기·무장 전 무피해·반납·클러스터 격리 / 예고 주기·반경 배율·폭발 타이밍·densest 조준 / 앵커·정면 발사·조준·지연 추종) + 리터럴 계약 12파일 전부 → **384 통과**(366→384) · `check.mjs` exit 0.
  - 테스트 3건은 **테스트가 틀렸다**: 드론은 사거리 밖이면 «정면 발사»가 정본 동작(§1.1)이고, 바라지 볼리는 `strikeIntervalSec` 간격이라 한 틱에 1발이다. 코드가 아니라 기대치를 정본에 맞췄다.

### 2026-07-20 — B4: 중간보스(§8.9) — 「선택적」이 코드가 되다 + ★ 렌더 회귀망

- **`midboss.js` 신설.** 정본이 「선택적」을 정의한 방식을 그대로 옮겼다: 등장 후 30초(`midBossLeaveAfterSec`)면 **이탈 = 보상 0** · **무시해도 쫓아오지 않는다**(추격 금지 — 그러면 "선택"이 거짓이 된다) · 다만 **이탈 전까지는 계속 쏜다**(공짜 회피가 아니다).
- **3종 전부 살아났다**: `mbHammer`(anchor, fan+zone 교대) · `mbLancer`(charge, laser) · `mbNest`(anchor, aimed + 잡몹 소환).
  - ★ **`charge` 가 처음으로 도달 가능해졌다** — 동결 어휘 `moveId` 8종 중 잡몹 사용자가 0이고 **유일한 사용자가 mbLancer** 였다(§9.8.2가 지적한 그 구멍).
  - ★ **§8.9-R8 이미터 2개 동시 구동**: `emitT/emitPhase` 한 벌로는 표현이 안 돼 **두 벌**(`emitT2/emitPhase2`)로 돌린다. 두 악절의 유효 주기가 같고(2×6+3=15) `offsetSec` 이 0/3 이라 **동시 텔레그래프 1개**가 정적으로 유지된다.
  - ★ **§8.9-R9 소환**: `formationId` 의 **원점이 소환자**다(산란모함이 자기 자리에서 알을 뿌린다). 이를 위해 편대 배치를 `formations.js` 로 뽑아 `enemies.js`(원점 = 스폰 라인)와 공유한다 — 구현 1벌, 원점만 다르다.
- **속성 = 보스전의 예고편**: `notThemeAndNotNormal` 런타임 주입(`rng.spawn`). 테마 정답 스탠스를 켜둔 채로는 안 죽으므로 **스탠스를 바꾸게 만든다**. 최종 스테이지는 테마가 없어 "테마가 아님"이 **공허참** → 후보 3종 + **비복원 2마리**. HP 는 `bossHpScale`(★ `enemyHpScale` 이 아니다 — 그러면 DPS 체크가 소멸한다).
- **새 풀 0** (§12.1 S2 유지). 개체 필드 4개(`midBossId`·`emitT2`·`emitPhase2`·`summonT`)만 늘었고, `isBoss` 는 **켜지 않는다** — 중간보스는 보스 타이머·페이즈 무적·코어 격파 규칙과 무관해야 한다.
- **★ 버그 1건 — mbLancer 가 영원히 조준만 했다**: 스폰 라인은 **아레나 위쪽 밖**이라 돌진 첫 틱에 「밖에 있다」로 판정돼 즉시 리셋됐다. 돌진 상태를 «진입 전/진입함»으로 쪼개 해결 — 25초에 돌진 6회, 아레나를 세로로 관통한다.
- **★ 렌더 회귀망 신설 (`tests/render.test.mjs`)**: 2D 컨텍스트를 **스텁**(모든 호출 noop, 모든 대입 삼킴)으로 세워 **한 판 11,400 프레임을 실제 렌더 경로로 통과**시킨다. 칠은 안 보고 배선만 본다 → 「보스 코어가 화면에 있는 모든 프레임에서 던졌던」 그 사고 클래스가 이제 **테스트로 잡힌다**. 결정 화면 4종(드래프트·상점·결과·사망)도 함께 건다.
- **검증**: 중간보스 테스트 12개(등장 시각·마릿수 == midBossCount·동시 1마리 / 속성 규칙 12시드·최종 비복원 / HP 곡선 / 이탈 무보상·위기 강제이탈 / anchor 왕복·charge 회귀 / S17·소환 주기·원점 / 이미터 2벌 / 처치 보상) + 렌더 2개 → **400 통과**(384→400) · `check.mjs` exit 0 · 봇 12시드 무크래시.

### 2026-07-20 — D2: `tools/sim.mjs` — 인증기가 서다 + ★ 계측기(봇) 결함 2건 규명

- **`tools/sim.mjs` 신설.** §10.4 대로 **`src/core/step.js` 를 그대로 import** 한다 — 렌더·오디오·DOM 은 애초에 core 에 없으므로 **스텁조차 필요 없다.** 그래서 「시뮬이 인증한 것 = 출시되는 것」이 성립한다.
  - **두 모드**: `run`(런 클리어율, 봇은 죽는다) · `dpsProbe`(무적 봇, **28 셀** = (보스,스테이지) 쌍 3+24+1). 이 분리가 없으면 "보스당 격파율"과 "런 클리어율"이 같은 지표로 오인된다.
  - **산출물 7종**: `report/summary.json` + `weapons·elements·bosses·stages·economy·deaths.csv`.
  - **인증 판정**: `meta.certify` 임계값으로 22개 지표를 PASS/FAIL/**UNMEASURED** 로 찍고 실패 시 exit 1. ★ 아직 안 재는 지표는 **null 로 내보낸다** — 측정 안 한 것을 통과로 위장하지 않는다.
- **텔레메트리 계기를 core 에 심었다**(전부 `world.tele` 가 있을 때만 동작 → 게임 실행엔 **무영향**, 결정성 불변): `noteDamage`(무기별 피해 = 지배도) · `noteDamageTaken`(아키타입별 치사 지분, 적 탄에 `srcArch` 를 실었다) · `addKill` 의 처치/위기처치 집계 · XP 획득량.
- **★ 계측기 결함 ① — 봇이 회피에 갇혀 총구를 못 맞췄다**: 회피가 조준을 «대체»하는 구조라, 화면에 적이 많을수록 영원히 도망만 다녔다. 실측 — 스테이지1 120초에서 **잠재 화력의 20%만 명중**(잠재 1800 vs 실제 356). → 회피를 **목적지가 아니라 변위**로 바꿨다(의도를 세우고 그 위에 더한다). 비켜서는 거리는 새 키 없이 `dodgeLookaheadSec × 이동속도`로 유도. **처치율 21% → 39%, 피해량 2.3배.**
- **★ 계측기 결함 ② — 봇이 장판·빔을 위협으로 안 봤다**: 치사 지분 1위가 «출처 불명»(= 중간보스의 zone/laser) 2002 였다. 탄만 피하는 봇은 사람의 하한이 아니다. → zone(원)·laser(선, 수직거리)를 회피 벡터에 넣었다. **출처 불명 피해 2002 → 1499.**
- **★ 밸런스 실측 (인증기가 처음으로 낸 숫자)**:
  - `dpsProbe` **전 28셀 0% 통과** — 스테이지1 보스전 실효 DPS **1.3** vs §13 목표 **49**.
  - XP 경제 자체는 정합하다: **100% 처치 시** 스테이지1 이 ~285 XP = **8 레벨업** → 6스테이지면 목표 54 에 근접. 그런데 실제 처치율이 **39%** 라 레벨업이 **2~5** 에 그친다.
  - 스테이지1 스폰 총 HP **1116** vs 80% 처치에 필요한 실제 명중 피해 **~890**(7.4 dps) — 현재 봇의 실제 명중 피해는 **~820(6.8 dps)**. ★ **레버는 여전히 처치율이고, 이제 그 격차가 «두 배»가 아니라 «10% 남짓»으로 좁혀져 정량화됐다.**
- **검증**: 400 테스트 통과 · `check.mjs` exit 0 · 시뮬 run/probe 양 모드 실행 확인.

### 2026-07-20 — 밸런스 1차: ★ **죽어 있던 데이터 커브 2개** + XP 예산을 정본에 맞추다

시뮬이 서자마자 «추측 대신 측정»이 가능해졌고, 첫 측정이 **밸런스 문제가 아니라 구현 누락**을 찾아냈다.

- **★ 버그 ① — `curve.xpScale` 이 코드 어디에서도 적용되지 않았다.** `enemyHpScale`·`bossHpScale`·`swarmTotalScale` 은 쓰이는데 **스테이지별 XP 배율만 죽어 있었다** → 후반 스테이지가 저작 의도의 1/6 XP 만 줬다. `spawnEnemy` 에 적용(= xp 가 개체 정의에서 직접 오므로 유일한 적용 자리).
- **★ 버그 ② — `curve.spawnDensityScale` 도 미적용.** 스테이지 1 이 저작 의도(0.7배)보다 **30% 더 몰려왔고** 후반은 반대로 헐거웠다. `spawnWave` 의 count 에 적용.
- **★ XP 예산이 정본과 ×4.9 로 어긋나 있었다 (여섯 스테이지 «전부» 균일)**:

  | 스테이지 | 저작 XP 총량 | §13.5 요구 | 배율 |
  |---|---|---|---|
  | 1~6 | 163·304·590·894·1633·1806 | 794·1534·2724·4444·6771·10183 | **×4.87 4.62 4.97 5.05 4.15 5.64** |
  | 합계 | **5,390** | **26,450** | **×4.91** |

  ★ 여섯 칸이 **전부 같은 배율**이라는 것은 여섯 개의 저작 실수가 아니라 **스칼라 하나가 빠진 서명**이다. `xpScale` 의 **비율**은 정본과 정확히 맞으므로(§13.5 는 비율만 쓴다) **절대 배율만** 교정: `[1,1.6,2.4,3.4,4.6,6] → [4.9,7.84,11.76,16.66,22.54,29.4]`. 새 키 0 · 규칙 0 · 구조 0 — **숫자 여섯 개.**
- **★ 계측기(봇) 결함 ③ — 「선택적」을 몰랐다**: 봇이 `nearestEnemy` 로 중간보스를 조준해 **그 아래에 주차**했다 → 치사 지분 1위가 중간보스의 zone/laser(«출처 불명» 1317)였다. §8.9 가 정의한 두 선택지 중 베이스라인 봇은 **무시하는 쪽**을 고르게 했다(회피는 여전히 그 장판·빔을 위협으로 본다). 1317 → 881.
- **★ 계측기 결함 ④ — 사격선을 안 지켰다**(같은 날 D2 에 이어): 파밍·회피가 겹치면 봇이 아레나 최상단 y=56 까지 올라가 **보스(y=170)보다 위에서 위로** 쐈다. 이 게임의 주 무기는 전부 위로 나간다(§1.1) — 표적 아래에 있는 것은 전술이 아니라 **사격의 전제**다. 단, 이 규칙을 내려오는 잡몹에까지 걸면 봇이 물러나며 요격을 포기한다(처치율 39%→24% 실측) → **자리를 지키는 표적(보스·중간보스)에만** 적용. 보스전 실효 DPS **1.3 → 7.0**.
- **성과와 남은 벽**: 런당 레벨업 **2.3 → 7.9(최대 11)** — ★ 11 은 §13.5 사다리가 스테이지1 보스 진입에 요구하는 픽 수와 **정확히 일치**한다. 그러나 클리어율은 아직 **0%**이고 사인의 **83%가 HP 사망**이다 → **레버가 «성장»에서 «생존»으로 넘어갔다.** 다음 측정 대상: 피격 예산(§2.1 hp 100 · i-frame 1s vs 실측 피격 빈도), 그리고 zone/laser 피해의 출처 귀속(현재 «출처 불명»으로 뭉쳐 있어 어느 적을 손봐야 하는지 아직 못 가리킨다).
- **검증**: 400 테스트 통과(2건은 새 배율을 반영해 기대치 수정) · `check.mjs` exit 0.

### 2026-07-22 — 밸런스 진단 (다중 에이전트 검증): ★ **게임은 옳다, 계측기가 약하다**

D2 시뮬로 «측정»이 가능해지자, 클리어율 0%의 뿌리를 **4전선 워크플로(각 전선을 독립 스켑틱이 스크립트 재실행으로 반증)**로 규명했다. 결론은 일관됐다 — **밸런스가 아니라 계측기(봇)다.**

- **★ 게임 데이터는 검증상 옳다**:
  - **보스 HP 정합**: 스테이지1 보스(코어 976 · armor 2×2391 = φ 4.90)는 §13.6.2 가 noElementPass 목표에서 «유도»한 값. seeker(유도) 빌드는 예산 안(100~140초)에 격파한다.
  - **★ §13.5.1 사다리 빌드가 실제로 클리어한다**: L3,3,2,2 + 2속성(무진화)로 [forward,fan,seeker,omni] = **mortal 6/8**. 게임은 설계대로 클리어 가능하다.
  - **XP 처리량 정합**: 무적 봇이 스테이지당 레벨업 ~9 → 6스테이지 = 54 = §13.1 목표와 «정확히» 일치.
  - **강제 최강빌드(L8진화+풀속성) = mortal 12/12**: 게임 클리어 가능성의 결정적 증명.
- **★ 뿌리 = 모브 페이즈 생존/킬레이트 → 보스 진입 빌드**: 봇은 보스에 «사다리보다 약한» 빌드로 도달한다 — 실측 무기레벨 **[3,2,1,1] + 1.3속성** vs 사다리 **[3,3,2,2] + 2속성**. 무적이면 레벨 10.4(사다리 12에 근접)이나, mortal 은 모브에서 30% 사망 + 보스전 HP 사망으로 빌드가 덜 여문 채 도달 → 화력 미달. **격차는 작고 순수 계측기 문제다.**
- **이번에 고친 계측기·공정성 결함**:
  1. ★ **죽은 커브 #3 — 봇이 속성을 한 번도 투자 안 했다**: generalist 픽 순서가 elementLevel 을 **맨 뒤**에 둬 보장·피티로 2~6장 깔린 속성 카드가 매 런 «0장» 뽑혔다(xpScale 급). §13.5.1 사다리대로 속성을 패시브 앞으로. 0 → 1.3픽/런.
  2. ★ **레이저가 예고 없이 즉발했다(§7.4 공정성)**: 빔이 age 0 부터 즉시 피해 — 사람도 못 피한다. §7.4 「충전이 곧 텔레그래프」대로 **2단(충전 경고 → 활성 피해)**으로 구현 + 렌더 점선 경고. 실측 지시 아닌 «기획 구현»(정본에 이미 있던 설계).
  3. **치사 지분 귀속**: zone/laser 에 srcArch 추가 → «출처 불명»이 최대 사인(2002)에서 8%로. 이제 어느 적을 손볼지 이름이 나온다.
  4. **스탠스 = 사격 표적**: 스탠스가 nearestEnemy(코어=노말=무상성)를 봐서 armor 를 ×2 로 때리는 시간이 75%뿐이었다 → 사격 표적을 넘겨 ~95%.
  5. **armor 우선 + 고정 타겟**: 게이트를 열려면 armor 부터. 두 부위 오가며(런당 30~180회) 화력을 반씩 나눠 어느 쪽도 안 죽던 것을 부위 하나에 고정.
  6. **시뮬 텔레메트리 버그**: driveRun 이 card.kind(없는 필드)를 읽어 픽/속성 집계가 전부 0이었다 → card.category.
- **검증**: 400 테스트 · check.mjs exit 0 · 6 커밋. 남은 것은 **봇 모브 생존/픽 분배**(계측기 경쟁력)로 클리어율을 인증 밴드(0.35~0.65)에 올리는 일 — 게임 콘텐츠가 아니라 계측기.

### 2026-07-22 — 플로우 화면 + 레이저 공정성 + 계측기 정비 (긴 세션)

- **★ 플로우 화면 (§6.5)** — 게임이 난이도 `normal` 로 바로 런에 진입하던 것을 정본의 플로우로: **TITLE**(Space 시작·O 옵션) → **DIFFICULTY**(4난이도 + 속도/점수 배율, ↑↓·Enter) → 스테이지마다 **THEME_BANNER**(스테이지 n/6 + 테마명, Space 스킵) → 런 → **RESULTS**(Space 같은 난이도 재시작·Esc 타이틀). OPTIONS 자리 표시.
  - 라이프사이클 리팩터: `startRun(diffId)` 가 시드·world·보간·FX 를 매 런 새로 만들고 `world.difficultyId` 를 세팅(★ tally 의 점수 배율이 읽는데 main 이 안 넣던 잠재 버그 수정). `renderFrame()` 이 `world===null` 을 가드해 메뉴 화면을 그린다. **난이도가 화면에서 선택돼 hard/hell/disaster 속도 램프가 실게임에서 처음으로 도달 가능**.
  - 브라우저 실측: TITLE·DIFFICULTY·THEME_BANNER 전부 콘솔 에러 0 으로 렌더 확인.
- **★ 레이저 공정성 (§7.4)** — 빔이 예고 없이 즉발(사람도 못 피함)하던 것을 「충전이 곧 텔레그래프」대로 **2단(충전 경고 → 활성 피해)** + 점선 경고 렌더. 기획 구현(정본에 이미 있던 설계).
- **★ 밸런스 진단 (다중 에이전트 검증)** — 클리어율 0%의 뿌리를 규명: **게임 데이터는 검증상 옳다**(사다리 빌드 6/8·최강빌드 12/12 클리어·보스 HP 유도값·XP 처리량 = §13.1 목표). 0%는 **봇(계측기)이 사다리보다 약한 빌드로 보스에 도달**하는 것 — 순수 계측기 문제. 계측기 결함 다수 수정(속성 죽은 커브·치사 귀속·스탠스=사격표적·armor 우선·몸통 회피·sim 텔레메트리 버그).
- **검증**: 400 테스트 · check.mjs exit 0 · 브라우저 스모크(로드·렌더·에러 0). 커밋 11개.
- 남음: 봇 모브 생존/빌드 성숙 → 인증 밴드(계측기 경쟁력, 열린 과제) · OPTIONS 실구현 · D3 동적게이트 채점 · 제출물.

### 2026-07-22 — D3 동적게이트 채점 + ★ 첫 실인증 (게임은 옳다, 봇이 유일 병목)

- **★ D3 — check.mjs 가 동적 게이트를 채점한다 (§23)**: STUB 27개로만 신고하던 것을 `report/summary.json`(= `sim --certify` 산출)이 있으면 그 `certify.results` 를 읽어 **PASS/FAIL/UNMEASURED [DYNAMIC] 섹션**으로 채점(없으면 STUB 폴백). 정적 종료코드와 분리(정본 정합만 exit 를 몬다) — sim --certify 가 커밋 게이트. **파이프라인 폐쇄: AI 데이터 → check(정적) → sim --certify(동적) → check 가 동적 판정도 표면화.** `tools/report/` 는 gitignore(재생성 산출물).
- **★ 첫 실인증 (120 런)** — `8 PASS · 16 FAIL · 3 UNMEASURED`. **분할이 진단 그대로다**:
  - **PASS (구조 밸런스 정합)**: `capHits 0`(전 예산 준수) · `maxLevelUps 12≤60`(XP 정합) · `farmXpRatio 2.85≥2` · `maxWeaponPickShare 0.11≤0.16` · `maxElementWinShare 0.38≤0.42`(지배 없음) · `themeClearStddev 0.012` · 코인 희소성.
  - **FAIL (전부 `runClearRate 0` 의 하류)**: 클리어 의존 게이트(stanceValue·noDeadLuck·dpsProbe·difficultySpread) 전부. = **저작 밸런스가 아니라 계측기(봇)가 못 깨는 것.**
  - 참고 신호 2개(둘 다 약한 봇에 오염됨, 봇 향상 시 하락 예상): `startWeaponDamageShare 0.45`(forward 편중) · `maxArchetypeLethalityShare 0.49`(접촉 다이버 과치사).
- **봇 = 확정된 병목**: 이번 세션에 봇 경쟁력 개선을 다각도로 시도(속성 죽은커브·치사귀속·스탠스=사격표적·armor 우선/고정·몸통 회피·저체력 수비·밀집열 조준). 대부분 **한계효용 체감**으로 스테이지1 클리어 ~3/48 에서 정체. **결론: 인라인 튜닝은 소진됐다 — 봇은 재설계(예측 회피 + 무기별 포지셔닝을 통합 정책으로)가 필요하며 별도의 집중 작업 대상.** 게임 콘텐츠·데이터는 검증상 완성·정합.
- **검증**: 400 테스트 · check.mjs exit 0(정적) · 실인증 리포트 산출.

### 2026-07-22 — ★ 버그 헌트(다중 에이전트 리뷰) + 1000+판 스트레스 → 20건 수정

사용자 요청: (1) 코드 정독으로 버그를 찾아 고치고, (2) 1000+판을 돌려 버그·밸런스를 확인해 수정 계획을 제안.

- **★ 1000+판 스트레스 감사 (2회, 수정 전·후 모두 클린)**: 필멸 1200런(4난이도 × 12정책) + 무적 딥스캔 80런(전 6스테이지·중간보스·위기·finale) + 결정성 120쌍 + 틱스캔. **크래시 0 · NaN 0 · capHits 0 · 비결정성 0 · 이상상태 0.** 엔진은 런타임상 견고 — 아래 버그는 전부 «런타임이 우연히 안 밟은» 논리·계약 결함이며 코드 정독으로만 잡힌다.
- **★ 다중 에이전트 코드 리뷰**: 12 서브시스템을 병렬 리뷰, **각 findings 를 독립 스켑틱이 repro 재실행으로 반증**. 27건 중 24 생존(3 정확히 반증). **20건 수정**:
  - **HIGH**: ① 플로우 리팩터가 **상점을 완전 먹통**·드래프트 Enter 무효로 만들었다(상단 엣지 소비가 tickShop/tickDraft 재폴링을 훔침) — 내가 넣은 회귀, 엣지를 넘겨주게 수정. ② **lance 가 같은 y 의 한 줄을 1명만 관통**(편대는 정확히 같은 y) — idx 동점처리로 한 행 전부 관통. ③ **타임아웃 사망이 가짜 퍼펙트 +48000** — 미클리어 스테이지가 기본 무피격 → cleared·won 게이팅. ④ **막타가 동일틱 타임아웃에 짐**(tickRun 이 collide 앞) — 만료 1틱 지연으로 clutch 격파가 이긴다.
  - **MEDIUM**: 적 **유도탄이 직진**(turnRate 미통합) → 재조준 통합 · 자석이 base 이속만 써 업그레이드 후 픽업을 못 잡음 · **직접피해 6무기(fan/aura/nova/lance/mine/barrage)가 페이즈전환 무적 우회 + 초효과 처치보너스 누락**(공유 hitEnemy 헬퍼로 일괄) · **orbit 공전체가 레벨업해도 안 커짐** · **위기 전용 새떼가 정상 웨이브에 유출** · **wall 이 gapWidthPx 무시 + 양끝 우회 가능**(frostCrown/tetrarch 보스가 실제 사용) · sim 이 **finale 격파를 집계 누락**(stagesCleared −1) · uptime tick-proxy 는 §10.4.3 지표가 아니라 **UNMEASURED** 로 정직 신고.
  - **LOW**: formations scatter 원점 무시(소환 배치)·arc 볼록 방향 반전 · drone family-키·앵커 재각인 · boomerang 체인 gen 확인 · spawnPickup 포화 시 손실(손실0 위반) · midboss stunSec 무시.
  - **미수정**: swapSlots(슬롯 재정렬) 의존 2건 — swapSlots 가 export 됐으나 **호출처 0**(도달 불가). drone/orbit/boomerang 의 family-키 수정이 재정렬 도입 시의 올바른 패턴.
- **검증**: 403 테스트(2 갱신·2 추가) · check.mjs exit 0 · 스트레스 재감사 클린 · 대표 repro 확인(lance 한 행 관통 · nova 초효과 +30 · homing −90°→−30° · 타임아웃 사망 non-perfect).

### 2026-07-22 — ★ 봇 재설계(연속 스티어링) — 토너먼트 승자 채택

계획 1단계(임계경로) 실행. 봇이 인증을 막는 유일 병목이라, **격리 worktree에서 3개 내비게이션 설계를 병렬로 구현·튜닝·측정하는 토너먼트**를 돌렸다.

- **후보 3종**: A 포텐셜 필드(후보 방향 채점) · B 연속 스티어링(반응주기로 지각지연 + 매 틱 조향벡터) · C 모드 머신.
- **고-N(N=200) 정면 비교로 승자 선정**(64-seed 지표는 노이즈라 신뢰 불가 — 세 에이전트 모두 지적): **B 연속 스티어링 압승** — 평균 도달 스테이지 **0.025 → 0.265(~10배)**, 스테이지1 클리어 **2% → 19%**, 전 정책에서 최강.
- **설계**: 의도(표적·위협SET·스탠스)는 reactionSec 마다 갱신(지연 유지), **조향 벡터는 매 틱** 재계산 — 사격위치 인력(위로 쏘니 표적 «아래»·x정렬, 상한) + 외삽 위협 1/거리 반발 + 경계 반발. 위협은 사전할당 Float64Array 스냅샷(핫패스 0-alloc), rng.bot 만(결정성 유지).
- **가장 큰 이득(실측 순)**: ① 보스 표적 중엔 픽업 안 좇기(열 이탈로 보스 명중 ≈1% → 클리어 ~2배) · ② 보스전 x인력 강·상한 큼(열 고정 = 위로 쏘기의 전제) · ③ 모브 중단 사격선 + 픽업 우회 반경 제한(킬레이트↑ → 레벨 ~8.3→10) · ④ 저체력 후퇴 + 반발 증폭.
- **남은 천장**: 전 6스테이지 클리어는 아직 0% — 스테이지1 보스의 armor 2×2391 을 뚫을 빌드를 모브 페이즈 XP(≈레벨 9.5)가 못 만든다. **다음 레버는 내비게이션이 아니라 XP/빌드**(봇 에이전트들의 공통 진단).
- **검증**: 403 테스트 · check.mjs exit 0 · 결정성 확인 · **신규 스트레스(필멸 1000 + 무적딥 60) 전면 클린**(크래시·NaN·캡·비결정성·이상 0). 난이도 램프가 이제 제대로 읽힌다(normal 0.30 → disaster 0.17). 무적 딥스캔은 2~5 스테이지 도달(이전 0~2).

### 2026-07-22 — XP 곡선 프론트로드 + 보스전 상성 진단 (사용자 결정 실행)

**사용자 결정**: 「6스테이지 중 1스테이지는 쑥쑥 레벨업하며 깨는 맛」 → 레벨업을 빨리(초반 특히 싸게). 4지선다에서 «레벨업 빠르게» 채택.

**진단(수치)**: 새 봇으로도 클리어 0% — 조작이 아니라 **빌드 강도**. 무적팜 봇이 s1 보스 진입 시 8.5픽(≈레벨 9.5)인데 §13.5 사다리는 11픽(레벨 12)이 필요(그 빌드는 6/8 클리어). 원인 = §13.5 XP예산이 ~100% 처치율 가정, 현실 ~65% → 빌드 ~2레벨 부족.

**수정(숫자만, C-4 「곡선=값」)**: meta.xp 를 base 6/exp 1.32 → **base 1.38106/exp 1.73** 로 **프론트로드**. 초반 레벨 대폭 저렴(L1→2: 6→1.4, 누적L12: 746→397), **총예산 Σ(1..53)=26,450 고정**(§13.5 동결 불변량 — late 페이싱·maxLevelUps≤60 게이트 불변). 초반만 빨라짐.

**실측 효과**(cert 120런):
- s1보스 진입 빌드 8.5픽 → **11.5~12픽(레벨 ~12.5)** — 목표 달성
- **dpsProbe.killTimeMedianBalanced 169s(FAIL) → 129s(PASS, 밴드 120~150)** — 보스 DPS 천장 해결
- dpsProbe s1 6%→33% · s2 6%→53% · runClearRate 0→1.7% · meanStages 0.32→0.52 · bossTimeout 0.35→0.275
- 무적 s1보스 격파율 6~33% → **58~75%**(드래프트별)

**남은 갭 진단**: cert 밴드(probe 95%, run 35~65%)까지는 **빌드 강도가 아니라 보스전 실행**. s1 보스(manta)는 의도된 **상성 스위칭 퍼즐** — finL(fire→water 2×)·finR(grass→fire 2×)가 **반대 스탠스**를 요구(water↔grass 상호 0.5× 저항). 봇은 스탠스를 바꾸지만(fire37/water35/grass28%, normal0%) **옴니 무기가 오프-핀을 저항피해로 긁어** 상성 효율이 샌다. 포커스 빌드(specialist/greedyDps)가 제너럴리스트보다 격파 많음(14 vs 18).

**검증**: 403테스트(§13.5 Σ=26,450 체크섬 통과 위해 base 5자리), check exit 0, 스트레스 400런 클린(픽업 병합 안전망 1회 = 손실 0, §12.1). 커밋 a8e0711.

### 2026-07-22 — 봇 실행력 그라인딩 (사용자 「봇을 더 갈기」 선택) → 천장 확인

사용자가 4지선다에서 «봇 실행력을 더 갈기» 선택. 철저히 갈았고 **결정적 진단**을 얻음.

**측정 도구 교정**: 손수 짠 루프는 보스 이후(샵·스테이지 전환)를 못 몰아 신뢰 불가 → `driveRun`(전 플로우 처리)로 전환. 진짜 baseline: **필멸 s1클리어 ~30%(N=400 0.532 avgStage)** · 무적 s1보스 격파 **62%**.

**진단 1 — 스티어링은 최적점.** 보스 열고정(BOSS_CAP_X 500→300→200) · x인력(BOSS_ATTRACT_X 4.5→3.0→2.2) · 생존부스트(반발↑) 전부 스윕 → **평평하거나 악화**. 작아 보인 이득은 시드 노이즈(같은 baseline이 시드표본에 따라 avgStage 0.42~0.53). 토너먼트 승자 스티어링은 파라미터 튜닝으로 못 이김.

**진단 2 — 보스 격파 천장 = 스티어링 아니라 «원소 투자 전략».** ★ 핵심 기제 발견(§4.3 recomputeStamps): **슈퍼이펙트 2×는 그 원소로 «각인된» 무기에만** 걸리고, **각인 무기 수 = 그 스탠스 원소의 투자 레벨**(invest[stance]). 투자 0이면 스탠스 바꿔도 각인 0 → 전부 노말. manta는 두 날개가 **반대 원소**(finL화염→물, finR풀→불)라 **물+불 분산 투자**가 필요 — 원소 집중(테스트함)은 오히려 wash(manta가 집중을 무력화). 시드마다 다른 보스 원소를 «미리 못 보는» 일반 봇은 이 전략을 못 짬.

**진단 3 — cert 밴드는 구조적으로 숙련 인간용.** runClearRate 35~65%는 **baseline 봇의 전 6스테이지 완주** 기준. 스테이지별 확률이 곱해지는 상승형 6판(bossHpScale 1→14.4)에선 아무리 좋은 봇도 35% 완주 불가. 봇은 **보수적 하한 계측기**.

**결론**: 봇은 달성 가능 천장 근처(무적 62% / 필멸 s1 ~30%). 스티어링·드래프트 미세조정으로 못 넘음. 남은 유일한 레버 = **보스-인지 원소 투자**(다가올 보스 약점 원소에 투자 집중) — 단 봇을 «순진한 하한»에서 «숙련 플레이어»로 성격 변경. 사용자에게 방향 문의 예정.

**게임 밸런스는 건전**(진짜 버그 DPS천장 해결·램프 정상·크래시0). bot.js 미변경(커밋본 유지), 403테스트·check 0.

### 2026-07-23 — ★ 진화 재설계 구현 (뱀서식 무기+패시브) + 유추가능성 실증

사용자 검증 요청 2건을 다중 에이전트 워크플로로 실증(적대적 검증 통과):
- **진화 메커니즘**: v1.4까지 «무기 Lv8 도달 그 자체»(패시브 무관, canon §9.5 의도적 확정)였음 → 사용자 오해 확인. 사용자 결정: **뱀서식 «무기+특정 패시브 콤보»로 변경.**
- **속성 게이트**(초반무료/후반필수): canon엔 명시(noElementPass 밴드)됐으나 실측상 약함(무속성 봇이 파이널도 클리어). 파이널 진화 ≥3 필수는 이미 성립. 사용자 결정: **게이트 숫자로 강화**(진화 구현 후).

**진화 페어링 — 유추가능성 실증**: 7명 독립 추측(설명만·의도 은닉)→합의. 직관이 무기 «유형»으로 뭉침(다투사체→autoload, 범위→coil, 연사→overclock, 관통→coating). 애매한 orbit·barrage는 진화 효과가 가리키는 짝으로 분산 확정. **12쌍**: forward→overclock·lance→coating·aura/nova/barrage→coil·fan/seeker/mine/omni/boomerang→autoload·drone→afterimage·orbit→reactive.

**구현(§9.5 v1.5)**: Lv7→Lv8 진화 카드는 짝 패시브 Lv≥3 일 때만 등장, 없으면 Lv7 정지. CANON 확정 → data/weapons.json(requiresPassive) → schema.mjs·check.mjs(**S41** 실재·범위·기계적유효성 전수검사) → draft.js(게이트) → bot.js(진화-인지 드래프트) → hud.js(**«Lv N/8»** + Lv7 진화임박 강조). 403테스트·check 0·크래시0·브라우저 클린. 커밋 7019ad6.

**부수효과(의도)**: 진화가 콤보가 되어 후반 진화수↓·화력↓ → 속성 게이트 자연 강화. **다음**: dpsRef 재도출 + 속성 게이트 수치 강화(사용자 플레이테스트 난이도 피드백과 병행 권장).

### 2026-07-23 — 플레이테스트 1차 피드백 반영 (7건)

사용자 실플레이 피드백 → 즉시 반영(전부 check 0·403테스트):
- **#1** 초반 레벨업 과속(젬1개=2레벨) → XP곡선 완화 base 1.38/exp1.73 → 5.02337/1.37 (총 26,450 고정). 젬1개=레벨1회.
- **#5** XP 원칙 위배(dustRunner hp4가 xp15로 최고) → hp+느림 기반 재매핑(dustRunner15→1·stalker6→12·mortarHulk10→13, 평균 6.06→6.12 보존) + **젬 크기 값비례**.
- **#6** 중간보스 이탈 로직 → 이미 있음(30초 후 이탈·보상0·추격X) 답변.
- **#8a** 스폰 밀도 [0.7..1.5]→[1.05..2.10].
- **#9** 보스 레이저 «충전 내내 추적» 회피난 → 텔레그래프↑·폭↓·빈도↓(lancer/kilnTurret). 근본 «잠금창» 코드화는 후속 후보.
- **#11** 유도탄 과함 → homingM 60→36°/s, driftHoming 30→20°/s.
- **#12** 코인 0회 → chaff·line 코인0은 §8.6 확정(S23)이라 유지, turret 0.15→0.30·bruiser→0.40 노출↑. 원인=클리어(보스코인12) 전 사망.

**남은 백로그(우선순위)**: #4 무기 재밸런스(편의성↔위력, 철학 승인) → #2 효과음 확장 → #8b 터렛 등장감(터렛 아키타입 이미 존재) → #10 탄 속도 다양성(패턴 타입은 이미 다양) → #11b «무조건 맞는 탄» HP게이트(신규 공격) → #3 테마 그래픽 → #7 HUD 하단 띠 → #9 레이저 잠금창(수치로 부족 시).

### 2026-07-23 — 플레이테스트 2차 (#13 드론 렌더 · #14 레이저 잠금창)

- **#13** 드론(옵션/위성) 무기가 «안 보임» → ★ **렌더가 아예 없었다**(interp 캡처만·그리는 루프 부재 = 투명 무기). `drawDrones` 신설: 플레이어와 잇는 테더 + 위성 본체(링+코어). 이제 «내 편대»로 보인다.
- **#14** 추적 레이저 회피난 → **잠금창** 구현(사용자가 원한 「path 확정→뜸→발사」). 빔이 활성 `beamLockSec`(0.6s) 전에 각을 잠그고 뜸 들이다 발사. `rules.fairness.beamLockSec` 소유(C-4). hazards 테스트 3단계 계약으로 갱신. check0·403테스트·브라우저 클린.

**남은 백로그**(우선순위): #4 무기 재밸런스 → #2 효과음 → #8b 터렛 등장감 → #10 탄 속도 다양성 → #11b 무조건맞는탄 HP게이트 → #3 테마 그래픽 → #7 HUD 하단 띠.

### 2026-07-23 — 플레이테스트 백로그 진행 (#4 #2 #8b #10)

사용자 «네 우선순위대로 쭉 진행» → 남은 백로그 순차 반영(전부 check0·403테스트):
- **#4** 무기 밸런스(편의성↔위력): forward ×1.30 보강 / seeker ×0.65·barrage ×0.65·omni ×0.80 하향(자동 편의값만큼). 레벨 오버라이드까지 비례 스케일. state 테스트 하드코딩→데이터유도로 강화.
- **#2** 효과음 확장: 상성 히트음 3종뿐이라 대부분 무음 → levelup/coin/hurt/stance 4종 추가(WebAudio 절차합성, player 상태 델타 감지). 뮤트 기본off·resume 기존대로. (core 무음 유지=결정성 불변.)
- **#8b** 터렛 «고정»감: swayAmpPx 축소(turretPod 40→14·mortarHulk 70→22·frostLance 25→10). 등장은 #8a 밀도로↑.
- **#10** 탄 속도: 이미 90~150 8종 제각각 + #9/#11 로 패턴 노출↑ → 대체로 해소 판단.

**남은 것(큰 3개)**: #3 스테이지 테마 그래픽(렌더 대공사) · #7 HUD 하단 띠(레이아웃, 실기 확인 필요) · #11b «무조건 맞는 탄» HP게이트(신규 공격 설계).

### 2026-07-23 — 플레이테스트 백로그 완료 (#3 #7 #11b)

남은 큰 3개 반영 → **피드백 16개 전부 완료**:
- **#3** 스테이지 테마 배경색(무채색 TODO 해소): 스테이지 id 로 Lab a·b 틴트(화산=빨강 등). finale 포함.
- **#7** HUD 하단 띠 «떠 보임» → 불투명 판(0.94)+상단 바닥 구분선으로 «가라앉은» 별도 띠. 플레이어 maxY(664)<띠(672) 무침범.
- **#11b** «무조건 맞는 탄» HP게이트: fairness 기둥과 충돌 → «예고되나 회피불가»로 해소. kiln(s3) vent P3 를 대형 열파(tele1.9·radius470·dmg11·악절 repeat2/rest6)로 재활용. 부위이미터 66 고정·§9.8.1 준수.

**플레이테스트 피드백 최종(16/16)**: #1레벨링 #2효과음 #3테마 #4무기 #5XP #6중보스(답) #7HUD #8a밀도 #8b터렛 #9레이저수치 #10탄속(기존+노출) #11유도 #11b HP게이트 #12코인 #13드론렌더 #14레이저잠금창. 전부 check0·403테스트.

### 2026-07-23 — 플레이테스트 3차 (#15 #16 #17 #18) — 4개 시스템 병렬조사 후 반영

ultracode 워크플로로 4개 시스템 병렬 조사 → 근본원인·설계긴장 파악 후 구현:
- **#15 랜스**: ★렌더가 아예 없어 «투명»(드론과 동일 부류). drawLance 신설(차지 예고+발사 섬광, a1 스크래치 신호, evoFullHeight 미러).
- **#17 보스 발사체 밀도**: 스테이지가 바꾸던 건 bossHpScale(체력)뿐 → curve.bossBulletScale[1→1.8] 신설, emitters.js effCount()가 발사시 count 곱함(보스만·count타입만). 66슬롯·풀 불변, capHits0.
- **#18 보스 HP**: 6px 실오라기 → 굵은 바+수치+armor 파트별 속성색 세그먼트(§7.6 스탠스 단서).
- **#16 테마↔속성**: ★조사결과 «반영 안 됨»이 아니라 «의도적 반대»(보스=예고편, R4/R5 강제). 사용자=테마 일관성 → **절충안**(퍼즐 살림): 보스 2테마+1다른(장갑 distinct 유지, R4→2·R5→false, R3/R6 유지) · 중간보스 themeElseNonTheme(테마 주입) · 몹 mix 이미 0.70 테마라 불변. CANON §8.9/§8.13.1 «예고편» 논리 폐기 정정.

**플레이테스트 피드백 누적 20/20**: 1차 7 + 2차 2(#13,#14) + 3차 4(#15~#18) + 그 외. 전부 check0·403테스트·브라우저 클린.

---

## 다음 할 일

- [x] 저장소 위치 결정 → **방법 1** (블로그 `projects/nan2026/`, 커밋 `nan2026:` 접두어로 분리)
- [x] AI 활용 방향 → **A: 제작 도구로서의 AI** (무API·오프라인·저용량; 빌드타임 콘텐츠 생성 활용)
- [x] 장르 = **survivor-like 로그라이트** + 정교화 타깃(선택이 만든 양학 + 적당한 숙련 + 탈-클론)
- [x] 이동 설계 = 바운드 아레나 + 자동수집 + 의미있는 이동만
- [x] 핵심 동사 = **능동 공격**, 난이도 = 얻어낸 양학(밸런스 핵심)
- [x] **선택형 드래프트(3택) 필수** / 런앤건 제외 / **194X 액티브 슈팅으로 수렴**
- [x] 조작 = **자동발사 + 유저는 방향/위치만**
- [x] 시그니처 = **뱀서식 무기 아스널 × 타입 상성 × 비행슈팅 × 복합 보스 + 테마 스테이지**(정보 기반 선택 > 운빨)
- [x] 타입 **4종(물·불·풀·노말)** 확정 / **뱀서식 무기 진화** 채택
- [x] **한 판 스펙 [DESIGN.md](DESIGN.md) v0.1** 작성 (타입표·무기·스테이지·복합보스·난이도·AI 파이프라인)
- [x] 적 공격 **무속성(A)** 확정 / **속성 스탠스 시스템** 채택 / 슬롯 4무기+5~6패시브 / 코인·상점·점수·회복 설계 → [DESIGN.md](DESIGN.md) v0.3
- [x] 키 = **QWER** 확정 / 보스 타이머 = **게임시간 3분** 확정
- [x] **설계 확정** — [design/CANON.md](design/CANON.md) v1.2 + 섹션 4종([02](design/02-readability-hud.md)·[03](design/03-weapons-passives-items.md)·[04](design/04-enemies-bosses-stages.md)·[05](design/05-flow-shop-score-onboarding.md)). **게이트 12계열 전부 통과**
- [x] **전사 완료** — [`data/*.json`](data/) 9종 + [`tools/check.mjs`](tools/check.mjs) 3,121행. `__AMBIGUOUS__` 0 · dangling 0 · S1~S40 38/38
- [x] 정본 **v1.4** — 전사 감사 59건 + C-7 근본 결함 + S34 문면 정정
- [x] ★ **`node` 설치 + `node tools/check.mjs` 실행 → exit 0** = **기계의 판정으로 기획 완료**
- [x] 문서 정리 — `DESIGN.md`를 입구로 전환(좀비 스펙 제거) + [`design/README.md`](design/README.md) 지도 신설
- [x] **1주차 프로토타입** — `src/` 착수, 브라우저 플레이 확인, check.mjs exit 0 (S1 발화·통과)
- [x] 1주차 코드 청소 — 감사 24건 + SILENT 버그(관통 hitGen 등) 수정, 정본 14편집, D1/D2/D3 닫힘, check.mjs exit 0, 브라우저 회귀 0
- [x] **모듈 단위 테스트 224개** — 순수 core 완전 커버, 뮤테이션 증명, 회귀 6종·효과 8종 고정, test.mjs+check.mjs exit 0
- [x] 적 스포너 슬라이스(`enemies.js`) — 4속성 섞인 적 스폰, 상성 손맛 headless 실증, 233 테스트·check exit 0
- [x] 플레이 피드백 1차 반영 — 적 공격(emitters.js)·드래프트 문구·HP밸런스, 244 테스트·check exit 0
- [x] 상성 히트 피드백 §7.7 구현 (×2 폭발/×0.5 방패호/×1 플래시 + SFX) — 255 테스트·check exit 0
- [x] ★ **역상성(×0.5) 인식 강화** — 실플레이 "×0.5가 안 읽힌다" 반영: 방패 굵게(sweep 120→160°·stroke 2→4px + 겹판·프레임 틱) + **적 본체 회색 차폐 플래시**(super 색팝의 무채색 대응) + tin SFX 또렷. 정본 §7.7·§9.4.3 동기화, 신규 키 0, 255 테스트·check exit 0
- [x] ★ **사용자 재플레이 → 상성 읽힘 확인** → 2주차 콘텐츠 착수 승인
- [x] **2주차 gap-map** — 6서브시스템 정본 vs 구현 계약 매핑. 의존성 순서 A→B→C→D 확정
- [x] **A. 무기 9종** — ★ **12 패밀리 전부 구현 완료**. A1 `omni`·`boomerang` / A2a `aura`·`nova`·`lance` / A2b `orbit`·`mine`·`barrage`·`drone`(기존 풀 재사용, 새 풀 0)
- [~] **B. 보스+스테이지** — done: 런 오케스트레이션(`stage.js`) · `boss.js` 복합보스 · §8.5 이미터 8종 · 위기 세션 · 상단 띠 HUD · **중간보스 3종(§8.9)**. **플로우 화면(TITLE/DIFFICULTY/THEME_BANNER/RESULTS)·레이저 2단 공정성 done**. 남음: 중간보스 렌더 폴리시·OPTIONS 실구현
- [x] **C. 메타** — `shop.js`+상점화면 · `score.js`+RESULTS · DEATH+컨티뉴(1회/런) · 시간 토큰
- [~] **D. AI 밸런싱** — D1 `bot.js` · **D2 `tools/sim.mjs`** · **D3 동적게이트 채점** · **실인증**(8P/16F/3U — 구조 밸런스 PASS, 봇이 유일 병목) done. 남음: **봇 재설계**로 클리어율을 인증 밴드에(인라인 튜닝 소진 → 예측 회피+무기별 포지셔닝 통합 정책)
- [ ] AI 콘텐츠 생성 파이프라인 (공모전 'AI 활용 문서'의 핵심)
- [ ] AI 콘텐츠 생성 + 헤드리스 시뮬 밸런싱 → 제출물
- [ ] 제목 미정
- [ ] MVP 범위 → `index.html` 스켈레톤 → **1주차 프로토타입**(핵심 손맛 검증) → AI 콘텐츠 생성·시뮬 밸런싱
- [ ] 제출물: 플레이 빌드(GitHub Pages) · YouTube 30~60초 · 게임 소개 PDF · AI 활용 기술 문서 PDF(log.md 기반) · (개인 참여 시 팀원 롤 기술서 생략)

> **마감: 2026-08-10** (사전과제 제출). 오늘 기준 약 4주.
